{
  "components": {
    "schemas": {
      "AbandonCampaignRequest": {
        "description": "Body for `POST /campaigns/{campaign_id}/abandon`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the Campaign is being abandoned. REQUIRED at\nthe abandon path (mirrors `RunAbortReason` REQUIRED-on-abort\nprecedent).",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the abandon transition (audit-log breadcrumb). REQUIRED.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "AbandonCampaignRequest",
        "type": "object"
      },
      "AbiTier": {
        "description": "ABI-tier floor used by permits and the seal.\n\n- `Testing`:  pre-stable; SHOULD NOT be relied on for production.\n- `Stable`:   supported and recommended for production consumers.\n- `Obsolete`: still served, MAY be removed; consumers should migrate.\n- `Removed`:  no longer served; included as a closed enum arm so\n              historical event payloads remain decodable.",
        "enum": [
          "Testing",
          "Stable",
          "Obsolete",
          "Removed"
        ],
        "title": "AbiTier",
        "type": "string"
      },
      "AbortCredentialRotationRequest": {
        "description": "Body for `POST /federation/credentials/{credential_id}/rotation/abort`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the rotation is being aborted. Examples: \"peer\nrefused new material\", \"SecretStore generation failed mid-flight\",\n\"operator changed their mind\".",
        "properties": {
          "reason": {
            "anyOf": [
              {
                "maxLength": 500,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional operator-supplied reason for aborting the rotation (audit-log breadcrumb).",
            "title": "Reason"
          }
        },
        "title": "AbortCredentialRotationRequest",
        "type": "object"
      },
      "AbortProcedureRequest": {
        "description": "Body for `POST /procedures/{procedure_id}/abort`.",
        "properties": {
          "reason": {
            "description": "Free-form reason for the abort (1-500 chars after trimming). Today the field is unstructured; structured taxonomy is future-additive (mirrors RunAborted.reason posture).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "AbortProcedureRequest",
        "type": "object"
      },
      "AbortRunRequest": {
        "description": "Body for `POST /runs/{run_id}/abort`.",
        "properties": {
          "decided_by_decision_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional Decision id that justified this abort (most commonly an OperatorAbortDecision or EquipmentAbortDecision per RunDebrief's 5-value choice enum). Maps to `prov:wasInformedBy` at the future PROV-O export adapter. NOT verified at the write path (eventual-consistency stance). Operators can record ad-hoc / emergency aborts without a Decision (Decision\u2192Run linkage).",
            "title": "Decided By Decision Id"
          },
          "reason": {
            "description": "Free-form reason for the abort (1-500 chars after trimming). Today the field is unstructured; structured taxonomy is future-additive.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "AbortRunRequest",
        "type": "object"
      },
      "AbortVisitRequest": {
        "description": "Body for `POST /visits/{visit_id}/abort`.",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the abort (no PII).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "AbortVisitRequest",
        "type": "object"
      },
      "ActorListResponse": {
        "description": "Page of actors plus opaque next-page cursor.\n\n`next_cursor` is None when no more pages. Pass it back as\n`?cursor=...` to fetch the next page.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/ActorSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "ActorListResponse",
        "type": "object"
      },
      "ActorResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format from\nthe domain model so the two can evolve independently (for example\nan ActorName invariant change doesn't break older clients).\n\n`kind` distinguishes human-registered Actors from\nagent-co-registered Actors (via the Agent BC `define_agent`\ncross-BC atomic write). Existing consumers that don't read `kind`\nare unaffected; consumers that need the distinction (Decision\nprojections, audit-policy filters) gain it without polymorphism.",
        "properties": {
          "active": {
            "title": "Active",
            "type": "boolean"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "enum": [
              "human",
              "agent",
              "service_account"
            ],
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "id",
          "name",
          "kind",
          "active"
        ],
        "title": "ActorResponse",
        "type": "object"
      },
      "ActorSummaryDTO": {
        "description": "One actor in a paginated list.",
        "properties": {
          "actor_id": {
            "format": "uuid",
            "title": "Actor Id",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "kind": {
            "enum": [
              "human",
              "agent",
              "service_account"
            ],
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "status": {
            "enum": [
              "active",
              "deactivated"
            ],
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "actor_id",
          "name",
          "kind",
          "status",
          "created_at"
        ],
        "title": "ActorSummaryDTO",
        "type": "object"
      },
      "AddAssetAlternateIdentifierRequest": {
        "description": "Body for `POST /assets/{asset_id}/add-alternate-identifier`.\n\nBoth fields required. Pydantic enforces non-empty value at the\nboundary; the `AlternateIdentifier` VO then trims and re-validates\nlength within the decider (the VO construction site).",
        "properties": {
          "identifier": {
            "$ref": "#/components/schemas/AlternateIdentifierBody",
            "description": "The alternate identifier to add. Uniqueness keyed on the (kind, value) pair at the Asset scope ONLY; cross-Asset duplicates are NOT rejected in v1."
          }
        },
        "required": [
          "identifier"
        ],
        "title": "AddAssetAlternateIdentifierRequest",
        "type": "object"
      },
      "AddAssetFamilyRequest": {
        "description": "Body for `POST /assets/{asset_id}/add-family`.\n\nEventual-consistency: `family_id` is NOT verified against the\nFamily stream at decide time; mismatch surfaces at Plan\nbinding (6e).",
        "properties": {
          "family_id": {
            "description": "Family id to add to the asset's family set. Eventual-consistency: existence is NOT verified.",
            "format": "uuid",
            "title": "Family Id",
            "type": "string"
          }
        },
        "required": [
          "family_id"
        ],
        "title": "AddAssetFamilyRequest",
        "type": "object"
      },
      "AddAssetOwnerRequest": {
        "description": "Body for `POST /assets/{asset_id}/add-owner`.\n\nThe full `AssetOwner` block is required. Pydantic enforces\nbounded-text length checks at the boundary; the AssetOwner VO\nre-validates (trim + length + pairing) inside the decider.",
        "properties": {
          "owner": {
            "$ref": "#/components/schemas/AssetOwnerBody",
            "description": "The institutional owner block to add. Uniqueness keyed on `name` at the Asset scope; cross-Asset duplicates are NOT rejected in v1."
          }
        },
        "required": [
          "owner"
        ],
        "title": "AddAssetOwnerRequest",
        "type": "object"
      },
      "AddAssetPortRequest": {
        "description": "Body for `POST /assets/{asset_id}/add-port`.\n\nAll three fields are required. Pydantic enforces non-empty\nname/signal_type at the boundary; the AssetPort VO then trims\nand re-validates length within the decider.",
        "properties": {
          "direction": {
            "description": "Port direction: 'Input' or 'Output' (PortDirection enum value).",
            "enum": [
              "Input",
              "Output"
            ],
            "title": "Direction",
            "type": "string"
          },
          "port_name": {
            "description": "Port name within the Asset's scope (for example, 'trigger_in', 'encoder_a', 'sync_clock'). Must be unique among the Asset's existing ports.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Port Name",
            "type": "string"
          },
          "signal_type": {
            "description": "Operator-supplied signal type (free text 1-50 chars). Common values: 'TTL', 'LVDS', 'Encoder', 'Network', 'Sync', 'Optical'.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Signal Type",
            "type": "string"
          }
        },
        "required": [
          "port_name",
          "direction",
          "signal_type"
        ],
        "title": "AddAssetPortRequest",
        "type": "object"
      },
      "AddModelFamilyRequest": {
        "description": "Body for `POST /models/{model_id}/families`.",
        "properties": {
          "family_id": {
            "description": "Family id to add to the Model.declared_family_ids set.",
            "format": "uuid",
            "title": "Family Id",
            "type": "string"
          }
        },
        "required": [
          "family_id"
        ],
        "title": "AddModelFamilyRequest",
        "type": "object"
      },
      "AddPlanWireRequest": {
        "description": "Body for `POST /plans/{plan_id}/add-wire`.\n\nAll four fields are required. Pydantic enforces non-empty port\nnames at the boundary; the `Wire` VO then trims and re-validates\nlength within the decider. Direction + signal_type validation\nhappens in the decider against the loaded Asset.ports.",
        "properties": {
          "source_asset_id": {
            "description": "The Asset whose OUTPUT port is the wire's source.",
            "format": "uuid",
            "title": "Source Asset Id",
            "type": "string"
          },
          "source_port_name": {
            "description": "Port name on the source Asset (for example, 'trigger_out', 'data_out'). Must exist on Asset.ports and have direction=OUTPUT.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Source Port Name",
            "type": "string"
          },
          "target_asset_id": {
            "description": "The Asset whose INPUT port is the wire's target.",
            "format": "uuid",
            "title": "Target Asset Id",
            "type": "string"
          },
          "target_port_name": {
            "description": "Port name on the target Asset (for example, 'trigger_in'). Must exist on Asset.ports, have direction=INPUT, and match source port's signal_type exactly. At most one Wire can target a given (target_asset_id, target_port_name) pair (fan-in forbidden; use a Combiner Family Asset if you genuinely need multi-source aggregation).",
            "maxLength": 100,
            "minLength": 1,
            "title": "Target Port Name",
            "type": "string"
          }
        },
        "required": [
          "source_asset_id",
          "source_port_name",
          "target_asset_id",
          "target_port_name"
        ],
        "title": "AddPlanWireRequest",
        "type": "object"
      },
      "AdjustRunRequest": {
        "description": "Body for `POST /runs/{run_id}/adjust`.",
        "properties": {
          "decided_by_decision_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional Decision id that justified this adjustment. Maps to `prov:wasInformedBy` at the future PROV-O export adapter (same export contract used by Decision.parent_id). NOT verified at the write path (eventual-consistency stance per cross-BC reference precedent). Operators can record ad-hoc adjustments without a Decision.",
            "title": "Decided By Decision Id"
          },
          "parameters_patch": {
            "additionalProperties": true,
            "description": "RFC 7396 JSON Merge Patch on top of the Run's current `effective_parameters`. The post-merge result is validated against the owning Method's `parameters_schema` (when the Method declares one). Must be non-empty (empty patches rejected with 400; they would silently no-op and mislead the audit).",
            "title": "Parameters Patch",
            "type": "object"
          },
          "reason": {
            "description": "Free-form justification (1-500 chars after trimming). Required: steering without recorded intent is the abort+restart anti-pattern relocated. Today the field is unstructured; structured taxonomy is future-additive on the same triggers as RunAbortReason.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "parameters_patch",
          "reason"
        ],
        "title": "AdjustRunRequest",
        "type": "object"
      },
      "Affordance": {
        "description": "Device-level operational primitive. Closed v1 enum (28 items, 2 patterns).\n\nValues are PascalCase strings so they serialize naturally as JSON\ndiscriminators without translation.\n\nPattern A, Operational affordances (`-able` / `-ible` / `-ing`):\n    \"device supports doing X\" (action) or \"device performs X\"\n    (role/flow). Set-membership claim. 27 items.\n\nPattern C, Lifecycle affordances (noun):\n    \"device has lifecycle property X\". 1 item.",
        "enum": [
          "Rotatable",
          "Translatable",
          "Homeable",
          "Limitable",
          "Capturable",
          "Posable",
          "Indexable",
          "Following",
          "Leading",
          "Imageable",
          "Binnable",
          "Triggerable",
          "Gateable",
          "Synchronizable",
          "Marking",
          "Pulsing",
          "Streamable",
          "Bufferable",
          "Compressible",
          "Recording",
          "Coolable",
          "PIDControllable",
          "Shutterable",
          "Attenuable",
          "Bendable",
          "Identifiable",
          "Reportable",
          "Consumable"
        ],
        "title": "Affordance",
        "type": "string"
      },
      "AgentResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model so the two can evolve independently.\n\n`defined_at` / `versioned_at` / `deprecated_at` are projection-\nsourced lifecycle timestamps (Path C);\nsee module docstring for null-semantics. `defined_at` is\nnullable (changed from required during lifecycle widening) because\nthe projection can lag; once the projection has folded\n`AgentDefined`, `defined_at` is non-null on every response.",
        "properties": {
          "canonical_uri": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Canonical Uri"
          },
          "capabilities": {
            "items": {
              "type": "string"
            },
            "title": "Capabilities",
            "type": "array"
          },
          "defined_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Defined At"
          },
          "deprecated_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Deprecated At"
          },
          "deprecation_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Deprecation Reason"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "title": "Kind",
            "type": "string"
          },
          "model_ref": {
            "$ref": "#/components/schemas/ModelRefResponse"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "prompt_template_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Prompt Template Id"
          },
          "status": {
            "$ref": "#/components/schemas/AgentStatus"
          },
          "version": {
            "title": "Version",
            "type": "string"
          },
          "versioned_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Versioned At"
          }
        },
        "required": [
          "id",
          "kind",
          "name",
          "version",
          "model_ref",
          "status",
          "capabilities"
        ],
        "title": "AgentResponse",
        "type": "object"
      },
      "AgentStatus": {
        "description": "The Agent's lifecycle state.\n\nFour values:\n\n  - `Defined`    -- registered as config; NOT yet ready for\n                    invocation (8f-b's subscriber filters on\n                    Versioned only).\n  - `Versioned`  -- promoted to ready-for-invocation; rainbow-\n                    deploy-style signal. Multiple Versioned\n                    Agents may exist concurrently (different\n                    `id`s sharing `kind`).\n  - `Suspended`  -- non-terminal operator-pause from\n                    `Versioned`. Returns to\n                    `Versioned` via `resume_agent`. Config\n                    changes (tools, budget) still permitted so\n                    the operator can fix permissions while\n                    paused. Cannot re-Version from Suspended\n                    (resume is its own dedicated verb).\n  - `Deprecated` -- terminal; cannot be re-Defined or re-\n                    Versioned. Future invocations must pick a\n                    non-Deprecated Agent of the same kind.\n                    Reachable from `Defined`, `Versioned`, or\n                    `Suspended`.",
        "enum": [
          "Defined",
          "Versioned",
          "Suspended",
          "Deprecated"
        ],
        "title": "AgentStatus",
        "type": "string"
      },
      "AlternateIdentifierBody": {
        "description": "Wire format for an `AlternateIdentifier` value object.",
        "properties": {
          "kind": {
            "$ref": "#/components/schemas/AlternateIdentifierKind",
            "description": "Closed PIDINST v1.0 vocabulary: SerialNumber (manufacturer per-unit identifier), InventoryNumber (facility asset tag), or Other (vendor-specific or unconventional scheme)."
          },
          "value": {
            "description": "Operator-supplied opaque string identifying the Asset under the given scheme. Trimmed at the domain boundary.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "value"
        ],
        "title": "AlternateIdentifierBody",
        "type": "object"
      },
      "AlternateIdentifierKind": {
        "description": "Closed vocabulary for an Asset's alternate-identifier kind.\n\nValues are verbatim from PIDINST v1.0 spec page 8 (Table 1)\nProperty 13 `alternateIdentifierType` controlled vocabulary:\nSerialNumber, InventoryNumber, Other. Operationally:\n\n  - `SerialNumber` is the manufacturer's per-unit identifier\n    (the value engraved on the chassis or printed on the QR\n    sticker; for example, an Aerotech ANT130-L's `12345-ABC`).\n  - `InventoryNumber` is the facility-issued asset tag (for\n    example, an APS-issued `APS-2BM-CAM-001`).\n  - `Other` is the catch-all for vendor-specific or\n    unconventional identifier schemes that don't fit the prior\n    two; resolution is operator-supplied free text in the\n    `value` field.\n\nAdding a fourth member is an additive enum change at a future\nmigration boundary. The closed-enum stance mirrors\n`ManufacturerIdentifierType` (Model BC) and the broader\n[[project-family-affordance-design]] closed-vocabulary\nprecedent. See [[project-asset-alternate-identifiers-design]]\nLock B for the design rationale.",
        "enum": [
          "SerialNumber",
          "InventoryNumber",
          "Other"
        ],
        "title": "AlternateIdentifierKind",
        "type": "string"
      },
      "AmendClearanceRequest": {
        "description": "Body for `POST /clearances/{parent_clearance_id}/amend`.\n\nMirrors `RegisterClearanceRequest`'s child-fields exactly. The\n`parent_clearance_id` comes from the URL path, not the body.",
        "properties": {
          "bindings": {
            "description": "Child's bindings; same discriminated-union shape as register_clearance.",
            "items": {
              "discriminator": {
                "mapping": {
                  "Asset": "#/components/schemas/BindingAssetDTO",
                  "External": "#/components/schemas/BindingExternalDTO",
                  "Procedure": "#/components/schemas/BindingProcedureDTO",
                  "Run": "#/components/schemas/BindingRunDTO",
                  "Subject": "#/components/schemas/BindingSubjectDTO"
                },
                "propertyName": "kind"
              },
              "oneOf": [
                {
                  "$ref": "#/components/schemas/BindingSubjectDTO"
                },
                {
                  "$ref": "#/components/schemas/BindingAssetDTO"
                },
                {
                  "$ref": "#/components/schemas/BindingRunDTO"
                },
                {
                  "$ref": "#/components/schemas/BindingProcedureDTO"
                },
                {
                  "$ref": "#/components/schemas/BindingExternalDTO"
                }
              ]
            },
            "minItems": 1,
            "title": "Bindings",
            "type": "array"
          },
          "declarations": {
            "description": "Child's hazard declarations; empty list allowed.",
            "items": {
              "$ref": "#/components/schemas/HazardDeclarationDTO"
            },
            "title": "Declarations",
            "type": "array"
          },
          "external_id": {
            "anyOf": [
              {
                "maxLength": 100,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "External Id"
          },
          "facility_asset_id": {
            "description": "Reference to the Asset.Level.Site for the facility issuing the child clearance. Typically matches the parent's facility.",
            "format": "uuid",
            "title": "Facility Asset Id",
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/ClearanceKind",
            "description": "Form-type for the child clearance (may differ from parent's)."
          },
          "risk_band": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RiskBand"
              },
              {
                "type": "null"
              }
            ]
          },
          "title": {
            "description": "Operator-readable title for the child clearance.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Title",
            "type": "string"
          },
          "valid_from": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Valid From"
          },
          "valid_until": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Valid Until"
          }
        },
        "required": [
          "kind",
          "facility_asset_id",
          "title",
          "bindings"
        ],
        "title": "AmendClearanceRequest",
        "type": "object"
      },
      "AmendClearanceResponse": {
        "description": "Response body for `POST /clearances/{parent_clearance_id}/amend`.",
        "properties": {
          "clearance_id": {
            "description": "The new child clearance's id.",
            "format": "uuid",
            "title": "Clearance Id",
            "type": "string"
          }
        },
        "required": [
          "clearance_id"
        ],
        "title": "AmendClearanceResponse",
        "type": "object"
      },
      "AppendCalibrationRevisionRequest": {
        "description": "Body for `POST /calibrations/{calibration_id}/revisions`.",
        "properties": {
          "decided_by_decision_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional Decision id that justified appending this revision (operator pivot, agent advisory). Maps to `prov:wasInformedBy` at the future PROV-O export adapter. Mirrors AdjustRun / StartRun / AbortRun pattern. NOT verified at the write path.",
            "title": "Decided By Decision Id"
          },
          "source": {
            "description": "Tagged source provenance: `{kind: 'Measured', procedure_id}` / `{kind: 'Computed', dataset_id}` / `{kind: 'Asserted', actor_id}`. The kind discriminator is required. Source FK targets are NOT cross-BC validated at the write path (eventual-consistency stance).",
            "discriminator": {
              "mapping": {
                "Asserted": "#/components/schemas/SourceAssertedDTO",
                "Computed": "#/components/schemas/SourceComputedDTO",
                "Measured": "#/components/schemas/SourceMeasuredDTO"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/components/schemas/SourceMeasuredDTO"
              },
              {
                "$ref": "#/components/schemas/SourceComputedDTO"
              },
              {
                "$ref": "#/components/schemas/SourceAssertedDTO"
              }
            ],
            "title": "Source"
          },
          "status": {
            "$ref": "#/components/schemas/CalibrationStatus",
            "description": "Confidence tier of this revision: `Provisional` (initial estimate or early-data-derived) or `Verified` (blessed for production reconstructions). A 2-tier ladder; a 3-tier `Refined` middle is a future option, not currently supported."
          },
          "supersedes_revision_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional prior revision id (on the SAME aggregate) that this revision supersedes. The direct derivation edge saves consumers a graph walk vs traversal-only. Cross-aggregate supersession is forbidden: the supersedes target must exist on this calibration.",
            "title": "Supersedes Revision Id"
          },
          "value": {
            "additionalProperties": true,
            "description": "JSON-shaped revision value (for example, `{center: 1024.5}`). Validated STRICT against the calibration's quantity-specific VALUE_SCHEMA at the decider; missing required fields or additional properties raise 400.",
            "title": "Value",
            "type": "object"
          }
        },
        "required": [
          "value",
          "status",
          "source"
        ],
        "title": "AppendCalibrationRevisionRequest",
        "type": "object"
      },
      "AppendCalibrationRevisionResponse": {
        "description": "Response body for `POST /calibrations/{calibration_id}/revisions`.",
        "properties": {
          "revision_id": {
            "format": "uuid",
            "title": "Revision Id",
            "type": "string"
          }
        },
        "required": [
          "revision_id"
        ],
        "title": "AppendCalibrationRevisionResponse",
        "type": "object"
      },
      "AppendClearanceReviewStepRequest": {
        "description": "Body for `POST /clearances/{clearance_id}/review-steps`.",
        "properties": {
          "decided_at": {
            "description": "Operator-supplied timestamp when the reviewer made the decision.",
            "format": "date-time",
            "title": "Decided At",
            "type": "string"
          },
          "decision": {
            "description": "Reviewer decision for this step. 'Approved' / 'Rejected' are non-terminal at the chain level; the chain continues until `approve_clearance` / `reject_clearance` consume the chain.",
            "enum": [
              "Approved",
              "Rejected",
              "RequestedChanges"
            ],
            "title": "Decision",
            "type": "string"
          },
          "notes": {
            "anyOf": [
              {
                "maxLength": 2000,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional reviewer notes (audit breadcrumb).",
            "title": "Notes"
          },
          "role": {
            "description": "Facility-vocabulary reviewer role.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Role",
            "type": "string"
          },
          "step_index": {
            "description": "0-based step index. MUST equal `len(review_steps)` at append time (append-only contract); out-of-order writes return 400.",
            "minimum": 0.0,
            "title": "Step Index",
            "type": "integer"
          }
        },
        "required": [
          "step_index",
          "role",
          "decision",
          "decided_at"
        ],
        "title": "AppendClearanceReviewStepRequest",
        "type": "object"
      },
      "AppendProcedureStepsRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /procedures/{procedure_id}/steps`.",
        "properties": {
          "entries": {
            "description": "List of step entries to append (1-500).",
            "items": {
              "$ref": "#/components/schemas/ProcedureStepRequest"
            },
            "maxItems": 500,
            "minItems": 1,
            "title": "Entries",
            "type": "array"
          }
        },
        "required": [
          "entries"
        ],
        "title": "AppendProcedureStepsRequest",
        "type": "object"
      },
      "AppendProcedureStepsResponse": {
        "description": "Response body for the append slice.",
        "properties": {
          "event_count": {
            "description": "Number of entries accepted by the store (includes silently-deduped retries; producer can re-call with the same event_ids safely).",
            "minimum": 0.0,
            "title": "Event Count",
            "type": "integer"
          }
        },
        "required": [
          "event_count"
        ],
        "title": "AppendProcedureStepsResponse",
        "type": "object"
      },
      "AppendReasoningEntriesRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /decisions/{decision_id}/reasoning-entries`.",
        "properties": {
          "entries": {
            "description": "List of reasoning entries to append (1-100).",
            "items": {
              "$ref": "#/components/schemas/ReasoningEntryRequest"
            },
            "maxItems": 100,
            "minItems": 1,
            "title": "Entries",
            "type": "array"
          }
        },
        "required": [
          "entries"
        ],
        "title": "AppendReasoningEntriesRequest",
        "type": "object"
      },
      "AppendReasoningEntriesResponse": {
        "description": "Response body for the append slice.",
        "properties": {
          "event_count": {
            "description": "Number of entries accepted by the store (includes silently-deduped retries; producer can re-call with the same event_ids safely).",
            "minimum": 0.0,
            "title": "Event Count",
            "type": "integer"
          }
        },
        "required": [
          "event_count"
        ],
        "title": "AppendReasoningEntriesResponse",
        "type": "object"
      },
      "AppendRunReadingsRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /runs/{run_id}/readings`.",
        "properties": {
          "entries": {
            "description": "List of reading entries to append (1-500).",
            "items": {
              "$ref": "#/components/schemas/RunReadingRequest"
            },
            "maxItems": 500,
            "minItems": 1,
            "title": "Entries",
            "type": "array"
          }
        },
        "required": [
          "entries"
        ],
        "title": "AppendRunReadingsRequest",
        "type": "object"
      },
      "AppendRunReadingsResponse": {
        "description": "Response body for the append slice.",
        "properties": {
          "event_count": {
            "description": "Number of entries accepted by the store (includes silently-deduped retries; producer can re-call with the same event_ids safely).",
            "minimum": 0.0,
            "title": "Event Count",
            "type": "integer"
          }
        },
        "required": [
          "event_count"
        ],
        "title": "AppendRunReadingsResponse",
        "type": "object"
      },
      "ApproveClearanceRequest": {
        "description": "Body for `POST /clearances/{clearance_id}/approve`.",
        "properties": {
          "valid_from": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional override of the effective-from timestamp.",
            "title": "Valid From"
          },
          "valid_until": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional override of the effective-until timestamp. Must be strictly greater than `valid_from` when both are provided.",
            "title": "Valid Until"
          }
        },
        "title": "ApproveClearanceRequest",
        "type": "object"
      },
      "AssetIntegrationViewResponse": {
        "description": "Read-side DTO at the API boundary.\n\nDecouples wire format from the domain `AssetIntegrationView`. Lists\nserialize in deterministic order (families/cautions/capabilities by\ntheir canonical sort; ports already sorted by name in the handler).\n\n`incomplete` is TRUE if any Family in `asset.family_ids` failed to load\n(eventual-consistency tolerance per\n[[project-dataset-lineage-design]]); consumers should treat the\nbundle as partial in that case.",
        "properties": {
          "active_cautions": {
            "items": {
              "$ref": "#/components/schemas/CautionViewDTO"
            },
            "title": "Active Cautions",
            "type": "array"
          },
          "applicable_capabilities": {
            "items": {
              "$ref": "#/components/schemas/CapabilityViewDTO"
            },
            "title": "Applicable Capabilities",
            "type": "array"
          },
          "asset_id": {
            "format": "uuid",
            "title": "Asset Id",
            "type": "string"
          },
          "condition": {
            "title": "Condition",
            "type": "string"
          },
          "families": {
            "items": {
              "$ref": "#/components/schemas/FamilyViewDTO"
            },
            "title": "Families",
            "type": "array"
          },
          "incomplete": {
            "title": "Incomplete",
            "type": "boolean"
          },
          "level": {
            "title": "Level",
            "type": "string"
          },
          "lifecycle": {
            "title": "Lifecycle",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "parent_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          },
          "ports": {
            "items": {
              "$ref": "#/components/schemas/PortViewDTO"
            },
            "title": "Ports",
            "type": "array"
          },
          "settings": {
            "additionalProperties": true,
            "title": "Settings",
            "type": "object"
          }
        },
        "required": [
          "asset_id",
          "name",
          "level",
          "lifecycle",
          "condition",
          "parent_id",
          "families",
          "ports",
          "settings",
          "active_cautions",
          "applicable_capabilities",
          "incomplete"
        ],
        "title": "AssetIntegrationViewResponse",
        "type": "object"
      },
      "AssetLevel": {
        "description": "The hierarchical level of an Asset.\n\nPer the BC map (ISA-88-derived, single-word convention):\n  - `Enterprise`: root; the institution itself\n  - `Site`: a facility (for example, APS)\n  - `Area`: a section of a site (for example, the experimental hall)\n  - `Unit`: an operational unit (for example, a beamline)\n  - `Component`: a composed sub-system (ISA-88 \"Equipment Module\" tier)\n  - `Device`: an addressable control surface (ISA-88 \"Control Module\")\n\nCommon pattern is the strict ordering above, but Device-in-Device\nis allowed when reality demands it (smart instruments). Levels\nare conventional, not enforced: the decider does not check that\na Device's parent is a Component.",
        "enum": [
          "Enterprise",
          "Site",
          "Area",
          "Unit",
          "Component",
          "Device"
        ],
        "title": "AssetLevel",
        "type": "string"
      },
      "AssetListResponse": {
        "description": "Page of assets plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/AssetSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "AssetListResponse",
        "type": "object"
      },
      "AssetOwnerBody": {
        "description": "Wire format for an `AssetOwner` value object (PIDINST v1.0 Property 5).",
        "properties": {
          "contact": {
            "anyOf": [
              {
                "maxLength": 255,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional contact string for the owner (PIDINST 5.2). The spec hints at email; CORA accepts arbitrary free text (phone numbers, contact-form URLs, mail-team aliases all appear in DataCite corpora). Email-format validation is not enforced here.",
            "title": "Contact"
          },
          "identifier": {
            "anyOf": [
              {
                "maxLength": 255,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional opaque owner identifier (PIDINST 5.3). The spec recommends a globally unique identifier; ROR is the preferred scheme. Both bare codes and full URLs are accepted; no normalization. Paired with identifier_type: both must be set or both must be absent.",
            "title": "Identifier"
          },
          "identifier_type": {
            "anyOf": [
              {
                "maxLength": 64,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional scheme label for `identifier` (PIDINST 5.3.1). Free text by spec design (unlike sibling identifier-type fields): the field accepts ROR, GRID, ISNI, RAID, IGSN, internal facility codes, or any other authority. CORA does NOT case-normalize the recorded value. Paired with identifier.",
            "title": "Identifier Type"
          },
          "name": {
            "description": "Mandatory institutional owner name (PIDINST v1.0 Property 5.1). Trimmed at the domain boundary. Unique within a single Asset's owners set.",
            "maxLength": 255,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "AssetOwnerBody",
        "type": "object"
      },
      "AssetPortDTO": {
        "description": "Read-side DTO for a single Asset port (5h).",
        "properties": {
          "direction": {
            "title": "Direction",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "signal_type": {
            "title": "Signal Type",
            "type": "string"
          }
        },
        "required": [
          "name",
          "direction",
          "signal_type"
        ],
        "title": "AssetPortDTO",
        "type": "object"
      },
      "AssetResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model so the two can evolve independently.\n`level`, `lifecycle`, and `condition` are the StrEnum string\nvalues (PascalCase per the BC map). `parent_id` is null only for\nEnterprise-level roots. `family_ids` serializes as a sorted list\nof UUIDs (frozenset semantics in domain state, list at the JSON\nboundary; sorted by UUID string form for response determinism).\n`settings` is the operator-supplied dict (operationally typed by\nFamily schemas at write time). `ports` (5h) serializes as a\nlist sorted by port name.",
        "properties": {
          "condition": {
            "title": "Condition",
            "type": "string"
          },
          "family_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Family Ids",
            "type": "array"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "level": {
            "title": "Level",
            "type": "string"
          },
          "lifecycle": {
            "title": "Lifecycle",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "parent_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          },
          "ports": {
            "items": {
              "$ref": "#/components/schemas/AssetPortDTO"
            },
            "title": "Ports",
            "type": "array"
          },
          "settings": {
            "additionalProperties": true,
            "title": "Settings",
            "type": "object"
          }
        },
        "required": [
          "id",
          "name",
          "level",
          "parent_id",
          "lifecycle",
          "condition",
          "family_ids",
          "settings",
          "ports"
        ],
        "title": "AssetResponse",
        "type": "object"
      },
      "AssetSummaryDTO": {
        "description": "One asset in a paginated list.",
        "properties": {
          "asset_id": {
            "format": "uuid",
            "title": "Asset Id",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "level": {
            "enum": [
              "Enterprise",
              "Site",
              "Area",
              "Unit",
              "Component",
              "Device"
            ],
            "title": "Level",
            "type": "string"
          },
          "lifecycle": {
            "enum": [
              "Commissioned",
              "Active",
              "Maintenance",
              "Decommissioned"
            ],
            "title": "Lifecycle",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "parent_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          }
        },
        "required": [
          "asset_id",
          "name",
          "level",
          "lifecycle",
          "parent_id",
          "created_at"
        ],
        "title": "AssetSummaryDTO",
        "type": "object"
      },
      "AttachAssetToFixtureRequest": {
        "description": "Body for `POST /assets/{asset_id}/attach-to-fixture`.",
        "properties": {
          "fixture_id": {
            "description": "Fixture.id this Asset will be bound into. Must resolve to a registered Fixture whose slot_asset_bindings include this asset_id.",
            "format": "uuid",
            "title": "Fixture Id",
            "type": "string"
          }
        },
        "required": [
          "fixture_id"
        ],
        "title": "AttachAssetToFixtureRequest",
        "type": "object"
      },
      "BindingAssetDTO": {
        "properties": {
          "id": {
            "description": "Target Asset's id.",
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "const": "Asset",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "id"
        ],
        "title": "BindingAssetDTO",
        "type": "object"
      },
      "BindingExternalDTO": {
        "properties": {
          "id": {
            "description": "Facility-minted external ID (for example 'GUP-12345').",
            "maxLength": 200,
            "minLength": 1,
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "const": "External",
            "title": "Kind",
            "type": "string"
          },
          "scheme": {
            "description": "External-ref scheme: 'proposal' | 'btr' | 'lab_visit' | 'session' | <future>. Anti-corruption pattern for upstream-deferred concepts CORA does NOT model (per BC map line 111).",
            "maxLength": 50,
            "minLength": 1,
            "title": "Scheme",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "scheme",
          "id"
        ],
        "title": "BindingExternalDTO",
        "type": "object"
      },
      "BindingProcedureDTO": {
        "properties": {
          "id": {
            "description": "Target Procedure's id.",
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "const": "Procedure",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "id"
        ],
        "title": "BindingProcedureDTO",
        "type": "object"
      },
      "BindingRunDTO": {
        "properties": {
          "id": {
            "description": "Target Run's id.",
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "const": "Run",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "id"
        ],
        "title": "BindingRunDTO",
        "type": "object"
      },
      "BindingSubjectDTO": {
        "properties": {
          "id": {
            "description": "Target Subject's id.",
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "const": "Subject",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "id"
        ],
        "title": "BindingSubjectDTO",
        "type": "object"
      },
      "BindingsByKind": {
        "description": "Per-kind binding-id arrays surfaced on the list response.\n\nFour explicit `<kind>_ids` fields (matching the projection's UUID[]\ncolumns 1:1, mirroring the view-model's `subject_binding_ids` /\n`asset_binding_ids` / `run_binding_ids` / `procedure_binding_ids`\nplural shape) instead of a `dict[str, list[UUID]]`: OpenAPI codegen\nproduces typed accessors; SDK consumers see the locked set of keys\nwithout `additionalProperties` ambiguity. ExternalBinding refs are\nNOT surfaced here (anti-corruption refs, not projected; see route\nsummary).",
        "properties": {
          "asset_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Asset Ids",
            "type": "array"
          },
          "procedure_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Procedure Ids",
            "type": "array"
          },
          "run_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Run Ids",
            "type": "array"
          },
          "subject_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Subject Ids",
            "type": "array"
          }
        },
        "title": "BindingsByKind",
        "type": "object"
      },
      "CalibrationListPageResponse": {
        "description": "Page of calibration summaries plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/CalibrationSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "CalibrationListPageResponse",
        "type": "object"
      },
      "CalibrationQuantity": {
        "description": "Closed catalog of supported calibration quantities.\n\nDay-one set covers the 2-BM tomography pilot's load-bearing\ncalibrations. Growth happens by PR per the Affordance enum precedent.\n\nString values are stable-by-design (event payloads carry them\nverbatim); renaming a value would orphan stored events. The enum\nmember name follows SCREAMING_SNAKE_CASE; the value follows\nsnake_case to match jsonb-key conventions in operating_point and\nvalue dicts.",
        "enum": [
          "rotation_center",
          "detector_pixel_size",
          "magnification",
          "effective_thickness"
        ],
        "title": "CalibrationQuantity",
        "type": "string"
      },
      "CalibrationResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives + the polymorphic-source wire shape. Decouples\nthe wire format from the domain model so the two can evolve\nindependently.\n\nPer Path C, `defined_at` / `last_revised_at` are projection-\nsourced (not aggregate state) and therefore nullable on the\nwire: the projection may transiently lag behind the event store,\nor the deps may lack a configured pool (in-memory test mode).\nMirrors the Method / Plan / Family read DTOs.",
        "properties": {
          "defined_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Defined At"
          },
          "defined_by_actor_id": {
            "format": "uuid",
            "title": "Defined By Actor Id",
            "type": "string"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "last_revised_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Revised At"
          },
          "operating_point": {
            "additionalProperties": true,
            "title": "Operating Point",
            "type": "object"
          },
          "quantity": {
            "title": "Quantity",
            "type": "string"
          },
          "revisions": {
            "items": {
              "$ref": "#/components/schemas/RevisionResponse"
            },
            "title": "Revisions",
            "type": "array"
          },
          "target_id": {
            "format": "uuid",
            "title": "Target Id",
            "type": "string"
          }
        },
        "required": [
          "id",
          "target_id",
          "quantity",
          "operating_point",
          "description",
          "revisions",
          "defined_by_actor_id"
        ],
        "title": "CalibrationResponse",
        "type": "object"
      },
      "CalibrationStatus": {
        "description": "A revision's confidence tier for downstream consumption.\n\nTwo values:\n\n  - `Provisional` -- initial estimate or early-data-derived figure;\n                     downstream consumers may still use it but\n                     should be aware it is unblessed.\n  - `Verified`    -- blessed for production reconstructions /\n                     analyses; downstream consumers can rely on it\n                     without operator follow-up.\n\n2-tier matches when no PCL-shaped middle latency window exists with\ndistinct statistical maturity (ERA5 reanalysis precedent). 3-tier\n(CMS Express/Prompt/ReReco pattern) is deferred to phase 12f when\nrefined-after-provisional becomes a frequent structured workflow.",
        "enum": [
          "Provisional",
          "Verified"
        ],
        "title": "CalibrationStatus",
        "type": "string"
      },
      "CalibrationSummaryDTO": {
        "description": "One calibration in a paginated list.",
        "properties": {
          "calibration_id": {
            "format": "uuid",
            "title": "Calibration Id",
            "type": "string"
          },
          "defined_at": {
            "format": "date-time",
            "title": "Defined At",
            "type": "string"
          },
          "defined_by_actor_id": {
            "format": "uuid",
            "title": "Defined By Actor Id",
            "type": "string"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "last_revised_at": {
            "format": "date-time",
            "title": "Last Revised At",
            "type": "string"
          },
          "latest_revision_source_kind": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Latest Revision Source Kind"
          },
          "latest_revision_status": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Latest Revision Status"
          },
          "operating_point": {
            "additionalProperties": true,
            "title": "Operating Point",
            "type": "object"
          },
          "quantity": {
            "title": "Quantity",
            "type": "string"
          },
          "revision_count": {
            "minimum": 0.0,
            "title": "Revision Count",
            "type": "integer"
          },
          "target_id": {
            "format": "uuid",
            "title": "Target Id",
            "type": "string"
          }
        },
        "required": [
          "calibration_id",
          "target_id",
          "quantity",
          "operating_point",
          "defined_at",
          "last_revised_at",
          "defined_by_actor_id",
          "revision_count"
        ],
        "title": "CalibrationSummaryDTO",
        "type": "object"
      },
      "CampaignIntent": {
        "description": "Intent-shape vocabulary; what KIND of coordination this Campaign carries.\n\nFour abstract shape categories: what the operator is coordinating,\nnot the scientific technique. Technique-specific tagging (in-situ,\noperando, tomography, EDD, etc.) goes on the free\n`tags: frozenset[CampaignTag]` field. Closed enum + free tags\nyields a reportable shape vocabulary plus a flexible domain-\nvocabulary surface.\n\n  - `Series`       -- multiple measurements coordinated over time on\n                      shared resources\n  - `Sweep`        -- parametric exploration (axes swept across runs)\n  - `Coordination` -- multi-modal or multi-Subject coordinated\n                      acquisition\n  - `Block`        -- scheduling envelope (proposal / beamtime block /\n                      cycle)",
        "enum": [
          "Series",
          "Sweep",
          "Coordination",
          "Block"
        ],
        "title": "CampaignIntent",
        "type": "string"
      },
      "CampaignListPageResponse": {
        "description": "Page of campaigns plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/CampaignSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "CampaignListPageResponse",
        "type": "object"
      },
      "CampaignResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model so the two can evolve independently.\n\n`run_ids` / `tags` / `external_refs` are sorted for deterministic\nbytes on read (helps response-shape stability for clients that\ndiff responses; also harmonises with how events serialise the\nsame fields).",
        "properties": {
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "external_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "External Id"
          },
          "external_refs": {
            "items": {
              "$ref": "#/components/schemas/ExternalRefDTO-Output"
            },
            "title": "External Refs",
            "type": "array"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "intent": {
            "$ref": "#/components/schemas/CampaignIntent"
          },
          "last_status_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Operator-supplied audit breadcrumb from the last Held or Abandoned transition. Preserved across Resume so the 'why was it held?' answer stays readable.",
            "title": "Last Status Reason"
          },
          "lead_actor_id": {
            "format": "uuid",
            "title": "Lead Actor Id",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "run_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Run Ids",
            "type": "array"
          },
          "status": {
            "$ref": "#/components/schemas/CampaignStatus"
          },
          "subject_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subject Id"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          }
        },
        "required": [
          "id",
          "name",
          "intent",
          "lead_actor_id",
          "subject_id",
          "description",
          "tags",
          "external_refs",
          "external_id",
          "run_ids",
          "status"
        ],
        "title": "CampaignResponse",
        "type": "object"
      },
      "CampaignStatus": {
        "description": "The Campaign's lifecycle state.\n\nFive values locked day one per `[[project_campaign_design]]`:\n\n  - `Planned`   -- registered; not yet started; members addable\n  - `Active`    -- in progress; members addable; Runs may execute\n  - `Held`      -- paused (operator decision); members addable\n  - `Closed`    -- normal terminal; members locked\n  - `Abandoned` -- early terminal with reason; members locked\n\n5-state lifecycle is the BC-map locked shape (line 94), corpus-\nvalidated as light-but-sufficient: no standard has a tighter\nCampaign FSM, and PackML's 17-state cascade is the documented\nover-engineering counter-example. UPPER_SNAKE_CASE identifier +\nPascalCase value per cross-BC convention.",
        "enum": [
          "Planned",
          "Active",
          "Held",
          "Closed",
          "Abandoned"
        ],
        "title": "CampaignStatus",
        "type": "string"
      },
      "CampaignSummaryDTO": {
        "description": "One campaign in a paginated list.",
        "properties": {
          "campaign_id": {
            "format": "uuid",
            "title": "Campaign Id",
            "type": "string"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "external_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "External Id"
          },
          "intent": {
            "$ref": "#/components/schemas/CampaignIntent"
          },
          "last_status_changed_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Status Changed At"
          },
          "last_status_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Status Reason"
          },
          "lead_actor_id": {
            "format": "uuid",
            "title": "Lead Actor Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "registered_at": {
            "format": "date-time",
            "title": "Registered At",
            "type": "string"
          },
          "run_count": {
            "default": 0,
            "title": "Run Count",
            "type": "integer"
          },
          "started_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Started At"
          },
          "status": {
            "$ref": "#/components/schemas/CampaignStatus"
          },
          "subject_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subject Id"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          }
        },
        "required": [
          "campaign_id",
          "name",
          "intent",
          "status",
          "lead_actor_id",
          "registered_at"
        ],
        "title": "CampaignSummaryDTO",
        "type": "object"
      },
      "CancelVisitRequest": {
        "description": "Body for `POST /visits/{visit_id}/cancel`.",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the cancellation (audit-log breadcrumb; no PII).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "CancelVisitRequest",
        "type": "object"
      },
      "CandidateAssetItem": {
        "description": "Per-Asset candidate for a missing affordance on the wire.\n\n`contributing_family_ids` is deliberately named to differ from\n`WiredAssetItem.family_ids`: it lists ONLY the candidate's\nFamilies that declare the parent missing affordance, not the\ncandidate's full Family set. Other state (condition, lifecycle)\nis unfiltered so the operator can see Faulted / Decommissioned\ncandidates and decide whether to act.",
        "properties": {
          "asset_id": {
            "format": "uuid",
            "title": "Asset Id",
            "type": "string"
          },
          "asset_name": {
            "title": "Asset Name",
            "type": "string"
          },
          "condition": {
            "title": "Condition",
            "type": "string"
          },
          "contributing_family_ids": {
            "description": "Subset of the candidate's Families that declare the parent missing affordance, NOT the candidate's full Family set. Naming distinguishes this from WiredAssetItem.family_ids (the full set) so SDK consumers don't naively union the two.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Contributing Family Ids",
            "type": "array"
          },
          "lifecycle": {
            "title": "Lifecycle",
            "type": "string"
          }
        },
        "required": [
          "asset_id",
          "asset_name",
          "condition",
          "lifecycle",
          "contributing_family_ids"
        ],
        "title": "CandidateAssetItem",
        "type": "object"
      },
      "CapabilityResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. `status` is the StrEnum's\nstring value (Defined / Versioned / Deprecated). `version` is\nthe operator-supplied label of the most recent version_capability\ncall (null until first version). `required_affordances` and\n`executor_shapes` serialize as sorted lists for response\ndeterminism. `replaced_by_capability_id` is null on Defined /\nVersioned / Deprecated-without-replacement; populated when a\ndeprecation supplied a successor pointer. `created_at` /\n`versioned_at` / `deprecated_at` are projection-sourced lifecycle\ntimestamps (Path C); see module\ndocstring for null-semantics.",
        "properties": {
          "code": {
            "maxLength": 200,
            "title": "Code",
            "type": "string"
          },
          "created_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "deprecated_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Deprecated At"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "executor_shapes": {
            "items": {
              "$ref": "#/components/schemas/ExecutorShape"
            },
            "title": "Executor Shapes",
            "type": "array"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "parameters_schema": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parameters Schema"
          },
          "replaced_by_capability_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Replaced By Capability Id"
          },
          "required_affordances": {
            "items": {
              "$ref": "#/components/schemas/Affordance"
            },
            "title": "Required Affordances",
            "type": "array"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "version": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version"
          },
          "versioned_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Versioned At"
          }
        },
        "required": [
          "id",
          "code",
          "name",
          "status",
          "version",
          "description",
          "required_affordances",
          "executor_shapes",
          "parameters_schema",
          "replaced_by_capability_id"
        ],
        "title": "CapabilityResponse",
        "type": "object"
      },
      "CapabilityViewDTO": {
        "description": "Read-side DTO for one applicable Capability on the integration-view bundle.",
        "properties": {
          "capability_id": {
            "format": "uuid",
            "title": "Capability Id",
            "type": "string"
          },
          "code": {
            "title": "Code",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "capability_id",
          "code",
          "name",
          "status"
        ],
        "title": "CapabilityViewDTO",
        "type": "object"
      },
      "CautionCategory": {
        "description": "Closed controlled vocabulary, day-one lock at 6 values.\n\nDerived from synchrotron + EHS corpus 'observed in the wild' lists:\nmotion + thermal + electrical -> engineering quirks; calibration +\ninterlock; procedure. Tags absorb the rest (ICAT `ParameterType` +\nOlog `level` + `tags` precedent). Adding categories is cheap\n(additive StrEnum); pruning is expensive.",
        "enum": [
          "Wear",
          "Calibration",
          "Wiring",
          "OperationalWindow",
          "InterlockQuirk",
          "ProcedureGotcha"
        ],
        "title": "CautionCategory",
        "type": "string"
      },
      "CautionListPageResponse": {
        "description": "Page of cautions plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/CautionSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "CautionListPageResponse",
        "type": "object"
      },
      "CautionResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model so the two can evolve independently.\n`target` is rendered via the public `serialize_target` helper as\n`{kind, id}`.",
        "properties": {
          "author_actor_id": {
            "format": "uuid",
            "title": "Author Actor Id",
            "type": "string"
          },
          "category": {
            "$ref": "#/components/schemas/CautionCategory"
          },
          "expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Expires At"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "parent_caution_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Caution Id"
          },
          "propagate_to_children": {
            "title": "Propagate To Children",
            "type": "boolean"
          },
          "retired_reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/CautionRetireReason"
              },
              {
                "type": "null"
              }
            ]
          },
          "severity": {
            "$ref": "#/components/schemas/CautionSeverity"
          },
          "status": {
            "$ref": "#/components/schemas/CautionStatus"
          },
          "superseded_by_caution_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Superseded By Caution Id"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "target": {
            "additionalProperties": true,
            "description": "Discriminated target: {kind: 'Asset'|'Procedure', id: <uuid>}",
            "title": "Target",
            "type": "object"
          },
          "text": {
            "title": "Text",
            "type": "string"
          },
          "workaround": {
            "title": "Workaround",
            "type": "string"
          }
        },
        "required": [
          "id",
          "target",
          "category",
          "severity",
          "text",
          "workaround",
          "author_actor_id",
          "tags",
          "expires_at",
          "propagate_to_children",
          "status"
        ],
        "title": "CautionResponse",
        "type": "object"
      },
      "CautionRetireReason": {
        "description": "Closed reason enum for retire_caution slice.\n\nThree values mirror the `truncate_run` / `truncate_procedure`\nreason precedent. Free-form `reason: str` rejected: forces\nread-side analytics to bucket, and operators already pick from a\nsmall mental list.",
        "enum": [
          "Resolved",
          "NoLongerApplies",
          "WrongTarget"
        ],
        "title": "CautionRetireReason",
        "type": "string"
      },
      "CautionSeverity": {
        "description": "ANSI Z535 signal-word ladder, downshifted one notch.\n\nNo `Danger` tier -- formal lockout / regulated-blocking lives in\nSafety BC Clearance. The downshifted ladder is the explicit\nBC-boundary marker.\n\n  - `Notice`   -- informational FYI; default for asset-quirks ledger\n  - `Caution`  -- will not block, may bite; default for operator-discovered issue\n  - `Warning`  -- could harm work; reserve for things ESH would care about\n                  but hasn't formalised",
        "enum": [
          "Notice",
          "Caution",
          "Warning"
        ],
        "title": "CautionSeverity",
        "type": "string"
      },
      "CautionStatus": {
        "description": "The Caution's lifecycle state.\n\nThree values locked day one per [[project_caution_design]]:\n\n  - `Active`     -- currently in force; shows on Asset/Procedure view +\n                    future Run.start banner (11b-c)\n  - `Superseded` -- replaced by a newer caution; `superseded_by_caution_id`\n                    links to the child\n  - `Retired`    -- operator marked as resolved / no-longer-applies /\n                    wrong-target; closed-reason enum on `retired_reason`\n\n3-state lifecycle is the corpus convergence for operator-authored\nlightweight artifacts (KEDB minus Resolved, KCS Article minus\nApproval, Cority Observation). Heavy regulated lifecycles\n(CAPA 6-state, MIL-STD-882 7-state) are explicitly rejected per\nAnti-hooks.",
        "enum": [
          "Active",
          "Superseded",
          "Retired"
        ],
        "title": "CautionStatus",
        "type": "string"
      },
      "CautionSummaryDTO": {
        "description": "One caution in a paginated list.",
        "properties": {
          "author_actor_id": {
            "format": "uuid",
            "title": "Author Actor Id",
            "type": "string"
          },
          "category": {
            "$ref": "#/components/schemas/CautionCategory"
          },
          "caution_id": {
            "format": "uuid",
            "title": "Caution Id",
            "type": "string"
          },
          "expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Expires At"
          },
          "last_status_changed_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Status Changed At"
          },
          "parent_caution_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Caution Id"
          },
          "propagate_to_children": {
            "default": false,
            "title": "Propagate To Children",
            "type": "boolean"
          },
          "registered_at": {
            "format": "date-time",
            "title": "Registered At",
            "type": "string"
          },
          "retired_reason": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/CautionRetireReason"
              },
              {
                "type": "null"
              }
            ]
          },
          "severity": {
            "$ref": "#/components/schemas/CautionSeverity"
          },
          "status": {
            "$ref": "#/components/schemas/CautionStatus"
          },
          "superseded_by_caution_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Superseded By Caution Id"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "target": {
            "discriminator": {
              "mapping": {
                "Asset": "#/components/schemas/TargetAssetDTO",
                "Procedure": "#/components/schemas/TargetProcedureDTO"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/components/schemas/TargetAssetDTO"
              },
              {
                "$ref": "#/components/schemas/TargetProcedureDTO"
              }
            ],
            "title": "Target"
          },
          "text": {
            "maxLength": 2000,
            "title": "Text",
            "type": "string"
          },
          "workaround": {
            "maxLength": 2000,
            "title": "Workaround",
            "type": "string"
          }
        },
        "required": [
          "caution_id",
          "target",
          "category",
          "severity",
          "text",
          "workaround",
          "author_actor_id",
          "status",
          "registered_at"
        ],
        "title": "CautionSummaryDTO",
        "type": "object"
      },
      "CautionViewDTO": {
        "description": "Read-side DTO for one active Caution on the integration-view bundle.",
        "properties": {
          "category": {
            "title": "Category",
            "type": "string"
          },
          "caution_id": {
            "format": "uuid",
            "title": "Caution Id",
            "type": "string"
          },
          "severity": {
            "title": "Severity",
            "type": "string"
          },
          "text": {
            "title": "Text",
            "type": "string"
          }
        },
        "required": [
          "caution_id",
          "category",
          "severity",
          "text"
        ],
        "title": "CautionViewDTO",
        "type": "object"
      },
      "CheckInVisitRequest": {
        "description": "Body for `POST /visits/{visit_id}/check-in`.",
        "properties": {
          "actor_id": {
            "description": "Actor checking in to the Visit.",
            "format": "uuid",
            "title": "Actor Id",
            "type": "string"
          },
          "mode": {
            "$ref": "#/components/schemas/PresenceMode",
            "description": "physical (on-site) or remote (e.g., PI driving via API)."
          }
        },
        "required": [
          "actor_id",
          "mode"
        ],
        "title": "CheckInVisitRequest",
        "type": "object"
      },
      "CheckOutVisitRequest": {
        "description": "Body for `POST /visits/{visit_id}/check-out`.",
        "properties": {
          "actor_id": {
            "description": "Actor checking out of the Visit.",
            "format": "uuid",
            "title": "Actor Id",
            "type": "string"
          }
        },
        "required": [
          "actor_id"
        ],
        "title": "CheckOutVisitRequest",
        "type": "object"
      },
      "ChecksumRequest": {
        "description": "Bulk-content integrity hash on the registration request body.",
        "properties": {
          "algorithm": {
            "description": "Checksum algorithm name. Only 'sha256' is accepted today; the field is structured for forward-compatible algorithm addition (BLAKE3, SHA3, etc.).",
            "maxLength": 32,
            "minLength": 1,
            "title": "Algorithm",
            "type": "string"
          },
          "value": {
            "description": "Canonical checksum value. For sha256: exactly 64 lowercase hex chars.",
            "maxLength": 64,
            "minLength": 1,
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "algorithm",
          "value"
        ],
        "title": "ChecksumRequest",
        "type": "object"
      },
      "ChecksumResponse": {
        "description": "Bulk-content integrity hash on the response body.",
        "properties": {
          "algorithm": {
            "title": "Algorithm",
            "type": "string"
          },
          "value": {
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "algorithm",
          "value"
        ],
        "title": "ChecksumResponse",
        "type": "object"
      },
      "ClearanceKind": {
        "description": "The form-type (template) this Clearance wraps.\n\nTen facility-independent form-types covering the 9 surveyed facilities\n(cross-facility portability research v3 at /tmp/cora_hazard_research_v3.md).\nFacility identity is carried separately via `Clearance.facility_asset_id`\n(a reference to the `Asset.Level.Site` for the facility); it is NOT\nsmushed into the kind value.\n\n  - `ESAF`     -- Experiment Safety Assessment Form (used by APS + ALS;\n                  facility distinguishes between the two)\n  - `SAF`      -- Safety Approval Form (used by NSLS-II + ESRF)\n  - `AForm`    -- ESRF A-form (per-proposal)\n  - `DUO`      -- MAX IV DUO (proposal-level)\n  - `ESRA`     -- MAX IV ESRA (per-experiment, pairs with DUO)\n  - `ERA`      -- DLS Experiment Risk Assessment (per-session)\n  - `PLHD`     -- DLS Personal Lab Hazard Declaration (per-lab-visit)\n  - `DOOR`     -- DESY DOOR (per-beamtime)\n  - `BTR`      -- SLAC Beam Time Request safety review\n  - `Form9`    -- SPring-8 Form 9 (per-visit)\n\n`(kind=ESAF, facility_asset_id=<APS_SITE_UUID>)` and\n`(kind=ESAF, facility_asset_id=<ALS_SITE_UUID>)` are distinct without\nstring-mangling. Two orthogonal concepts (form-type + facility) are\ncleanly separated; no `ESAF_APS` / `ESAF_ALS` smush.\n\nPer [[project_safety_clearance_design]] \u00a7\"Facility lives at\nAsset.Level.Site\": facility identity is the existing Asset.Site in\nCORA's Equipment hierarchy, NOT a parallel StrEnum. Adding a new\nfacility requires no enum change at all (just register a new Site\nAsset). A 13th form-type (rare; the global form-type registry is\nrelatively stable) lands as a new enum member.\n\nFuture ClearanceTemplate aggregate (watch item): when first\nper-template body-schema-validation OR template-versioning need\nsurfaces, this StrEnum gets replaced by `template_id: UUID`\nreferencing a typed `ClearanceTemplate` aggregate (mirrors Family\ninside Equipment BC). Same trigger pattern as ProcedureTemplate watch\nitem in [[project_operation_design]].",
        "enum": [
          "ESAF",
          "SAF",
          "AForm",
          "DUO",
          "ESRA",
          "ERA",
          "PLHD",
          "DOOR",
          "BTR",
          "Form9"
        ],
        "title": "ClearanceKind",
        "type": "string"
      },
      "ClearanceListResponse": {
        "description": "Page of clearances plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/ClearanceSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "ClearanceListResponse",
        "type": "object"
      },
      "ClearanceResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives + JSON dicts (for polymorphic fields), not domain\nVOs. Decouples the wire format from the domain model so the two can\nevolve independently.\n\n`bindings`, `declarations` are JSON lists; each item is a dict whose\n`kind` discriminator selects the variant shape (Subject / Asset /\nRun / Procedure / External for bindings; NFPA704 / RiskBand / GHS /\nScheme for classifications).",
        "properties": {
          "bindings": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "title": "Bindings",
            "type": "array"
          },
          "declarations": {
            "items": {
              "additionalProperties": true,
              "type": "object"
            },
            "title": "Declarations",
            "type": "array"
          },
          "external_id": {
            "anyOf": [
              {
                "maxLength": 100,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "External Id"
          },
          "facility_asset_id": {
            "format": "uuid",
            "title": "Facility Asset Id",
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/ClearanceKind"
          },
          "next_review_due_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Review Due At"
          },
          "parent_clearance_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Clearance Id"
          },
          "review_steps": {
            "items": {
              "$ref": "#/components/schemas/ReviewStepResponse"
            },
            "title": "Review Steps",
            "type": "array"
          },
          "risk_band": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RiskBand"
              },
              {
                "type": "null"
              }
            ]
          },
          "status": {
            "$ref": "#/components/schemas/ClearanceStatus"
          },
          "title": {
            "maxLength": 200,
            "title": "Title",
            "type": "string"
          },
          "valid_from": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Valid From"
          },
          "valid_until": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Valid Until"
          }
        },
        "required": [
          "id",
          "kind",
          "facility_asset_id",
          "title",
          "bindings",
          "declarations",
          "review_steps",
          "status"
        ],
        "title": "ClearanceResponse",
        "type": "object"
      },
      "ClearanceStatus": {
        "description": "The Clearance's lifecycle state.\n\nEight values locked day one per [[project_safety_clearance_design]]:\n\n  - `Defined`     -- registered, not yet submitted\n  - `Submitted`   -- awaiting first reviewer pickup\n  - `UnderReview` -- >=1 reviewer step recorded but not terminal\n  - `Approved`    -- all required reviewers approved; not yet effective\n  - `Active`      -- currently in force; gates Run.start / Procedure.start\n  - `Expired`     -- validity window passed OR explicit expire_clearance\n  - `Rejected`    -- terminal-bad\n  - `Superseded`  -- replaced by amended child (parent_clearance_id link)\n\nCompresses the union of all 9 surveyed facility-form lifecycles per\nthe cross-facility portability research (v3 pass). Multi-step review\nrolls up into UnderReview + the `review_steps: tuple[...]` field; no\nfacility introduces states outside this set.",
        "enum": [
          "Defined",
          "Submitted",
          "UnderReview",
          "Approved",
          "Active",
          "Expired",
          "Rejected",
          "Superseded"
        ],
        "title": "ClearanceStatus",
        "type": "string"
      },
      "ClearanceSummaryDTO": {
        "description": "One clearance in a paginated list.",
        "properties": {
          "bindings": {
            "$ref": "#/components/schemas/BindingsByKind"
          },
          "clearance_id": {
            "format": "uuid",
            "title": "Clearance Id",
            "type": "string"
          },
          "external_id": {
            "anyOf": [
              {
                "maxLength": 100,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "External Id"
          },
          "facility_asset_id": {
            "format": "uuid",
            "title": "Facility Asset Id",
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/ClearanceKind"
          },
          "last_reviewed_by_actor_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Reviewed By Actor Id"
          },
          "last_status_changed_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Status Changed At"
          },
          "last_status_reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Status Reason"
          },
          "next_review_due_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Review Due At"
          },
          "parent_clearance_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Clearance Id"
          },
          "registered_at": {
            "format": "date-time",
            "title": "Registered At",
            "type": "string"
          },
          "risk_band": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RiskBand"
              },
              {
                "type": "null"
              }
            ]
          },
          "status": {
            "$ref": "#/components/schemas/ClearanceStatus"
          },
          "title": {
            "maxLength": 200,
            "title": "Title",
            "type": "string"
          },
          "valid_from": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Valid From"
          },
          "valid_until": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Valid Until"
          }
        },
        "required": [
          "clearance_id",
          "kind",
          "facility_asset_id",
          "title",
          "status",
          "bindings",
          "registered_at"
        ],
        "title": "ClearanceSummaryDTO",
        "type": "object"
      },
      "CompleteSealRepublishingBody": {
        "additionalProperties": false,
        "description": "Optional complete-seal-republishing request body.",
        "properties": {
          "new_head_hash": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "SHA-256 (lowercase hex) of the fresh head pointer published at the close of the republish window. Must be supplied together with new_sequence_number or omitted together (the back-edge carries both or neither).",
            "title": "New Head Hash"
          },
          "new_sequence_number": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Monotonic sequence for the fresh head pointer. Strictly greater than the current sequence number. Must be supplied together with new_head_hash or omitted together.",
            "title": "New Sequence Number"
          }
        },
        "title": "CompleteSealRepublishingBody",
        "type": "object"
      },
      "ConductProcedureRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /procedures/{procedure_id}/conduct`.",
        "properties": {
          "steps": {
            "description": "Steps the Conductor walks in order (0-500). Empty list is valid: start + complete fire with no steps.",
            "items": {
              "discriminator": {
                "mapping": {
                  "action": "#/components/schemas/_ActionStepRequest",
                  "check": "#/components/schemas/_CheckStepRequest",
                  "setpoint": "#/components/schemas/_SetpointStepRequest"
                },
                "propertyName": "kind"
              },
              "oneOf": [
                {
                  "$ref": "#/components/schemas/_SetpointStepRequest"
                },
                {
                  "$ref": "#/components/schemas/_ActionStepRequest"
                },
                {
                  "$ref": "#/components/schemas/_CheckStepRequest"
                }
              ]
            },
            "maxItems": 500,
            "title": "Steps",
            "type": "array"
          }
        },
        "title": "ConductProcedureRequest",
        "type": "object"
      },
      "ConductProcedureResponse": {
        "description": "Response body for the conduct_procedure slice.\n\n`succeeded` is the canonical pass/fail bit; `failure` is non-null\niff `succeeded` is False. `completed_count` is informational\n(the number of steps that ran successfully before halt, if any).",
        "properties": {
          "completed_count": {
            "title": "Completed Count",
            "type": "integer"
          },
          "failure": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/_ConductorFailureResponse"
              },
              {
                "type": "null"
              }
            ]
          },
          "procedure_id": {
            "format": "uuid",
            "title": "Procedure Id",
            "type": "string"
          },
          "succeeded": {
            "title": "Succeeded",
            "type": "boolean"
          }
        },
        "required": [
          "procedure_id",
          "completed_count",
          "succeeded"
        ],
        "title": "ConductProcedureResponse",
        "type": "object"
      },
      "ConduitListResponse": {
        "description": "Page of conduits plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/ConduitSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "ConduitListResponse",
        "type": "object"
      },
      "ConduitSummaryDTO": {
        "description": "One conduit in a paginated list.",
        "properties": {
          "conduit_id": {
            "format": "uuid",
            "title": "Conduit Id",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "source_zone_id": {
            "format": "uuid",
            "title": "Source Zone Id",
            "type": "string"
          },
          "target_zone_id": {
            "format": "uuid",
            "title": "Target Zone Id",
            "type": "string"
          }
        },
        "required": [
          "conduit_id",
          "name",
          "source_zone_id",
          "target_zone_id",
          "created_at"
        ],
        "title": "ConduitSummaryDTO",
        "type": "object"
      },
      "CredentialListResponse": {
        "description": "Page of credentials plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/CredentialSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "CredentialListResponse",
        "type": "object"
      },
      "CredentialPurpose": {
        "description": "The role this credential plays in federation traffic.\n\nSix arms locked at Memo 1 Q4:\n\n  - `Signing`: credential signs outbound payloads (manifests,\n    receipts, attestations).\n  - `Verification`: credential verifies inbound payloads signed\n    by peer facilities.\n  - `Authentication`: credential authenticates the local facility\n    to a peer at connection setup.\n  - `Encryption`: credential decrypts inbound payloads or wraps\n    outbound payloads.\n  - `SealOnlineSigning`: credential signs seal pointer updates\n    online; rotated frequently. Referenced by\n    `Seal.online_credential_id`.\n  - `SealOfflineRoot`: credential is the offline root of trust\n    for the seal; rotated rarely and held offline. Referenced by\n    `Seal.offline_credential_id`.\n\nThe two seal purposes are distinct enum values rather than a\nsingle `Seal` arm so the `verify_key_separation` guard in `Seal`\ncan statically detect a same-purpose collision between the\nonline and offline references.",
        "enum": [
          "Signing",
          "Verification",
          "Authentication",
          "Encryption",
          "SealOnlineSigning",
          "SealOfflineRoot"
        ],
        "title": "CredentialPurpose",
        "type": "string"
      },
      "CredentialResponse": {
        "description": "Read-side DTO at the API boundary.\n\nMirrors the Credential aggregate state plus projection-sourced\nlifecycle timestamps. `secret_ref` / `public_material_ref` /\n`rotation_pending_*_ref` are OPAQUE pointers (URI / KMS ARN /\nvault path), never raw secret material.\n\nPer Path C, `registered_at` / `rotation_started_at` are\nprojection-sourced (not aggregate state) and therefore nullable\non the wire: the projection may transiently lag behind the event\nstore, or the deps may lack a configured pool (in-memory test\nmode). Mirrors the Calibration / Method / Plan read DTOs.",
        "properties": {
          "audience": {
            "title": "Audience",
            "type": "string"
          },
          "expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Expires At"
          },
          "facility_id": {
            "title": "Facility Id",
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "public_material_ref": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Public Material Ref"
          },
          "purpose": {
            "$ref": "#/components/schemas/CredentialPurpose"
          },
          "registered_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Registered At"
          },
          "registered_by_actor_id": {
            "format": "uuid",
            "title": "Registered By Actor Id",
            "type": "string"
          },
          "rotation_pending_public_material_ref": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Rotation Pending Public Material Ref"
          },
          "rotation_pending_secret_ref": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Rotation Pending Secret Ref"
          },
          "rotation_started_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Rotation Started At"
          },
          "secret_ref": {
            "title": "Secret Ref",
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/CredentialStatus"
          }
        },
        "required": [
          "id",
          "facility_id",
          "audience",
          "purpose",
          "secret_ref",
          "public_material_ref",
          "expires_at",
          "registered_by_actor_id",
          "rotation_pending_secret_ref",
          "rotation_pending_public_material_ref",
          "status"
        ],
        "title": "CredentialResponse",
        "type": "object"
      },
      "CredentialStatus": {
        "description": "Lifecycle status of a Credential.\n\nThree values per Memo 1:\n\n  - `Active`: credential is in service for its declared purpose.\n  - `Rotating`: a rotation is in flight; the aggregate now\n    carries both the current and a pending pair of refs.\n    Completes back to `Active` (promoting pending to current)\n    or aborts back to `Active` (discarding pending).\n  - `Revoked`: terminal. The credential is no longer usable; no\n    further transitions are valid.",
        "enum": [
          "Active",
          "Rotating",
          "Revoked"
        ],
        "title": "CredentialStatus",
        "type": "string"
      },
      "CredentialSummaryDTO": {
        "description": "One credential in a paginated list. Opaque refs intentionally omitted.",
        "properties": {
          "audience": {
            "title": "Audience",
            "type": "string"
          },
          "credential_id": {
            "format": "uuid",
            "title": "Credential Id",
            "type": "string"
          },
          "expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Expires At"
          },
          "facility_id": {
            "title": "Facility Id",
            "type": "string"
          },
          "purpose": {
            "$ref": "#/components/schemas/CredentialPurpose"
          },
          "registered_at": {
            "format": "date-time",
            "title": "Registered At",
            "type": "string"
          },
          "revoked_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Revoked At"
          },
          "rotation_started_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Rotation Started At"
          },
          "status": {
            "$ref": "#/components/schemas/CredentialStatus"
          }
        },
        "required": [
          "credential_id",
          "facility_id",
          "audience",
          "purpose",
          "status",
          "registered_at"
        ],
        "title": "CredentialSummaryDTO",
        "type": "object"
      },
      "DatasetListResponse": {
        "description": "Page of datasets plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/DatasetSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "DatasetListResponse",
        "type": "object"
      },
      "DatasetResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model. `conforms_to` and `derived_from` are\nsorted lists in the wire shape (matches the on-the-wire event\npayload sort).",
        "properties": {
          "byte_size": {
            "title": "Byte Size",
            "type": "integer"
          },
          "checksum": {
            "$ref": "#/components/schemas/ChecksumResponse"
          },
          "derived_from": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Derived From",
            "type": "array"
          },
          "encoding": {
            "$ref": "#/components/schemas/EncodingResponse"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "producing_run_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Producing Run Id"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "subject_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subject Id"
          },
          "uri": {
            "title": "Uri",
            "type": "string"
          }
        },
        "required": [
          "id",
          "name",
          "uri",
          "checksum",
          "byte_size",
          "encoding",
          "producing_run_id",
          "subject_id",
          "derived_from",
          "status"
        ],
        "title": "DatasetResponse",
        "type": "object"
      },
      "DatasetSummaryDTO": {
        "description": "One dataset in a paginated list.",
        "properties": {
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "dataset_id": {
            "format": "uuid",
            "title": "Dataset Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "producing_run_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Producing Run Id"
          },
          "status": {
            "enum": [
              "Registered",
              "Discarded"
            ],
            "title": "Status",
            "type": "string"
          },
          "subject_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subject Id"
          },
          "uri": {
            "maxLength": 2048,
            "title": "Uri",
            "type": "string"
          }
        },
        "required": [
          "dataset_id",
          "name",
          "uri",
          "producing_run_id",
          "subject_id",
          "status",
          "created_at"
        ],
        "title": "DatasetSummaryDTO",
        "type": "object"
      },
      "DecisionConfidenceSource": {
        "description": "How the `confidence` value was computed.\n\nISO 42001 audit asks 'how was this confidence derived?'; this\nenum is the answer. Stored alongside the float so consumers can\ndistinguish calibrated probabilistic estimates from\nself-reported model claims.\n\nValues:\n  - `self_reported`: AI decider's own confidence claim, as a\n    learned linguistic pattern. NOT a posterior probability.\n    Lowest audit weight.\n  - `logprob`: derived from token log-probabilities. Closer to\n    a calibrated estimate but still model-internal.\n  - `ensemble`: aggregated over multiple deciders / runs /\n    models. Higher audit weight; carries the implicit promise\n    of uncertainty quantification.\n  - `human`: subjective human confidence rating. Audit-weight\n    is operator-dependent; treat as direction-of-confidence,\n    not a probability.",
        "enum": [
          "self_reported",
          "logprob",
          "ensemble",
          "human"
        ],
        "title": "DecisionConfidenceSource",
        "type": "string"
      },
      "DecisionListResponse": {
        "description": "Page of decisions plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/DecisionSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "DecisionListResponse",
        "type": "object"
      },
      "DecisionRating": {
        "description": "How an operator rates a Decision after the fact.",
        "enum": [
          "useful",
          "misleading",
          "ignored"
        ],
        "title": "DecisionRating",
        "type": "string"
      },
      "DecisionResponse": {
        "description": "Read-side DTO at the API boundary.\n\n`confidence_band` is a derived field (Low / Medium / High /\nCertain) computed at read time from the stored `confidence`\nfloat. Never stored; one source of truth = the float.\nReturns null when confidence is null.",
        "properties": {
          "actor_id": {
            "format": "uuid",
            "title": "Actor Id",
            "type": "string"
          },
          "alternatives": {
            "items": {
              "type": "string"
            },
            "title": "Alternatives",
            "type": "array"
          },
          "choice": {
            "maxLength": 500,
            "title": "Choice",
            "type": "string"
          },
          "confidence": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Confidence"
          },
          "confidence_band": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Confidence Band"
          },
          "confidence_source": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Confidence Source"
          },
          "context": {
            "maxLength": 100,
            "title": "Context",
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "inputs": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "title": "Inputs"
          },
          "override_kind": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Override Kind"
          },
          "parent_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          },
          "reasoning": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reasoning"
          },
          "reasoning_signature": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reasoning Signature"
          },
          "rule": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Rule"
          }
        },
        "required": [
          "id",
          "actor_id",
          "context",
          "choice",
          "parent_id",
          "override_kind",
          "rule",
          "reasoning",
          "confidence",
          "confidence_source",
          "confidence_band",
          "alternatives",
          "inputs",
          "reasoning_signature"
        ],
        "title": "DecisionResponse",
        "type": "object"
      },
      "DecisionSummaryDTO": {
        "description": "One decision in a paginated list.",
        "properties": {
          "actor_id": {
            "format": "uuid",
            "title": "Actor Id",
            "type": "string"
          },
          "choice": {
            "title": "Choice",
            "type": "string"
          },
          "confidence": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Confidence"
          },
          "confidence_band": {
            "anyOf": [
              {
                "enum": [
                  "Low",
                  "Medium",
                  "High",
                  "Certain"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Confidence Band"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "decision_id": {
            "format": "uuid",
            "title": "Decision Id",
            "type": "string"
          },
          "parent_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Id"
          },
          "rule": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Rule"
          }
        },
        "required": [
          "decision_id",
          "actor_id",
          "rule",
          "parent_id",
          "confidence",
          "confidence_band",
          "choice",
          "created_at"
        ],
        "title": "DecisionSummaryDTO",
        "type": "object"
      },
      "DecommissionFrameRequest": {
        "description": "Body for `DELETE /frames/{frame_id}`.",
        "properties": {
          "reason": {
            "description": "Operator-supplied free-text reason captured on the FrameDecommissioned event payload for audit.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DecommissionFrameRequest",
        "type": "object"
      },
      "DecommissionMountRequest": {
        "properties": {
          "reason": {
            "description": "Operator-supplied free-text reason for the audit log.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DecommissionMountRequest",
        "type": "object"
      },
      "DefineAgentRequest": {
        "description": "Body for `POST /agents`.",
        "properties": {
          "canonical_uri": {
            "anyOf": [
              {
                "maxLength": 2000,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional canonical https URI for A2A forward-compat. Must start with `https://` and contain no fragment.",
            "title": "Canonical Uri"
          },
          "capabilities": {
            "description": "Optional capability claims. Each entry 1-100 chars after trim. Empty list IS allowed.",
            "items": {
              "type": "string"
            },
            "maxItems": 32,
            "title": "Capabilities",
            "type": "array"
          },
          "description": {
            "anyOf": [
              {
                "maxLength": 2000,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional free-form description.",
            "title": "Description"
          },
          "kind": {
            "description": "Free-form agent kind discriminator (bare-str at MVP per Supply.kind precedent). Day-1 example: `RunDebriefer`.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Kind",
            "type": "string"
          },
          "model_ref": {
            "$ref": "#/components/schemas/ModelRefRequest",
            "description": "Required model identity (provider + model + optional snapshot pin) so 8f-b's LLM has the identity available immediately."
          },
          "name": {
            "description": "Human-readable display name.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "prompt_template_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional UUID into the Python module registry at `cora.agent.prompts`. Required by the RunDebriefer subscriber; may be null until the registry ships its first template.",
            "title": "Prompt Template Id"
          },
          "version": {
            "description": "Semver-like version identifier (`v1`, `1.0.0`, etc.).",
            "maxLength": 50,
            "minLength": 1,
            "title": "Version",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "name",
          "version",
          "model_ref"
        ],
        "title": "DefineAgentRequest",
        "type": "object"
      },
      "DefineAgentResponse": {
        "description": "Response body for `POST /agents`.",
        "properties": {
          "agent_id": {
            "format": "uuid",
            "title": "Agent Id",
            "type": "string"
          }
        },
        "required": [
          "agent_id"
        ],
        "title": "DefineAgentResponse",
        "type": "object"
      },
      "DefineAssemblyRequest": {
        "description": "Body for `POST /assemblies`.",
        "properties": {
          "drawing": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DrawingBody"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional engineering reference for the assembly itself. Excluded from the content_hash."
          },
          "name": {
            "description": "Human-readable display name. Non-unique; the UUID is the storage identity and the content_hash is the structural fingerprint.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "parameter_overrides_schema": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional JSON Schema subset declaring the shape of parameter_overrides accepted at instantiation.",
            "title": "Parameter Overrides Schema"
          },
          "presents_as_family_id": {
            "description": "FamilyId the instantiated Assembly stands in for at the Method.needed_families satisfaction boundary.",
            "format": "uuid",
            "title": "Presents As Family Id",
            "type": "string"
          },
          "required_slots": {
            "description": "Slots that compose this Assembly. Each names a slot (canonical identity within the blueprint), the FamilyIds any filling Asset must include, the cardinality, and optional template defaults. Wire shape is a list; duplicates collapse when the route handler converts to the domain frozenset.",
            "items": {
              "$ref": "#/components/schemas/TemplateSlotBody"
            },
            "title": "Required Slots",
            "type": "array"
          },
          "required_wires": {
            "description": "Slot-to-slot signal wires inside the Assembly, keyed by slot_name (NOT Asset UUID). Endpoints must reference slots declared in required_slots. Wire shape is a list; duplicates collapse when the route handler converts to the domain frozenset.",
            "items": {
              "$ref": "#/components/schemas/TemplateWireBody"
            },
            "title": "Required Wires",
            "type": "array"
          },
          "version": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Operator-curated free-form version label (no SemVer enforcement). Excluded from the content_hash.",
            "title": "Version"
          }
        },
        "required": [
          "name",
          "presents_as_family_id"
        ],
        "title": "DefineAssemblyRequest",
        "type": "object"
      },
      "DefineAssemblyResponse": {
        "description": "Response body for `POST /assemblies`.",
        "properties": {
          "assembly_id": {
            "format": "uuid",
            "title": "Assembly Id",
            "type": "string"
          }
        },
        "required": [
          "assembly_id"
        ],
        "title": "DefineAssemblyResponse",
        "type": "object"
      },
      "DefineCalibrationRequest": {
        "description": "Body for `POST /calibrations`.",
        "properties": {
          "description": {
            "anyOf": [
              {
                "maxLength": 2000,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional operator-prose notes (0-2000 chars after trim). Empty / whitespace-only collapses to None at the slice boundary. Matches Method/Plan/Family description precedent.",
            "title": "Description"
          },
          "operating_point": {
            "additionalProperties": true,
            "description": "JSON-shaped dict describing the operating regime (energy, optics_config, etc.). Validated STRICT against the quantity's operating_point_schema (`additionalProperties: False`; primitive types only). Identity-tuple uniqueness `(target_id, quantity, operating_point)` enforced via Postgres jsonb UNIQUE on the projection (Q6 lock: key-order normalized + numeric value-equality `25 == 25.0`).",
            "title": "Operating Point",
            "type": "object"
          },
          "quantity": {
            "$ref": "#/components/schemas/CalibrationQuantity",
            "description": "The physical quantity being calibrated. Closed catalog at `cora.calibration.quantities`; each value has registered operating_point + value JSON Schemas. Adding a quantity = PR. Day-one set: rotation_center, detector_pixel_size."
          },
          "target_id": {
            "description": "What this calibration is OF (typically the Asset id whose behavior is being measured, for example the rotary stage whose rotation-axis center is being tracked). Bare UUID reference; existence NOT verified at the write path per the cross-BC eventual-consistency stance.",
            "format": "uuid",
            "title": "Target Id",
            "type": "string"
          }
        },
        "required": [
          "target_id",
          "quantity",
          "operating_point"
        ],
        "title": "DefineCalibrationRequest",
        "type": "object"
      },
      "DefineCalibrationResponse": {
        "description": "Response body for `POST /calibrations`.",
        "properties": {
          "calibration_id": {
            "format": "uuid",
            "title": "Calibration Id",
            "type": "string"
          }
        },
        "required": [
          "calibration_id"
        ],
        "title": "DefineCalibrationResponse",
        "type": "object"
      },
      "DefineCapabilityRequest": {
        "description": "Body for `POST /capabilities`.",
        "properties": {
          "code": {
            "description": "Namespaced machine-readable code under `cora.capability.*` (closed core) or `cora.capability.<facility>.*` (namespaced facility extension). Immutable across versions.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Code",
            "type": "string"
          },
          "description": {
            "anyOf": [
              {
                "maxLength": 2000,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional human description (0-2000 chars).",
            "title": "Description"
          },
          "executor_shapes": {
            "description": "Closed-enum set of executor kinds that may implement this Capability ({Method, Procedure} at v1). Required non-empty (a Capability with no executor kinds has no operational meaning). Deduplicated server-side.",
            "items": {
              "$ref": "#/components/schemas/ExecutorShape"
            },
            "title": "Executor Shapes",
            "type": "array"
          },
          "name": {
            "description": "Display name for the new Capability.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "parameters_schema": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional declarative JSON Schema for the parameter contract. Method.parameters_schema (6g) must validate as a subset of this contract at define_method time per the cross-BC STRICT anchor. Constrained subset: $schema/type/required/properties/enum/minimum/maximum/pattern allowed; $ref/oneOf/anyOf/allOf/not forbidden.",
            "title": "Parameters Schema"
          },
          "required_affordances": {
            "description": "Family.affordance contract any implementer must satisfy. Required at definition time; supply `[]` explicitly when the Capability is parameter-driven without an affordance requirement. Deduplicated server-side.",
            "items": {
              "$ref": "#/components/schemas/Affordance"
            },
            "title": "Required Affordances",
            "type": "array"
          }
        },
        "required": [
          "code",
          "name",
          "required_affordances",
          "executor_shapes"
        ],
        "title": "DefineCapabilityRequest",
        "type": "object"
      },
      "DefineCapabilityResponse": {
        "description": "Response body for `POST /capabilities`.",
        "properties": {
          "capability_id": {
            "format": "uuid",
            "title": "Capability Id",
            "type": "string"
          }
        },
        "required": [
          "capability_id"
        ],
        "title": "DefineCapabilityResponse",
        "type": "object"
      },
      "DefineConduitRequest": {
        "description": "Body for `POST /conduits`.",
        "properties": {
          "name": {
            "description": "Display name for the new conduit.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "source_zone_id": {
            "description": "UUID of the source endpoint Zone (not validated for existence).",
            "format": "uuid",
            "title": "Source Zone Id",
            "type": "string"
          },
          "target_zone_id": {
            "description": "UUID of the target endpoint Zone (not validated for existence).",
            "format": "uuid",
            "title": "Target Zone Id",
            "type": "string"
          }
        },
        "required": [
          "name",
          "source_zone_id",
          "target_zone_id"
        ],
        "title": "DefineConduitRequest",
        "type": "object"
      },
      "DefineConduitResponse": {
        "description": "Response body for `POST /conduits`.",
        "properties": {
          "conduit_id": {
            "format": "uuid",
            "title": "Conduit Id",
            "type": "string"
          }
        },
        "required": [
          "conduit_id"
        ],
        "title": "DefineConduitResponse",
        "type": "object"
      },
      "DefineFamilyRequest": {
        "description": "Body for `POST /families`.",
        "properties": {
          "affordances": {
            "description": "Closed-enum set of device-level operational primitives this Family supports. Required at definition time: supply `[]` explicitly when no v1 Affordance applies. Deduplicated server-side. See `cora.equipment.aggregates.family.affordance.Affordance` for the 28-item closed enum and the 3-pattern rule.",
            "items": {
              "$ref": "#/components/schemas/Affordance"
            },
            "title": "Affordances",
            "type": "array"
          },
          "name": {
            "description": "Display name for the new Family.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name",
          "affordances"
        ],
        "title": "DefineFamilyRequest",
        "type": "object"
      },
      "DefineFamilyResponse": {
        "description": "Response body for `POST /families`.",
        "properties": {
          "family_id": {
            "format": "uuid",
            "title": "Family Id",
            "type": "string"
          }
        },
        "required": [
          "family_id"
        ],
        "title": "DefineFamilyResponse",
        "type": "object"
      },
      "DefineMethodRequest": {
        "description": "Body for `POST /methods`.\n\n`needed_family_ids` is required (use `[]` for procedural\nMethods that need no specific equipment family). Eventual-\nconsistency: each Family id is NOT verified against the\nFamily stream; mismatch surfaces at Plan binding.\n\n`needed_supplies` is optional; defaults to `[]` for\nbackward-compat (older clients keep working). Each element is\na Supply.kind STRING (1-50 chars), NOT a Supply instance UUID.\nAsymmetric vs needed_family_ids by design; see\n[[project_supply_design]] \u00a7\"Method.needed_supplies consumer\"\nfor the rationale (Family is TYPE registry,\nSupply is INSTANCE aggregate per facility sharing a `kind` label).\nEventual-consistency: kind strings are NOT verified against the\nSupply stream; mismatch surfaces at Plan binding.",
        "properties": {
          "capability_id": {
            "description": "Universal Capability template (Recipe BC) this Method realizes as a Method-shaped executor. REQUIRED per Pattern P. The bound Capability must declare `Method` in its executor_shapes set; otherwise 409. Eventual-consistency: the Capability stream is loaded at handler time, not API-boundary time; a missing stream surfaces as 404 via CapabilityNotFoundError.",
            "format": "uuid",
            "title": "Capability Id",
            "type": "string"
          },
          "name": {
            "description": "Display name for the new method.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "needed_assembly_ids": {
            "description": "Equipment Assembly ids this Method requires (composition blueprints). May be empty (Method needs only Assets satisfying needed_family_ids, no specific composition). Eventual-consistency: ids are NOT verified against the Assembly stream at decide time; mismatch surfaces at Plan binding when the picked Assets must include Fixtures whose assembly_id covers this set.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Needed Assembly Ids",
            "type": "array"
          },
          "needed_family_ids": {
            "description": "Family ids this Method requires. May be empty. Eventual-consistency: ids are NOT verified against the Family stream at decide time; mismatch surfaces at Plan binding.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Needed Family Ids",
            "type": "array"
          },
          "needed_supplies": {
            "description": "Supply.kind strings this Method requires. May be empty. Each element is a kind label (for example 'PhotonBeam', 'LiquidNitrogen', 'ComputePool'), 1-50 chars. NOT Supply instance UUIDs; Methods are facility-portable. Eventual-consistency: kinds are NOT verified against the Supply stream at decide time; mismatch surfaces at Plan binding.",
            "items": {
              "maxLength": 50,
              "minLength": 1,
              "type": "string"
            },
            "title": "Needed Supplies",
            "type": "array"
          }
        },
        "required": [
          "name",
          "capability_id",
          "needed_family_ids"
        ],
        "title": "DefineMethodRequest",
        "type": "object"
      },
      "DefineMethodResponse": {
        "description": "Response body for `POST /methods`.",
        "properties": {
          "method_id": {
            "format": "uuid",
            "title": "Method Id",
            "type": "string"
          }
        },
        "required": [
          "method_id"
        ],
        "title": "DefineMethodResponse",
        "type": "object"
      },
      "DefineModelRequest": {
        "description": "Body for `POST /models`.",
        "properties": {
          "declared_family_ids": {
            "description": "Family ids the catalog entry satisfies. At least one required; deduplicated server-side.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "minItems": 1,
            "title": "Declared Family Ids",
            "type": "array"
          },
          "manufacturer": {
            "$ref": "#/components/schemas/ManufacturerBody",
            "description": "Vendor identity (name plus optional ROR/GRID/ISNI identifier)."
          },
          "name": {
            "description": "Display name for the new Model.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "part_number": {
            "description": "Vendor SKU; case-sensitive (RV120CCHL and rv120cchl are different Newport entries).",
            "maxLength": 100,
            "minLength": 1,
            "title": "Part Number",
            "type": "string"
          },
          "version_tag": {
            "anyOf": [
              {
                "maxLength": 50,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional initial revision label (e.g., 'rev-A').",
            "title": "Version Tag"
          }
        },
        "required": [
          "name",
          "manufacturer",
          "part_number",
          "declared_family_ids"
        ],
        "title": "DefineModelRequest",
        "type": "object"
      },
      "DefineModelResponse": {
        "description": "Response body for `POST /models`.",
        "properties": {
          "model_id": {
            "format": "uuid",
            "title": "Model Id",
            "type": "string"
          }
        },
        "required": [
          "model_id"
        ],
        "title": "DefineModelResponse",
        "type": "object"
      },
      "DefinePermitRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /federation/permits`.",
        "properties": {
          "abi_tier_floor": {
            "$ref": "#/components/schemas/AbiTier",
            "description": "Lowest tier the permit honors on either side."
          },
          "allowed_artifact_kinds": {
            "description": "Artifact-kind strings honored on either side of the relationship.",
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "title": "Allowed Artifact Kinds",
            "type": "array"
          },
          "allowed_credential_ids": {
            "description": "Bounded set of Credential ids permitted under this permit.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "minItems": 1,
            "title": "Allowed Credential Ids",
            "type": "array"
          },
          "allowed_payload_types": {
            "description": "Payload-type strings honored on either side of the relationship.",
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "title": "Allowed Payload Types",
            "type": "array"
          },
          "direction": {
            "$ref": "#/components/schemas/Direction",
            "description": "Query-convenience discriminator mirroring `type(terms)`. Must match the `terms.kind` arm; the decider rejects mismatches."
          },
          "expires_at": {
            "description": "Contractual upper bound; must lie strictly after the server's now.",
            "format": "date-time",
            "title": "Expires At",
            "type": "string"
          },
          "peer_facility_id": {
            "description": "Opaque string id of the peer facility. Federation peers are external entities; CORA does NOT mint their ids.",
            "minLength": 1,
            "title": "Peer Facility Id",
            "type": "string"
          },
          "terms": {
            "description": "Direction-specific contractual fields. Discriminated by `kind`: `Outbound` carries `scopes` + `read_scope` + `onward_action_scope`; `Inbound` carries `inbound_allowed_artifact_kinds` + optional canonicalization / receipt config.",
            "discriminator": {
              "mapping": {
                "Inbound": "#/components/schemas/_InboundTermsRequest",
                "Outbound": "#/components/schemas/_OutboundTermsRequest"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/components/schemas/_OutboundTermsRequest"
              },
              {
                "$ref": "#/components/schemas/_InboundTermsRequest"
              }
            ],
            "title": "Terms"
          }
        },
        "required": [
          "peer_facility_id",
          "direction",
          "allowed_credential_ids",
          "allowed_payload_types",
          "allowed_artifact_kinds",
          "abi_tier_floor",
          "expires_at",
          "terms"
        ],
        "title": "DefinePermitRequest",
        "type": "object"
      },
      "DefinePermitResponse": {
        "description": "Response body for `POST /federation/permits`.",
        "properties": {
          "permit_id": {
            "format": "uuid",
            "title": "Permit Id",
            "type": "string"
          }
        },
        "required": [
          "permit_id"
        ],
        "title": "DefinePermitResponse",
        "type": "object"
      },
      "DefinePlanRequest": {
        "description": "Body for `POST /plans`.",
        "properties": {
          "asset_ids": {
            "description": "Set of Asset ids this Plan binds. Multi-asset binding; at least one required. Each Asset's existence verified at handler-load time (missing \u2192 404); decider checks no Asset is Decommissioned (\u2192 409) and that the union of bound Assets' families covers the Method's needed_family_ids.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "minItems": 1,
            "title": "Asset Ids",
            "type": "array",
            "uniqueItems": true
          },
          "name": {
            "description": "Display name for the new plan.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "practice_id": {
            "description": "Practice id this Plan binds. Existence verified at handler-load time; missing \u2192 404. Practice's status verified by decider (Deprecated \u2192 409).",
            "format": "uuid",
            "title": "Practice Id",
            "type": "string"
          }
        },
        "required": [
          "name",
          "practice_id",
          "asset_ids"
        ],
        "title": "DefinePlanRequest",
        "type": "object"
      },
      "DefinePlanResponse": {
        "description": "Response body for `POST /plans`.",
        "properties": {
          "plan_id": {
            "format": "uuid",
            "title": "Plan Id",
            "type": "string"
          }
        },
        "required": [
          "plan_id"
        ],
        "title": "DefinePlanResponse",
        "type": "object"
      },
      "DefinePolicyRequest": {
        "description": "Body for `POST /policies`.",
        "properties": {
          "conduit_id": {
            "description": "UUID of the Conduit this policy governs (not validated for existence).",
            "format": "uuid",
            "title": "Conduit Id",
            "type": "string"
          },
          "name": {
            "description": "Display name for the new policy.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "permitted_commands": {
            "description": "Command names (for example 'RegisterActor', 'DefineZone') allowed via this conduit. Empty list yields a deny-all policy.",
            "items": {
              "type": "string"
            },
            "title": "Permitted Commands",
            "type": "array"
          },
          "permitted_principal_ids": {
            "description": "Principals (UUIDs) allowed to act via this conduit. Empty list yields a deny-all policy.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Permitted Principal Ids",
            "type": "array"
          },
          "surface_id": {
            "description": "UUID of the Surface this policy governs. Defaults to nil so V1-shape callers don't need to send it; V2 bootstrap policy and Iter-C+ policies bind to a real Surface seeded by the deployment.",
            "format": "uuid",
            "title": "Surface Id",
            "type": "string"
          }
        },
        "required": [
          "name",
          "conduit_id",
          "permitted_principal_ids",
          "permitted_commands"
        ],
        "title": "DefinePolicyRequest",
        "type": "object"
      },
      "DefinePolicyResponse": {
        "description": "Response body for `POST /policies`.",
        "properties": {
          "policy_id": {
            "format": "uuid",
            "title": "Policy Id",
            "type": "string"
          }
        },
        "required": [
          "policy_id"
        ],
        "title": "DefinePolicyResponse",
        "type": "object"
      },
      "DefinePracticeRequest": {
        "description": "Body for `POST /practices`.",
        "properties": {
          "method_id": {
            "description": "Method id this Practice adapts. Eventual-consistency: existence is NOT verified against the Method stream at decide time; mismatch surfaces at Plan binding.",
            "format": "uuid",
            "title": "Method Id",
            "type": "string"
          },
          "name": {
            "description": "Display name for the new practice.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "site_id": {
            "description": "Site-level Asset id this Practice belongs to (institutional ownership). Eventual-consistency: existence and level are NOT verified at decide time.",
            "format": "uuid",
            "title": "Site Id",
            "type": "string"
          }
        },
        "required": [
          "name",
          "method_id",
          "site_id"
        ],
        "title": "DefinePracticeRequest",
        "type": "object"
      },
      "DefinePracticeResponse": {
        "description": "Response body for `POST /practices`.",
        "properties": {
          "practice_id": {
            "format": "uuid",
            "title": "Practice Id",
            "type": "string"
          }
        },
        "required": [
          "practice_id"
        ],
        "title": "DefinePracticeResponse",
        "type": "object"
      },
      "DefineRecipeRequest": {
        "description": "Body for `POST /recipes`.",
        "properties": {
          "capability_id": {
            "description": "Capability this Recipe realizes. REQUIRED and IMMUTABLE across versions; re-binding requires authoring a new Recipe.",
            "format": "uuid",
            "title": "Capability Id",
            "type": "string"
          },
          "name": {
            "description": "Display name for the new Recipe.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "steps": {
            "additionalProperties": true,
            "description": "Wire-format step sequence: `{steps: [{kind: setpoint|action|check, ...}]}`. Each `value` or `params[k]` position may carry `{__binding__: name}` to reference a Capability parameter.",
            "title": "Steps",
            "type": "object"
          }
        },
        "required": [
          "name",
          "capability_id",
          "steps"
        ],
        "title": "DefineRecipeRequest",
        "type": "object"
      },
      "DefineRecipeResponse": {
        "description": "Response body for `POST /recipes`.",
        "properties": {
          "recipe_id": {
            "format": "uuid",
            "title": "Recipe Id",
            "type": "string"
          }
        },
        "required": [
          "recipe_id"
        ],
        "title": "DefineRecipeResponse",
        "type": "object"
      },
      "DefineSurfaceRequest": {
        "description": "Body for `POST /surfaces`.",
        "properties": {
          "kind": {
            "$ref": "#/components/schemas/SurfaceKind",
            "description": "Process-level arrival kind. Closed enum; one of http / mcp_stdio / mcp_streamable_http."
          },
          "name": {
            "description": "Display name for the new surface.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name",
          "kind"
        ],
        "title": "DefineSurfaceRequest",
        "type": "object"
      },
      "DefineSurfaceResponse": {
        "description": "Response body for `POST /surfaces`.",
        "properties": {
          "surface_id": {
            "format": "uuid",
            "title": "Surface Id",
            "type": "string"
          }
        },
        "required": [
          "surface_id"
        ],
        "title": "DefineSurfaceResponse",
        "type": "object"
      },
      "DefineZoneRequest": {
        "description": "Body for `POST /zones`.",
        "properties": {
          "name": {
            "description": "Display name for the new zone.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "DefineZoneRequest",
        "type": "object"
      },
      "DefineZoneResponse": {
        "description": "Response body for `POST /zones`.",
        "properties": {
          "zone_id": {
            "format": "uuid",
            "title": "Zone Id",
            "type": "string"
          }
        },
        "required": [
          "zone_id"
        ],
        "title": "DefineZoneResponse",
        "type": "object"
      },
      "DegradeAssetRequest": {
        "description": "Body for `POST /assets/{asset_id}/degrade`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the asset is being marked Degraded. Examples:\n\"hot pixel detected at (12, 42)\", \"redundant motor #2 failed\nself-test\", \"calibration drift exceeds tolerance\".",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the degrade transition (audit-log breadcrumb).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DegradeAssetRequest",
        "type": "object"
      },
      "DegradeSupplyRequest": {
        "description": "Body for `POST /supplies/{supply_id}/degrade`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the supply is being marked Degraded. Examples:\n\"photon beam at half-current after partial top-up\", \"LN2 dewar\npressure margin dropped to 20%\", \"compressed-air pressure drop\ndetected by gauge X-12\".",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the degrade transition (audit-log breadcrumb).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DegradeSupplyRequest",
        "type": "object"
      },
      "DemoteDatasetRequest": {
        "description": "Body for `POST /datasets/{dataset_id}/demote`.",
        "properties": {
          "reason": {
            "description": "Free-form reason for the demotion (1-500 chars after trimming). Captured verbatim in the audit log immutably. Operationally: 'retracting authoritative status because <X>' (calibration error, methodology challenged, sample compromised, etc.). Today the field is unstructured; structured taxonomy is future-additive on the same triggers as DatasetPromoted reason.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DemoteDatasetRequest",
        "type": "object"
      },
      "DeprecateAgentRequest": {
        "description": "Body for `POST /agents/{agent_id}/deprecate`.",
        "properties": {
          "reason": {
            "anyOf": [
              {
                "maxLength": 500,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional operator-supplied deprecation reason (1-500 chars after trim). Pass null to omit.",
            "title": "Reason"
          }
        },
        "title": "DeprecateAgentRequest",
        "type": "object"
      },
      "DeprecateAssemblyRequest": {
        "description": "Body for `POST /assemblies/{assembly_id}/deprecate`.",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the deprecation (audit-log breadcrumb). REQUIRED. Mirrors decommission_mount's reason field shape.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DeprecateAssemblyRequest",
        "type": "object"
      },
      "DeprecateCapabilityRequest": {
        "description": "Body for `POST /capabilities/{capability_id}/deprecate`.\n\nOptional `replaced_by_capability_id` pointer for the successor\nCapability. Omit entirely for deprecated-without-replacement.",
        "properties": {
          "replaced_by_capability_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional pointer to a successor Capability (LOINC `MAP_TO` precedent). None means deprecated-without-replacement.",
            "title": "Replaced By Capability Id"
          }
        },
        "title": "DeprecateCapabilityRequest",
        "type": "object"
      },
      "DeprecateModelRequest": {
        "description": "Body for `POST /models/{model_id}/deprecation`.\n\n`reason` is operator free text recording why the catalog entry is\nbeing retired (for example \"superseded by part RV120CCHL\", \"vendor\nEOL 2026\"). Trimmed and length-validated at the\n`ModelDeprecationReason` VO; whitespace-only is rejected as a\ndomain invariant violation (400).",
        "properties": {
          "reason": {
            "description": "Operator-supplied rationale for retiring this Model (for example 'superseded by RV120CCHL', 'vendor EOL 2026'). Free text; trimmed server-side.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DeprecateModelRequest",
        "type": "object"
      },
      "DeprecateRecipeRequest": {
        "description": "Body for `POST /recipes/{recipe_id}/deprecate`.\n\nOptional `replaced_by_recipe_id` pointer for the successor\nRecipe. Omit entirely for deprecated-without-replacement.",
        "properties": {
          "replaced_by_recipe_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional pointer to a successor Recipe (LOINC `MAP_TO` precedent). None means deprecated-without-replacement.",
            "title": "Replaced By Recipe Id"
          }
        },
        "title": "DeprecateRecipeRequest",
        "type": "object"
      },
      "DeregisterSupplyRequest": {
        "description": "Body for `POST /supplies/{supply_id}/deregister`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the supply is being deregistered. Examples:\n\"typo on scope at registration; re-registering correctly\",\n\"beamline retired\", \"duplicate of supply <id>\".",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the deregister transition (audit-log breadcrumb).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DeregisterSupplyRequest",
        "type": "object"
      },
      "DetachAssetFromFixtureRequest": {
        "description": "Body for `POST /assets/{asset_id}/detach-from-fixture`.",
        "properties": {
          "fixture_id": {
            "description": "Fixture.id the Asset is expected to be currently attached to. Defensive race-condition guard: if the Asset is actually attached to a different Fixture, the request is rejected.",
            "format": "uuid",
            "title": "Fixture Id",
            "type": "string"
          }
        },
        "required": [
          "fixture_id"
        ],
        "title": "DetachAssetFromFixtureRequest",
        "type": "object"
      },
      "Direction": {
        "description": "Query-convenience discriminator mirroring `type(state.terms)`.\n\nLives on the aggregate root so projections and read-side filters\ncan index by direction without crossing the polymorphic terms\nboundary. Decider enforces `direction == Outbound iff\nisinstance(terms, OutboundTerms)`.",
        "enum": [
          "Outbound",
          "Inbound"
        ],
        "title": "Direction",
        "type": "string"
      },
      "DiscardDatasetRequest": {
        "description": "Body for `POST /datasets/{dataset_id}/discard`.",
        "properties": {
          "reason": {
            "description": "Free-form reason for the discard (1-500 chars after trimming). Today the field is unstructured; structured taxonomy is future-additive on the same triggers as RunStopped/RunAborted/RunTruncated reasons.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DiscardDatasetRequest",
        "type": "object"
      },
      "DiscardSubjectRequest": {
        "description": "Body for `POST /subjects/{subject_id}/discard`.",
        "properties": {
          "reason": {
            "description": "Free-form reason for the discard (1-500 chars after trimming). Captured verbatim for GDPR + sample-handling audit. Today the field is unstructured; structured taxonomy is future-additive on the same triggers as DatasetDiscarded / RunStopped reasons.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DiscardSubjectRequest",
        "type": "object"
      },
      "DismissEventInReactionRequest": {
        "description": "Body for `POST /agent/reactions/{subscriber_name}/dismiss-event`.",
        "properties": {
          "event_id": {
            "description": "The event_id the operator wants the subscriber's bookmark advanced past. Read from the operator dashboard's wedged-bookmark surface (last_error_message + last_event_recorded_at on projection_bookmarks); pasting an arbitrary event_id risks rewinding the bookmark, which the decider catches via the lexicographic (transaction_id, position) check.",
            "format": "uuid",
            "title": "Event Id",
            "type": "string"
          },
          "reason": {
            "description": "Free-form reason the operator gives for the dismissal (1-500 chars after trimming). Captured verbatim into DecisionRegistered.reasoning for audit. Operationally: 'cannot deserialize this event_type since the rename, advancing past' / 'LLM returned hallucinated context, manual retry tomorrow' etc.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "event_id",
          "reason"
        ],
        "title": "DismissEventInReactionRequest",
        "type": "object"
      },
      "DismissEventInReactionResponse": {
        "description": "Body returned on successful dismissal.",
        "properties": {
          "decision_id": {
            "description": "The Decision audit record's id.",
            "format": "uuid",
            "title": "Decision Id",
            "type": "string"
          }
        },
        "required": [
          "decision_id"
        ],
        "title": "DismissEventInReactionResponse",
        "type": "object"
      },
      "DismountSubjectRequest": {
        "description": "Body for `POST /subjects/{subject_id}/dismount`.",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the dismount (audit-log breadcrumb). Examples: 'run complete', 'transport break', 'transferring to detector stage', 'end-of-day'.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "DismountSubjectRequest",
        "type": "object"
      },
      "DrawingBody": {
        "description": "Wire format for a `Drawing` value object.",
        "properties": {
          "number": {
            "description": "Document identifier within the system.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Number",
            "type": "string"
          },
          "revision": {
            "anyOf": [
              {
                "maxLength": 100,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional revision pin; None means 'resolves to latest' (ISO 7200 / DOI / EDMS default-resolution semantics).",
            "title": "Revision"
          },
          "system": {
            "$ref": "#/components/schemas/DrawingSystem",
            "description": "The external document-management system (ICMS / EDMS / DOI)."
          }
        },
        "required": [
          "system",
          "number"
        ],
        "title": "DrawingBody",
        "type": "object"
      },
      "DrawingSystem": {
        "description": "The external document-management system a Drawing lives in.\n\nClosed StrEnum: v1 ships with three systems. Adding a new value\nis a paired change: extend this enum AND ship a migration that\ndrops + re-adds the CHECK constraint on `drawing_system` in any\nprojection that materializes drawings (today: `proj_equipment_\nasset_summary`). Whether a given system value is actually wired\nto an adapter is enforced per-facility at the adapter boundary,\nNOT here.\n\n  - `ICMS`: APS Information Content Management System (Documentum-\n    backed).\n  - `EDMS`: generic Engineering Document Management System\n    (CERN EDMS and similar).\n  - `DOI`: DataCite Digital Object Identifier (used by PIDINST\n    for instrument-level identifiers).",
        "enum": [
          "ICMS",
          "EDMS",
          "DOI"
        ],
        "title": "DrawingSystem",
        "type": "string"
      },
      "EncodingRequest": {
        "description": "Structured encoding descriptor on the registration request body.",
        "properties": {
          "conforms_to": {
            "description": "Profile URIs the Dataset claims to conform to (NeXus, OME-Zarr, CIF, etc.). Empty when no profile is claimed; multi-entry when the Dataset conforms to more than one profile.",
            "items": {
              "type": "string"
            },
            "maxItems": 16,
            "title": "Conforms To",
            "type": "array"
          },
          "media_type": {
            "description": "Loose MIME-type-ish string describing the wire encoding (for example 'application/x-hdf5', 'application/x-zarr').",
            "maxLength": 200,
            "minLength": 1,
            "title": "Media Type",
            "type": "string"
          }
        },
        "required": [
          "media_type"
        ],
        "title": "EncodingRequest",
        "type": "object"
      },
      "EncodingResponse": {
        "description": "Structured encoding descriptor on the response body.",
        "properties": {
          "conforms_to": {
            "items": {
              "type": "string"
            },
            "title": "Conforms To",
            "type": "array"
          },
          "media_type": {
            "title": "Media Type",
            "type": "string"
          }
        },
        "required": [
          "media_type",
          "conforms_to"
        ],
        "title": "EncodingResponse",
        "type": "object"
      },
      "ErrorResponse": {
        "description": "Shared error body for OpenAPI documentation.",
        "properties": {
          "detail": {
            "title": "Detail",
            "type": "string"
          }
        },
        "required": [
          "detail"
        ],
        "title": "ErrorResponse",
        "type": "object"
      },
      "EvaluatePolicyResponse": {
        "description": "Read-side DTO at the API boundary.\n\n`reason` is populated only when `decision == \"Deny\"`. The Allow\nbranch leaves `reason` null (omitted on the wire when\n`model_dump(exclude_none=True)` is used; FastAPI default keeps\nthe field present as null which is also fine).",
        "properties": {
          "decision": {
            "enum": [
              "Allow",
              "Deny"
            ],
            "title": "Decision",
            "type": "string"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Reason"
          }
        },
        "required": [
          "decision"
        ],
        "title": "EvaluatePolicyResponse",
        "type": "object"
      },
      "ExecutorShape": {
        "description": "Closed v1 enum of executor kinds that may implement a Capability.",
        "enum": [
          "Method",
          "Procedure"
        ],
        "title": "ExecutorShape",
        "type": "string"
      },
      "ExpireClearanceRequest": {
        "description": "Body for `POST /clearances/{clearance_id}/expire`.",
        "properties": {
          "reason": {
            "description": "Operator-supplied free-form reason for the expiration. Audit breadcrumb explaining why the clearance was retired (for example, 'validity window elapsed', 'scope-change incident').",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "ExpireClearanceRequest",
        "type": "object"
      },
      "ExternalRefDTO-Input": {
        "description": "Wire shape for an ExternalRef on the register_campaign request body.",
        "properties": {
          "id": {
            "description": "Facility-issued opaque id under the named scheme.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Id",
            "type": "string"
          },
          "scheme": {
            "description": "Scheme identifier for the upstream-deferred concept (for example 'proposal', 'btr', 'visit', 'cycle').",
            "maxLength": 50,
            "minLength": 1,
            "title": "Scheme",
            "type": "string"
          }
        },
        "required": [
          "scheme",
          "id"
        ],
        "title": "ExternalRefDTO",
        "type": "object"
      },
      "ExternalRefDTO-Output": {
        "description": "Wire shape for an ExternalRef in the GET response.",
        "properties": {
          "id": {
            "title": "Id",
            "type": "string"
          },
          "scheme": {
            "title": "Scheme",
            "type": "string"
          }
        },
        "required": [
          "scheme",
          "id"
        ],
        "title": "ExternalRefDTO",
        "type": "object"
      },
      "FamilyListResponse": {
        "description": "Page of families plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/FamilySummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "FamilyListResponse",
        "type": "object"
      },
      "FamilyResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model so the two can evolve independently.\n`status` is the StrEnum's string value (Defined / Versioned /\nDeprecated). `version` is the operator-supplied label of the most\nrecent version_family call (null until first version).\n`affordances` (5j) serializes as a sorted list of Affordance\nenum string values (frozenset semantics in domain state, list at\nthe JSON boundary; sorted alphabetically for response determinism).\n`created_at` / `versioned_at` / `deprecated_at` are projection-\nsourced lifecycle timestamps (Path C);\nsee module docstring for null-semantics.",
        "properties": {
          "affordances": {
            "items": {
              "$ref": "#/components/schemas/Affordance"
            },
            "title": "Affordances",
            "type": "array"
          },
          "created_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "deprecated_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Deprecated At"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "version": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version"
          },
          "versioned_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Versioned At"
          }
        },
        "required": [
          "id",
          "name",
          "status",
          "version",
          "affordances"
        ],
        "title": "FamilyResponse",
        "type": "object"
      },
      "FamilySummaryDTO": {
        "description": "One family in a paginated list.",
        "properties": {
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "family_id": {
            "format": "uuid",
            "title": "Family Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "status": {
            "enum": [
              "Defined",
              "Versioned",
              "Deprecated"
            ],
            "title": "Status",
            "type": "string"
          },
          "version_tag": {
            "anyOf": [
              {
                "maxLength": 50,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version Tag"
          }
        },
        "required": [
          "family_id",
          "name",
          "status",
          "created_at"
        ],
        "title": "FamilySummaryDTO",
        "type": "object"
      },
      "FamilyViewDTO": {
        "description": "Read-side DTO for one Family on the integration-view bundle.",
        "properties": {
          "affordances": {
            "items": {
              "type": "string"
            },
            "title": "Affordances",
            "type": "array"
          },
          "family_id": {
            "format": "uuid",
            "title": "Family Id",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "family_id",
          "name",
          "affordances"
        ],
        "title": "FamilyViewDTO",
        "type": "object"
      },
      "FaultAssetRequest": {
        "description": "Body for `POST /assets/{asset_id}/fault`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the asset is being marked Faulted. Examples:\n\"detector saturated, BIOS won't boot\", \"vacuum pump seized\",\n\"drive motor controller off-line per EPICS PV\".",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the fault transition (audit-log breadcrumb).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "FaultAssetRequest",
        "type": "object"
      },
      "FederationErrorDTO": {
        "description": "Wire shape for Federation BC error responses.",
        "properties": {
          "detail": {
            "description": "Human-readable error reason.",
            "title": "Detail",
            "type": "string"
          }
        },
        "required": [
          "detail"
        ],
        "title": "FederationErrorDTO",
        "type": "object"
      },
      "FixtureListResponse": {
        "description": "Page of Fixtures plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/FixtureSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "FixtureListResponse",
        "type": "object"
      },
      "FixtureResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. slot_asset_bindings serializes\nas a sorted list of binding objects (sorted by slot_name then\nasset_id for response determinism; frozenset semantics in domain\nstate, list at the JSON boundary).",
        "properties": {
          "assembly_content_hash": {
            "title": "Assembly Content Hash",
            "type": "string"
          },
          "assembly_id": {
            "format": "uuid",
            "title": "Assembly Id",
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "parameter_overrides": {
            "additionalProperties": true,
            "title": "Parameter Overrides",
            "type": "object"
          },
          "registered_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Registered At"
          },
          "slot_asset_bindings": {
            "items": {
              "$ref": "#/components/schemas/SlotAssetBindingResponse"
            },
            "title": "Slot Asset Bindings",
            "type": "array"
          },
          "surface_id": {
            "format": "uuid",
            "title": "Surface Id",
            "type": "string"
          }
        },
        "required": [
          "id",
          "assembly_id",
          "assembly_content_hash",
          "surface_id",
          "registered_at"
        ],
        "title": "FixtureResponse",
        "type": "object"
      },
      "FixtureSummaryDTO": {
        "description": "One Fixture in a paginated list (summary-only).",
        "properties": {
          "assembly_content_hash": {
            "title": "Assembly Content Hash",
            "type": "string"
          },
          "assembly_id": {
            "format": "uuid",
            "title": "Assembly Id",
            "type": "string"
          },
          "binding_count": {
            "title": "Binding Count",
            "type": "integer"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "fixture_id": {
            "format": "uuid",
            "title": "Fixture Id",
            "type": "string"
          },
          "override_count": {
            "title": "Override Count",
            "type": "integer"
          },
          "surface_id": {
            "format": "uuid",
            "title": "Surface Id",
            "type": "string"
          }
        },
        "required": [
          "fixture_id",
          "assembly_id",
          "assembly_content_hash",
          "surface_id",
          "binding_count",
          "override_count",
          "created_at"
        ],
        "title": "FixtureSummaryDTO",
        "type": "object"
      },
      "GhsDto": {
        "properties": {
          "code": {
            "description": "GHS pictogram code; one of ['GHS01', 'GHS02', 'GHS03', 'GHS04', 'GHS05', 'GHS06', 'GHS07', 'GHS08', 'GHS09'].",
            "title": "Code",
            "type": "string"
          },
          "kind": {
            "const": "GHS",
            "title": "Kind",
            "type": "string"
          },
          "statement_codes": {
            "description": "GHS H-statement codes triggering this pictogram (for example 'H300').",
            "items": {
              "type": "string"
            },
            "title": "Statement Codes",
            "type": "array"
          }
        },
        "required": [
          "kind",
          "code"
        ],
        "title": "GhsDto",
        "type": "object"
      },
      "GrantToolToAgentRequest": {
        "description": "Body for `POST /agents/{agent_id}/tools/grant`.",
        "properties": {
          "tool_name": {
            "description": "MCP tool name to add to the Agent's allowlist (1-100 chars).",
            "maxLength": 100,
            "minLength": 1,
            "title": "Tool Name",
            "type": "string"
          }
        },
        "required": [
          "tool_name"
        ],
        "title": "GrantToolToAgentRequest",
        "type": "object"
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "title": "Detail",
            "type": "array"
          }
        },
        "title": "HTTPValidationError",
        "type": "object"
      },
      "HazardDeclarationDTO": {
        "properties": {
          "classifications": {
            "items": {
              "discriminator": {
                "mapping": {
                  "GHS": "#/components/schemas/GhsDto",
                  "NFPA704": "#/components/schemas/NFPA704DTO",
                  "RiskBand": "#/components/schemas/RiskBandDTO",
                  "Scheme": "#/components/schemas/SchemeDTO"
                },
                "propertyName": "kind"
              },
              "oneOf": [
                {
                  "$ref": "#/components/schemas/NFPA704DTO"
                },
                {
                  "$ref": "#/components/schemas/RiskBandDTO"
                },
                {
                  "$ref": "#/components/schemas/GhsDto"
                },
                {
                  "$ref": "#/components/schemas/SchemeDTO"
                }
              ]
            },
            "title": "Classifications",
            "type": "array"
          },
          "mitigations": {
            "description": "Free-form mitigation refs: PPE codes, training cert refs, procedure IDs.",
            "items": {
              "type": "string"
            },
            "title": "Mitigations",
            "type": "array"
          },
          "notes": {
            "anyOf": [
              {
                "maxLength": 2000,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Notes"
          },
          "target": {
            "discriminator": {
              "mapping": {
                "Asset": "#/components/schemas/BindingAssetDTO",
                "External": "#/components/schemas/BindingExternalDTO",
                "Procedure": "#/components/schemas/BindingProcedureDTO",
                "Run": "#/components/schemas/BindingRunDTO",
                "Subject": "#/components/schemas/BindingSubjectDTO"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/components/schemas/BindingSubjectDTO"
              },
              {
                "$ref": "#/components/schemas/BindingAssetDTO"
              },
              {
                "$ref": "#/components/schemas/BindingRunDTO"
              },
              {
                "$ref": "#/components/schemas/BindingProcedureDTO"
              },
              {
                "$ref": "#/components/schemas/BindingExternalDTO"
              }
            ],
            "title": "Target"
          }
        },
        "required": [
          "target"
        ],
        "title": "HazardDeclarationDTO",
        "type": "object"
      },
      "HoldCampaignRequest": {
        "description": "Body for `POST /campaigns/{campaign_id}/hold`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the Campaign is being paused. Examples: \"beam\ninterruption\", \"operator break\", \"instrument alignment after\nthermal soak\".",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the hold (audit-log breadcrumb).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "HoldCampaignRequest",
        "type": "object"
      },
      "HoldVisitRequest": {
        "description": "Body for `POST /visits/{visit_id}/hold`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb).\nExamples: \"beam dump\", \"equipment fault\", \"safety hold pending\nradiation door reset\", \"extended user break\". MUST NOT contain PII.",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the hold (audit-log breadcrumb; no PII).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "HoldVisitRequest",
        "type": "object"
      },
      "InitializeSealRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /federation/seals`.",
        "properties": {
          "facility_id": {
            "description": "Opaque string id of the facility this Seal binds to. Facility identity is external to CORA; we do NOT mint facility ids. Doubles as the singleton identity (one Seal per facility).",
            "minLength": 1,
            "title": "Facility Id",
            "type": "string"
          },
          "offline_credential_id": {
            "description": "Credential.id of the cold root key (purpose SealOfflineRoot). Must differ from online_credential_id.",
            "format": "uuid",
            "title": "Offline Credential Id",
            "type": "string"
          },
          "online_credential_id": {
            "description": "Credential.id of the warm signing key (purpose SealOnlineSigning). Must differ from offline_credential_id.",
            "format": "uuid",
            "title": "Online Credential Id",
            "type": "string"
          }
        },
        "required": [
          "facility_id",
          "online_credential_id",
          "offline_credential_id"
        ],
        "title": "InitializeSealRequest",
        "type": "object"
      },
      "InitializeSealResponse": {
        "description": "Response body for `POST /federation/seals`.",
        "properties": {
          "facility_id": {
            "title": "Facility Id",
            "type": "string"
          },
          "seal_stream_id": {
            "format": "uuid",
            "title": "Seal Stream Id",
            "type": "string"
          }
        },
        "required": [
          "seal_stream_id",
          "facility_id"
        ],
        "title": "InitializeSealResponse",
        "type": "object"
      },
      "InspectPlanBindingRequest": {
        "description": "Wire-side input for the inspect_plan_binding endpoint.",
        "properties": {
          "asset_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "minItems": 1,
            "title": "Asset Ids",
            "type": "array"
          },
          "practice_id": {
            "format": "uuid",
            "title": "Practice Id",
            "type": "string"
          }
        },
        "required": [
          "practice_id",
          "asset_ids"
        ],
        "title": "InspectPlanBindingRequest",
        "type": "object"
      },
      "InspectPlanBindingResponse": {
        "description": "Wire-side diagnostic returned by the endpoint.\n\n`missing_family_ids` and `missing_affordances` are sorted lists;\nempty means that dimension is satisfied. `binding_status` is\nthe at-a-glance verdict (Satisfied / MissingFamilies /\nMissingAffordances / MissingCapability).\n\n`missing_affordance_candidates` enumerates, per missing\naffordance, other facility Assets whose Families declare it.\nEmpty when no affordances are missing; in in-memory test mode\n(no pool) the projection-backed lookup is skipped and this\nstays empty regardless.",
        "properties": {
          "binding_status": {
            "title": "Binding Status",
            "type": "string"
          },
          "capability_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Capability Id"
          },
          "capability_required_affordances": {
            "items": {
              "type": "string"
            },
            "title": "Capability Required Affordances",
            "type": "array"
          },
          "method_id": {
            "format": "uuid",
            "title": "Method Id",
            "type": "string"
          },
          "method_needed_family_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Method Needed Family Ids",
            "type": "array"
          },
          "missing_affordance_candidates": {
            "items": {
              "$ref": "#/components/schemas/MissingAffordanceCandidatesItem"
            },
            "title": "Missing Affordance Candidates",
            "type": "array"
          },
          "missing_affordances": {
            "items": {
              "type": "string"
            },
            "title": "Missing Affordances",
            "type": "array"
          },
          "missing_family_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Missing Family Ids",
            "type": "array"
          },
          "practice_id": {
            "format": "uuid",
            "title": "Practice Id",
            "type": "string"
          },
          "wired_assets": {
            "items": {
              "$ref": "#/components/schemas/WiredAssetItem"
            },
            "title": "Wired Assets",
            "type": "array"
          }
        },
        "required": [
          "practice_id",
          "method_id",
          "capability_id",
          "method_needed_family_ids",
          "capability_required_affordances",
          "wired_assets",
          "missing_family_ids",
          "missing_affordances",
          "missing_affordance_candidates",
          "binding_status"
        ],
        "title": "InspectPlanBindingResponse",
        "type": "object"
      },
      "InstallAssetRequest": {
        "properties": {
          "asset_id": {
            "description": "The Asset specimen to install.",
            "format": "uuid",
            "title": "Asset Id",
            "type": "string"
          }
        },
        "required": [
          "asset_id"
        ],
        "title": "InstallAssetRequest",
        "type": "object"
      },
      "InstrumentTypeDTO": {
        "description": "PIDINST v1.0 Property 9: a typology category for the instrument.",
        "properties": {
          "identifier": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Identifier"
          },
          "identifier_type": {
            "title": "Identifier Type",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name",
          "identifier_type"
        ],
        "title": "InstrumentTypeDTO",
        "type": "object"
      },
      "ListPermissionsResponse": {
        "description": "Read-side DTO at the API boundary.\n\n`incomplete` is always False at v1, required from day 1 per the\ndesign lock's anti-hook to forward-compat with future ABAC\npolicies that may make enumeration lossy.",
        "properties": {
          "evaluated_conduit_id": {
            "format": "uuid",
            "title": "Evaluated Conduit Id",
            "type": "string"
          },
          "evaluated_principal_id": {
            "format": "uuid",
            "title": "Evaluated Principal Id",
            "type": "string"
          },
          "incomplete": {
            "description": "True if some permissions could not be enumerated (always False at v1).",
            "title": "Incomplete",
            "type": "boolean"
          },
          "permitted_commands": {
            "items": {
              "type": "string"
            },
            "title": "Permitted Commands",
            "type": "array"
          },
          "policy_id": {
            "format": "uuid",
            "title": "Policy Id",
            "type": "string"
          }
        },
        "required": [
          "policy_id",
          "evaluated_principal_id",
          "evaluated_conduit_id",
          "permitted_commands",
          "incomplete"
        ],
        "title": "ListPermissionsResponse",
        "type": "object"
      },
      "ManufacturerBody": {
        "description": "Pydantic mirror of the Manufacturer VO for the request body.\n\n`identifier` and `identifier_type` are both optional but must be\nsupplied together or both omitted (the pairing invariant is\nenforced at the VO constructor; a bare identifier with no scheme\ncannot be resolved).",
        "properties": {
          "identifier": {
            "anyOf": [
              {
                "maxLength": 200,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional opaque identifier value. If supplied, `identifier_type` is required (and vice versa).",
            "title": "Identifier"
          },
          "identifier_type": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ManufacturerIdentifierType"
              },
              {
                "type": "null"
              }
            ],
            "description": "Closed scheme for the optional manufacturer identifier."
          },
          "name": {
            "description": "Display name of the manufacturer.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "ManufacturerBody",
        "type": "object"
      },
      "ManufacturerDTO": {
        "description": "PIDINST v1.0 Property 6: the body manufacturing the instrument.",
        "properties": {
          "identifier": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Identifier"
          },
          "identifier_type": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Identifier Type"
          },
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "ManufacturerDTO",
        "type": "object"
      },
      "ManufacturerIdentifierType": {
        "description": "Closed scheme for the optional manufacturer identifier.\n\nThree members ship in v1: ROR (Research Organization Registry),\nGRID (Global Research Identifier Database; subsumed by ROR but\nstill in active use at many facilities), ISNI (International\nStandard Name Identifier). Adding a fourth scheme is an additive\nenum change.",
        "enum": [
          "ROR",
          "GRID",
          "ISNI"
        ],
        "title": "ManufacturerIdentifierType",
        "type": "string"
      },
      "ManufacturerResponse": {
        "description": "Nested DTO for a model's manufacturer.\n\n`name` is required; `identifier` and `identifier_type` are both\nset or both null (pairing invariant enforced by the domain VO).\n`identifier_type` is the closed-StrEnum scheme string value\n(ROR / GRID / ISNI) when present.",
        "properties": {
          "identifier": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Identifier"
          },
          "identifier_type": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Identifier Type"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "ManufacturerResponse",
        "type": "object"
      },
      "MarkSupplyAvailableRequest": {
        "description": "Body for `POST /supplies/{supply_id}/mark-available`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining the first-observation declaration. Examples:\n\"operator walkdown confirms LN2 dewar pressure nominal\", \"control\nroom reports beam delivered after morning startup\", \"first-time\ncommissioning verified by ops\".",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for declaring the supply Available for the first time (audit-log breadcrumb).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "MarkSupplyAvailableRequest",
        "type": "object"
      },
      "MarkSupplyRecoveringRequest": {
        "description": "Body for `POST /supplies/{supply_id}/mark-recovering`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the supply is being marked Recovering. Examples:\n\"beam current detected at 50% nominal\", \"LN2 dewar refill in\nprogress\", \"vacuum pump-down underway, awaiting target pressure\".",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the mark-recovering transition (audit-log breadcrumb).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "MarkSupplyRecoveringRequest",
        "type": "object"
      },
      "MarkSupplyUnavailableRequest": {
        "description": "Body for `POST /supplies/{supply_id}/mark-unavailable`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the supply is being marked Unavailable. Examples:\n\"beam dump at 09:32\", \"LN2 dewar empty\", \"vacuum loss in\nsample chamber\", \"scheduled maintenance window starts\".",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the mark-unavailable transition (audit-log breadcrumb).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "MarkSupplyUnavailableRequest",
        "type": "object"
      },
      "MeasuredVariableDTO": {
        "description": "PIDINST v1.0 Property 10: a physical quantity the instrument measures.",
        "properties": {
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "MeasuredVariableDTO",
        "type": "object"
      },
      "MeasurementTechniqueDTO": {
        "description": "PIDINST v1.0 Property 14: a measurement technique applied by the instrument.",
        "properties": {
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "MeasurementTechniqueDTO",
        "type": "object"
      },
      "MethodListResponse": {
        "description": "Page of methods plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/MethodSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "MethodListResponse",
        "type": "object"
      },
      "MethodResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model so the two can evolve independently.\n`needed_family_ids` is sorted by UUID string form.\n`needed_supplies` is sorted lexically by kind string;\nSupply.kind values (NOT instance UUIDs).\n`status` is the StrEnum's string value (Defined / Versioned /\nDeprecated). `version` is the operator-supplied label of the most\nrecent version_method call (null until first version).\n`created_at` / `versioned_at` / `deprecated_at` are projection-\nsourced lifecycle timestamps (Path C). Null\nsemantics under eventual consistency: read together with `status`.\nIf `status >= Defined` but `created_at` is null, the projection\nhas not yet folded `MethodDefined` (transient lag). If\n`status == Versioned` but `versioned_at` is null, the projection\nhas not yet folded the latest `MethodVersioned`. If\n`status == Deprecated` but `deprecated_at` is null, the projection\nhas not yet folded `MethodDeprecated`. A 404 means the Method\naggregate itself does not exist (fold-on-read miss); a 200 with\nnull timestamps always means projection lag, never a missing\ntransition.",
        "properties": {
          "created_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "deprecated_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Deprecated At"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "needed_family_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Needed Family Ids",
            "type": "array"
          },
          "needed_supplies": {
            "items": {
              "type": "string"
            },
            "title": "Needed Supplies",
            "type": "array"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "version": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version"
          },
          "versioned_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Versioned At"
          }
        },
        "required": [
          "id",
          "name",
          "needed_family_ids",
          "needed_supplies",
          "status",
          "version"
        ],
        "title": "MethodResponse",
        "type": "object"
      },
      "MethodSummaryDTO": {
        "description": "One method in a paginated list.",
        "properties": {
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "method_id": {
            "format": "uuid",
            "title": "Method Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "parameters_schema_present": {
            "default": false,
            "description": "True iff the most recent `MethodParametersSchemaUpdated` event for this Method carried a non-NULL parameters_schema (Phase 6g-a). The schema content itself is loaded on demand.",
            "title": "Parameters Schema Present",
            "type": "boolean"
          },
          "status": {
            "enum": [
              "Defined",
              "Versioned",
              "Deprecated"
            ],
            "title": "Status",
            "type": "string"
          },
          "version_tag": {
            "anyOf": [
              {
                "maxLength": 50,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version Tag"
          }
        },
        "required": [
          "method_id",
          "name",
          "status",
          "created_at"
        ],
        "title": "MethodSummaryDTO",
        "type": "object"
      },
      "MissingAffordanceCandidatesItem": {
        "description": "Per-missing-affordance group on the wire.\n\n`affordance` is one of the entries in `missing_affordances`.\n`candidates` is sorted by asset_id string form (mirrors the\nwired_assets ordering convention). Empty list means the\nfacility has no Asset that could cover this requirement.",
        "properties": {
          "affordance": {
            "title": "Affordance",
            "type": "string"
          },
          "candidates": {
            "items": {
              "$ref": "#/components/schemas/CandidateAssetItem"
            },
            "title": "Candidates",
            "type": "array"
          }
        },
        "required": [
          "affordance",
          "candidates"
        ],
        "title": "MissingAffordanceCandidatesItem",
        "type": "object"
      },
      "ModelRefRequest": {
        "description": "Sub-body for the typed ModelRef VO.",
        "properties": {
          "model": {
            "description": "Provider-specific model identifier (`claude-sonnet-4-6`, etc.).",
            "maxLength": 200,
            "minLength": 1,
            "title": "Model",
            "type": "string"
          },
          "provider": {
            "description": "LLM provider name (`anthropic`, `openai`, `google`, etc.).",
            "maxLength": 100,
            "minLength": 1,
            "title": "Provider",
            "type": "string"
          },
          "snapshot_pin": {
            "anyOf": [
              {
                "maxLength": 100,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional provider-specific snapshot identifier (Anthropic snapshot string, OpenAI fingerprint, etc.) for reproducibility. Pass null to leave unpinned.",
            "title": "Snapshot Pin"
          }
        },
        "required": [
          "provider",
          "model"
        ],
        "title": "ModelRefRequest",
        "type": "object"
      },
      "ModelRefResponse": {
        "description": "Sub-DTO for the typed ModelRef VO.",
        "properties": {
          "model": {
            "title": "Model",
            "type": "string"
          },
          "provider": {
            "title": "Provider",
            "type": "string"
          },
          "snapshot_pin": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Snapshot Pin"
          }
        },
        "required": [
          "provider",
          "model"
        ],
        "title": "ModelRefResponse",
        "type": "object"
      },
      "ModelResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model so the two can evolve independently.\n`status` is the StrEnum's string value (Defined / Versioned /\nDeprecated). `version_tag` is the operator-supplied label of the\nmost recent version_model call (null until first version).\n`declared_family_ids` serializes as a sorted list of Family UUIDs\n(frozenset semantics in domain state, list at the JSON boundary;\nsorted by UUID string form for response determinism).",
        "properties": {
          "declared_family_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Declared Family Ids",
            "type": "array"
          },
          "manufacturer": {
            "$ref": "#/components/schemas/ManufacturerResponse"
          },
          "model_id": {
            "format": "uuid",
            "title": "Model Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "part_number": {
            "maxLength": 100,
            "title": "Part Number",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "version_tag": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version Tag"
          }
        },
        "required": [
          "model_id",
          "name",
          "manufacturer",
          "part_number",
          "declared_family_ids",
          "status",
          "version_tag"
        ],
        "title": "ModelResponse",
        "type": "object"
      },
      "MountSubjectRequest": {
        "description": "Body for `POST /subjects/{subject_id}/mount`.",
        "properties": {
          "asset_id": {
            "description": "Sample-environment Asset id (Equipment.Asset, must be Active).",
            "format": "uuid",
            "title": "Asset Id",
            "type": "string"
          },
          "reason": {
            "description": "Operator-supplied reason for the mount (audit-log breadcrumb). Examples: 'loaded for run #1234', 'calibration mount', 'transport break complete'.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "asset_id",
          "reason"
        ],
        "title": "MountSubjectRequest",
        "type": "object"
      },
      "NFPA704DTO": {
        "properties": {
          "flammability": {
            "maximum": 4.0,
            "minimum": 0.0,
            "title": "Flammability",
            "type": "integer"
          },
          "health": {
            "maximum": 4.0,
            "minimum": 0.0,
            "title": "Health",
            "type": "integer"
          },
          "instability": {
            "maximum": 4.0,
            "minimum": 0.0,
            "title": "Instability",
            "type": "integer"
          },
          "kind": {
            "const": "NFPA704",
            "title": "Kind",
            "type": "string"
          },
          "special": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional NFPA 704 'special' code: one of ['ACID', 'ALK', 'BIO', 'COR', 'CRYO', 'OX', 'POI', 'RA', 'SA', 'W'].",
            "title": "Special"
          }
        },
        "required": [
          "kind",
          "health",
          "flammability",
          "instability"
        ],
        "title": "NFPA704DTO",
        "type": "object"
      },
      "OnwardActionScope": {
        "description": "What the peer may do with the data after reading under an outbound permit.\n\n- `ReadOnly`:               read-and-display; no persistence.\n- `MayCacheLocally`:        local caching permitted; not exposed\n                            to the peer's own peers.\n- `MayRePublishToOwnPeers`: the peer may federate the data\n                            onward to its own subscribers.\n- `MayExportOffPlatform`:   the peer may export the data out of\n                            the federation entirely.",
        "enum": [
          "ReadOnly",
          "MayCacheLocally",
          "MayRePublishToOwnPeers",
          "MayExportOffPlatform"
        ],
        "title": "OnwardActionScope",
        "type": "string"
      },
      "OwnerDTO": {
        "description": "PIDINST v1.0 Property 5: a body owning or curating the instrument.",
        "properties": {
          "contact": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Contact"
          },
          "identifier": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Identifier"
          },
          "identifier_type": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Identifier Type"
          },
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "OwnerDTO",
        "type": "object"
      },
      "PermitListResponse": {
        "description": "Page of permits plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/PermitSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "PermitListResponse",
        "type": "object"
      },
      "PermitResponse": {
        "description": "Read-side DTO at the API boundary.\n\nLifecycle timestamps are projection-sourced per Path C;\n`defined_at` is always present, transition timestamps stay `None`\nuntil their event fires.",
        "properties": {
          "abi_tier_floor": {
            "title": "Abi Tier Floor",
            "type": "string"
          },
          "activated_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Activated At"
          },
          "allowed_artifact_kinds": {
            "items": {
              "type": "string"
            },
            "title": "Allowed Artifact Kinds",
            "type": "array"
          },
          "allowed_credential_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Allowed Credential Ids",
            "type": "array"
          },
          "allowed_payload_types": {
            "items": {
              "type": "string"
            },
            "title": "Allowed Payload Types",
            "type": "array"
          },
          "defined_at": {
            "format": "date-time",
            "title": "Defined At",
            "type": "string"
          },
          "defined_by_actor_id": {
            "format": "uuid",
            "title": "Defined By Actor Id",
            "type": "string"
          },
          "direction": {
            "title": "Direction",
            "type": "string"
          },
          "expires_at": {
            "format": "date-time",
            "title": "Expires At",
            "type": "string"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "peer_facility_id": {
            "title": "Peer Facility Id",
            "type": "string"
          },
          "resumed_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Resumed At"
          },
          "revoked_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Revoked At"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "suspended_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Suspended At"
          },
          "terms": {
            "discriminator": {
              "mapping": {
                "Inbound": "#/components/schemas/_InboundTermsResponse",
                "Outbound": "#/components/schemas/_OutboundTermsResponse"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/components/schemas/_OutboundTermsResponse"
              },
              {
                "$ref": "#/components/schemas/_InboundTermsResponse"
              }
            ],
            "title": "Terms"
          }
        },
        "required": [
          "id",
          "peer_facility_id",
          "direction",
          "allowed_credential_ids",
          "allowed_payload_types",
          "allowed_artifact_kinds",
          "abi_tier_floor",
          "expires_at",
          "defined_by_actor_id",
          "status",
          "terms",
          "defined_at"
        ],
        "title": "PermitResponse",
        "type": "object"
      },
      "PermitStatus": {
        "description": "FSM positions for a Permit (shared across directions).\n\n- `Defined`:   created but not yet active; the flow is closed.\n- `Active`:    in force; pulls / publishes are permitted under terms.\n- `Suspended`: temporarily paused; resumable back to Active.\n- `Revoked`:   terminal; the permit is closed and cannot be revived.",
        "enum": [
          "Defined",
          "Active",
          "Suspended",
          "Revoked"
        ],
        "title": "PermitStatus",
        "type": "string"
      },
      "PermitSummaryDTO": {
        "description": "One permit in a paginated list.",
        "properties": {
          "abi_tier_floor": {
            "$ref": "#/components/schemas/AbiTier"
          },
          "activated_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Activated At"
          },
          "allowed_artifact_kinds": {
            "items": {
              "type": "string"
            },
            "title": "Allowed Artifact Kinds",
            "type": "array"
          },
          "allowed_credential_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Allowed Credential Ids",
            "type": "array"
          },
          "allowed_payload_types": {
            "items": {
              "type": "string"
            },
            "title": "Allowed Payload Types",
            "type": "array"
          },
          "defined_at": {
            "format": "date-time",
            "title": "Defined At",
            "type": "string"
          },
          "defined_by_actor_id": {
            "format": "uuid",
            "title": "Defined By Actor Id",
            "type": "string"
          },
          "direction": {
            "$ref": "#/components/schemas/Direction"
          },
          "expires_at": {
            "format": "date-time",
            "title": "Expires At",
            "type": "string"
          },
          "peer_facility_id": {
            "minLength": 1,
            "title": "Peer Facility Id",
            "type": "string"
          },
          "permit_id": {
            "format": "uuid",
            "title": "Permit Id",
            "type": "string"
          },
          "resumed_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Resumed At"
          },
          "revoked_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Revoked At"
          },
          "status": {
            "$ref": "#/components/schemas/PermitStatus"
          },
          "suspended_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Suspended At"
          },
          "terms_kind": {
            "$ref": "#/components/schemas/Direction"
          }
        },
        "required": [
          "permit_id",
          "peer_facility_id",
          "direction",
          "abi_tier_floor",
          "expires_at",
          "defined_by_actor_id",
          "status",
          "terms_kind",
          "defined_at"
        ],
        "title": "PermitSummaryDTO",
        "type": "object"
      },
      "PidinstAlternateIdentifierDTO": {
        "description": "PIDINST v1.0 Property 13: an alternate identifier under a known scheme.",
        "properties": {
          "kind": {
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Name"
          },
          "value": {
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "value",
          "kind"
        ],
        "title": "PidinstAlternateIdentifierDTO",
        "type": "object"
      },
      "PidinstDateDTO": {
        "description": "PIDINST v1.0 Property 11: a date marker on the instrument lifecycle.",
        "properties": {
          "date_type": {
            "title": "Date Type",
            "type": "string"
          },
          "value": {
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "value",
          "date_type"
        ],
        "title": "PidinstDateDTO",
        "type": "object"
      },
      "PidinstIdentifierDTO": {
        "description": "PIDINST v1.0 Property 1: persistent identifier of the instrument.",
        "properties": {
          "scheme": {
            "title": "Scheme",
            "type": "string"
          },
          "value": {
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "value",
          "scheme"
        ],
        "title": "PidinstIdentifierDTO",
        "type": "object"
      },
      "PidinstModelDTO": {
        "description": "PIDINST v1.0 Property 7: the model identification of the instrument.",
        "properties": {
          "identifier": {
            "title": "Identifier",
            "type": "string"
          },
          "identifier_type": {
            "title": "Identifier Type",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name",
          "identifier",
          "identifier_type"
        ],
        "title": "PidinstModelDTO",
        "type": "object"
      },
      "PidinstRecordResponse": {
        "description": "Read-side DTO mirroring the slice-C `PidinstRecord` dataclass.\n\nPydantic mirror so FastAPI can generate an OpenAPI schema. Decouples\nthe wire format from the slice-C `PidinstRecord` so the two can\nevolve independently. Field shapes verbatim from the slice-C tree.",
        "properties": {
          "alternate_identifiers": {
            "items": {
              "$ref": "#/components/schemas/PidinstAlternateIdentifierDTO"
            },
            "title": "Alternate Identifiers",
            "type": "array"
          },
          "dates": {
            "items": {
              "$ref": "#/components/schemas/PidinstDateDTO"
            },
            "title": "Dates",
            "type": "array"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Description"
          },
          "identifier": {
            "$ref": "#/components/schemas/PidinstIdentifierDTO"
          },
          "instrument_types": {
            "items": {
              "$ref": "#/components/schemas/InstrumentTypeDTO"
            },
            "title": "Instrument Types",
            "type": "array"
          },
          "landing_page": {
            "title": "Landing Page",
            "type": "string"
          },
          "manufacturers": {
            "items": {
              "$ref": "#/components/schemas/ManufacturerDTO"
            },
            "title": "Manufacturers",
            "type": "array"
          },
          "measured_variables": {
            "items": {
              "$ref": "#/components/schemas/MeasuredVariableDTO"
            },
            "title": "Measured Variables",
            "type": "array"
          },
          "measurement_techniques": {
            "items": {
              "$ref": "#/components/schemas/MeasurementTechniqueDTO"
            },
            "title": "Measurement Techniques",
            "type": "array"
          },
          "model": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/PidinstModelDTO"
              },
              {
                "type": "null"
              }
            ]
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "owners": {
            "items": {
              "$ref": "#/components/schemas/OwnerDTO"
            },
            "title": "Owners",
            "type": "array"
          },
          "publication_year": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Publication Year"
          },
          "publisher": {
            "title": "Publisher",
            "type": "string"
          },
          "related_identifiers": {
            "items": {
              "$ref": "#/components/schemas/RelatedIdentifierDTO"
            },
            "title": "Related Identifiers",
            "type": "array"
          },
          "schema_version": {
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "identifier",
          "schema_version",
          "landing_page",
          "name",
          "publisher",
          "publication_year",
          "owners",
          "manufacturers",
          "model",
          "description",
          "instrument_types",
          "measured_variables",
          "dates",
          "related_identifiers",
          "alternate_identifiers",
          "measurement_techniques"
        ],
        "title": "PidinstRecordResponse",
        "type": "object"
      },
      "PlacementBody": {
        "description": "Wire format for a Placement value object.\n\nMirrors the domain `Placement` dataclass field-for-field. Field\nsemantics, units, and tolerance rules live with the dataclass\nin `cora.equipment.aggregates._placement`; this body is purely\nthe wire shape that Pydantic parses.\n\nDomain validation (negative tolerance rejection) happens during\nthe dataclass construction in `to_domain`, not here, so the\nroute surfaces `InvalidPlacementError` -> 400 through the BC's\nexception handler.",
        "properties": {
          "parent_frame_id": {
            "description": "The Frame whose origin these coordinates are measured against. For a child Frame's placement, this MUST equal the owning Frame's parent_frame_id.",
            "format": "uuid",
            "title": "Parent Frame Id",
            "type": "string"
          },
          "reference_surface": {
            "$ref": "#/components/schemas/ReferenceSurface",
            "description": "Which physical feature of the part the position values are measured FROM. One of: ThermalFace, OpticCenter, ShieldingFace, MountingFace."
          },
          "rx": {
            "description": "Rotation around x (rad in SI_mm_rad).",
            "title": "Rx",
            "type": "number"
          },
          "ry": {
            "description": "Rotation around y.",
            "title": "Ry",
            "type": "number"
          },
          "rz": {
            "description": "Rotation around z.",
            "title": "Rz",
            "type": "number"
          },
          "tol_rx": {
            "description": "Bilateral plus-or-minus tolerance on rx.",
            "minimum": 0.0,
            "title": "Tol Rx",
            "type": "number"
          },
          "tol_ry": {
            "description": "Bilateral plus-or-minus tolerance on ry.",
            "minimum": 0.0,
            "title": "Tol Ry",
            "type": "number"
          },
          "tol_rz": {
            "description": "Bilateral plus-or-minus tolerance on rz.",
            "minimum": 0.0,
            "title": "Tol Rz",
            "type": "number"
          },
          "tol_x": {
            "description": "Bilateral plus-or-minus tolerance on x.",
            "minimum": 0.0,
            "title": "Tol X",
            "type": "number"
          },
          "tol_y": {
            "description": "Bilateral plus-or-minus tolerance on y.",
            "minimum": 0.0,
            "title": "Tol Y",
            "type": "number"
          },
          "tol_z": {
            "description": "Bilateral plus-or-minus tolerance on z.",
            "minimum": 0.0,
            "title": "Tol Z",
            "type": "number"
          },
          "units": {
            "$ref": "#/components/schemas/UnitSystem",
            "default": "SI_mm_rad",
            "description": "Unit system. v1 ships one value (SI_mm_rad): mm for translation, rad for rotation."
          },
          "x": {
            "description": "Translation along world x (mm in SI_mm_rad).",
            "title": "X",
            "type": "number"
          },
          "y": {
            "description": "Translation along world y.",
            "title": "Y",
            "type": "number"
          },
          "z": {
            "description": "Translation along beam direction z.",
            "title": "Z",
            "type": "number"
          }
        },
        "required": [
          "x",
          "y",
          "z",
          "rx",
          "ry",
          "rz",
          "parent_frame_id",
          "reference_surface",
          "tol_x",
          "tol_y",
          "tol_z",
          "tol_rx",
          "tol_ry",
          "tol_rz"
        ],
        "title": "PlacementBody",
        "type": "object"
      },
      "PlanListResponse": {
        "description": "Page of plans plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/PlanSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "PlanListResponse",
        "type": "object"
      },
      "PlanResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. `status` is the StrEnum's\nstring value (Defined / Versioned / Deprecated). `asset_ids`\nis sorted by UUID string form (deterministic). `version` is the\noperator-supplied label of the most recent version_plan call\n(null until first version). `created_at` / `versioned_at` /\n`deprecated_at` are projection-sourced lifecycle timestamps\n(Path C); see module docstring for\nnull-semantics.",
        "properties": {
          "asset_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Asset Ids",
            "type": "array"
          },
          "created_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "deprecated_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Deprecated At"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "practice_id": {
            "format": "uuid",
            "title": "Practice Id",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "version": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version"
          },
          "versioned_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Versioned At"
          }
        },
        "required": [
          "id",
          "name",
          "practice_id",
          "asset_ids",
          "status",
          "version"
        ],
        "title": "PlanResponse",
        "type": "object"
      },
      "PlanSummaryDTO": {
        "description": "One plan in a paginated list.",
        "properties": {
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "default_parameters_present": {
            "default": false,
            "description": "True iff the most recent `PlanDefaultParametersUpdated` event for this Plan carried a non-empty default_parameters dict. The dict content itself is loaded on demand.",
            "title": "Default Parameters Present",
            "type": "boolean"
          },
          "method_id": {
            "format": "uuid",
            "title": "Method Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "plan_id": {
            "format": "uuid",
            "title": "Plan Id",
            "type": "string"
          },
          "practice_id": {
            "format": "uuid",
            "title": "Practice Id",
            "type": "string"
          },
          "status": {
            "enum": [
              "Defined",
              "Versioned",
              "Deprecated"
            ],
            "title": "Status",
            "type": "string"
          },
          "version_tag": {
            "anyOf": [
              {
                "maxLength": 50,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version Tag"
          }
        },
        "required": [
          "plan_id",
          "name",
          "practice_id",
          "method_id",
          "status",
          "created_at"
        ],
        "title": "PlanSummaryDTO",
        "type": "object"
      },
      "PolicyListResponse": {
        "description": "Page of policies plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/PolicySummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "PolicyListResponse",
        "type": "object"
      },
      "PolicySummaryDTO": {
        "description": "One policy in a paginated list.",
        "properties": {
          "conduit_id": {
            "format": "uuid",
            "title": "Conduit Id",
            "type": "string"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "policy_id": {
            "format": "uuid",
            "title": "Policy Id",
            "type": "string"
          }
        },
        "required": [
          "policy_id",
          "name",
          "conduit_id",
          "created_at"
        ],
        "title": "PolicySummaryDTO",
        "type": "object"
      },
      "PortViewDTO": {
        "description": "Read-side DTO for one Asset port on the integration-view bundle.",
        "properties": {
          "direction": {
            "title": "Direction",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "signal_type": {
            "title": "Signal Type",
            "type": "string"
          }
        },
        "required": [
          "name",
          "direction",
          "signal_type"
        ],
        "title": "PortViewDTO",
        "type": "object"
      },
      "PracticeListResponse": {
        "description": "Page of practices plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/PracticeSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "PracticeListResponse",
        "type": "object"
      },
      "PracticeResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. `status` is the StrEnum's\nstring value (Defined / Versioned / Deprecated). `version` is the\noperator-supplied label of the most recent version_practice call\n(null until first version). `created_at` / `versioned_at` /\n`deprecated_at` are projection-sourced lifecycle timestamps\n(Path C); see module docstring for\nnull-semantics.",
        "properties": {
          "created_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "deprecated_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Deprecated At"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "method_id": {
            "format": "uuid",
            "title": "Method Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "site_id": {
            "format": "uuid",
            "title": "Site Id",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "version": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version"
          },
          "versioned_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Versioned At"
          }
        },
        "required": [
          "id",
          "name",
          "method_id",
          "site_id",
          "status",
          "version"
        ],
        "title": "PracticeResponse",
        "type": "object"
      },
      "PracticeSummaryDTO": {
        "description": "One practice in a paginated list.",
        "properties": {
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "method_id": {
            "format": "uuid",
            "title": "Method Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "practice_id": {
            "format": "uuid",
            "title": "Practice Id",
            "type": "string"
          },
          "site_id": {
            "format": "uuid",
            "title": "Site Id",
            "type": "string"
          },
          "status": {
            "enum": [
              "Defined",
              "Versioned",
              "Deprecated"
            ],
            "title": "Status",
            "type": "string"
          },
          "version_tag": {
            "anyOf": [
              {
                "maxLength": 50,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version Tag"
          }
        },
        "required": [
          "practice_id",
          "name",
          "method_id",
          "site_id",
          "status",
          "created_at"
        ],
        "title": "PracticeSummaryDTO",
        "type": "object"
      },
      "PresenceMode": {
        "description": "How a participant is present at the Visit.\n\nTwo values closed day-one per `[[project_visit_aggregate_design]]`:\n\n  - `physical`  -- on-site (badge-in at the beamline)\n  - `remote`    -- remote-checked-in (e.g., PI driving via API from\n                   another time zone while the on-site postdoc runs\n                   the bench; S3 scenario)",
        "enum": [
          "physical",
          "remote"
        ],
        "title": "PresenceMode",
        "type": "string"
      },
      "ProcedureListResponse": {
        "description": "Page of procedures plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/ProcedureSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "ProcedureListResponse",
        "type": "object"
      },
      "ProcedureResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model so the two can evolve independently.\n`target_asset_ids` is sorted by UUID string form. `status` is the\nStrEnum's string value (Defined / Running / Completed / Aborted /\nTruncated). `parent_run_id` is null for standalone procedures.",
        "properties": {
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "maxLength": 50,
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "parent_run_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Run Id"
          },
          "status": {
            "$ref": "#/components/schemas/ProcedureStatus"
          },
          "target_asset_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Target Asset Ids",
            "type": "array"
          }
        },
        "required": [
          "id",
          "name",
          "kind",
          "target_asset_ids",
          "status",
          "parent_run_id"
        ],
        "title": "ProcedureResponse",
        "type": "object"
      },
      "ProcedureStatus": {
        "description": "The Procedure's lifecycle state.\n\nFive values declared day one for forward-compat\n(additive-state pattern; legacy events fold cleanly because\nonly DEFINED is reachable after register_procedure):\n\n  - `Defined`     -- registration-time genesis; pre-execution.\n                      Operator can edit / inspect / submit for\n                      review (future Decision BC integration).\n                      Cannot accept step events yet.\n  - `Running`     -- post-start_procedure. Step events accepted\n                      via append_procedure_steps.\n  - `Completed`   -- happy path via complete_procedure.\n                      Strict-not-idempotent.\n  - `Aborted`     -- emergency exit via abort_procedure.\n  - `Truncated`   -- retroactive cleanup via truncate_procedure.\n                      Mirrors RunTruncated.\n\n`Verifying` and `Held / Resumed` are deliberately NOT in this\nenum. Per [[project_operation_design]] standards-corpus research:\n`Verifying` is NOT standards-blessed at FSM level (PackML uses\n`Completing` for closeout/check work; OPC UA Programs has no\nVerify state). Per-step Check happens within Running synchronously\n(via the Step logbook's check_passed field). Held / Resumed\ndeferred until pilot operator feedback surfaces a need.\n\nNaming convention (per Run BC gate review): gerund /\nadjective for active steady-states (matches PackML / Bluesky);\npast-participle for terminals. `Defined` is past-participle (a\nprocedure WAS defined); `Running` is gerund-as-adjective; the\nrest are past-participle terminals.\n\nEnum values are PascalCase strings (matches BC-map status\nvocabulary; log lines and DTOs read naturally without mapping).",
        "enum": [
          "Defined",
          "Running",
          "Completed",
          "Aborted",
          "Truncated"
        ],
        "title": "ProcedureStatus",
        "type": "string"
      },
      "ProcedureStepRequest": {
        "additionalProperties": false,
        "description": "One step entry's input payload (polymorphic, ISA-106-aligned).\n\nRequired: event_id + step_kind + payload + sampled_at.\n`occurred_at` is optional.",
        "properties": {
          "event_id": {
            "description": "Producer-supplied UUIDv7 entry id. Idempotency / dedup key; re-issuing the same id is a silent no-op.",
            "format": "uuid",
            "title": "Event Id",
            "type": "string"
          },
          "occurred_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional handler-time override (ISO-8601 with timezone). Defaults to server clock when omitted; producers with their own ingest-time clock can populate explicitly.",
            "title": "Occurred At"
          },
          "payload": {
            "additionalProperties": true,
            "description": "Kind-specific body. Setpoint: channel + target_value + units? + ramp_rate?. Action: action_name + params. Check: channel + passed + expected? + actual? + tolerance?. Per-kind Pydantic model validation lands when pilot vocabulary settles (watch item).",
            "title": "Payload",
            "type": "object"
          },
          "sampled_at": {
            "description": "phenomenonTime (ISO-8601 with timezone): when the step physically happened in the field.",
            "format": "date-time",
            "title": "Sampled At",
            "type": "string"
          },
          "step_kind": {
            "description": "ISA-106-aligned discriminator. 'setpoint' = control-point change applied. 'action' = discrete operation performed. 'check' = verification recorded. Future values land additively (no migration). Single source of truth: the `StepKind` Literal exported from cora.operation.aggregates.procedure.",
            "enum": [
              "setpoint",
              "action",
              "check"
            ],
            "title": "Step Kind",
            "type": "string"
          }
        },
        "required": [
          "event_id",
          "step_kind",
          "payload",
          "sampled_at"
        ],
        "title": "ProcedureStepRequest",
        "type": "object"
      },
      "ProcedureSummaryDTO": {
        "description": "One procedure in a paginated list.",
        "properties": {
          "interrupted_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Interrupted At"
          },
          "kind": {
            "maxLength": 50,
            "title": "Kind",
            "type": "string"
          },
          "last_status_changed_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Status Changed At"
          },
          "last_status_reason": {
            "anyOf": [
              {
                "maxLength": 500,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Status Reason"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "parent_run_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Parent Run Id"
          },
          "procedure_id": {
            "format": "uuid",
            "title": "Procedure Id",
            "type": "string"
          },
          "registered_at": {
            "format": "date-time",
            "title": "Registered At",
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/ProcedureStatus"
          },
          "steps_logbook_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Steps Logbook Id"
          },
          "target_asset_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Target Asset Ids",
            "type": "array"
          }
        },
        "required": [
          "procedure_id",
          "name",
          "kind",
          "target_asset_ids",
          "status",
          "registered_at"
        ],
        "title": "ProcedureSummaryDTO",
        "type": "object"
      },
      "PromoteCautionProposalResponse": {
        "description": "Successful promotion response body.",
        "properties": {
          "caution_id": {
            "format": "uuid",
            "title": "Caution Id",
            "type": "string"
          }
        },
        "required": [
          "caution_id"
        ],
        "title": "PromoteCautionProposalResponse",
        "type": "object"
      },
      "PromoteDatasetRequest": {
        "description": "Body for `POST /datasets/{dataset_id}/promote`.",
        "properties": {
          "reason": {
            "description": "Free-form reason for the promotion (1-500 chars after trimming). Captured verbatim in the audit log immutably. Operationally: 'this is the publication-grade dataset because <X>'. Today the field is unstructured; structured taxonomy is future-additive on the same triggers as DatasetDiscarded reason.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "PromoteDatasetRequest",
        "type": "object"
      },
      "PublishCalibrationRevisionRequest": {
        "description": "Body for `POST /calibrations/{id}/revisions/{revision_id}/publish`.",
        "properties": {
          "peer_facility_id": {
            "description": "Opaque id of the peer facility this publication is targeted at. Resolved at the handler via PermitLookup to locate the matching Active outbound Permit; missing or inactive permits raise 409.",
            "title": "Peer Facility Id",
            "type": "string"
          }
        },
        "required": [
          "peer_facility_id"
        ],
        "title": "PublishCalibrationRevisionRequest",
        "type": "object"
      },
      "PublishCalibrationRevisionResponse": {
        "description": "Response body for the publish action.",
        "properties": {
          "receipt_id": {
            "format": "uuid",
            "title": "Receipt Id",
            "type": "string"
          }
        },
        "required": [
          "receipt_id"
        ],
        "title": "PublishCalibrationRevisionResponse",
        "type": "object"
      },
      "RateDecisionRequest": {
        "description": "Body for `POST /decisions/{decision_id}/ratings`.",
        "properties": {
          "comment": {
            "anyOf": [
              {
                "maxLength": 2000,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional free-form comment (1-2000 chars after trim). Pass null to omit; do not pass empty string.",
            "title": "Comment"
          },
          "rating": {
            "$ref": "#/components/schemas/DecisionRating",
            "description": "Closed rating value: `useful` (decision helped), `misleading` (decision led astray), or `ignored` (operator saw the decision and chose not to act). Note `ignored` is a positive marker, distinct from no-rating-at-all."
          }
        },
        "required": [
          "rating"
        ],
        "title": "RateDecisionRequest",
        "type": "object"
      },
      "ReadScope": {
        "description": "How much the peer may read under an outbound permit.\n\n- `ListMetadataOnly`:     index entries (titles, ids, tags) only;\n                          no artifact bodies.\n- `ReadAllArtifacts`:     full artifact bytes for every entry\n                          in scope.\n- `ReadByABITierMinimum`: artifact bytes only for entries at or\n                          above `abi_tier_floor`.",
        "enum": [
          "ListMetadataOnly",
          "ReadAllArtifacts",
          "ReadByABITierMinimum"
        ],
        "title": "ReadScope",
        "type": "string"
      },
      "ReasoningEntryRequest": {
        "additionalProperties": false,
        "description": "One reasoning entry's input payload (OTel gen_ai.* shape).\n\nRequired: provider_name + operation_name + request_model\n(NOT NULL discriminators per OTel semconv v1.38). Rest are\noptional; messages is the opt-in PII-gated payload.",
        "properties": {
          "agent_description": {
            "anyOf": [
              {
                "maxLength": 2000,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Description"
          },
          "agent_id": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Id"
          },
          "agent_name": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Agent Name"
          },
          "conversation_id": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Conversation Id"
          },
          "duration": {
            "anyOf": [
              {
                "minimum": 0.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Span duration (end_time - start_time).",
            "title": "Duration"
          },
          "event_id": {
            "description": "Producer-supplied UUIDv7 entry id. Idempotency / dedup key; re-issuing the same id is a silent no-op.",
            "format": "uuid",
            "title": "Event Id",
            "type": "string"
          },
          "finish_reasons": {
            "description": "OTel gen_ai.response.finish_reasons (multiple stops possible).",
            "items": {
              "type": "string"
            },
            "maxItems": 16,
            "title": "Finish Reasons",
            "type": "array"
          },
          "input_tokens": {
            "anyOf": [
              {
                "minimum": 0.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Input Tokens"
          },
          "messages": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "Opt-in PII-gated payload: prompt + completion message bodies (OTel event-payload split). Producer responsible for JSON-roundtrippability.",
            "title": "Messages"
          },
          "occurred_at": {
            "description": "Domain time (ISO-8601 with timezone) of the trace event.",
            "format": "date-time",
            "title": "Occurred At",
            "type": "string"
          },
          "operation_name": {
            "description": "OTel gen_ai.operation.name. Well-known values: chat, text_completion, embeddings, execute_tool, invoke_agent, create_agent. Open string; new operation names accepted.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Operation Name",
            "type": "string"
          },
          "output_tokens": {
            "anyOf": [
              {
                "minimum": 0.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Output Tokens"
          },
          "output_type": {
            "anyOf": [
              {
                "maxLength": 100,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Output Type"
          },
          "provider_name": {
            "description": "OTel gen_ai.provider.name (replaces deprecated gen_ai.system). Examples: anthropic, openai, google.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Provider Name",
            "type": "string"
          },
          "request_max_tokens": {
            "anyOf": [
              {
                "minimum": 0.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Request Max Tokens"
          },
          "request_model": {
            "description": "OTel gen_ai.request.model (the requested model id).",
            "maxLength": 200,
            "minLength": 1,
            "title": "Request Model",
            "type": "string"
          },
          "request_temperature": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Request Temperature"
          },
          "request_top_p": {
            "anyOf": [
              {
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "title": "Request Top P"
          },
          "response_id": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Response Id"
          },
          "response_model": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Response Model"
          },
          "tool_call_id": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Tool Call Id"
          },
          "tool_name": {
            "anyOf": [
              {
                "maxLength": 200,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Tool Name"
          },
          "tool_type": {
            "anyOf": [
              {
                "maxLength": 100,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "OTel gen_ai.tool.type. Values: Extension, Function, Datastore.",
            "title": "Tool Type"
          }
        },
        "required": [
          "event_id",
          "occurred_at",
          "operation_name",
          "provider_name",
          "request_model"
        ],
        "title": "ReasoningEntryRequest",
        "type": "object"
      },
      "ReceiptKind": {
        "description": "Receipt schemes an inbound permit requires for accepted artifacts.\n\n  - `scitt`:        transparency-log receipt arm\n  - `rekor_sct`:    certificate-transparency receipt arm\n  - `ts_authority`: timestamp-authority token arm\n\nThe inbound side decides which receipt kinds it requires; an\ninbound pull refuses an artifact lacking every required kind.\nDefaulting to empty frozenset means \"no receipt required\" (initial\ndeployment posture; tighten via update slice as ecosystem matures).\n\nSee [[project_federation_adapter_design]] for the wire-tier per-arm\nformat vocabulary.",
        "enum": [
          "scitt",
          "rekor_sct",
          "ts_authority"
        ],
        "title": "ReceiptKind",
        "type": "string"
      },
      "RecipeResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. `status` is the StrEnum's\nstring value (Defined / Versioned / Deprecated). `version` is\nthe operator-supplied label of the most recent `version_recipe`\ncall (null until first version). `steps` is the wire-format\ndict (BindingRef sentinels serialize as `{__binding__: name}`).\n`replaced_by_recipe_id` is null on Defined / Versioned /\nDeprecated-without-replacement; populated when a deprecation\nsupplied a successor pointer. `created_at` / `versioned_at` /\n`deprecated_at` are projection-sourced lifecycle timestamps\n(Path C); see module docstring for null semantics.",
        "properties": {
          "capability_id": {
            "format": "uuid",
            "title": "Capability Id",
            "type": "string"
          },
          "created_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Created At"
          },
          "deprecated_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Deprecated At"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "replaced_by_recipe_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Replaced By Recipe Id"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "steps": {
            "additionalProperties": true,
            "title": "Steps",
            "type": "object"
          },
          "version": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Version"
          },
          "versioned_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Versioned At"
          }
        },
        "required": [
          "id",
          "name",
          "capability_id",
          "status",
          "version",
          "steps",
          "replaced_by_recipe_id"
        ],
        "title": "RecipeResponse",
        "type": "object"
      },
      "ReferenceSurface": {
        "description": "The physical feature of a part that a Placement is measured FROM.\n\nClosed enum; CORA precedent is the Affordance StrEnum. Widening\nrules and GD&T-term-collision rationale live in the module docstring.",
        "enum": [
          "ThermalFace",
          "OpticCenter",
          "ShieldingFace",
          "MountingFace"
        ],
        "title": "ReferenceSurface",
        "type": "string"
      },
      "RegenerateRunDebriefRequest": {
        "description": "Body for `POST /agents/run-debriefer/runs/{run_id}/regenerate-debrief`.",
        "properties": {
          "parent_decision_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional ref to the prior RunDebrief Decision being re-evaluated. When supplied, the new Decision's `parent_id` is set, forming a PROV-O `wasInformedBy` chain. Must reference the same Run as the path's `run_id`.",
            "title": "Parent Decision Id"
          }
        },
        "title": "RegenerateRunDebriefRequest",
        "type": "object"
      },
      "RegenerateRunDebriefResponse": {
        "description": "Response body for `POST /agents/run-debriefer/runs/{run_id}/regenerate-debrief`.",
        "properties": {
          "decision_id": {
            "format": "uuid",
            "title": "Decision Id",
            "type": "string"
          }
        },
        "required": [
          "decision_id"
        ],
        "title": "RegenerateRunDebriefResponse",
        "type": "object"
      },
      "RegisterActorRequest": {
        "description": "Body for `POST /actors`.",
        "properties": {
          "kind": {
            "default": "human",
            "description": "Closed discriminator. 'human' (default) for operator registration. 'service_account' for machine callers: CI bridges, autonomous agent runtime processes, future TomoScan / EPICS bridges. 'agent'-kind Actors are minted exclusively via POST /agents (cross-BC atomic write); this endpoint rejects it with 400.",
            "enum": [
              "human",
              "service_account"
            ],
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "description": "Display name for the new actor.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "RegisterActorRequest",
        "type": "object"
      },
      "RegisterActorResponse": {
        "description": "Response body for `POST /actors`.",
        "properties": {
          "actor_id": {
            "format": "uuid",
            "title": "Actor Id",
            "type": "string"
          }
        },
        "required": [
          "actor_id"
        ],
        "title": "RegisterActorResponse",
        "type": "object"
      },
      "RegisterAssetRequest": {
        "description": "Body for `POST /assets`.\n\n`level` accepts the StrEnum's PascalCase string values\n(\"Enterprise\" / \"Site\" / \"Area\" / \"Unit\" / \"Component\" /\n\"Device\"); Pydantic rejects unknowns with 422.\n\n`parent_id` is required for non-Enterprise levels and must be\nnull for Enterprise; that's a domain invariant enforced by\nthe decider (raises InvalidAssetParentError \u2192 400), not by\nPydantic, since the rule is conditional on `level`.",
        "properties": {
          "alternate_identifiers": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/AlternateIdentifierBody"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional PIDINST v1.0 Property 13 alternate-identifier tuples (operator-supplied serial numbers, inventory tags, vendor-specific schemes) seeded at registration. Each entry is a flat (kind, value) pair; kind is closed vocabulary SerialNumber | InventoryNumber | Other. Cross-Asset uniqueness on (kind, value) is NOT enforced in v1.",
            "title": "Alternate Identifiers"
          },
          "drawing": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DrawingBody"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional engineering reference for the physical specimen (distinct from Mount.drawing, which references the slot). Captured at registration only; not mutable in v1."
          },
          "level": {
            "$ref": "#/components/schemas/AssetLevel",
            "description": "Hierarchical level. One of: Enterprise (root, requires null parent_id), Site, Area, Unit, Component, Device."
          },
          "model_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional reference to the Model catalog entry this Asset is an instance of (Family -> Model -> Assembly -> Asset ladder). Set ONCE at registration; rebind path is decommission + re-register. The handler verifies the Model stream exists before invoking the decider (404 if missing); no subset check at register time because the genesis Asset families set is empty.",
            "title": "Model Id"
          },
          "name": {
            "description": "Display name for the new asset.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "owners": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/AssetOwnerBody"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional PIDINST v1.0 Property 5 institutional owner blocks seeded at registration. Each entry carries a mandatory `name` plus optional `contact`, `identifier`, and `identifier_type`. Owner names must be unique within the payload (rejected as 409 otherwise). Aggregate allows 0-n owners; the PIDINST serializer enforces the 1-n cardinality at emission time.",
            "title": "Owners"
          },
          "parent_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Immediate parent in the hierarchy tree. Must be null for Enterprise-level assets; required for all others. Eventual-consistency: parent's existence is NOT verified.",
            "title": "Parent Id"
          }
        },
        "required": [
          "name",
          "level",
          "parent_id"
        ],
        "title": "RegisterAssetRequest",
        "type": "object"
      },
      "RegisterAssetResponse": {
        "description": "Response body for `POST /assets`.",
        "properties": {
          "asset_id": {
            "format": "uuid",
            "title": "Asset Id",
            "type": "string"
          }
        },
        "required": [
          "asset_id"
        ],
        "title": "RegisterAssetResponse",
        "type": "object"
      },
      "RegisterCampaignRequest": {
        "description": "Body for `POST /campaigns`.",
        "properties": {
          "description": {
            "anyOf": [
              {
                "maxLength": 2000,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional free-form description. Trimmed at the domain layer.",
            "title": "Description"
          },
          "external_id": {
            "anyOf": [
              {
                "maxLength": 100,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional facility-minted or DataCite Project DOI assigned lazily. Today: operator-supplied at register time only; no mint slice yet.",
            "title": "External Id"
          },
          "external_refs": {
            "description": "Anti-corruption refs to upstream-deferred concepts (proposal, btr, visit, cycle). Empty list IS allowed.",
            "items": {
              "$ref": "#/components/schemas/ExternalRefDTO-Input"
            },
            "title": "External Refs",
            "type": "array"
          },
          "intent": {
            "$ref": "#/components/schemas/CampaignIntent",
            "description": "Closed intent-shape vocabulary: Series, Sweep, Coordination, Block. Describes what KIND of coordination the Campaign carries, not the scientific technique (technique tagging lives on tags)."
          },
          "lead_actor_id": {
            "description": "REQUIRED. The Campaign's PI / lead operator. Operator-asserted; may differ from the registering principal (LIMS Study Director precedent).",
            "format": "uuid",
            "title": "Lead Actor Id",
            "type": "string"
          },
          "name": {
            "description": "Operator-meaningful Campaign name. Trimmed at the domain layer.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "subject_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional informational Subject reference. LOOSE policy: NOT enforced as a member-Run invariant. Multi-Subject Campaigns (Block, Sweep) are legitimate.",
            "title": "Subject Id"
          },
          "tags": {
            "description": "Optional free-form tags. Each tag 1-50 chars after trim. Empty list IS allowed.",
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          }
        },
        "required": [
          "name",
          "intent",
          "lead_actor_id"
        ],
        "title": "RegisterCampaignRequest",
        "type": "object"
      },
      "RegisterCampaignResponse": {
        "description": "Response body for `POST /campaigns`.",
        "properties": {
          "campaign_id": {
            "format": "uuid",
            "title": "Campaign Id",
            "type": "string"
          }
        },
        "required": [
          "campaign_id"
        ],
        "title": "RegisterCampaignResponse",
        "type": "object"
      },
      "RegisterCautionRequest": {
        "description": "Body for `POST /cautions`.",
        "properties": {
          "category": {
            "$ref": "#/components/schemas/CautionCategory",
            "description": "Closed controlled-vocabulary category. One of Wear, Calibration, Wiring, OperationalWindow, InterlockQuirk, ProcedureGotcha."
          },
          "expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional auto-retire hint. Must be strictly in the future (relative to server clock at register time).",
            "title": "Expires At"
          },
          "propagate_to_children": {
            "default": false,
            "description": "Opt-in for Asset-hierarchy inheritance (AVEVA AF template-inheritance anti-pattern guard). Default False.",
            "title": "Propagate To Children",
            "type": "boolean"
          },
          "severity": {
            "$ref": "#/components/schemas/CautionSeverity",
            "description": "ANSI Z535 signal-word ladder (downshifted). One of Notice, Caution, Warning. No Danger tier; formal lockout lives in Safety BC."
          },
          "tags": {
            "description": "Optional free-form tags. Each tag 1-50 chars after trim. Empty list IS allowed.",
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "target": {
            "description": "Polymorphic target: `{kind: 'Asset', id: <uuid>}` or `{kind: 'Procedure', id: <uuid>}`. Day-1 lock; Run + Subject variants land additively per design memo Watch items.",
            "discriminator": {
              "mapping": {
                "Asset": "#/components/schemas/TargetAssetDTO",
                "Procedure": "#/components/schemas/TargetProcedureDTO"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/components/schemas/TargetAssetDTO"
              },
              {
                "$ref": "#/components/schemas/TargetProcedureDTO"
              }
            ],
            "title": "Target"
          },
          "text": {
            "description": "Free-form caution body. Trimmed at the domain layer.",
            "maxLength": 2000,
            "minLength": 1,
            "title": "Text",
            "type": "string"
          },
          "workaround": {
            "description": "REQUIRED. Free-form 'what does the operator do about it' field (corpus's strongest convergence: KEDB, MIL-STD-882, OSHA, CAPA all mandate it). Trimmed at the domain layer.",
            "maxLength": 2000,
            "minLength": 1,
            "title": "Workaround",
            "type": "string"
          }
        },
        "required": [
          "target",
          "category",
          "severity",
          "text",
          "workaround"
        ],
        "title": "RegisterCautionRequest",
        "type": "object"
      },
      "RegisterCautionResponse": {
        "description": "Response body for `POST /cautions`.",
        "properties": {
          "caution_id": {
            "format": "uuid",
            "title": "Caution Id",
            "type": "string"
          }
        },
        "required": [
          "caution_id"
        ],
        "title": "RegisterCautionResponse",
        "type": "object"
      },
      "RegisterClearanceRequest": {
        "description": "Body for `POST /clearances`.",
        "properties": {
          "bindings": {
            "description": "Multi-binding: what this clearance gates against. Discriminated by `kind` ('Subject' | 'Asset' | 'Run' | 'Procedure' | 'External'). ExternalBinding is the anti-corruption escape hatch for upstream-deferred refs (proposal / btr / lab_visit / session).",
            "items": {
              "discriminator": {
                "mapping": {
                  "Asset": "#/components/schemas/BindingAssetDTO",
                  "External": "#/components/schemas/BindingExternalDTO",
                  "Procedure": "#/components/schemas/BindingProcedureDTO",
                  "Run": "#/components/schemas/BindingRunDTO",
                  "Subject": "#/components/schemas/BindingSubjectDTO"
                },
                "propertyName": "kind"
              },
              "oneOf": [
                {
                  "$ref": "#/components/schemas/BindingSubjectDTO"
                },
                {
                  "$ref": "#/components/schemas/BindingAssetDTO"
                },
                {
                  "$ref": "#/components/schemas/BindingRunDTO"
                },
                {
                  "$ref": "#/components/schemas/BindingProcedureDTO"
                },
                {
                  "$ref": "#/components/schemas/BindingExternalDTO"
                }
              ]
            },
            "minItems": 1,
            "title": "Bindings",
            "type": "array"
          },
          "declarations": {
            "description": "Hazard claims (intrinsic descriptor + mitigations) per binding target. Empty list allowed.",
            "items": {
              "$ref": "#/components/schemas/HazardDeclarationDTO"
            },
            "title": "Declarations",
            "type": "array"
          },
          "external_id": {
            "anyOf": [
              {
                "maxLength": 100,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional facility-minted regulatory ID (for example 'ESAF-12345'); lazy-mint per the PID landscape pattern.",
            "title": "External Id"
          },
          "facility_asset_id": {
            "description": "Reference to the Asset.Level.Site for the facility that issued (or will issue) this clearance. CORA does NOT verify the Asset exists at register time (eventual-consistency convention).",
            "format": "uuid",
            "title": "Facility Asset Id",
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/ClearanceKind",
            "description": "Form-type (template). 10 facility-independent form-types covering 9 surveyed facilities. Facility identity carried separately in `facility_asset_id`."
          },
          "risk_band": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/RiskBand"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional triage band (Green / Yellow / Red) per HSE ALARP semantics. Used by ESRF / MAX IV / DLS / DESY / SLAC variants; None for APS-style ESAF."
          },
          "title": {
            "description": "Operator-readable title for the clearance.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Title",
            "type": "string"
          },
          "valid_from": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Effective-from timestamp; ISO-8601 on the wire, parsed by Pydantic. Malformed values 422; tz-naive timestamps are accepted but discouraged.",
            "title": "Valid From"
          },
          "valid_until": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Effective-until timestamp; ISO-8601 on the wire, parsed by Pydantic. Must be strictly greater than `valid_from` (zero-duration windows rejected at decider).",
            "title": "Valid Until"
          }
        },
        "required": [
          "kind",
          "facility_asset_id",
          "title",
          "bindings"
        ],
        "title": "RegisterClearanceRequest",
        "type": "object"
      },
      "RegisterClearanceResponse": {
        "description": "Response body for `POST /clearances`.",
        "properties": {
          "clearance_id": {
            "format": "uuid",
            "title": "Clearance Id",
            "type": "string"
          }
        },
        "required": [
          "clearance_id"
        ],
        "title": "RegisterClearanceResponse",
        "type": "object"
      },
      "RegisterCredentialRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /federation/credentials`.",
        "properties": {
          "audience": {
            "description": "Opaque string scoping the credential to a particular peer / endpoint.",
            "minLength": 1,
            "title": "Audience",
            "type": "string"
          },
          "expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional contractual upper bound. When set, must lie strictly after the server's now.",
            "title": "Expires At"
          },
          "facility_id": {
            "description": "Opaque string id of the facility this credential binds to. Facility identity is external to CORA; we do NOT mint facility ids.",
            "minLength": 1,
            "title": "Facility Id",
            "type": "string"
          },
          "public_material_ref": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Opaque pointer to the public counterpart when the purpose has one (verification key, encryption-public-key, certificate handle). None for symmetric purposes.",
            "title": "Public Material Ref"
          },
          "purpose": {
            "$ref": "#/components/schemas/CredentialPurpose",
            "description": "Closed enum selecting which of the six roles this credential plays (Signing / Verification / Authentication / Encryption / SealOnlineSigning / SealOfflineRoot)."
          },
          "secret_ref": {
            "description": "Opaque pointer (URI / KMS ARN / vault path) to the secret material held in a SecretStore adapter. The aggregate NEVER carries raw secret bytes; the caller is responsible for landing bytes in SecretStore before invoking this slice.",
            "minLength": 1,
            "title": "Secret Ref",
            "type": "string"
          }
        },
        "required": [
          "facility_id",
          "audience",
          "purpose",
          "secret_ref"
        ],
        "title": "RegisterCredentialRequest",
        "type": "object"
      },
      "RegisterCredentialResponse": {
        "description": "Response body for `POST /federation/credentials`.",
        "properties": {
          "credential_id": {
            "format": "uuid",
            "title": "Credential Id",
            "type": "string"
          }
        },
        "required": [
          "credential_id"
        ],
        "title": "RegisterCredentialResponse",
        "type": "object"
      },
      "RegisterDatasetRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /datasets`.",
        "properties": {
          "byte_size": {
            "description": "Bulk content size in bytes. Zero is valid (an empty file is a valid Dataset). Upper bound is not enforced at the BC; storage backends impose their own.",
            "minimum": 0.0,
            "title": "Byte Size",
            "type": "integer"
          },
          "checksum": {
            "$ref": "#/components/schemas/ChecksumRequest",
            "description": "Bulk-content integrity hash (algorithm + value)."
          },
          "derived_from": {
            "description": "Optional lineage edges to other Datasets this one was derived from. Empty for raw/captured data; multi-entry for fusions / comparative derivations.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "maxItems": 64,
            "title": "Derived From",
            "type": "array"
          },
          "encoding": {
            "$ref": "#/components/schemas/EncodingRequest",
            "description": "Structured encoding descriptor (media_type + conforms_to profile URIs)."
          },
          "name": {
            "description": "Display name for the new Dataset.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "producing_run_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional id of the Run that produced this Dataset. None for externally-sourced or pre-existing data.",
            "title": "Producing Run Id"
          },
          "subject_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional id of the Subject the Dataset is about. None for calibration / dark-field / synthetic data with no sample.",
            "title": "Subject Id"
          },
          "uri": {
            "description": "Opaque URI string pointing at the bulk content (s3://, file://, https://, globus://, etc.).",
            "maxLength": 2048,
            "minLength": 1,
            "title": "Uri",
            "type": "string"
          },
          "used_calibration_ids": {
            "description": "Optional list of CalibrationRevision ids the reconstruction (or any derivative) actually used (Calibration BC AsShot citation pattern). Symmetric to Run.pinned_calibration_ids on the acquired-from Run; the two sets are independent: reconstruction may legitimately cite refined revisions not in the producing Run's pin set (Current vs AsShot pattern). NO cross-BC existence check at the write path (revision-cited atomic-ID model; eventual-consistency stance per [[project_calibration_design]]). IMMUTABLE on the aggregate after register. Order on the wire is not significant; the aggregate carries a frozenset and the decider sorts for deterministic event-payload bytes.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "maxItems": 64,
            "title": "Used Calibration Ids",
            "type": "array"
          }
        },
        "required": [
          "name",
          "uri",
          "checksum",
          "byte_size",
          "encoding"
        ],
        "title": "RegisterDatasetRequest",
        "type": "object"
      },
      "RegisterDatasetResponse": {
        "description": "Response body for `POST /datasets`.",
        "properties": {
          "dataset_id": {
            "format": "uuid",
            "title": "Dataset Id",
            "type": "string"
          }
        },
        "required": [
          "dataset_id"
        ],
        "title": "RegisterDatasetResponse",
        "type": "object"
      },
      "RegisterDecisionRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /decisions`.",
        "properties": {
          "actor_id": {
            "description": "WHO made the decision. Existence-checked; 404 if the Actor doesn't exist. Decision can be made by any Actor including Deactivated (historical fact still holds).",
            "format": "uuid",
            "title": "Actor Id",
            "type": "string"
          },
          "alternatives": {
            "description": "Options considered (ORDER PRESERVED for AI top-k). For PolicyGrant context, carries the determining policy IDs (Cedar-style).",
            "items": {
              "type": "string"
            },
            "maxItems": 32,
            "title": "Alternatives",
            "type": "array"
          },
          "choice": {
            "description": "Free-form text capturing what was decided (1-500 chars after trim).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Choice",
            "type": "string"
          },
          "confidence": {
            "anyOf": [
              {
                "maximum": 1.0,
                "minimum": 0.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Confidence in the decision, in [0.0, 1.0]. Pair with confidence_source by convention so consumers can audit how the value was computed (BC does not enforce pairing; either field can be set independently).",
            "title": "Confidence"
          },
          "confidence_source": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DecisionConfidenceSource"
              },
              {
                "type": "null"
              }
            ],
            "description": "How confidence was computed. self_reported = AI claim (lowest audit weight); logprob = derived from token log-probabilities; ensemble = aggregated over multiple deciders; human = subjective rating."
          },
          "context": {
            "description": "Decision discriminator. Open-ended string; well-known values include RecipeApproval, RunAbort, RunStop, RunTruncate, ResourceAllocation, PolicyGrant, ProcedureExecution, DatasetDiscard.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Context",
            "type": "string"
          },
          "inputs": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional dict carrying the values the rule was applied to (per ILAC-G8:09/2019: a rule without its inputs is unauditable). Cap 64 keys.",
            "title": "Inputs"
          },
          "override_kind": {
            "anyOf": [
              {
                "enum": [
                  "correction",
                  "exception",
                  "appeal",
                  "supersession",
                  "invalidation"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When parent_id is set: WHY this Decision overrides the prior one. correction = we got it wrong; exception = explicit deviation; appeal = formal challenge; supersession = newer context replaces older. Required-with-parent invariant: set both or neither (400 otherwise).",
            "title": "Override Kind"
          },
          "parent_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional ref to a prior Decision being overridden / corrected / appealed / superseded. Existence-checked; 404 if missing.",
            "title": "Parent Id"
          },
          "reasoning": {
            "anyOf": [
              {
                "maxLength": 5000,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free-form prose explaining the decision (human-readable summary). AI token-by-token traces go to a separate logbook on the Decision aggregate (8c).",
            "title": "Reasoning"
          },
          "reasoning_signature": {
            "anyOf": [
              {
                "maxLength": 4096,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional opaque blob for tamper-evidence beyond row-level INSERT-only (typically sha256 of the full reasoning trace, or vendor-supplied encrypted summary).",
            "title": "Reasoning Signature"
          },
          "rule": {
            "anyOf": [
              {
                "maxLength": 500,
                "minLength": 1,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional rule citation per ISO 17025 Clause 7.1.3. Convention encourages prefixed identifiers like 'iso17025:7.1.3:simple_acceptance' or 'cora:policy:recipe_approval:v1'.",
            "title": "Rule"
          }
        },
        "required": [
          "actor_id",
          "context",
          "choice"
        ],
        "title": "RegisterDecisionRequest",
        "type": "object"
      },
      "RegisterDecisionResponse": {
        "description": "Response body for `POST /decisions`.",
        "properties": {
          "decision_id": {
            "format": "uuid",
            "title": "Decision Id",
            "type": "string"
          }
        },
        "required": [
          "decision_id"
        ],
        "title": "RegisterDecisionResponse",
        "type": "object"
      },
      "RegisterFixtureRequest": {
        "description": "Body for `POST /assemblies/{assembly_id}/fixtures`.",
        "properties": {
          "parameter_overrides": {
            "additionalProperties": true,
            "description": "Operator-supplied parameter overrides validated against the Assembly's parameter_overrides_schema. STRICT posture: non-empty overrides on an Assembly with no schema rejects.",
            "title": "Parameter Overrides",
            "type": "object"
          },
          "slot_asset_bindings": {
            "description": "Bindings of slot_name to pre-existing Asset.id. Each binding's slot_name must reference a TemplateSlot in the Assembly's required_slots; each asset_id must resolve to a registered Asset whose family_ids intersect the slot's required_family_ids. Cardinality of each slot must be satisfied by the count of bindings carrying its slot_name. Wire shape is a list; duplicates collapse when the route handler converts to the domain frozenset.",
            "items": {
              "$ref": "#/components/schemas/SlotAssetBindingBody"
            },
            "title": "Slot Asset Bindings",
            "type": "array"
          }
        },
        "title": "RegisterFixtureRequest",
        "type": "object"
      },
      "RegisterFixtureResponse": {
        "description": "Response body for `POST /assemblies/{assembly_id}/fixtures`.",
        "properties": {
          "fixture_id": {
            "format": "uuid",
            "title": "Fixture Id",
            "type": "string"
          }
        },
        "required": [
          "fixture_id"
        ],
        "title": "RegisterFixtureResponse",
        "type": "object"
      },
      "RegisterFrameRequest": {
        "description": "Body for `POST /frames`.\n\nRoot frames pass `parent_frame_id=null` AND\n`placement=null`. Child frames pass both\nnon-null, and the embedded `placement.parent_frame_id` must equal\n`parent_frame_id` (decider enforces this with\nInvalidFrameRootError -> 400).",
        "properties": {
          "name": {
            "description": "Display name for the new frame.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "parent_frame_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Immediate parent in the frame tree. Must be null for root frames; required for all others. Goes together with placement (both null or both non-null).",
            "title": "Parent Frame Id"
          },
          "placement": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/PlacementBody"
              },
              {
                "type": "null"
              }
            ],
            "description": "Pose of this frame's origin relative to its parent. Must be null for root frames; required for child frames and must reference parent_frame_id."
          }
        },
        "required": [
          "name",
          "parent_frame_id",
          "placement"
        ],
        "title": "RegisterFrameRequest",
        "type": "object"
      },
      "RegisterFrameResponse": {
        "description": "Response body for `POST /frames`.",
        "properties": {
          "frame_id": {
            "format": "uuid",
            "title": "Frame Id",
            "type": "string"
          }
        },
        "required": [
          "frame_id"
        ],
        "title": "RegisterFrameResponse",
        "type": "object"
      },
      "RegisterMountRequest": {
        "description": "Body for `POST /mounts`.",
        "properties": {
          "drawing": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DrawingBody"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional engineering reference for the slot itself (distinct from the installed Asset's build-to drawing)."
          },
          "parent_mount_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Immediate parent in the slot hierarchy. Null for top-level slots. Hierarchy axis is distinct from coordinate-frame axis (which lives on placement.parent_frame_id).",
            "title": "Parent Mount Id"
          },
          "placement": {
            "$ref": "#/components/schemas/PlacementBody",
            "description": "Pose of this slot relative to a Frame."
          },
          "slot_code": {
            "description": "External alias for this slot (e.g., APS 2-BM RSS tag '02-BM-A-K-01'). Must be unique across Active mounts.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Slot Code",
            "type": "string"
          }
        },
        "required": [
          "slot_code",
          "parent_mount_id",
          "placement"
        ],
        "title": "RegisterMountRequest",
        "type": "object"
      },
      "RegisterMountResponse": {
        "description": "Response body for `POST /mounts`.",
        "properties": {
          "mount_id": {
            "format": "uuid",
            "title": "Mount Id",
            "type": "string"
          }
        },
        "required": [
          "mount_id"
        ],
        "title": "RegisterMountResponse",
        "type": "object"
      },
      "RegisterProcedureFromRecipeRequest": {
        "description": "Body for `POST /procedures/from-recipe`.",
        "properties": {
          "bindings": {
            "additionalProperties": true,
            "description": "Operator-supplied parameter values keyed by the parameter names declared in the bound Recipe's Capability's parameters_schema. Substituted into BindingRef sentinels at expansion time. Empty dict valid when the Recipe carries no BindingRefs.",
            "title": "Bindings",
            "type": "object"
          },
          "kind": {
            "description": "Free-form ISA-106 procedure-kind discriminator (bakeout, calibration, alignment, recovery, etc.). Mirrors register_procedure's kind field exactly.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "description": "Operator-readable display name for the procedure. Mirrors register_procedure's name field exactly.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "parent_run_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional parent Run binding. None = standalone procedure. Set = Phase-of-Run procedure.",
            "title": "Parent Run Id"
          },
          "recipe_id": {
            "description": "Recipe whose templated steps will be expanded into this Procedure. Loaded cross-BC at handler time; missing -> 404.",
            "format": "uuid",
            "title": "Recipe Id",
            "type": "string"
          },
          "target_asset_ids": {
            "description": "Asset ids this procedure acts on. May be empty (valid for facility-envelope procedures). Eventual-consistency: ids are NOT verified at register time.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Target Asset Ids",
            "type": "array"
          }
        },
        "required": [
          "name",
          "kind",
          "recipe_id"
        ],
        "title": "RegisterProcedureFromRecipeRequest",
        "type": "object"
      },
      "RegisterProcedureFromRecipeResponse": {
        "description": "Response body for `POST /procedures/from-recipe`.",
        "properties": {
          "procedure_id": {
            "format": "uuid",
            "title": "Procedure Id",
            "type": "string"
          }
        },
        "required": [
          "procedure_id"
        ],
        "title": "RegisterProcedureFromRecipeResponse",
        "type": "object"
      },
      "RegisterProcedureRequest": {
        "description": "Body for `POST /procedures`.",
        "properties": {
          "capability_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional cross-BC binding to the universal Capability template (Recipe BC 6k) this Procedure realizes as a Procedure-shaped executor. None for ceremony Procedures with no matching template. When supplied, the bound Capability must declare `Procedure` in its executor_shapes set; otherwise 409. Eventual-consistency: the Capability stream is loaded at handler time, not API-boundary time. Same additive shape as Method.capability_id (6l-additive).",
            "title": "Capability Id"
          },
          "kind": {
            "description": "Free-form ISA-106 procedure-kind discriminator (bakeout, calibration, alignment, recovery, beam_mode_change, id_maintenance, kb_switching, etc.). Documented starter vocabulary in project_operation_design memo; closed StrEnum promotion deferred until pilot vocabulary settles.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "description": "Operator-readable display name for the procedure (for example '35-BM rotation-axis alignment 2026-05-15', 'Vessel-A bakeout').",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "parent_run_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional parent Run binding. None = standalone procedure (bakeout, calibration sweep run between Runs). Set = Phase-of-Run procedure (calibration sweep invoked mid-Run; resolves the Phase aggregate question per project_operation_design memo).",
            "title": "Parent Run Id"
          },
          "target_asset_ids": {
            "description": "Asset ids this procedure acts on. May be empty (valid for facility-envelope procedures like beam-mode change that don't act on a specific Asset). Eventual-consistency: ids are NOT verified at register time; gating happens at start_procedure (10c-b) via ProcedureStartContext.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Target Asset Ids",
            "type": "array"
          }
        },
        "required": [
          "name",
          "kind"
        ],
        "title": "RegisterProcedureRequest",
        "type": "object"
      },
      "RegisterProcedureResponse": {
        "description": "Response body for `POST /procedures`.",
        "properties": {
          "procedure_id": {
            "format": "uuid",
            "title": "Procedure Id",
            "type": "string"
          }
        },
        "required": [
          "procedure_id"
        ],
        "title": "RegisterProcedureResponse",
        "type": "object"
      },
      "RegisterSubjectRequest": {
        "description": "Body for `POST /subjects`.",
        "properties": {
          "name": {
            "description": "Display name for the new subject.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "RegisterSubjectRequest",
        "type": "object"
      },
      "RegisterSubjectResponse": {
        "description": "Response body for `POST /subjects`.",
        "properties": {
          "subject_id": {
            "format": "uuid",
            "title": "Subject Id",
            "type": "string"
          }
        },
        "required": [
          "subject_id"
        ],
        "title": "RegisterSubjectResponse",
        "type": "object"
      },
      "RegisterSupplyRequest": {
        "description": "Body for `POST /supplies`.",
        "properties": {
          "kind": {
            "description": "Free-form supply-kind discriminator (PhotonBeam, LiquidNitrogen, CompressedAir, ElectricalPower, Vacuum, ProcessGas, ComputePool, etc.). Documented starter vocabulary in project_supply_design memo; closed StrEnum promotion deferred until pilot vocabulary settles.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "description": "Operator-readable display name for this Supply instance (for example '35-BM LN2 drop', 'APS storage-ring beam', 'central N2 supply').",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "scope": {
            "$ref": "#/components/schemas/SupplyScope",
            "description": "Hierarchical scope at which the supply is provisioned. Facility = facility-wide (storage-ring photon beam, central LN2 plant), Sector = ring sector / gas-manifold loop, Beamline = beamline-local (per-beamline drop)."
          }
        },
        "required": [
          "scope",
          "kind",
          "name"
        ],
        "title": "RegisterSupplyRequest",
        "type": "object"
      },
      "RegisterSupplyResponse": {
        "description": "Response body for `POST /supplies`.",
        "properties": {
          "supply_id": {
            "format": "uuid",
            "title": "Supply Id",
            "type": "string"
          }
        },
        "required": [
          "supply_id"
        ],
        "title": "RegisterSupplyResponse",
        "type": "object"
      },
      "RegisterVisitRequest": {
        "description": "Body for `POST /visits`.",
        "properties": {
          "external_refs": {
            "description": "Anti-corruption refs to upstream-deferred concepts (proposal, btr, visit, cycle). Stored on the event; surfaced through the list_visits_by_external_ref query slice.",
            "items": {
              "$ref": "#/components/schemas/_ExternalRefBody"
            },
            "title": "External Refs",
            "type": "array"
          },
          "part_of_visit_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional self-FK for nested commissioning. The decider enforces parent existence and same-Surface cohesion.",
            "title": "Part Of Visit Id"
          },
          "planned_end_at": {
            "description": "Scheduled end; must be strictly after planned_start_at.",
            "format": "date-time",
            "title": "Planned End At",
            "type": "string"
          },
          "planned_start_at": {
            "description": "Scheduled start (operator-supplied).",
            "format": "date-time",
            "title": "Planned Start At",
            "type": "string"
          },
          "policy_id": {
            "description": "Policy that scopes this visit's authz.",
            "format": "uuid",
            "title": "Policy Id",
            "type": "string"
          },
          "surface_id": {
            "description": "Surface this visit binds to (e.g., 2-BM endstation).",
            "format": "uuid",
            "title": "Surface Id",
            "type": "string"
          },
          "type": {
            "$ref": "#/components/schemas/VisitType",
            "description": "Operational nature: user / commissioning / maintenance / calibration / staff."
          },
          "visit_id": {
            "description": "Caller-supplied UUID. BSS subscriber uses deterministic uuid5; operator-direct may use uuid4.",
            "format": "uuid",
            "title": "Visit Id",
            "type": "string"
          }
        },
        "required": [
          "visit_id",
          "policy_id",
          "surface_id",
          "type",
          "planned_start_at",
          "planned_end_at"
        ],
        "title": "RegisterVisitRequest",
        "type": "object"
      },
      "RegisterVisitResponse": {
        "description": "Response body for `POST /visits`.",
        "properties": {
          "visit_id": {
            "format": "uuid",
            "title": "Visit Id",
            "type": "string"
          }
        },
        "required": [
          "visit_id"
        ],
        "title": "RegisterVisitResponse",
        "type": "object"
      },
      "RejectClearanceRequest": {
        "description": "Body for `POST /clearances/{clearance_id}/reject`.",
        "properties": {
          "reason": {
            "description": "Operator-supplied free-form reason for the rejection. Audit breadcrumb explaining why the reviewer rejected the clearance.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "RejectClearanceRequest",
        "type": "object"
      },
      "RelatedIdentifierDTO": {
        "description": "PIDINST v1.0 Property 12: a related identifier (parent asset, etc.).",
        "properties": {
          "identifier_type": {
            "title": "Identifier Type",
            "type": "string"
          },
          "relation_type": {
            "title": "Relation Type",
            "type": "string"
          },
          "value": {
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "value",
          "identifier_type",
          "relation_type"
        ],
        "title": "RelatedIdentifierDTO",
        "type": "object"
      },
      "ReleaseControlOfSurfaceBody": {
        "additionalProperties": false,
        "description": "Body for POST /visits/{visit_id}/surface-control/release.",
        "properties": {
          "surface_id": {
            "description": "Surface that the requesting Visit relinquishes control of. Must match the Visit's surface_id.",
            "format": "uuid",
            "title": "Surface Id",
            "type": "string"
          }
        },
        "required": [
          "surface_id"
        ],
        "title": "ReleaseControlOfSurfaceBody",
        "type": "object"
      },
      "RelocateAssetRequest": {
        "description": "Body for `POST /assets/{asset_id}/relocate`.\n\n`to_parent_id` is required (non-null); Enterprise assets cannot\nrelocate, and non-Enterprise assets always have a parent. Per\neventual-consistency stance, the parent's existence is NOT\nverified by the decider.\n\n`reason` is operator-supplied free text (audit-log breadcrumb).",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the relocation (audit-log breadcrumb), for example 'site reorganization', 'moved to BL2-IBP'.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          },
          "to_parent_id": {
            "description": "New parent in the hierarchy tree. Must be non-null. Eventual-consistency: parent's existence is NOT verified.",
            "format": "uuid",
            "title": "To Parent Id",
            "type": "string"
          }
        },
        "required": [
          "to_parent_id",
          "reason"
        ],
        "title": "RelocateAssetRequest",
        "type": "object"
      },
      "RemoveAssetAlternateIdentifierRequest": {
        "description": "Body for `POST /assets/{asset_id}/remove-alternate-identifier`.\n\nBoth fields required. Pydantic enforces non-empty value at the\nboundary; the AlternateIdentifier VO then trims and re-validates\nlength within the decider.",
        "properties": {
          "identifier": {
            "$ref": "#/components/schemas/AlternateIdentifierBody",
            "description": "The alternate identifier to remove. Matched against the asset's stored alternate identifiers by exact (kind, value) pair."
          }
        },
        "required": [
          "identifier"
        ],
        "title": "RemoveAssetAlternateIdentifierRequest",
        "type": "object"
      },
      "RemoveAssetFamilyRequest": {
        "description": "Body for `POST /assets/{asset_id}/remove-family`.",
        "properties": {
          "family_id": {
            "description": "Family id to remove from the asset's family set.",
            "format": "uuid",
            "title": "Family Id",
            "type": "string"
          }
        },
        "required": [
          "family_id"
        ],
        "title": "RemoveAssetFamilyRequest",
        "type": "object"
      },
      "RemoveAssetOwnerRequest": {
        "description": "Body for `POST /assets/{asset_id}/remove-owner`.\n\nCarries only `owner_name` (Lock 5: removal keys on name, not\non the full VO).",
        "properties": {
          "owner_name": {
            "description": "Name of the institutional owner to remove. Matched against the asset's stored owners by exact name.",
            "maxLength": 255,
            "minLength": 1,
            "title": "Owner Name",
            "type": "string"
          }
        },
        "required": [
          "owner_name"
        ],
        "title": "RemoveAssetOwnerRequest",
        "type": "object"
      },
      "RemoveAssetPortRequest": {
        "description": "Body for `POST /assets/{asset_id}/remove-port`.",
        "properties": {
          "port_name": {
            "description": "Name of the port to remove.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Port Name",
            "type": "string"
          }
        },
        "required": [
          "port_name"
        ],
        "title": "RemoveAssetPortRequest",
        "type": "object"
      },
      "RemovePlanWireRequest": {
        "description": "Body for `POST /plans/{plan_id}/remove-wire`.\n\nAll four fields are required (the 4-tuple identifies the Wire to\nremove). Pydantic enforces non-empty port names at the boundary.\nRemoval is strict-not-idempotent: the Wire MUST currently exist\nin the Plan's wire set.",
        "properties": {
          "source_asset_id": {
            "description": "The source-side Asset of the Wire to remove.",
            "format": "uuid",
            "title": "Source Asset Id",
            "type": "string"
          },
          "source_port_name": {
            "description": "Source port name of the Wire to remove.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Source Port Name",
            "type": "string"
          },
          "target_asset_id": {
            "description": "The target-side Asset of the Wire to remove.",
            "format": "uuid",
            "title": "Target Asset Id",
            "type": "string"
          },
          "target_port_name": {
            "description": "Target port name of the Wire to remove.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Target Port Name",
            "type": "string"
          }
        },
        "required": [
          "source_asset_id",
          "source_port_name",
          "target_asset_id",
          "target_port_name"
        ],
        "title": "RemovePlanWireRequest",
        "type": "object"
      },
      "RemoveRunFromCampaignRequest": {
        "description": "Body for `POST /campaigns/{campaign_id}/runs/{run_id}/remove`.\n\n`reason` is REQUIRED -- ungrouping is meaningful and operators must\nsay why. Per-membership audit breadcrumb; not the Campaign's\n`last_status_reason` (that field is for status transitions only).",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for removing the Run from the Campaign (per-membership audit breadcrumb, REQUIRED).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "RemoveRunFromCampaignRequest",
        "type": "object"
      },
      "RestoreAssetRequest": {
        "description": "Body for `POST /assets/{asset_id}/restore`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining the repair. Examples: \"replaced flat cable\", \"cleaned\nsample stage and recalibrated\", \"vacuum pump rebuild complete\".",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the restore transition (audit-log breadcrumb).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "RestoreAssetRequest",
        "type": "object"
      },
      "RestoreSupplyRequest": {
        "description": "Body for `POST /supplies/{supply_id}/restore`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nconfirming why the operator believes the supply is fully back.\nExamples: \"control room confirms beam stable for 5 minutes\",\n\"LN2 dewar refilled and pressure stable for 2 minutes\",\n\"vacuum confirmed below target pressure for 30 seconds\".",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason for the restore acknowledgement (audit-log breadcrumb).",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "RestoreSupplyRequest",
        "type": "object"
      },
      "RetireCautionRequest": {
        "description": "Body for `POST /cautions/{caution_id}/retire`.",
        "properties": {
          "reason": {
            "$ref": "#/components/schemas/CautionRetireReason",
            "description": "Closed reason enum: Resolved (underlying condition fixed), NoLongerApplies (asset removed / scope changed), or WrongTarget (caution was attached to wrong Asset / Procedure)."
          }
        },
        "required": [
          "reason"
        ],
        "title": "RetireCautionRequest",
        "type": "object"
      },
      "ReviewStepResponse": {
        "description": "One step in the reviewer chain on the wire.",
        "properties": {
          "actor_id": {
            "format": "uuid",
            "title": "Actor Id",
            "type": "string"
          },
          "decided_at": {
            "format": "date-time",
            "title": "Decided At",
            "type": "string"
          },
          "decision": {
            "title": "Decision",
            "type": "string"
          },
          "notes": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Notes"
          },
          "role": {
            "title": "Role",
            "type": "string"
          },
          "step_index": {
            "title": "Step Index",
            "type": "integer"
          }
        },
        "required": [
          "step_index",
          "role",
          "actor_id",
          "decision",
          "decided_at"
        ],
        "title": "ReviewStepResponse",
        "type": "object"
      },
      "ReviseAgentBudgetRequest": {
        "description": "Body for `POST /agents/{agent_id}/budget`.",
        "properties": {
          "daily_token_cap": {
            "anyOf": [
              {
                "minimum": 0.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Daily token cap. Pass null to clear this field. Setting both caps to null clears the entire budget.",
            "title": "Daily Token Cap"
          },
          "monthly_usd_cap": {
            "anyOf": [
              {
                "minimum": 0.0,
                "type": "number"
              },
              {
                "type": "null"
              }
            ],
            "description": "Monthly USD cap. Pass null to clear this field. Setting both caps to null clears the entire budget.",
            "title": "Monthly Usd Cap"
          }
        },
        "title": "ReviseAgentBudgetRequest",
        "type": "object"
      },
      "RevisionResponse": {
        "description": "Read-side DTO for one CalibrationRevision.",
        "properties": {
          "decided_by_decision_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Decided By Decision Id"
          },
          "established_at": {
            "format": "date-time",
            "title": "Established At",
            "type": "string"
          },
          "established_by_actor_id": {
            "format": "uuid",
            "title": "Established By Actor Id",
            "type": "string"
          },
          "revision_id": {
            "format": "uuid",
            "title": "Revision Id",
            "type": "string"
          },
          "source": {
            "description": "Tagged source provenance: {kind, procedure_id|dataset_id|actor_id}. The wire envelope mirrors Caution / Safety polymorphic conventions; in-aggregate the source is a tagged-union VO and on the event payload it's exclusive-arc fields (Q5 lock).",
            "discriminator": {
              "mapping": {
                "Asserted": "#/components/schemas/SourceAssertedDTO",
                "Computed": "#/components/schemas/SourceComputedDTO",
                "Measured": "#/components/schemas/SourceMeasuredDTO"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/components/schemas/SourceMeasuredDTO"
              },
              {
                "$ref": "#/components/schemas/SourceComputedDTO"
              },
              {
                "$ref": "#/components/schemas/SourceAssertedDTO"
              }
            ],
            "title": "Source"
          },
          "status": {
            "$ref": "#/components/schemas/CalibrationStatus"
          },
          "supersedes_revision_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Supersedes Revision Id"
          },
          "value": {
            "additionalProperties": true,
            "title": "Value",
            "type": "object"
          }
        },
        "required": [
          "revision_id",
          "value",
          "status",
          "source",
          "established_at",
          "established_by_actor_id"
        ],
        "title": "RevisionResponse",
        "type": "object"
      },
      "RevokeCredentialBody": {
        "description": "Optional revoke-credential request body.",
        "properties": {
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional operator-supplied reason for revoking the credential (audit-log breadcrumb). Flows onto the CredentialRevoked event payload.",
            "title": "Reason"
          }
        },
        "title": "RevokeCredentialBody",
        "type": "object"
      },
      "RevokePermitRequest": {
        "description": "Body for `POST /federation/permits/{permit_id}/revoke`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the Permit is being revoked. Examples: \"peer\nfacility decommissioned\", \"credential compromise\", \"policy\nchange ended sharing agreement\".",
        "properties": {
          "reason": {
            "anyOf": [
              {
                "maxLength": 500,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional operator-supplied reason for revoking the permit (audit-log breadcrumb).",
            "title": "Reason"
          }
        },
        "title": "RevokePermitRequest",
        "type": "object"
      },
      "RevokeToolFromAgentRequest": {
        "description": "Body for `POST /agents/{agent_id}/tools/revoke`.",
        "properties": {
          "tool_name": {
            "description": "MCP tool name to remove from the Agent's allowlist (1-100 chars).",
            "maxLength": 100,
            "minLength": 1,
            "title": "Tool Name",
            "type": "string"
          }
        },
        "required": [
          "tool_name"
        ],
        "title": "RevokeToolFromAgentRequest",
        "type": "object"
      },
      "RiskBand": {
        "description": "Three-band risk triage; mirrors HSE ALARP semantics.\n\n6 of 8 surveyed facility safety forms use Green/Yellow/Red triage:\n  - `Green`  -- broadly acceptable; baseline operating posture\n  - `Yellow` -- tolerable with controls; safety review required\n  - `Red`    -- requires authorization-by-licensing; restricted\n\nPer the Safety design memo, this lives ON Clearance as an optional\nfield (`risk_band: RiskBand | None`). APS ESAF doesn't use bands;\nthe field is None for ESAF Clearances and populated for ESRF / MAX\nIV / DLS / DESY / SLAC variants. Future Risk aggregate (when the\nfour-primitive split's bottom half lands) carries the calculated\nband; today the band is captured at submit time as audit data.",
        "enum": [
          "Green",
          "Yellow",
          "Red"
        ],
        "title": "RiskBand",
        "type": "string"
      },
      "RiskBandDTO": {
        "properties": {
          "band": {
            "$ref": "#/components/schemas/RiskBand"
          },
          "kind": {
            "const": "RiskBand",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "band"
        ],
        "title": "RiskBandDTO",
        "type": "object"
      },
      "RotateSealOnlineKeyBody": {
        "additionalProperties": false,
        "description": "Request body for rotate-seal-online-key.",
        "properties": {
          "new_online_credential_id": {
            "description": "Credential id of the fresh online (warm) signing key. Must differ from both the current online_credential_id (no-op rotation rejected) and the current offline_credential_id (key-separation invariant).",
            "format": "uuid",
            "title": "New Online Credential Id",
            "type": "string"
          },
          "signed_by_offline_root": {
            "description": "Operator affirmation that the offline (cold) root countersigned this rotation. Required for the audit denorm; the offline-signature verification itself is out of scope for this slice.",
            "title": "Signed By Offline Root",
            "type": "boolean"
          }
        },
        "required": [
          "new_online_credential_id",
          "signed_by_offline_root"
        ],
        "title": "RotateSealOnlineKeyBody",
        "type": "object"
      },
      "RunListResponse": {
        "description": "Page of runs plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/RunSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "RunListResponse",
        "type": "object"
      },
      "RunReadingRequest": {
        "additionalProperties": false,
        "description": "One reading entry's input payload (polymorphic, SOSA-aligned).\n\nRequired: event_id + channel_name + value + sampled_at +\nsampling_procedure. `units` and `occurred_at` are optional.",
        "properties": {
          "channel_name": {
            "description": "Sensor or motor identifier (operator-meaningful name; for example 'T_sample', 'motor_x', 'ring_current').",
            "maxLength": 255,
            "minLength": 1,
            "title": "Channel Name",
            "type": "string"
          },
          "event_id": {
            "description": "Producer-supplied UUIDv7 entry id. Idempotency / dedup key; re-issuing the same id is a silent no-op.",
            "format": "uuid",
            "title": "Event Id",
            "type": "string"
          },
          "occurred_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional handler-time override (ISO-8601 with timezone). Defaults to server clock when omitted; producers with their own ingest-time clock can populate explicitly.",
            "title": "Occurred At"
          },
          "sampled_at": {
            "description": "SOSA phenomenonTime (ISO-8601 with timezone): when the sensor captured the value.",
            "format": "date-time",
            "title": "Sampled At",
            "type": "string"
          },
          "sampling_procedure": {
            "description": "SOSA-aligned discriminator. 'baseline' is a snapshot at run boundary (start / end). 'monitor' is sub-Hz time-series during the run (Bluesky monitor stream pattern). Future values land additively (no migration).",
            "enum": [
              "baseline",
              "monitor"
            ],
            "title": "Sampling Procedure",
            "type": "string"
          },
          "units": {
            "anyOf": [
              {
                "maxLength": 64,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional unit string (for example 'K', 'mm', 'mA').",
            "title": "Units"
          },
          "value": {
            "description": "Scalar reading value. NaN and Infinity rejected.",
            "title": "Value",
            "type": "number"
          }
        },
        "required": [
          "event_id",
          "channel_name",
          "value",
          "sampled_at",
          "sampling_procedure"
        ],
        "title": "RunReadingRequest",
        "type": "object"
      },
      "RunResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. `status` is the StrEnum's\nstring value. `subject_id` is null for calibration / dark-field\nruns. `raid` is null when no Research Activity Identifier was\nsupplied at start time (additive retrofit).\n\n`override_parameters` and `effective_parameters` carry the\nparameter set: overrides the operator supplied at start\ntime, and the resolved merge of Plan defaults + overrides that\nactually governed this Run. Both default `{}`. `trigger_source`\ncaptures what initiated the Run (None if unrecorded).\n\n`campaign_id` (6i-c) is the Campaign this Run is a member of, set\neither at start time (StartRun.campaign_id) or post-hoc via\nadd_run_to_campaign. None when the Run is standalone (not part of\nany Campaign). Closes design-memo Watch #17 (per Caution-design\ncross-BC consistency precedent).",
        "properties": {
          "campaign_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Campaign Id"
          },
          "effective_parameters": {
            "additionalProperties": true,
            "title": "Effective Parameters",
            "type": "object"
          },
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "override_parameters": {
            "additionalProperties": true,
            "title": "Override Parameters",
            "type": "object"
          },
          "plan_id": {
            "format": "uuid",
            "title": "Plan Id",
            "type": "string"
          },
          "raid": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Raid"
          },
          "status": {
            "title": "Status",
            "type": "string"
          },
          "subject_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subject Id"
          },
          "trigger_source": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Trigger Source"
          }
        },
        "required": [
          "id",
          "name",
          "plan_id",
          "subject_id",
          "raid",
          "status"
        ],
        "title": "RunResponse",
        "type": "object"
      },
      "RunSummaryDTO": {
        "description": "One run in a paginated list.",
        "properties": {
          "campaign_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Campaign this Run is a member of. Set at start time (StartRun.campaign_id) or post-hoc (add_run_to_campaign). NULL for standalone Runs. Campaign membership snapshot.",
            "title": "Campaign Id"
          },
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "override_parameters_present": {
            "default": false,
            "description": "True iff RunStarted's override_parameters payload was non-empty (operator customized parameters at start time). The full overrides + effective_parameters dicts are loaded on demand via `get_run`.",
            "title": "Override Parameters Present",
            "type": "boolean"
          },
          "plan_id": {
            "format": "uuid",
            "title": "Plan Id",
            "type": "string"
          },
          "raid": {
            "anyOf": [
              {
                "maxLength": 2048,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Raid"
          },
          "run_id": {
            "format": "uuid",
            "title": "Run Id",
            "type": "string"
          },
          "status": {
            "enum": [
              "Running",
              "Held",
              "Completed",
              "Aborted",
              "Stopped",
              "Truncated"
            ],
            "title": "Status",
            "type": "string"
          },
          "subject_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Subject Id"
          }
        },
        "required": [
          "run_id",
          "name",
          "plan_id",
          "subject_id",
          "status",
          "created_at"
        ],
        "title": "RunSummaryDTO",
        "type": "object"
      },
      "SchemeDTO": {
        "properties": {
          "code": {
            "maxLength": 100,
            "minLength": 1,
            "title": "Code",
            "type": "string"
          },
          "kind": {
            "const": "Scheme",
            "title": "Kind",
            "type": "string"
          },
          "scheme": {
            "maxLength": 50,
            "minLength": 1,
            "title": "Scheme",
            "type": "string"
          },
          "severity_label": {
            "default": "",
            "maxLength": 100,
            "title": "Severity Label",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "scheme",
          "code"
        ],
        "title": "SchemeDTO",
        "type": "object"
      },
      "SealListResponse": {
        "description": "Page of Seal singletons plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/SealSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "SealListResponse",
        "type": "object"
      },
      "SealResponse": {
        "description": "Read-side DTO at the API boundary.\n\nMirrors the Seal aggregate state plus projection-sourced lifecycle\ntimestamps. `online_credential_id` / `offline_credential_id` are Credential\nreferences (their referenced secrets never cross this wire).",
        "properties": {
          "current_head_hash": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Current Head Hash"
          },
          "current_sequence_number": {
            "title": "Current Sequence Number",
            "type": "integer"
          },
          "facility_id": {
            "title": "Facility Id",
            "type": "string"
          },
          "initialized_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Initialized At"
          },
          "initialized_by_actor_id": {
            "format": "uuid",
            "title": "Initialized By Actor Id",
            "type": "string"
          },
          "last_signed_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Signed At"
          },
          "last_signed_by_actor_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Signed By Actor Id"
          },
          "offline_credential_id": {
            "format": "uuid",
            "title": "Offline Credential Id",
            "type": "string"
          },
          "online_credential_id": {
            "format": "uuid",
            "title": "Online Credential Id",
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/SealStatus"
          }
        },
        "required": [
          "facility_id",
          "online_credential_id",
          "offline_credential_id",
          "current_head_hash",
          "current_sequence_number",
          "initialized_by_actor_id",
          "status"
        ],
        "title": "SealResponse",
        "type": "object"
      },
      "SealStatus": {
        "description": "The Seal singleton's lifecycle posture.\n\nTwo values locked day one per [[project_federation_port_design]]:\n\n  - `Live`: normal posture; the online key signs each new head\n    pointer; consumers trust the pointer chain.\n  - `Republishing`: the offline root is republishing the full\n    registry tree against a new head shape; consumers may defer\n    trust on new pointers until republish completes (the indicator\n    lets them decide, the head still signs).\n\nThe mini-FSM keeps the singleton legible: republishing is a\nbounded window that ends with `SealRepublishingCompleted`.",
        "enum": [
          "Live",
          "Republishing"
        ],
        "title": "SealStatus",
        "type": "string"
      },
      "SealSummaryDTO": {
        "description": "One Seal singleton in a paginated list.",
        "properties": {
          "current_head_hash": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Current Head Hash"
          },
          "current_sequence_number": {
            "title": "Current Sequence Number",
            "type": "integer"
          },
          "facility_id": {
            "title": "Facility Id",
            "type": "string"
          },
          "initialized_at": {
            "format": "date-time",
            "title": "Initialized At",
            "type": "string"
          },
          "initialized_by_actor_id": {
            "format": "uuid",
            "title": "Initialized By Actor Id",
            "type": "string"
          },
          "last_signed_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Signed At"
          },
          "last_signed_by_actor_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Signed By Actor Id"
          },
          "offline_credential_id": {
            "format": "uuid",
            "title": "Offline Credential Id",
            "type": "string"
          },
          "online_credential_id": {
            "format": "uuid",
            "title": "Online Credential Id",
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/SealStatus"
          }
        },
        "required": [
          "facility_id",
          "online_credential_id",
          "offline_credential_id",
          "current_sequence_number",
          "initialized_by_actor_id",
          "status",
          "initialized_at"
        ],
        "title": "SealSummaryDTO",
        "type": "object"
      },
      "SignSealPointerRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /federation/seals/{facility_id}/pointer/sign`.",
        "properties": {
          "new_head_hash": {
            "description": "SHA-256 lowercase hex of the canonicalized head pointer body just signed by the online key.",
            "minLength": 1,
            "title": "New Head Hash",
            "type": "string"
          },
          "new_sequence_number": {
            "description": "Monotonic counter for the signed pointer chain. Must strictly exceed the Seal's current_sequence_number; the decider rejects regressions.",
            "minimum": 1.0,
            "title": "New Sequence Number",
            "type": "integer"
          }
        },
        "required": [
          "new_head_hash",
          "new_sequence_number"
        ],
        "title": "SignSealPointerRequest",
        "type": "object"
      },
      "SlotAssetBindingBody": {
        "description": "A single slot-to-asset binding within the body.",
        "properties": {
          "asset_id": {
            "description": "Asset.id of the pre-existing Asset that fills the slot.",
            "format": "uuid",
            "title": "Asset Id",
            "type": "string"
          },
          "slot_name": {
            "description": "Name of the TemplateSlot in the Assembly's required_slots this binding fills.",
            "maxLength": 100,
            "minLength": 1,
            "title": "Slot Name",
            "type": "string"
          }
        },
        "required": [
          "slot_name",
          "asset_id"
        ],
        "title": "SlotAssetBindingBody",
        "type": "object"
      },
      "SlotAssetBindingResponse": {
        "description": "A single (slot_name, asset_id) binding within a Fixture.",
        "properties": {
          "asset_id": {
            "format": "uuid",
            "title": "Asset Id",
            "type": "string"
          },
          "slot_name": {
            "title": "Slot Name",
            "type": "string"
          }
        },
        "required": [
          "slot_name",
          "asset_id"
        ],
        "title": "SlotAssetBindingResponse",
        "type": "object"
      },
      "SlotCardinality": {
        "description": "How many Assets can fill a slot at instantiation time.\n\nClosed enum: adding a fifth member is a deliberate widen, not\nan additive default. Numeric bounds (`AtLeast2`, `AtMost3`) are\nexplicitly out of scope to keep the closed-enum discipline.",
        "enum": [
          "Exactly1",
          "ZeroOrOne",
          "OneOrMore",
          "ZeroOrMore"
        ],
        "title": "SlotCardinality",
        "type": "string"
      },
      "SourceAssertedDTO": {
        "description": "Wire shape for an Asserted source (operator-typed value).",
        "properties": {
          "actor_id": {
            "description": "Actor id who asserted the value. Usually equals the request envelope's principal_id but may differ when an operator asserts a value on behalf of another.",
            "format": "uuid",
            "title": "Actor Id",
            "type": "string"
          },
          "kind": {
            "const": "Asserted",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "actor_id"
        ],
        "title": "SourceAssertedDTO",
        "type": "object"
      },
      "SourceComputedDTO": {
        "description": "Wire shape for a Computed source (numerical analysis of a Dataset).",
        "properties": {
          "dataset_id": {
            "description": "Dataset id whose data this revision was computed from (for example, `tomopy.find_center_vo` on the projections).",
            "format": "uuid",
            "title": "Dataset Id",
            "type": "string"
          },
          "kind": {
            "const": "Computed",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "dataset_id"
        ],
        "title": "SourceComputedDTO",
        "type": "object"
      },
      "SourceMeasuredDTO": {
        "description": "Wire shape for a Measured source (alignment Procedure).",
        "properties": {
          "kind": {
            "const": "Measured",
            "title": "Kind",
            "type": "string"
          },
          "procedure_id": {
            "description": "Alignment Procedure id whose run measured this value.",
            "format": "uuid",
            "title": "Procedure Id",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "procedure_id"
        ],
        "title": "SourceMeasuredDTO",
        "type": "object"
      },
      "StartClearanceReviewRequest": {
        "description": "Body for `POST /clearances/{clearance_id}/start-review`.",
        "properties": {
          "first_reviewer_role": {
            "description": "Facility-vocabulary label for the first reviewer in the chain (for example, 'BeamlineScientist', 'LocalContact', 'ESH', 'ESRB').",
            "maxLength": 50,
            "minLength": 1,
            "title": "First Reviewer Role",
            "type": "string"
          }
        },
        "required": [
          "first_reviewer_role"
        ],
        "title": "StartClearanceReviewRequest",
        "type": "object"
      },
      "StartCredentialRotationRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /federation/credentials/{credential_id}/rotation/start`.",
        "properties": {
          "new_public_material_ref": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional opaque pointer to the pending public counterpart (verification key, certificate handle). None when the purpose is symmetric or the public half lives elsewhere.",
            "title": "New Public Material Ref"
          },
          "new_secret_ref": {
            "description": "Opaque pointer to the pending secret material. Must differ from the credential's current secret_ref. The raw secret bytes never cross this boundary; the caller is responsible for SecretStore provisioning.",
            "minLength": 1,
            "title": "New Secret Ref",
            "type": "string"
          }
        },
        "required": [
          "new_secret_ref"
        ],
        "title": "StartCredentialRotationRequest",
        "type": "object"
      },
      "StartRunRequest": {
        "description": "Body for `POST /runs`.",
        "properties": {
          "campaign_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional Campaign id this Run joins at start time. When provided, the handler pre-loads the Campaign (404 if missing), verifies it is in Planned, Active, or Held (409 RunCannotJoinCampaign otherwise), and atomically writes both `RunStarted` (carrying campaign_id) on the Run stream AND `CampaignRunAdded` on the Campaign stream via `EventStore.append_streams`. Omit (or null) for a standalone Run; membership can also be added post-hoc via `POST /campaigns/{id}/runs/{run_id}/add`.",
            "title": "Campaign Id"
          },
          "decided_by_decision_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional Decision id that justified starting this Run (most commonly a cross-Plan operator pivot: EnergyChange, PivotToHighResolution, etc.). Maps to `prov:wasInformedBy` at the future PROV-O export adapter (same export contract used by Decision.parent_id and AdjustRun.decided_by_decision_id). NOT verified at the write path (eventual-consistency stance per cross-BC reference precedent). Operators can start ad-hoc Runs without a Decision (Decision\u2192Run linkage).",
            "title": "Decided By Decision Id"
          },
          "name": {
            "description": "Display name for the new run.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "override_parameters": {
            "additionalProperties": true,
            "description": "Operator-supplied overrides on top of `Plan.default_parameters` (RFC 7396 merge semantics). The post-merge result is validated against the owning Method's `parameters_schema`; STRICT when the Method declares no schema (non-empty effective parameters rejected with 400; declare an empty `{}` schema for parameter-less Methods, or omit overrides and ensure Plan defaults are empty).",
            "title": "Override Parameters",
            "type": "object"
          },
          "pinned_calibration_ids": {
            "description": "Optional list of `CalibrationRevision.id`s pinned at this Run's start time (Calibration BC AsShot anchor per Phase 12b / Q5/Q6 design memo). IMMUTABLE on the aggregate after start \u2014 every Run transition arm preserves it verbatim. NOT verified at the write path (cross-BC eventual-consistency stance). Empty list when no calibrations apply (today's default; legacy Runs fold the same way via `payload.get(..., [])`). Order on the wire is not significant \u2014 the aggregate carries a frozenset; the decider sorts for deterministic event-payload bytes.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Pinned Calibration Ids",
            "type": "array"
          },
          "plan_id": {
            "description": "Plan id this Run executes. Existence verified at handler-load time; missing \u2192 404. Plan's status verified by decider (Deprecated \u2192 409). Family superset re-validated against current Asset state (drift since Plan-bind \u2192 409).",
            "format": "uuid",
            "title": "Plan Id",
            "type": "string"
          },
          "raid": {
            "anyOf": [
              {
                "maxLength": 2048,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Research Activity Identifier (ISO 23527) for the project this Run belongs to. Optional opaque string carried verbatim. Used at PROV-O / DataCite export boundaries to link Datasets, Subjects, Instruments, and people for cross-facility provenance.",
            "title": "Raid"
          },
          "subject_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Subject being measured. Omit (or null) for dark-field / flat-field calibration runs. If given, Subject must be in Mounted or Measured state (else 409).",
            "title": "Subject Id"
          },
          "trigger_source": {
            "anyOf": [
              {
                "maxLength": 500,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free-form text capturing what initiated this Run (operator-manual, scheduler id, prior-run id, automation). Optional.",
            "title": "Trigger Source"
          }
        },
        "required": [
          "name",
          "plan_id"
        ],
        "title": "StartRunRequest",
        "type": "object"
      },
      "StartRunResponse": {
        "description": "Response body for `POST /runs`.",
        "properties": {
          "run_id": {
            "format": "uuid",
            "title": "Run Id",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "StartRunResponse",
        "type": "object"
      },
      "StartSealRepublishingRequest": {
        "additionalProperties": false,
        "description": "Body for `POST /federation/seals/{facility_id}/republishing/start`.",
        "properties": {
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional operator note explaining why republishing was started (key compromise drill, root rotation, tree rewrite). Flows onto the SealRepublishingStarted event payload (audit-log breadcrumb).",
            "title": "Reason"
          }
        },
        "title": "StartSealRepublishingRequest",
        "type": "object"
      },
      "StopRunRequest": {
        "description": "Body for `POST /runs/{run_id}/stop`.",
        "properties": {
          "reason": {
            "description": "Free-form reason for the stop (1-500 chars after trimming). Today the field is unstructured; structured taxonomy is future-additive.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "StopRunRequest",
        "type": "object"
      },
      "SubjectListResponse": {
        "description": "Page of subjects plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/SubjectSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "SubjectListResponse",
        "type": "object"
      },
      "SubjectResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model so the two can evolve independently\n(for example, a SubjectName invariant change doesn't break older\nclients). `status` is the StrEnum's string value (Received /\nMounted / Measured / Removed / Returned / Stored / Discarded).\n`mounted_on_asset_id` (4f) is the Equipment.Asset id the Subject\nis currently mounted on, or null when not mounted (Received,\npost-dismount, Removed, or any terminal state).",
        "properties": {
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "mounted_on_asset_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Mounted On Asset Id"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "status": {
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "id",
          "name",
          "status",
          "mounted_on_asset_id"
        ],
        "title": "SubjectResponse",
        "type": "object"
      },
      "SubjectSummaryDTO": {
        "description": "One subject in a paginated list.",
        "properties": {
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "status": {
            "enum": [
              "Received",
              "Mounted",
              "Measured",
              "Removed",
              "Returned",
              "Stored",
              "Discarded"
            ],
            "title": "Status",
            "type": "string"
          },
          "subject_id": {
            "format": "uuid",
            "title": "Subject Id",
            "type": "string"
          }
        },
        "required": [
          "subject_id",
          "name",
          "status",
          "created_at"
        ],
        "title": "SubjectSummaryDTO",
        "type": "object"
      },
      "SupersedeCautionRequest": {
        "description": "Body for `POST /cautions/{parent_caution_id}/supersede`.\n\nMirrors `RegisterCautionRequest`'s child fields exactly. The\n`parent_caution_id` comes from the URL path, not the body.",
        "properties": {
          "category": {
            "$ref": "#/components/schemas/CautionCategory"
          },
          "expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Expires At"
          },
          "propagate_to_children": {
            "default": false,
            "title": "Propagate To Children",
            "type": "boolean"
          },
          "severity": {
            "$ref": "#/components/schemas/CautionSeverity"
          },
          "tags": {
            "items": {
              "type": "string"
            },
            "title": "Tags",
            "type": "array"
          },
          "target": {
            "description": "Must match parent's target.",
            "discriminator": {
              "mapping": {
                "Asset": "#/components/schemas/TargetAssetDTO",
                "Procedure": "#/components/schemas/TargetProcedureDTO"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/components/schemas/TargetAssetDTO"
              },
              {
                "$ref": "#/components/schemas/TargetProcedureDTO"
              }
            ],
            "title": "Target"
          },
          "text": {
            "maxLength": 2000,
            "minLength": 1,
            "title": "Text",
            "type": "string"
          },
          "workaround": {
            "maxLength": 2000,
            "minLength": 1,
            "title": "Workaround",
            "type": "string"
          }
        },
        "required": [
          "target",
          "category",
          "severity",
          "text",
          "workaround"
        ],
        "title": "SupersedeCautionRequest",
        "type": "object"
      },
      "SupersedeCautionResponse": {
        "description": "Response body for `POST /cautions/{parent_caution_id}/supersede`.",
        "properties": {
          "caution_id": {
            "description": "The new child caution's id.",
            "format": "uuid",
            "title": "Caution Id",
            "type": "string"
          }
        },
        "required": [
          "caution_id"
        ],
        "title": "SupersedeCautionResponse",
        "type": "object"
      },
      "SupplyListResponse": {
        "description": "Page of supplies plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/SupplySummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "SupplyListResponse",
        "type": "object"
      },
      "SupplyResponse": {
        "description": "Read-side DTO at the API boundary.\n\nCarries primitives, not domain VOs. Decouples the wire format\nfrom the domain model so the two can evolve independently.\n`scope` and `status` are the StrEnum string values; `kind` is the\nfree-form bare-str discriminator (1-50 chars).",
        "properties": {
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "maxLength": 50,
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "scope": {
            "$ref": "#/components/schemas/SupplyScope"
          },
          "status": {
            "$ref": "#/components/schemas/SupplyStatus"
          }
        },
        "required": [
          "id",
          "scope",
          "kind",
          "name",
          "status"
        ],
        "title": "SupplyResponse",
        "type": "object"
      },
      "SupplyScope": {
        "description": "The hierarchical scope at which a Supply is provisioned.\n\nThree values per APS LMA18-3 LN2 distribution layering and\nNeXus NXsource one-source-per-storage-ring precedent:\n\n  - `Facility`: facility-wide resource (storage-ring photon beam,\n    central LN2 plant, building electrical power, central\n    compressed air)\n  - `Sector`: a sub-portion of the facility (ring sector, gas-\n    manifold loop serving N beamlines)\n  - `Beamline`: beamline-local resource (per-beamline LN2 drop,\n    beamline-local vacuum subsystem, beamline-local compute)\n\nAdding finer-grained scopes (for example, Substrate, Chamber) is purely\nadditive when triggers fire.",
        "enum": [
          "Facility",
          "Sector",
          "Beamline"
        ],
        "title": "SupplyScope",
        "type": "string"
      },
      "SupplyStatus": {
        "description": "The Supply's availability state.\n\nFive health states + one lifecycle terminal per\n[[project_supply_design]] + [[project_deregister_supply_design]]:\n\n  - `Unknown`: registration-time default; no observation yet\n  - `Available`: resource is up and meeting consumer needs\n  - `Degraded`: resource is up but below nominal capacity / quality\n  - `Unavailable`: resource is down (planned or unplanned)\n  - `Recovering`: resource was Unavailable; observation suggests\n                    it may be coming back; operator must `restore_supply`\n                    to confirm `Recovering -> Available`\n  - `Decommissioned`: lifecycle terminal; the Supply was deregistered.\n                      NOT a health state; a tombstone parallel to\n                      `Actor.active=false`, `Subject.status=Discarded`,\n                      `Asset.lifecycle=Decommissioned`. No transition exits\n                      this state; re-registration creates a fresh `supply_id`.\n                      Do NOT interpret this value as license to add `Faulted`,\n                      `Maintenance`, or any other health-state widening; those\n                      remain fenced by Anti-hook 1 of [[project_supply_design]].\n\nNaming asymmetry (deliberate): the operator gesture is `deregister_supply`\n(paired with `register_supply` for natural register/deregister UX);\nthe event class is `SupplyDeregistered` (matches the gesture); but the\nresulting status value is `DECOMMISSIONED` (matches the cross-BC\ntombstone vocabulary established by `Asset.lifecycle.DECOMMISSIONED`).\nThe gesture-vs-status split is intentional: operator-facing surfaces\nspeak `register`/`deregister`; event-log + status-projection surfaces\nspeak the cross-BC tombstone vocabulary. See\n[[project_deregister_supply_design]].\n\nInitial `Unknown` is universal industrial + cloud-native consensus\n(Tango UNKNOWN, EPICS UDF, Azure Resource Health Unknown, k8s\nPending, Prometheus `up{}` UNKNOWN-before-first-scrape). The\noptimistic-Available default is an anti-pattern.",
        "enum": [
          "Unknown",
          "Available",
          "Degraded",
          "Unavailable",
          "Recovering",
          "Decommissioned"
        ],
        "title": "SupplyStatus",
        "type": "string"
      },
      "SupplySummaryDTO": {
        "description": "One supply in a paginated list.",
        "properties": {
          "kind": {
            "maxLength": 50,
            "title": "Kind",
            "type": "string"
          },
          "last_status_changed_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Status Changed At"
          },
          "last_status_reason": {
            "anyOf": [
              {
                "maxLength": 500,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Last Status Reason"
          },
          "last_trigger": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TriggerSource"
              },
              {
                "type": "null"
              }
            ]
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "registered_at": {
            "format": "date-time",
            "title": "Registered At",
            "type": "string"
          },
          "scope": {
            "$ref": "#/components/schemas/SupplyScope"
          },
          "status": {
            "$ref": "#/components/schemas/SupplyStatus"
          },
          "supply_id": {
            "format": "uuid",
            "title": "Supply Id",
            "type": "string"
          }
        },
        "required": [
          "supply_id",
          "scope",
          "kind",
          "name",
          "status",
          "registered_at"
        ],
        "title": "SupplySummaryDTO",
        "type": "object"
      },
      "SurfaceKind": {
        "enum": [
          "http",
          "mcp_stdio",
          "mcp_streamable_http"
        ],
        "title": "SurfaceKind",
        "type": "string"
      },
      "SurfaceResponse": {
        "description": "Read-side DTO at the API boundary.",
        "properties": {
          "id": {
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "$ref": "#/components/schemas/SurfaceKind"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "status": {
            "$ref": "#/components/schemas/SurfaceStatus"
          }
        },
        "required": [
          "id",
          "name",
          "kind",
          "status"
        ],
        "title": "SurfaceResponse",
        "type": "object"
      },
      "SurfaceStatus": {
        "description": "Lifecycle states. v1 only emits DEFINED; the other values are\npre-shipped so versioning / deprecation slices can land additively\nlater without breaking the state shape.\n\nWire values use PascalCase to match every other CORA status enum\n(`AgentStatus.DEFINED = \"Defined\"`, `CampaignStatus.PLANNED = \"Planned\"`,\n`CautionStatus.ACTIVE = \"Active\"`, etc.). The bare-lowercase shape\nthat shipped at lock time was an outlier.",
        "enum": [
          "Defined",
          "Versioned",
          "Deprecated"
        ],
        "title": "SurfaceStatus",
        "type": "string"
      },
      "SuspendAgentRequest": {
        "description": "Body for `POST /agents/{agent_id}/suspend`.",
        "properties": {
          "reason": {
            "description": "Operator-supplied suspension reason (1-500 chars after trim). REQUIRED: operator-pause is a high-information signal that the audit log should always carry context for.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "SuspendAgentRequest",
        "type": "object"
      },
      "SuspendPermitRequest": {
        "description": "Body for `POST /federation/permits/{permit_id}/suspend`.\n\n`reason` is operator-supplied free text (audit-log breadcrumb)\nexplaining why the Permit is being suspended. Examples: \"peer\nfacility paused outbound sharing pending PII review\",\n\"credential rotation in progress, expected back in 24h\".",
        "properties": {
          "reason": {
            "anyOf": [
              {
                "maxLength": 500,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional operator-supplied reason for suspending the permit (audit-log breadcrumb).",
            "title": "Reason"
          }
        },
        "title": "SuspendPermitRequest",
        "type": "object"
      },
      "TakeControlOfSurfaceBody": {
        "additionalProperties": false,
        "description": "Body for POST /visits/{visit_id}/surface-control/take.",
        "properties": {
          "surface_id": {
            "description": "Surface that the requesting Visit assumes operational control of. Must match the Visit's surface_id.",
            "format": "uuid",
            "title": "Surface Id",
            "type": "string"
          }
        },
        "required": [
          "surface_id"
        ],
        "title": "TakeControlOfSurfaceBody",
        "type": "object"
      },
      "TargetAssetDTO": {
        "description": "Wire shape for an Asset-targeted caution.",
        "properties": {
          "id": {
            "description": "Target Asset's id.",
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "const": "Asset",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "id"
        ],
        "title": "TargetAssetDTO",
        "type": "object"
      },
      "TargetProcedureDTO": {
        "description": "Wire shape for a Procedure-targeted caution.",
        "properties": {
          "id": {
            "description": "Target Procedure's id.",
            "format": "uuid",
            "title": "Id",
            "type": "string"
          },
          "kind": {
            "const": "Procedure",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "id"
        ],
        "title": "TargetProcedureDTO",
        "type": "object"
      },
      "TemplateSlotBody": {
        "description": "Wire format for a TemplateSlot value object.",
        "properties": {
          "cardinality": {
            "$ref": "#/components/schemas/SlotCardinality",
            "description": "How many Assets can fill this slot at instantiation: Exactly1, ZeroOrOne, OneOrMore, ZeroOrMore."
          },
          "default_placement": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/PlacementBody"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional template-default placement for the slot."
          },
          "default_settings": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional template defaults applied at instantiation unless overridden by the instantiator.",
            "title": "Default Settings"
          },
          "required_family_ids": {
            "description": "Set of FamilyIds an instantiated Asset must include at least one of. MUST be non-empty.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "minItems": 1,
            "title": "Required Family Ids",
            "type": "array",
            "uniqueItems": true
          },
          "slot_name": {
            "description": "Canonical name of this slot within the Assembly (e.g., 'camera', 'rotary', 'trigger_source').",
            "maxLength": 100,
            "minLength": 1,
            "title": "Slot Name",
            "type": "string"
          }
        },
        "required": [
          "slot_name",
          "required_family_ids",
          "cardinality"
        ],
        "title": "TemplateSlotBody",
        "type": "object"
      },
      "TemplateWireBody": {
        "description": "Wire format for a TemplateWire value object.",
        "properties": {
          "source_port_name": {
            "maxLength": 100,
            "minLength": 1,
            "title": "Source Port Name",
            "type": "string"
          },
          "source_slot_name": {
            "maxLength": 100,
            "minLength": 1,
            "title": "Source Slot Name",
            "type": "string"
          },
          "target_port_name": {
            "maxLength": 100,
            "minLength": 1,
            "title": "Target Port Name",
            "type": "string"
          },
          "target_slot_name": {
            "maxLength": 100,
            "minLength": 1,
            "title": "Target Slot Name",
            "type": "string"
          }
        },
        "required": [
          "source_slot_name",
          "source_port_name",
          "target_slot_name",
          "target_port_name"
        ],
        "title": "TemplateWireBody",
        "type": "object"
      },
      "TriggerSource": {
        "description": "The origin of a status-transition event.\n\nThree values locked day one per [[project_supply_design]] for\nforward-compat. Only `Operator` is wired today; `Monitor`\nand `Auto` are reserved for future slice families:\n\n  - `Operator`: explicit operator command\n  - `Monitor`: substream-derived observation (deferred; needs\n    first DAQ substream ingest, paired with run-reading trigger)\n  - `Auto`: timer-based auto-restore (deferred; needs first\n    operator complaint about `restore_supply` ack overhead OR\n    30+ days of substream-stable recoveries)\n\nLocking three values day one avoids enum-evolution churn when\nthe deferred features land.",
        "enum": [
          "Operator",
          "Monitor",
          "Auto"
        ],
        "title": "TriggerSource",
        "type": "string"
      },
      "TruncateProcedureRequest": {
        "description": "Body for `POST /procedures/{procedure_id}/truncate`.",
        "properties": {
          "interrupted_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Operator's best guess at when the actual interruption occurred (ISO-8601, timezone-aware). Distinct from when the truncate command itself runs (the system records that as occurred_at). Optional; null when unknown. Must not be in the future, the decider rejects future timestamps.",
            "title": "Interrupted At"
          },
          "reason": {
            "description": "Free-form reason for the truncation (1-500 chars after trimming). Today the field is unstructured; structured taxonomy is future-additive on the same triggers as ProcedureAborted.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "TruncateProcedureRequest",
        "type": "object"
      },
      "TruncateRunRequest": {
        "description": "Body for `POST /runs/{run_id}/truncate`.",
        "properties": {
          "interrupted_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Operator's best guess at when the actual interruption occurred (ISO-8601, timezone-aware). Distinct from when the truncate command itself runs (the system records that as occurred_at). Optional; null when unknown. Must not be in the future, the decider rejects future timestamps.",
            "title": "Interrupted At"
          },
          "reason": {
            "description": "Free-form reason for the truncation (1-500 chars after trimming). Today the field is unstructured; structured taxonomy is future-additive on the same triggers as RunStopped/RunAborted.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "TruncateRunRequest",
        "type": "object"
      },
      "UninstallAssetRequest": {
        "properties": {
          "reason": {
            "description": "Operator-supplied free-text reason for the audit log.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "UninstallAssetRequest",
        "type": "object"
      },
      "UnitSystem": {
        "description": "The unit system a Placement's values are stored in.\n\nv1 ships with one value (`SI_MM_RAD`): translation in millimetres,\nrotation in radians. CORA stores canonically; conversion to or\nfrom facility-local units happens at the adapter boundary per\n`project_units_design.md` (canonical-stored, convert-at-edge).\n\nThe single v1 value is what makes the design memo's\n\"tolerance units MUST match value units per axis\" invariant\nstructurally satisfied: with one UnitSystem, a Placement's\ntolerances are by construction in the same units as its\nvalues. When a second UnitSystem lands (non-SI deployment),\nthe per-axis coupling check must move from \"trivially true\" to\nan explicit `__post_init__` validation, OR Placement must widen\nto per-axis units fields. Capture this gate before widening\nthe enum.",
        "enum": [
          "SI_mm_rad"
        ],
        "title": "UnitSystem",
        "type": "string"
      },
      "UpdateAssetSettingsRequest": {
        "description": "Body for `PATCH /assets/{asset_id}/settings`.\n\n`settings_patch` is a dict applied with RFC 7396 (JSON Merge\nPatch) semantics:\n  - keys with non-null values are set / replaced\n  - keys with null are deleted from settings\n  - absent keys are preserved\n\nThe decider validates the post-merge result against the union\nof the Asset's currently-assigned Capabilities' settings_schemas\n(5g-a). Validation failure surfaces as HTTP 400.",
        "properties": {
          "settings_patch": {
            "additionalProperties": true,
            "description": "Partial settings dict. RFC 7396 merge semantics: non-null values set/replace; null values delete; absent keys are preserved.",
            "title": "Settings Patch",
            "type": "object"
          }
        },
        "required": [
          "settings_patch"
        ],
        "title": "UpdateAssetSettingsRequest",
        "type": "object"
      },
      "UpdateFamilySettingsSchemaRequest": {
        "description": "Body for `POST /families/{family_id}/settings-schema`.",
        "properties": {
          "settings_schema": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "JSON Schema (Draft 2020-12, constrained subset) declaring the shape of Asset.settings keys this Family owns. Pass `null` to clear an existing schema. Subset allows only: $schema, type, required, properties, enum, minimum, maximum, pattern. The $schema field is required (must be 'https://json-schema.org/draft/2020-12/schema').",
            "title": "Settings Schema"
          }
        },
        "required": [
          "settings_schema"
        ],
        "title": "UpdateFamilySettingsSchemaRequest",
        "type": "object"
      },
      "UpdateFramePlacementRequest": {
        "description": "Body for `PATCH /frames/{frame_id}/placement`.\n\n`new_placement.parent_frame_id` MUST equal the Frame's existing\n`parent_frame_id` (you cannot reparent via update_frame_placement); the\ndecider enforces this with InvalidFrameRootError -> 400.\n\n`survey` is an optional opaque payload (instrument, technician,\nresidual provenance) carried verbatim onto the FramePlacementUpdated\nevent. The VO shape is intentionally open until the first\nsurvey adapter lands.",
        "properties": {
          "new_placement": {
            "$ref": "#/components/schemas/PlacementBody",
            "description": "The new placement."
          },
          "survey": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional re-survey provenance carried verbatim onto the event payload. Shape is open until the first survey adapter lands.",
            "title": "Survey"
          }
        },
        "required": [
          "new_placement"
        ],
        "title": "UpdateFramePlacementRequest",
        "type": "object"
      },
      "UpdateMethodParametersSchemaRequest": {
        "description": "Body for `POST /methods/{method_id}/parameters-schema`.",
        "properties": {
          "parameters_schema": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "JSON Schema (Draft 2020-12, constrained subset) declaring the shape of parameter dicts that downstream Plans (6g-b) and Runs (6g-c) carry for this Method. Pass `null` to clear an existing schema. Subset allows only: $schema, type, required, properties, enum, minimum, maximum, pattern. The $schema field is required (must be 'https://json-schema.org/draft/2020-12/schema').",
            "title": "Parameters Schema"
          }
        },
        "required": [
          "parameters_schema"
        ],
        "title": "UpdateMethodParametersSchemaRequest",
        "type": "object"
      },
      "UpdateMountPlacementRequest": {
        "properties": {
          "new_placement": {
            "$ref": "#/components/schemas/PlacementBody",
            "description": "The new placement."
          },
          "survey": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional re-survey provenance carried onto the event payload.",
            "title": "Survey"
          }
        },
        "required": [
          "new_placement"
        ],
        "title": "UpdateMountPlacementRequest",
        "type": "object"
      },
      "UpdatePlanDefaultParametersRequest": {
        "description": "Body for `PATCH /plans/{plan_id}/default-parameters`.\n\n`default_parameters_patch` is a dict applied with RFC 7396\n(JSON Merge Patch) semantics:\n  - keys with non-null values are set / replaced\n  - keys with null are deleted from default_parameters\n  - absent keys are preserved\n\nThe decider validates the post-merge result against the owning\nMethod's `parameters_schema` (loaded by the handler). Strict when\nthe Method declares no schema: non-empty defaults are rejected\n(operators wanting parameter-less Methods declare\n`parameters_schema={}` explicitly). Validation failure surfaces\nas HTTP 400.",
        "properties": {
          "default_parameters_patch": {
            "additionalProperties": true,
            "description": "Partial default_parameters dict. RFC 7396 merge semantics: non-null values set/replace; null values delete; absent keys are preserved.",
            "title": "Default Parameters Patch",
            "type": "object"
          }
        },
        "required": [
          "default_parameters_patch"
        ],
        "title": "UpdatePlanDefaultParametersRequest",
        "type": "object"
      },
      "ValidationError": {
        "properties": {
          "ctx": {
            "title": "Context",
            "type": "object"
          },
          "input": {
            "title": "Input"
          },
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "title": "Location",
            "type": "array"
          },
          "msg": {
            "title": "Message",
            "type": "string"
          },
          "type": {
            "title": "Error Type",
            "type": "string"
          }
        },
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError",
        "type": "object"
      },
      "VersionAssemblyRequest": {
        "description": "Body for `POST /assemblies/{assembly_id}/versions`.",
        "properties": {
          "drawing": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DrawingBody"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional engineering reference for this revision. Excluded from the content_hash."
          },
          "name": {
            "description": "Display name for this revision snapshot. May change across revisions; non-unique.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "parameter_overrides_schema": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional JSON Schema subset declaring the shape of parameter_overrides accepted at instantiation. Excluded if the revision keeps the previous schema unchanged.",
            "title": "Parameter Overrides Schema"
          },
          "presents_as_family_id": {
            "description": "FamilyId the instantiated Assembly stands in for. May change across revisions (e.g., DCM-revisited may move from Monochromator to a wider BeamConditioning Family).",
            "format": "uuid",
            "title": "Presents As Family Id",
            "type": "string"
          },
          "required_slots": {
            "description": "Full slot set for this revision (replace-on-version). Wire shape is a list; duplicates collapse when the route handler converts to the domain frozenset.",
            "items": {
              "$ref": "#/components/schemas/TemplateSlotBody"
            },
            "title": "Required Slots",
            "type": "array"
          },
          "required_wires": {
            "description": "Full wire set for this revision (replace-on-version). Endpoints must reference slots declared in required_slots.",
            "items": {
              "$ref": "#/components/schemas/TemplateWireBody"
            },
            "title": "Required Wires",
            "type": "array"
          },
          "version": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Operator-curated free-form version label (no SemVer enforcement). Excluded from the content_hash.",
            "title": "Version"
          }
        },
        "required": [
          "name",
          "presents_as_family_id"
        ],
        "title": "VersionAssemblyRequest",
        "type": "object"
      },
      "VersionCapabilityRequest": {
        "description": "Body for `POST /capabilities/{capability_id}/version`.",
        "properties": {
          "description": {
            "anyOf": [
              {
                "maxLength": 2000,
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional human description (0-2000 chars).",
            "title": "Description"
          },
          "executor_shapes": {
            "description": "Replacement executor_shapes for the new version. Required non-empty (a Capability with no executor kinds has no operational meaning).",
            "items": {
              "$ref": "#/components/schemas/ExecutorShape"
            },
            "title": "Executor Shapes",
            "type": "array"
          },
          "parameters_schema": {
            "anyOf": [
              {
                "additionalProperties": true,
                "type": "object"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional declarative JSON Schema (constrained subset) for the parameter contract. Replaces the prior schema wholesale.",
            "title": "Parameters Schema"
          },
          "required_affordances": {
            "description": "Replacement required_affordances for the new version. A new version IS a new declaration; the supplied set REPLACES the prior set wholesale. Supply `[]` to clear all required affordances.",
            "items": {
              "$ref": "#/components/schemas/Affordance"
            },
            "title": "Required Affordances",
            "type": "array"
          },
          "version_tag": {
            "description": "Operator-supplied label for this revision (for example 'v2', '2026-Q3'). Free text; institution-specific.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Version Tag",
            "type": "string"
          }
        },
        "required": [
          "version_tag",
          "required_affordances",
          "executor_shapes"
        ],
        "title": "VersionCapabilityRequest",
        "type": "object"
      },
      "VersionFamilyRequest": {
        "description": "Body for `POST /families/{family_id}/version`.",
        "properties": {
          "affordances": {
            "description": "Replacement affordance set for the new version. A new version IS a new declaration; the supplied set REPLACES the prior affordance set wholesale (no diff/merge semantics). Supply `[]` explicitly to clear all affordances at this version.",
            "items": {
              "$ref": "#/components/schemas/Affordance"
            },
            "title": "Affordances",
            "type": "array"
          },
          "version_tag": {
            "description": "Operator-supplied label for this revision (for example 'v2', '2026-Q3'). Free text; institution-specific.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Version Tag",
            "type": "string"
          }
        },
        "required": [
          "version_tag",
          "affordances"
        ],
        "title": "VersionFamilyRequest",
        "type": "object"
      },
      "VersionMethodRequest": {
        "description": "Body for `POST /methods/{method_id}/version`.",
        "properties": {
          "version_tag": {
            "description": "Operator-supplied label for this revision (for example 'v2', '2026-Q3'). Free text; institution-specific.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Version Tag",
            "type": "string"
          }
        },
        "required": [
          "version_tag"
        ],
        "title": "VersionMethodRequest",
        "type": "object"
      },
      "VersionModelRequest": {
        "description": "Body for `POST /models/{model_id}/versions`.\n\nA new version IS a new declaration: every field REPLACES the prior\nvalue wholesale (no diff/merge semantics). `version_tag` is\nREQUIRED here, unlike `define_model` where it is optional.",
        "properties": {
          "declared_family_ids": {
            "description": "Replacement Family ids the catalog entry satisfies at this version. At least one required; deduplicated server-side.",
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "minItems": 1,
            "title": "Declared Family Ids",
            "type": "array"
          },
          "manufacturer": {
            "$ref": "#/components/schemas/ManufacturerBody",
            "description": "Replacement vendor identity (name plus optional ROR/GRID/ISNI identifier)."
          },
          "name": {
            "description": "Replacement display name for the Model at this version.",
            "maxLength": 200,
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "part_number": {
            "description": "Replacement vendor SKU; case-sensitive (RV120CCHL and rv120cchl are different Newport entries).",
            "maxLength": 100,
            "minLength": 1,
            "title": "Part Number",
            "type": "string"
          },
          "version_tag": {
            "description": "Operator-supplied label for this revision (for example 'rev-B', '2026-Q3'). Free text; institution-specific.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Version Tag",
            "type": "string"
          }
        },
        "required": [
          "name",
          "manufacturer",
          "part_number",
          "declared_family_ids",
          "version_tag"
        ],
        "title": "VersionModelRequest",
        "type": "object"
      },
      "VersionPlanRequest": {
        "description": "Body for `POST /plans/{plan_id}/version`.",
        "properties": {
          "version_tag": {
            "description": "Operator-supplied label for this revision (for example 'v2', '2026-Q3'). Free text; institution-specific.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Version Tag",
            "type": "string"
          }
        },
        "required": [
          "version_tag"
        ],
        "title": "VersionPlanRequest",
        "type": "object"
      },
      "VersionPracticeRequest": {
        "description": "Body for `POST /practices/{practice_id}/version`.",
        "properties": {
          "version_tag": {
            "description": "Operator-supplied label for this revision (for example 'v2', '2026-Q3'). Free text; institution-specific.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Version Tag",
            "type": "string"
          }
        },
        "required": [
          "version_tag"
        ],
        "title": "VersionPracticeRequest",
        "type": "object"
      },
      "VersionRecipeRequest": {
        "description": "Body for `POST /recipes/{recipe_id}/version`.",
        "properties": {
          "steps": {
            "additionalProperties": true,
            "description": "Replacement step sequence for the new version (wholesale replace; the prior steps are dropped). BindingRef sentinels are re-validated against the CURRENT Capability.parameters_schema.",
            "title": "Steps",
            "type": "object"
          },
          "version_tag": {
            "description": "Operator-supplied label for this revision (for example 'v2', '2026-Q3'). Free text; institution-specific. NOT constrained UNIQUE across versions; same tag + same steps re-emits the event as a re-attestation audit signal.",
            "maxLength": 50,
            "minLength": 1,
            "title": "Version Tag",
            "type": "string"
          }
        },
        "required": [
          "version_tag",
          "steps"
        ],
        "title": "VersionRecipeRequest",
        "type": "object"
      },
      "VisitType": {
        "description": "Closed enum classifying the operational nature of a Visit.\n\nFive values per `[[project_visit_aggregate_design]]`. Replaces the\nsentinel-value anti-pattern (DMagic `--gup 0`, NICOS demo=0).\n\n  - `user`         -- proposal-driven user beamtime\n  - `commissioning`-- detector / mechanism commissioning (nested as\n                      partOf a parent user Visit per S2 scenario)\n  - `maintenance`  -- preventative or corrective maintenance window\n  - `calibration`  -- standalone calibration block (CalibrationRevision\n                      is the data-side counterpart in Calibration BC)\n  - `staff`        -- staff-only work outside a proposal envelope",
        "enum": [
          "user",
          "commissioning",
          "maintenance",
          "calibration",
          "staff"
        ],
        "title": "VisitType",
        "type": "string"
      },
      "VoidVisitRequest": {
        "description": "Body for `POST /visits/{visit_id}/void`.",
        "properties": {
          "reason": {
            "description": "Operator-supplied reason explaining why this Visit should never have existed (e.g., 'BSS double-sent registration', 'duplicate of visit X'). No PII.",
            "maxLength": 500,
            "minLength": 1,
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "reason"
        ],
        "title": "VoidVisitRequest",
        "type": "object"
      },
      "WiredAssetItem": {
        "description": "Per-wired-Asset binding contribution on the wire.\n\n`family_ids` and `contributed_affordances` are sorted for\ndeterministic client diffs / cache validation.",
        "properties": {
          "asset_id": {
            "format": "uuid",
            "title": "Asset Id",
            "type": "string"
          },
          "asset_name": {
            "title": "Asset Name",
            "type": "string"
          },
          "condition": {
            "title": "Condition",
            "type": "string"
          },
          "contributed_affordances": {
            "items": {
              "type": "string"
            },
            "title": "Contributed Affordances",
            "type": "array"
          },
          "family_ids": {
            "items": {
              "format": "uuid",
              "type": "string"
            },
            "title": "Family Ids",
            "type": "array"
          },
          "lifecycle": {
            "title": "Lifecycle",
            "type": "string"
          }
        },
        "required": [
          "asset_id",
          "asset_name",
          "condition",
          "lifecycle",
          "family_ids",
          "contributed_affordances"
        ],
        "title": "WiredAssetItem",
        "type": "object"
      },
      "ZoneListResponse": {
        "description": "Page of zones plus opaque next-page cursor.",
        "properties": {
          "items": {
            "items": {
              "$ref": "#/components/schemas/ZoneSummaryDTO"
            },
            "title": "Items",
            "type": "array"
          },
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Next Cursor"
          }
        },
        "required": [
          "items"
        ],
        "title": "ZoneListResponse",
        "type": "object"
      },
      "ZoneSummaryDTO": {
        "description": "One zone in a paginated list.",
        "properties": {
          "created_at": {
            "format": "date-time",
            "title": "Created At",
            "type": "string"
          },
          "name": {
            "maxLength": 200,
            "title": "Name",
            "type": "string"
          },
          "zone_id": {
            "format": "uuid",
            "title": "Zone Id",
            "type": "string"
          }
        },
        "required": [
          "zone_id",
          "name",
          "created_at"
        ],
        "title": "ZoneSummaryDTO",
        "type": "object"
      },
      "_ActionStepRequest": {
        "additionalProperties": false,
        "description": "JSON wire shape for an `ActionStep`.",
        "properties": {
          "kind": {
            "const": "action",
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "params": {
            "additionalProperties": true,
            "title": "Params",
            "type": "object"
          }
        },
        "required": [
          "kind",
          "name"
        ],
        "title": "_ActionStepRequest",
        "type": "object"
      },
      "_CheckStepRequest": {
        "additionalProperties": false,
        "description": "JSON wire shape for a `CheckStep`.",
        "properties": {
          "address": {
            "minLength": 1,
            "title": "Address",
            "type": "string"
          },
          "criterion": {
            "discriminator": {
              "mapping": {
                "equals": "#/components/schemas/_EqualsCriterion",
                "within_tolerance": "#/components/schemas/_WithinToleranceCriterion"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/components/schemas/_EqualsCriterion"
              },
              {
                "$ref": "#/components/schemas/_WithinToleranceCriterion"
              }
            ],
            "title": "Criterion"
          },
          "kind": {
            "const": "check",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "address",
          "criterion"
        ],
        "title": "_CheckStepRequest",
        "type": "object"
      },
      "_ConductorFailureResponse": {
        "description": "JSON wire shape for `ConductorFailure`.",
        "properties": {
          "error_class": {
            "title": "Error Class",
            "type": "string"
          },
          "message": {
            "title": "Message",
            "type": "string"
          },
          "source_kind": {
            "title": "Source Kind",
            "type": "string"
          },
          "step_index": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "title": "Step Index"
          },
          "target": {
            "title": "Target",
            "type": "string"
          }
        },
        "required": [
          "step_index",
          "source_kind",
          "target",
          "error_class",
          "message"
        ],
        "title": "_ConductorFailureResponse",
        "type": "object"
      },
      "_EqualsCriterion": {
        "additionalProperties": false,
        "description": "JSON wire shape for an `EqualsCriterion`.",
        "properties": {
          "expected": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "number"
              },
              {
                "type": "boolean"
              },
              {
                "type": "string"
              },
              {
                "items": {},
                "type": "array"
              }
            ],
            "title": "Expected"
          },
          "kind": {
            "const": "equals",
            "title": "Kind",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "expected"
        ],
        "title": "_EqualsCriterion",
        "type": "object"
      },
      "_ExternalRefBody": {
        "description": "One ExternalRef carried on the request body.",
        "properties": {
          "id": {
            "maxLength": 200,
            "minLength": 1,
            "title": "Id",
            "type": "string"
          },
          "scheme": {
            "maxLength": 50,
            "minLength": 1,
            "title": "Scheme",
            "type": "string"
          }
        },
        "required": [
          "scheme",
          "id"
        ],
        "title": "_ExternalRefBody",
        "type": "object"
      },
      "_InboundTermsRequest": {
        "additionalProperties": false,
        "description": "JSON wire shape for `InboundTerms`.",
        "properties": {
          "accepted_canonicalization_versions": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "title": "Accepted Canonicalization Versions"
          },
          "inbound_allowed_artifact_kinds": {
            "items": {
              "type": "string"
            },
            "minItems": 1,
            "title": "Inbound Allowed Artifact Kinds",
            "type": "array"
          },
          "kind": {
            "const": "Inbound",
            "title": "Kind",
            "type": "string"
          },
          "publisher_grant_correlation_handle": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Publisher Grant Correlation Handle"
          },
          "required_receipt_kinds": {
            "items": {
              "$ref": "#/components/schemas/ReceiptKind"
            },
            "title": "Required Receipt Kinds",
            "type": "array"
          }
        },
        "required": [
          "kind",
          "inbound_allowed_artifact_kinds"
        ],
        "title": "_InboundTermsRequest",
        "type": "object"
      },
      "_InboundTermsResponse": {
        "description": "Wire shape for inbound terms in the response.",
        "properties": {
          "accepted_canonicalization_versions": {
            "items": {
              "type": "string"
            },
            "title": "Accepted Canonicalization Versions",
            "type": "array"
          },
          "inbound_allowed_artifact_kinds": {
            "items": {
              "type": "string"
            },
            "title": "Inbound Allowed Artifact Kinds",
            "type": "array"
          },
          "kind": {
            "const": "Inbound",
            "title": "Kind",
            "type": "string"
          },
          "publisher_grant_correlation_handle": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Publisher Grant Correlation Handle"
          },
          "required_receipt_kinds": {
            "items": {
              "type": "string"
            },
            "title": "Required Receipt Kinds",
            "type": "array"
          }
        },
        "required": [
          "kind",
          "inbound_allowed_artifact_kinds",
          "accepted_canonicalization_versions",
          "required_receipt_kinds"
        ],
        "title": "_InboundTermsResponse",
        "type": "object"
      },
      "_OutboundTermsRequest": {
        "additionalProperties": false,
        "description": "JSON wire shape for `OutboundTerms`.",
        "properties": {
          "kind": {
            "const": "Outbound",
            "title": "Kind",
            "type": "string"
          },
          "onward_action_scope": {
            "$ref": "#/components/schemas/OnwardActionScope"
          },
          "read_scope": {
            "$ref": "#/components/schemas/ReadScope"
          },
          "scopes": {
            "items": {
              "$ref": "#/components/schemas/_ScopeRefRequest"
            },
            "minItems": 1,
            "title": "Scopes",
            "type": "array"
          }
        },
        "required": [
          "kind",
          "scopes",
          "read_scope",
          "onward_action_scope"
        ],
        "title": "_OutboundTermsRequest",
        "type": "object"
      },
      "_OutboundTermsResponse": {
        "description": "Wire shape for outbound terms in the response.",
        "properties": {
          "kind": {
            "const": "Outbound",
            "title": "Kind",
            "type": "string"
          },
          "onward_action_scope": {
            "title": "Onward Action Scope",
            "type": "string"
          },
          "read_scope": {
            "title": "Read Scope",
            "type": "string"
          },
          "scopes": {
            "items": {
              "$ref": "#/components/schemas/_ScopeRefResponse"
            },
            "title": "Scopes",
            "type": "array"
          }
        },
        "required": [
          "kind",
          "scopes",
          "read_scope",
          "onward_action_scope"
        ],
        "title": "_OutboundTermsResponse",
        "type": "object"
      },
      "_ScopeRefRequest": {
        "additionalProperties": false,
        "description": "JSON wire shape for a `ScopeRef`.",
        "properties": {
          "kind": {
            "minLength": 1,
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "minLength": 1,
            "title": "Name",
            "type": "string"
          },
          "qualifier": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Qualifier"
          }
        },
        "required": [
          "kind",
          "name"
        ],
        "title": "_ScopeRefRequest",
        "type": "object"
      },
      "_ScopeRefResponse": {
        "description": "Wire shape for a `ScopeRef` in the outbound terms response.",
        "properties": {
          "kind": {
            "title": "Kind",
            "type": "string"
          },
          "name": {
            "title": "Name",
            "type": "string"
          },
          "qualifier": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "title": "Qualifier"
          }
        },
        "required": [
          "kind",
          "name"
        ],
        "title": "_ScopeRefResponse",
        "type": "object"
      },
      "_SetpointStepRequest": {
        "additionalProperties": false,
        "description": "JSON wire shape for a `SetpointStep`.",
        "properties": {
          "address": {
            "minLength": 1,
            "title": "Address",
            "type": "string"
          },
          "kind": {
            "const": "setpoint",
            "title": "Kind",
            "type": "string"
          },
          "value": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "number"
              },
              {
                "type": "boolean"
              },
              {
                "type": "string"
              },
              {
                "items": {},
                "type": "array"
              }
            ],
            "title": "Value"
          },
          "verify": {
            "default": false,
            "title": "Verify",
            "type": "boolean"
          }
        },
        "required": [
          "kind",
          "address",
          "value"
        ],
        "title": "_SetpointStepRequest",
        "type": "object"
      },
      "_WithinToleranceCriterion": {
        "additionalProperties": false,
        "description": "JSON wire shape for a `WithinToleranceCriterion`.",
        "properties": {
          "expected": {
            "title": "Expected",
            "type": "number"
          },
          "kind": {
            "const": "within_tolerance",
            "title": "Kind",
            "type": "string"
          },
          "tolerance": {
            "minimum": 0.0,
            "title": "Tolerance",
            "type": "number"
          }
        },
        "required": [
          "kind",
          "expected",
          "tolerance"
        ],
        "title": "_WithinToleranceCriterion",
        "type": "object"
      }
    }
  },
  "info": {
    "description": "Research facility operations platform",
    "title": "CORA",
    "version": "0.1.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/.well-known/oauth-protected-resource": {
      "get": {
        "operationId": "protected_resource_metadata__well_known_oauth_protected_resource_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "RFC 9728 Protected Resource Metadata",
        "tags": [
          "edge-auth"
        ]
      }
    },
    "/actors": {
      "get": {
        "operationId": "list_actors_actors_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter. Omit to return all statuses; no 'all' magic value is accepted.",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "active",
                    "deactivated"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter. Omit to return all statuses; no 'all' magic value is accepted.",
              "title": "Status"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActorListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List actors with cursor pagination",
        "tags": [
          "access"
        ]
      },
      "post": {
        "operationId": "post_actors_actors_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the actor. Reusing a key with a different body returns 422.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the actor. Reusing a key with a different body returns 422.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterActorRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterActorResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new actor",
        "tags": [
          "access"
        ]
      }
    },
    "/actors/{actor_id}": {
      "get": {
        "operationId": "get_actors_actors__actor_id__get",
        "parameters": [
          {
            "description": "Target actor's id.",
            "in": "path",
            "name": "actor_id",
            "required": true,
            "schema": {
              "description": "Target actor's id.",
              "format": "uuid",
              "title": "Actor Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ActorResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No actor exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get an actor by id",
        "tags": [
          "access"
        ]
      }
    },
    "/actors/{actor_id}/deactivate": {
      "post": {
        "operationId": "post_actors_deactivate_actors__actor_id__deactivate_post",
        "parameters": [
          {
            "description": "Target actor's id.",
            "in": "path",
            "name": "actor_id",
            "required": true,
            "schema": {
              "description": "Target actor's id.",
              "format": "uuid",
              "title": "Actor Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No actor exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Actor is already deactivated, OR a concurrent write to the same actor stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Deactivate an existing actor",
        "tags": [
          "access"
        ]
      }
    },
    "/actors/{actor_id}/profile": {
      "delete": {
        "operationId": "delete_actor_profile_actors__actor_id__profile_delete",
        "parameters": [
          {
            "description": "Target actor's id.",
            "in": "path",
            "name": "actor_id",
            "required": true,
            "schema": {
              "description": "Target actor's id.",
              "format": "uuid",
              "title": "Actor Id",
              "type": "string"
            }
          },
          {
            "description": "Optional client-supplied unique key per logical erasure request. Retries with the same key short-circuit so a double-click does not append two audit events.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical erasure request. Retries with the same key short-circuit so a double-click does not append two audit events.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No actor exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Concurrent write to the same actor stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter failed schema validation OR Idempotency-Key was reused with a different request."
          }
        },
        "summary": "Erase an actor's PII vault row (right to be forgotten)",
        "tags": [
          "access"
        ]
      }
    },
    "/agent/reactions/{subscriber_name}/dismiss-event": {
      "post": {
        "operationId": "post_agent_reactions_dismiss_event_agent_reactions__subscriber_name__dismiss_event_post",
        "parameters": [
          {
            "description": "Name of the subscriber (matches `Reaction.name`).",
            "in": "path",
            "name": "subscriber_name",
            "required": true,
            "schema": {
              "description": "Name of the subscriber (matches `Reaction.name`).",
              "maxLength": 200,
              "minLength": 1,
              "title": "Subscriber Name",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DismissEventInReactionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DismissEventInReactionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: empty / oversize reason (InvalidDismissalReasonError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No projection_bookmarks row for the named subscriber (SubscriberBookmarkNotFoundError) OR no event row for the supplied event_id (DismissalEventNotFoundError)."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "The bookmark is already at or past the target event (EventAlreadyDismissedError); refresh the dashboard and re-evaluate before retrying."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "The deployment has no Postgres pool (DismissalRequiresPostgresError); the slice is a no-op without projection_bookmarks. Production deployments never see this."
          }
        },
        "summary": "Dismiss a poison event on a Reaction's bookmark",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents": {
      "post": {
        "operationId": "post_agents_agents_post",
        "parameters": [
          {
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineAgentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineAgentResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only kind / name / version, over-cap capabilities, malformed canonical_uri, or invalid ModelRef)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Defensive guard: the target Agent or co-written Actor stream already has events. Essentially impossible in production with UUIDv7 ids."
          },
          "422": {
            "description": "Request body failed schema validation (missing field, length out of bounds, invalid UUID), OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new Agent (cross-BC atomic; co-registers an Actor with kind=agent)",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents/caution-drafter/decisions/{decision_id}/promote": {
      "post": {
        "operationId": "post_promote_caution_proposal_agents_caution_drafter_decisions__decision_id__promote_post",
        "parameters": [
          {
            "description": "Target Decision's id.",
            "in": "path",
            "name": "decision_id",
            "required": true,
            "schema": {
              "description": "Target Decision's id.",
              "format": "uuid",
              "title": "Decision Id",
              "type": "string"
            }
          },
          {
            "description": "Brandur-style idempotency key (optional).",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Brandur-style idempotency key (optional).",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PromoteCautionProposalResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Decision has wrong context, is non-actionable (NoAction), or carries a malformed `proposed_caution` payload."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command, OR the Decision was not emitted by a registered CautionDrafter agent (provenance gate)."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Decision exists with the given id, or the referenced Caution (for ProposeSupersede) does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "ProposeSupersede target Caution is not in Active."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Promote a CautionProposal Decision into a real Caution (cross-BC write to Caution BC)",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents/run-debriefer/runs/{run_id}/regenerate-debrief": {
      "post": {
        "operationId": "post_regenerate_run_debrief_agents_run_debriefer_runs__run_id__regenerate_debrief_post",
        "parameters": [
          {
            "description": "The Run to regenerate the debrief for.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "The Run to regenerate the debrief for.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/RegenerateRunDebriefRequest"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Body"
              }
            }
          }
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegenerateRunDebriefResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Cross-aggregate invariant violated: Run missing, agent not seeded / deactivated, parent_decision missing, or parent Decision references a different Run."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation, OR Idempotency-Key was reused with a different request body."
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "RunDebriefer subscriber is not wired (ANTHROPIC_API_KEY unset)."
          }
        },
        "summary": "Re-invoke RunDebriefer on demand against a specific Run.",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents/{agent_id}": {
      "get": {
        "operationId": "get_agents_agents__agent_id__get",
        "parameters": [
          {
            "description": "Target agent's id.",
            "in": "path",
            "name": "agent_id",
            "required": true,
            "schema": {
              "description": "Target agent's id.",
              "format": "uuid",
              "title": "Agent Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AgentResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No agent exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get an agent by id",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents/{agent_id}/budget": {
      "post": {
        "operationId": "post_agents_revise_budget_agents__agent_id__budget_post",
        "parameters": [
          {
            "description": "Target agent's id.",
            "in": "path",
            "name": "agent_id",
            "required": true,
            "schema": {
              "description": "Target agent's id.",
              "format": "uuid",
              "title": "Agent Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReviseAgentBudgetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Cap value invalid (negative)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No agent exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Agent is `Deprecated` (only blocking source state)."
          },
          "422": {
            "description": "Request body or path parameter failed schema validation."
          }
        },
        "summary": "Revise an Agent's declarative budget caps (PUT semantics; both null clears)",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents/{agent_id}/deprecate": {
      "post": {
        "operationId": "post_agents_deprecate_agents__agent_id__deprecate_post",
        "parameters": [
          {
            "description": "Target agent's id.",
            "in": "path",
            "name": "agent_id",
            "required": true,
            "schema": {
              "description": "Target agent's id.",
              "format": "uuid",
              "title": "Agent Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeprecateAgentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Reason is empty / whitespace-only / over-cap after trim."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No agent exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Agent is already Deprecated (strict-not-idempotent)."
          },
          "422": {
            "description": "Request body or path parameter failed schema validation."
          }
        },
        "summary": "Deprecate an Agent (Defined | Versioned -> Deprecated; terminal)",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents/{agent_id}/resume": {
      "post": {
        "operationId": "post_agents_resume_agents__agent_id__resume_post",
        "parameters": [
          {
            "description": "Target agent's id.",
            "in": "path",
            "name": "agent_id",
            "required": true,
            "schema": {
              "description": "Target agent's id.",
              "format": "uuid",
              "title": "Agent Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No agent exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Agent is not in `Suspended` (strict-not-idempotent: only a `Suspended` agent can be resumed)."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Resume an Agent (Suspended -> Versioned; non-terminal)",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents/{agent_id}/suspend": {
      "post": {
        "operationId": "post_agents_suspend_agents__agent_id__suspend_post",
        "parameters": [
          {
            "description": "Target agent's id.",
            "in": "path",
            "name": "agent_id",
            "required": true,
            "schema": {
              "description": "Target agent's id.",
              "format": "uuid",
              "title": "Agent Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SuspendAgentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Reason is empty / whitespace-only / over-cap after trim."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No agent exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Agent is not in `Versioned` (strict-not-idempotent: cannot re-suspend a `Suspended` agent, cannot suspend a `Defined` or `Deprecated` agent)."
          },
          "422": {
            "description": "Request body or path parameter failed schema validation."
          }
        },
        "summary": "Suspend an Agent (Versioned -> Suspended; non-terminal)",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents/{agent_id}/tools/grant": {
      "post": {
        "operationId": "post_agents_grant_tool_agents__agent_id__tools_grant_post",
        "parameters": [
          {
            "description": "Target agent's id.",
            "in": "path",
            "name": "agent_id",
            "required": true,
            "schema": {
              "description": "Target agent's id.",
              "format": "uuid",
              "title": "Agent Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/GrantToolToAgentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Tool name is empty / whitespace-only / over-cap, or grant would exceed the 32-entry cardinality cap."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No agent exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Agent is `Deprecated` (only blocking source state)."
          },
          "422": {
            "description": "Request body or path parameter failed schema validation."
          }
        },
        "summary": "Grant one MCP tool to an Agent (idempotent re-grant emits no event)",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents/{agent_id}/tools/revoke": {
      "post": {
        "operationId": "post_agents_revoke_tool_agents__agent_id__tools_revoke_post",
        "parameters": [
          {
            "description": "Target agent's id.",
            "in": "path",
            "name": "agent_id",
            "required": true,
            "schema": {
              "description": "Target agent's id.",
              "format": "uuid",
              "title": "Agent Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RevokeToolFromAgentRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Tool name is empty / whitespace-only / over-cap after trim."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No agent exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Agent is `Deprecated` (only blocking source state)."
          },
          "422": {
            "description": "Request body or path parameter failed schema validation."
          }
        },
        "summary": "Revoke one MCP tool from an Agent (idempotent revoke-of-non-granted emits no event)",
        "tags": [
          "agent"
        ]
      }
    },
    "/agents/{agent_id}/version": {
      "post": {
        "operationId": "post_agents_version_agents__agent_id__version_post",
        "parameters": [
          {
            "description": "Target agent's id.",
            "in": "path",
            "name": "agent_id",
            "required": true,
            "schema": {
              "description": "Target agent's id.",
              "format": "uuid",
              "title": "Agent Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No agent exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Agent is not in Defined status (version_agent is single-source from Defined only)."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Version a Defined Agent (Defined -> Versioned)",
        "tags": [
          "agent"
        ]
      }
    },
    "/assemblies": {
      "post": {
        "operationId": "post_assemblies_assemblies_post",
        "parameters": [
          {
            "description": "Optional client-supplied key for idempotent retry: replaying the same key returns the original assembly_id without creating a new Assembly.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied key for idempotent retry: replaying the same key returns the original assembly_id without creating a new Assembly.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineAssemblyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineAssemblyResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only name or slot_name, non-enum cardinality, malformed wire spec, wire references an unknown slot, invalid parameter_overrides_schema subset, or invalid Placement / Drawing nested VO."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "A referenced FamilyId (presents_as_family_id or any slot's required_family_ids member) does not resolve to a defined Family."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Stream collision: the generated UUID already has an AssemblyDefined event (essentially impossible with UUIDv7 ids; defensive guard)."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new Assembly composition blueprint",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assemblies/{assembly_id}/deprecate": {
      "post": {
        "operationId": "post_assemblies_deprecate_assemblies__assembly_id__deprecate_post",
        "parameters": [
          {
            "description": "Target Assembly's id.",
            "in": "path",
            "name": "assembly_id",
            "required": true,
            "schema": {
              "description": "Target Assembly's id.",
              "format": "uuid",
              "title": "Assembly Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeprecateAssemblyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No assembly exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Assembly is not in `Defined` or `Versioned` status (deprecate requires one of those; re-deprecating a Deprecated Assembly raises), OR a concurrent write to the same Assembly stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Request body failed schema validation."
          }
        },
        "summary": "Mark an existing Assembly as deprecated (terminal)",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assemblies/{assembly_id}/fixtures": {
      "post": {
        "operationId": "post_assemblies_fixtures_assemblies__assembly_id__fixtures_post",
        "parameters": [
          {
            "description": "Target Assembly's id.",
            "in": "path",
            "name": "assembly_id",
            "required": true,
            "schema": {
              "description": "Target Assembly's id.",
              "format": "uuid",
              "title": "Assembly Id",
              "type": "string"
            }
          },
          {
            "description": "Optional client-supplied key for idempotent retry: replaying the same key returns the original fixture_id without creating a new Fixture.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied key for idempotent retry: replaying the same key returns the original fixture_id without creating a new Fixture.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterFixtureRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterFixtureResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Slot cardinality not satisfied, a mapped Asset's family_ids do not intersect the slot's required_family_ids, OR parameter_overrides fail the Assembly's schema (STRICT: non-empty overrides on a schema-less Assembly)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Assembly does not exist, OR a referenced asset_id does not resolve to a registered Asset."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Assembly is Deprecated (cannot instantiate), OR a concurrent write to the new Fixture stream conflicted (optimistic concurrency; essentially impossible with UUIDv7 ids)."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new Fixture against an Assembly blueprint",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assemblies/{assembly_id}/versions": {
      "post": {
        "operationId": "post_assembly_version_assemblies__assembly_id__versions_post",
        "parameters": [
          {
            "description": "The target Assembly's UUID.",
            "in": "path",
            "name": "assembly_id",
            "required": true,
            "schema": {
              "description": "The target Assembly's UUID.",
              "format": "uuid",
              "title": "Assembly Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VersionAssemblyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only name or slot_name, non-enum cardinality, malformed wire spec, wire references an unknown slot, or invalid parameter_overrides_schema subset."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Assembly with the given assembly_id does not exist, OR a referenced FamilyId (presents_as_family_id or any slot's required_family_ids member) does not resolve to a defined Family."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Assembly is in a status that cannot be versioned (Deprecated is terminal; new revisions must fork via POST /assemblies with a fresh id)."
          },
          "422": {
            "description": "Request body failed schema validation."
          }
        },
        "summary": "Publish a new revision snapshot of an existing Assembly",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets": {
      "get": {
        "operationId": "list_assets_assets_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional hierarchy level filter.",
            "in": "query",
            "name": "level",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Enterprise",
                    "Site",
                    "Area",
                    "Unit",
                    "Component",
                    "Device"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional hierarchy level filter.",
              "title": "Level"
            }
          },
          {
            "description": "Optional lifecycle filter.",
            "in": "query",
            "name": "lifecycle",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Commissioned",
                    "Active",
                    "Maintenance",
                    "Decommissioned"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional lifecycle filter.",
              "title": "Lifecycle"
            }
          },
          {
            "description": "Direct-children-of filter (returns rows with parent_id = this).",
            "in": "query",
            "name": "parent_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Direct-children-of filter (returns rows with parent_id = this).",
              "title": "Parent Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AssetListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List assets with cursor pagination + level/lifecycle/parent filters",
        "tags": [
          "equipment"
        ]
      },
      "post": {
        "operationId": "post_assets_assets_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the asset.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the asset.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterAssetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterAssetResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only name, hierarchy rule (Enterprise must have null parent_id; other levels must have non-null parent_id), invalid Drawing (empty number, overlong revision, etc.), or invalid AlternateIdentifier value (empty after trimming, exceeds 200 chars)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "model_id was supplied but the referenced Model stream does not exist (ModelNotFoundError)."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Two or more entries in the owners payload share a name (AssetOwnerAlreadyPresentError); owner names must be unique within a single Asset."
          },
          "422": {
            "description": "Request body failed schema validation (unknown level, missing fields, malformed UUID) OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new physical equipment asset",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}": {
      "get": {
        "operationId": "get_assets_assets__asset_id__get",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AssetResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get an asset by id",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/activate": {
      "post": {
        "operationId": "post_assets_activate_assets__asset_id__activate_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset is not in `Commissioned` lifecycle (activate requires Commissioned), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Activate an existing (Commissioned) asset, putting it into service",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/add-alternate-identifier": {
      "post": {
        "operationId": "post_assets_add_alternate_identifier_assets__asset_id__add_alternate_identifier_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddAssetAlternateIdentifierRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Identifier value is empty / whitespace-only / exceeds the configured max length after trimming (InvalidAlternateIdentifierValueError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize policy denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset cannot accept the alternate identifier under current conditions: the asset is Decommissioned (AssetCannotAddAlternateIdentifierError), OR an alternate identifier with the same (kind, value) pair already exists on the asset (AssetAlternateIdentifierAlreadyPresentError), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation (missing field, invalid kind enum, etc.)."
          }
        },
        "summary": "Add an alternate identifier to an existing Asset's identifier set",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/add-family": {
      "post": {
        "operationId": "post_assets_add_family_assets__asset_id__add_family_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddAssetFamilyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset cannot accept the family under current conditions (asset is Decommissioned, OR the family is already in the asset's family set), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Add a Family to an existing asset's family set",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/add-owner": {
      "post": {
        "operationId": "post_assets_add_owner_assets__asset_id__add_owner_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddAssetOwnerRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Owner VO validation failed: empty / whitespace-only name, contact, identifier, or identifier_type; or the identifier <-> identifier_type pairing invariant was violated (InvalidAssetOwnerIdentifierPairingError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize policy denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset cannot accept the owner under current conditions: the asset is Decommissioned (AssetCannotAddOwnerError), OR an owner with the same name already exists on the asset (AssetOwnerAlreadyPresentError), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation (missing field, malformed UUID, length out of bounds at the wire layer)."
          }
        },
        "summary": "Add an institutional owner to an existing Asset's owners set",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/add-port": {
      "post": {
        "operationId": "post_assets_add_port_assets__asset_id__add_port_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddAssetPortRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Port name or signal_type is empty / whitespace-only / exceeds the configured max length after trimming (InvalidAssetPortNameError or InvalidAssetPortSignalTypeError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset cannot accept the port under current conditions (asset is Decommissioned, OR a port with the same name already exists), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation (missing field, invalid direction enum, etc.)."
          }
        },
        "summary": "Add a typed port to an existing Asset's port set",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/attach-to-fixture": {
      "post": {
        "operationId": "post_assets_attach_to_fixture_assets__asset_id__attach_to_fixture_post",
        "parameters": [
          {
            "description": "Target Asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target Asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AttachAssetToFixtureRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset does not appear in the Fixture's slot_asset_bindings (phantom back-reference rejected)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset or Fixture does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset is already attached to a Fixture (re-attach requires detach first), OR Asset is in a lifecycle that disallows attach (currently Decommissioned only), OR a concurrent write to the Asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Request body failed schema validation."
          }
        },
        "summary": "Bind an existing Asset to a registered Fixture",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/decommission": {
      "post": {
        "operationId": "post_assets_decommission_assets__asset_id__decommission_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset is not in `Commissioned` or `Active` lifecycle (decommission requires either), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Decommission an existing asset, retiring it from service",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/degrade": {
      "post": {
        "operationId": "post_assets_degrade_assets__asset_id__degrade_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DegradeAssetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "A concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Mark an existing asset as Degraded (works with reduced specs)",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/detach-from-fixture": {
      "post": {
        "operationId": "post_assets_detach_from_fixture_assets__asset_id__detach_from_fixture_post",
        "parameters": [
          {
            "description": "Target Asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target Asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DetachAssetFromFixtureRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset is not attached to any Fixture (strict-not-idempotent re-detach), OR Asset is attached to a different Fixture than the requested fixture_id (defensive race-condition guard), OR a concurrent write to the Asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Request body failed schema validation."
          }
        },
        "summary": "Detach an Asset from the Fixture it is currently attached to",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/enter-maintenance": {
      "post": {
        "operationId": "post_assets_enter_asset_maintenance_assets__asset_id__enter_maintenance_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset is not in `Active` lifecycle (enter_asset_maintenance requires Active), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Take an existing (Active) asset out of service for maintenance",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/exit-maintenance": {
      "post": {
        "operationId": "post_assets_exit_asset_maintenance_assets__asset_id__exit_maintenance_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset is not in `Maintenance` lifecycle (exit_asset_maintenance requires Maintenance), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Return an existing (Maintenance) asset to active service",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/fault": {
      "post": {
        "operationId": "post_assets_fault_assets__asset_id__fault_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/FaultAssetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "A concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Mark an existing asset as Faulted (does not work, requires repair)",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/integration-view": {
      "get": {
        "operationId": "get_assets_integration_view_assets__asset_id__integration_view_get",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AssetIntegrationViewResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get the integration-view bundle for an asset",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/pidinst": {
      "get": {
        "operationId": "get_asset_pidinst_assets__asset_id__pidinst_get",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PidinstRecordResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset state is missing a mandatory PIDINST source: at least one Owner (PIDINST Property 5) or a bound Model carrying the Manufacturer (Property 6)."
          },
          "422": {
            "description": "View preparation produced an empty landing page URL or asset name; or the path parameter failed schema validation."
          }
        },
        "summary": "Get the PIDINST v1.0 record for an asset",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/relocate": {
      "post": {
        "operationId": "post_assets_relocate_assets__asset_id__relocate_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RelocateAssetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset cannot be relocated under current conditions (asset is Enterprise / Decommissioned, target is the asset itself, or target equals current parent), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Request body failed schema validation (missing fields, malformed UUID, empty reason)."
          }
        },
        "summary": "Move an existing asset under a new parent in the hierarchy",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/remove-alternate-identifier": {
      "post": {
        "operationId": "post_assets_remove_alternate_identifier_assets__asset_id__remove_alternate_identifier_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveAssetAlternateIdentifierRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Identifier value is empty / whitespace-only / exceeds the configured max length after trimming (InvalidAlternateIdentifierValueError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize policy denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset cannot remove the alternate identifier under current conditions: the asset is Decommissioned (AssetCannotAddAlternateIdentifierError; the shared lifecycle-guard class is used by BOTH add and remove), OR no alternate identifier with the same (kind, value) pair exists on the asset (strict-not-idempotent, AssetAlternateIdentifierNotPresentError), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation (missing field, invalid kind enum, etc.)."
          }
        },
        "summary": "Remove an alternate identifier from an existing Asset's identifier set",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/remove-family": {
      "post": {
        "operationId": "post_assets_remove_family_assets__asset_id__remove_family_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveAssetFamilyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset cannot remove the family under current conditions (asset is Decommissioned, OR the family is not in the asset's family set), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Remove a Family from an existing asset's family set",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/remove-owner": {
      "post": {
        "operationId": "post_assets_remove_owner_assets__asset_id__remove_owner_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveAssetOwnerRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Owner name is empty / whitespace-only / exceeds the configured max length after trimming (InvalidAssetOwnerNameError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize policy denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id (AssetNotFoundError), OR no owner with the given name exists on the asset (AssetOwnerNotPresentError; strict-not-idempotent)."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset cannot remove the owner under current conditions: the asset is Decommissioned (AssetCannotAddOwnerError; the shared lifecycle-guard class is used by BOTH add and remove), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation (missing field, malformed UUID)."
          }
        },
        "summary": "Remove an institutional owner from an existing Asset's owners set",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/remove-port": {
      "post": {
        "operationId": "post_assets_remove_port_assets__asset_id__remove_port_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveAssetPortRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Asset cannot remove the port under current conditions (asset is Decommissioned, OR no port with this name exists on the asset), OR a concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Remove a typed port from an existing Asset's port set",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/restore": {
      "post": {
        "operationId": "post_assets_restore_assets__asset_id__restore_post",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RestoreAssetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "A concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Mark an existing asset as Nominal (fully repaired)",
        "tags": [
          "equipment"
        ]
      }
    },
    "/assets/{asset_id}/settings": {
      "patch": {
        "operationId": "patch_asset_settings_assets__asset_id__settings_patch",
        "parameters": [
          {
            "description": "Target asset's id.",
            "in": "path",
            "name": "asset_id",
            "required": true,
            "schema": {
              "description": "Target asset's id.",
              "format": "uuid",
              "title": "Asset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateAssetSettingsRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "The proposed settings (after merge) failed cross-Family validation: an orphan key, a constraint violation, or a true type conflict between two Capabilities declaring the same key."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "A concurrent write to the same asset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Request body failed schema validation (missing settings_patch field, malformed UUID in path, etc.)."
          }
        },
        "summary": "Update an existing asset's settings dict (RFC 7396 merge semantics)",
        "tags": [
          "equipment"
        ]
      }
    },
    "/calibrations": {
      "get": {
        "operationId": "list_calibrations_calibrations_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional scope: only calibrations OF this Asset/subsystem.",
            "in": "query",
            "name": "target_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional scope: only calibrations OF this Asset/subsystem.",
              "title": "Target Id"
            }
          },
          {
            "description": "Optional quantity filter (closed CalibrationQuantity enum).",
            "in": "query",
            "name": "quantity",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "$ref": "#/components/schemas/CalibrationQuantity"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional quantity filter (closed CalibrationQuantity enum).",
              "title": "Quantity"
            }
          },
          {
            "description": "Optional filter on the latest-revision's status; multi-value. Pass once for a single value, repeat for any-of.",
            "in": "query",
            "name": "latest_revision_status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "items": {
                    "enum": [
                      "Provisional",
                      "Verified"
                    ],
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional filter on the latest-revision's status; multi-value. Pass once for a single value, repeat for any-of.",
              "title": "Latest Revision Status"
            }
          },
          {
            "description": "Optional filter on the latest-revision's source kind (measured / computed / asserted); multi-value.",
            "in": "query",
            "name": "latest_revision_source_kind",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "items": {
                    "enum": [
                      "measured",
                      "computed",
                      "asserted"
                    ],
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional filter on the latest-revision's source kind (measured / computed / asserted); multi-value.",
              "title": "Latest Revision Source Kind"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalibrationListPageResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed."
          }
        },
        "summary": "List calibrations with cursor pagination + subsystem / quantity / latest-revision-status / latest-revision-source-kind filters.",
        "tags": [
          "calibration"
        ]
      },
      "post": {
        "operationId": "post_calibrations_calibrations_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of writing a duplicate definition.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of writing a duplicate definition.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineCalibrationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineCalibrationResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: operating_point fails the quantity's operating_point_schema (missing required field, additional property, type mismatch), or description exceeds 2000 chars after trim."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Calibration with the same (target_id, quantity, operating_point) identity already exists (Postgres jsonb UNIQUE on proj_calibration_summary). Operators querying for an existing calibration should use GET /calibrations with the same identity filters instead."
          },
          "422": {
            "description": "Request body failed schema validation (for example unknown CalibrationQuantity enum value) OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new Calibration record",
        "tags": [
          "calibration"
        ]
      }
    },
    "/calibrations/{calibration_id}": {
      "get": {
        "operationId": "get_calibrations_calibrations__calibration_id__get",
        "parameters": [
          {
            "description": "Target calibration's id.",
            "in": "path",
            "name": "calibration_id",
            "required": true,
            "schema": {
              "description": "Target calibration's id.",
              "format": "uuid",
              "title": "Calibration Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalibrationResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No calibration exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a calibration by id",
        "tags": [
          "calibration"
        ]
      }
    },
    "/calibrations/{calibration_id}/revisions": {
      "post": {
        "operationId": "post_calibration_revisions_calibrations__calibration_id__revisions_post",
        "parameters": [
          {
            "description": "Target calibration's id.",
            "in": "path",
            "name": "calibration_id",
            "required": true,
            "schema": {
              "description": "Target calibration's id.",
              "format": "uuid",
              "title": "Calibration Id",
              "type": "string"
            }
          },
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of appending a duplicate revision. REQUIRED in spirit for agent-subscriber callers.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of appending a duplicate revision. REQUIRED in spirit for agent-subscriber callers.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AppendCalibrationRevisionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AppendCalibrationRevisionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: value fails the quantity's value_schema, OR supersedes_revision_id does not match any revision on this aggregate."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No calibration exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Concurrent write to the same calibration stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation (for example unknown source `kind` discriminator) OR Idempotency-Key was reused with a different body."
          }
        },
        "summary": "Append a new revision to an existing Calibration",
        "tags": [
          "calibration"
        ]
      }
    },
    "/calibrations/{calibration_id}/revisions/{revision_id}/publish": {
      "post": {
        "operationId": "post_publish_calibration_revision_calibrations__calibration_id__revisions__revision_id__publish_post",
        "parameters": [
          {
            "description": "Target calibration's id.",
            "in": "path",
            "name": "calibration_id",
            "required": true,
            "schema": {
              "description": "Target calibration's id.",
              "format": "uuid",
              "title": "Calibration Id",
              "type": "string"
            }
          },
          {
            "description": "Revision on that calibration to publish.",
            "in": "path",
            "name": "revision_id",
            "required": true,
            "schema": {
              "description": "Revision on that calibration to publish.",
              "format": "uuid",
              "title": "Revision Id",
              "type": "string"
            }
          },
          {
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PublishCalibrationRevisionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PublishCalibrationRevisionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the publish command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No calibration or no revision exists with the given ids."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Publish-time FSM rejection: revision lacks content_hash, or no Active outbound Permit authorizes publishing this artifact to the peer."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Publish an existing Calibration revision to a peer facility",
        "tags": [
          "calibration"
        ]
      }
    },
    "/campaigns": {
      "get": {
        "operationId": "list_campaigns_campaigns_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter; multi-value. Omit to default to the OPEN set ([Planned, Active, Held]). Pass `all` alone to include every status, or one or more explicit values to narrow. `all` cannot be mixed with explicit values.",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "items": {
                    "enum": [
                      "Planned",
                      "Active",
                      "Held",
                      "Closed",
                      "Abandoned",
                      "all"
                    ],
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter; multi-value. Omit to default to the OPEN set ([Planned, Active, Held]). Pass `all` alone to include every status, or one or more explicit values to narrow. `all` cannot be mixed with explicit values.",
              "title": "Status"
            }
          },
          {
            "description": "Optional intent filter (one of the 4 CampaignIntent values).",
            "in": "query",
            "name": "intent",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Series",
                    "Sweep",
                    "Coordination",
                    "Block"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional intent filter (one of the 4 CampaignIntent values).",
              "title": "Intent"
            }
          },
          {
            "description": "Optional lead-actor filter ('campaigns I lead').",
            "in": "query",
            "name": "lead_actor_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional lead-actor filter ('campaigns I lead').",
              "title": "Lead Actor Id"
            }
          },
          {
            "description": "Optional subject filter (loose ref).",
            "in": "query",
            "name": "subject_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional subject filter (loose ref).",
              "title": "Subject Id"
            }
          },
          {
            "description": "Optional tag filter; matches any campaign whose tags array contains this value.",
            "in": "query",
            "name": "tag",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maxLength": 50,
                  "minLength": 1,
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional tag filter; matches any campaign whose tags array contains this value.",
              "title": "Tag"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CampaignListPageResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed OR `status=all` was mixed with explicit values."
          }
        },
        "summary": "List campaigns with cursor pagination + status / intent / lead_actor_id / subject_id / tag filters. Defaults to the OPEN set (Planned + Active + Held); pass status=all for the full set including Closed + Abandoned.",
        "tags": [
          "campaign"
        ]
      },
      "post": {
        "operationId": "post_campaigns_campaigns_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the Campaign.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the Campaign.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterCampaignRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterCampaignResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only name / description / tag / external_id, or out-of-bounds external_ref scheme/id)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Defensive guard: the target Campaign stream already has events. Essentially impossible in production with UUIDv7 ids."
          },
          "422": {
            "description": "Request body failed schema validation (missing field, invalid enum, length out of bounds), OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new Campaign (lands in Planned)",
        "tags": [
          "campaign"
        ]
      }
    },
    "/campaigns/{campaign_id}": {
      "get": {
        "operationId": "get_campaigns_campaigns__campaign_id__get",
        "parameters": [
          {
            "description": "Target Campaign's id.",
            "in": "path",
            "name": "campaign_id",
            "required": true,
            "schema": {
              "description": "Target Campaign's id.",
              "format": "uuid",
              "title": "Campaign Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CampaignResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Campaign exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a Campaign by id",
        "tags": [
          "campaign"
        ]
      }
    },
    "/campaigns/{campaign_id}/abandon": {
      "post": {
        "operationId": "post_campaigns_abandon_campaigns__campaign_id__abandon_post",
        "parameters": [
          {
            "description": "Target Campaign's id.",
            "in": "path",
            "name": "campaign_id",
            "required": true,
            "schema": {
              "description": "Target Campaign's id.",
              "format": "uuid",
              "title": "Campaign Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AbandonCampaignRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Campaign exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Campaign is not in an abandonable status. Source set: {Planned, Active, Held}. Closed / Abandoned terminals refuse."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Abandon a Campaign (Planned | Active | Held -> Abandoned)",
        "tags": [
          "campaign"
        ]
      }
    },
    "/campaigns/{campaign_id}/close": {
      "post": {
        "operationId": "post_campaigns_close_campaigns__campaign_id__close_post",
        "parameters": [
          {
            "description": "Target Campaign's id.",
            "in": "path",
            "name": "campaign_id",
            "required": true,
            "schema": {
              "description": "Target Campaign's id.",
              "format": "uuid",
              "title": "Campaign Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Campaign exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Campaign is not in a closable status. Source set: {Active, Held}. Closed / Abandoned terminals refuse re-close; Planned refuses (work never started)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Close a Campaign (Active | Held -> Closed)",
        "tags": [
          "campaign"
        ]
      }
    },
    "/campaigns/{campaign_id}/hold": {
      "post": {
        "operationId": "post_campaigns_hold_campaigns__campaign_id__hold_post",
        "parameters": [
          {
            "description": "Target Campaign's id.",
            "in": "path",
            "name": "campaign_id",
            "required": true,
            "schema": {
              "description": "Target Campaign's id.",
              "format": "uuid",
              "title": "Campaign Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/HoldCampaignRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Campaign exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Campaign is not in Active status (hold_campaign is single-source from Active only)."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Hold an Active Campaign (Active -> Held)",
        "tags": [
          "campaign"
        ]
      }
    },
    "/campaigns/{campaign_id}/resume": {
      "post": {
        "operationId": "post_campaigns_resume_campaigns__campaign_id__resume_post",
        "parameters": [
          {
            "description": "Target Campaign's id.",
            "in": "path",
            "name": "campaign_id",
            "required": true,
            "schema": {
              "description": "Target Campaign's id.",
              "format": "uuid",
              "title": "Campaign Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Campaign exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Campaign is not in Held status (resume_campaign is single-source from Held only)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Resume a Held Campaign (Held -> Active)",
        "tags": [
          "campaign"
        ]
      }
    },
    "/campaigns/{campaign_id}/runs/{run_id}/add": {
      "post": {
        "operationId": "post_campaign_runs_add_campaigns__campaign_id__runs__run_id__add_post",
        "parameters": [
          {
            "description": "Target Campaign's id.",
            "in": "path",
            "name": "campaign_id",
            "required": true,
            "schema": {
              "description": "Target Campaign's id.",
              "format": "uuid",
              "title": "Campaign Id",
              "type": "string"
            }
          },
          {
            "description": "Run to add as a member.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Run to add as a member.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Campaign or Run does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Campaign is in a terminal status (Closed / Abandoned) and refuses new members, OR the Run is already a member of this Campaign, OR the Run is already assigned to a different Campaign, OR an optimistic-concurrency conflict on either stream (concurrent transition; retry)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Add a Run as a member of a Campaign (atomic two-stream write)",
        "tags": [
          "campaign"
        ]
      }
    },
    "/campaigns/{campaign_id}/runs/{run_id}/remove": {
      "post": {
        "operationId": "post_campaign_runs_remove_campaigns__campaign_id__runs__run_id__remove_post",
        "parameters": [
          {
            "description": "Target Campaign's id.",
            "in": "path",
            "name": "campaign_id",
            "required": true,
            "schema": {
              "description": "Target Campaign's id.",
              "format": "uuid",
              "title": "Campaign Id",
              "type": "string"
            }
          },
          {
            "description": "Run to remove from the Campaign.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Run to remove from the Campaign.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemoveRunFromCampaignRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Campaign or Run does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Campaign is in a terminal status (Closed / Abandoned), OR the Run is not a member of this Campaign, OR an optimistic-concurrency conflict on either stream."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Remove a Run from a Campaign (atomic two-stream write)",
        "tags": [
          "campaign"
        ]
      }
    },
    "/campaigns/{campaign_id}/start": {
      "post": {
        "operationId": "post_campaigns_start_campaigns__campaign_id__start_post",
        "parameters": [
          {
            "description": "Target Campaign's id.",
            "in": "path",
            "name": "campaign_id",
            "required": true,
            "schema": {
              "description": "Target Campaign's id.",
              "format": "uuid",
              "title": "Campaign Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Campaign exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Campaign is not in Planned status (start_campaign is single-source from Planned only)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Start a Planned Campaign (Planned -> Active)",
        "tags": [
          "campaign"
        ]
      }
    },
    "/capabilities": {
      "post": {
        "operationId": "post_capabilities_capabilities_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the Capability.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the Capability.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineCapabilityRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineCapabilityResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only name, bad code namespace, empty executor_shapes, malformed parameters_schema)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new universal Capability template at the operations layer",
        "tags": [
          "recipe"
        ]
      }
    },
    "/capabilities/{capability_id}": {
      "get": {
        "operationId": "get_capabilities_capabilities__capability_id__get",
        "parameters": [
          {
            "description": "Target Capability's id.",
            "in": "path",
            "name": "capability_id",
            "required": true,
            "schema": {
              "description": "Target Capability's id.",
              "format": "uuid",
              "title": "Capability Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CapabilityResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Capability exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a Capability by id",
        "tags": [
          "recipe"
        ]
      }
    },
    "/capabilities/{capability_id}/deprecate": {
      "post": {
        "operationId": "post_capabilities_deprecate_capabilities__capability_id__deprecate_post",
        "parameters": [
          {
            "description": "Target Capability's id.",
            "in": "path",
            "name": "capability_id",
            "required": true,
            "schema": {
              "description": "Target Capability's id.",
              "format": "uuid",
              "title": "Capability Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeprecateCapabilityRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Capability exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Capability is already Deprecated."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Deprecate an existing Capability",
        "tags": [
          "recipe"
        ]
      }
    },
    "/capabilities/{capability_id}/version": {
      "post": {
        "operationId": "post_capabilities_version_capabilities__capability_id__version_post",
        "parameters": [
          {
            "description": "Target Capability's id.",
            "in": "path",
            "name": "capability_id",
            "required": true,
            "schema": {
              "description": "Target Capability's id.",
              "format": "uuid",
              "title": "Capability Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VersionCapabilityRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only version_tag, etc.)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Capability exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Capability is currently Deprecated."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Issue a new version label + replacement declarative contract for a Capability",
        "tags": [
          "recipe"
        ]
      }
    },
    "/cautions": {
      "get": {
        "operationId": "list_cautions_cautions_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional target-kind filter (Asset / Procedure).",
            "in": "query",
            "name": "target_kind",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Asset",
                    "Procedure"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional target-kind filter (Asset / Procedure).",
              "title": "Target Kind"
            }
          },
          {
            "description": "Optional target-id filter; typically combined with target_kind.",
            "in": "query",
            "name": "target_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional target-id filter; typically combined with target_kind.",
              "title": "Target Id"
            }
          },
          {
            "description": "Optional category filter (one of the 6 CautionCategory values).",
            "in": "query",
            "name": "category",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Wear",
                    "Calibration",
                    "Wiring",
                    "OperationalWindow",
                    "InterlockQuirk",
                    "ProcedureGotcha"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional category filter (one of the 6 CautionCategory values).",
              "title": "Category"
            }
          },
          {
            "description": "Optional exact severity filter; multi-value. Pass once for a single value, repeat for any-of (`?severity=Caution&severity=Warning`). Cannot be combined with `min_severity`.",
            "in": "query",
            "name": "severity",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "items": {
                    "enum": [
                      "Notice",
                      "Caution",
                      "Warning"
                    ],
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional exact severity filter; multi-value. Pass once for a single value, repeat for any-of (`?severity=Caution&severity=Warning`). Cannot be combined with `min_severity`.",
              "title": "Severity"
            }
          },
          {
            "description": "Optional severity-threshold filter; expands to the matching suffix of the Notice<Caution<Warning ladder. Cannot be combined with `severity`.",
            "in": "query",
            "name": "min_severity",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Notice",
                    "Caution",
                    "Warning"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional severity-threshold filter; expands to the matching suffix of the Notice<Caution<Warning ladder. Cannot be combined with `severity`.",
              "title": "Min Severity"
            }
          },
          {
            "description": "Optional status filter; multi-value. Omit to default to `[Active]` (hides Superseded + Retired). Pass `all` alone to include every status, or one or more explicit values to narrow. `all` cannot be mixed with explicit values.",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "items": {
                    "enum": [
                      "Active",
                      "Superseded",
                      "Retired",
                      "all"
                    ],
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter; multi-value. Omit to default to `[Active]` (hides Superseded + Retired). Pass `all` alone to include every status, or one or more explicit values to narrow. `all` cannot be mixed with explicit values.",
              "title": "Status"
            }
          },
          {
            "description": "Optional tag filter; matches any caution whose tags array contains this value.",
            "in": "query",
            "name": "tag",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maxLength": 50,
                  "minLength": 1,
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional tag filter; matches any caution whose tags array contains this value.",
              "title": "Tag"
            }
          },
          {
            "description": "Optional author filter ('cautions I authored').",
            "in": "query",
            "name": "author_actor_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional author filter ('cautions I authored').",
              "title": "Author Actor Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CautionListPageResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed OR `severity` and `min_severity` were both passed OR `status=all` was mixed with explicit status values."
          }
        },
        "summary": "List cautions with cursor pagination + target / category / severity / min_severity / status / tag / author filters. Defaults to status=[Active]; pass status=all for the full set. propagate_to_children is hint-only (no Asset hierarchy walk).",
        "tags": [
          "caution"
        ]
      },
      "post": {
        "operationId": "post_cautions_cautions_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the caution.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the caution.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterCautionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterCautionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only text / workaround / tag, past-dated expires_at, or supersede-target mismatch)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Defensive guard: the target caution stream already has events. Essentially impossible in production with UUIDv7 ids."
          },
          "422": {
            "description": "Request body failed schema validation (missing field, invalid enum, length out of bounds, malformed target discriminator), OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new caution (lands in Active)",
        "tags": [
          "caution"
        ]
      }
    },
    "/cautions/{caution_id}": {
      "get": {
        "operationId": "get_cautions_cautions__caution_id__get",
        "parameters": [
          {
            "description": "Target caution's id.",
            "in": "path",
            "name": "caution_id",
            "required": true,
            "schema": {
              "description": "Target caution's id.",
              "format": "uuid",
              "title": "Caution Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CautionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No caution exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a caution by id",
        "tags": [
          "caution"
        ]
      }
    },
    "/cautions/{caution_id}/retire": {
      "post": {
        "operationId": "post_cautions_retire_cautions__caution_id__retire_post",
        "parameters": [
          {
            "description": "Target caution's id.",
            "in": "path",
            "name": "caution_id",
            "required": true,
            "schema": {
              "description": "Target caution's id.",
              "format": "uuid",
              "title": "Caution Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RetireCautionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No caution exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Caution is not in Active status (retire_caution is single-source from Active only)."
          },
          "422": {
            "description": "Request body failed schema validation (unknown reason)."
          }
        },
        "summary": "Retire an Active caution (Active -> Retired)",
        "tags": [
          "caution"
        ]
      }
    },
    "/cautions/{parent_caution_id}/supersede": {
      "post": {
        "operationId": "post_cautions_supersede_cautions__parent_caution_id__supersede_post",
        "parameters": [
          {
            "description": "Parent caution's id.",
            "in": "path",
            "name": "parent_caution_id",
            "required": true,
            "schema": {
              "description": "Parent caution's id.",
              "format": "uuid",
              "title": "Parent Caution Id",
              "type": "string"
            }
          },
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the child caution.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the child caution.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SupersedeCautionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SupersedeCautionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated on child fields (whitespace-only text/workaround/tag, past-dated expires_at, OR child target does not match parent's target)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No parent caution exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Parent is not in Active status (supersede_caution is single-source from Active only) OR optimistic-concurrency conflict on the parent stream (concurrent transition; retry)."
          },
          "422": {
            "description": "Request body failed schema validation, OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Supersede an Active caution (atomic parent:Active->Superseded + child:Active)",
        "tags": [
          "caution"
        ]
      }
    },
    "/clearances": {
      "get": {
        "operationId": "list_clearances_clearances_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional form-type filter (one of the 10 ClearanceKind values).",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "ESAF",
                    "SAF",
                    "AForm",
                    "DUO",
                    "ESRA",
                    "ERA",
                    "PLHD",
                    "DOOR",
                    "BTR",
                    "Form9"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional form-type filter (one of the 10 ClearanceKind values).",
              "title": "Kind"
            }
          },
          {
            "description": "Optional status filter (one of the 8 ClearanceStatus values).",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Defined",
                    "Submitted",
                    "UnderReview",
                    "Approved",
                    "Active",
                    "Expired",
                    "Rejected",
                    "Superseded"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (one of the 8 ClearanceStatus values).",
              "title": "Status"
            }
          },
          {
            "description": "Optional risk-band filter (Green / Yellow / Red).",
            "in": "query",
            "name": "risk_band",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Green",
                    "Yellow",
                    "Red"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional risk-band filter (Green / Yellow / Red).",
              "title": "Risk Band"
            }
          },
          {
            "description": "Optional facility filter; matches Asset.Level.Site.",
            "in": "query",
            "name": "facility_asset_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional facility filter; matches Asset.Level.Site.",
              "title": "Facility Asset Id"
            }
          },
          {
            "description": "Filter clearances binding to this Subject id.",
            "in": "query",
            "name": "binds_to_subject_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Filter clearances binding to this Subject id.",
              "title": "Binds To Subject Id"
            }
          },
          {
            "description": "Filter clearances binding to this Asset id.",
            "in": "query",
            "name": "binds_to_asset_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Filter clearances binding to this Asset id.",
              "title": "Binds To Asset Id"
            }
          },
          {
            "description": "Filter clearances binding to this Run id.",
            "in": "query",
            "name": "binds_to_run_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Filter clearances binding to this Run id.",
              "title": "Binds To Run Id"
            }
          },
          {
            "description": "Filter clearances binding to this Procedure id.",
            "in": "query",
            "name": "binds_to_procedure_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Filter clearances binding to this Procedure id.",
              "title": "Binds To Procedure Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ClearanceListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed."
          }
        },
        "summary": "List clearances with cursor pagination + kind / status / risk_band / facility_asset_id / binds_to_*_id filters. ExternalBinding refs not filterable; review_steps chain not surfaced (fetch get_clearance for both).",
        "tags": [
          "safety"
        ]
      },
      "post": {
        "operationId": "post_clearances_clearances_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the clearance.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the clearance.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterClearanceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterClearanceResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only title, empty bindings, inverted validity window, oversized mitigation ref, etc.)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Defensive guard: the target clearance stream already has events. Essentially impossible in production with UUIDv7 ids; documented for OpenAPI completeness against the BC's exception handler."
          },
          "422": {
            "description": "Request body failed schema validation (missing field, invalid enum, length out of bounds, malformed datetime, invalid NFPA 704 quadrant), OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new safety-form clearance (lands in Defined)",
        "tags": [
          "safety"
        ]
      }
    },
    "/clearances/{clearance_id}": {
      "get": {
        "operationId": "get_clearances_clearances__clearance_id__get",
        "parameters": [
          {
            "description": "Target clearance's id.",
            "in": "path",
            "name": "clearance_id",
            "required": true,
            "schema": {
              "description": "Target clearance's id.",
              "format": "uuid",
              "title": "Clearance Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ClearanceResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No clearance exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a clearance by id",
        "tags": [
          "safety"
        ]
      }
    },
    "/clearances/{clearance_id}/activate": {
      "post": {
        "operationId": "post_clearances_activate_clearances__clearance_id__activate_post",
        "parameters": [
          {
            "description": "Target clearance's id.",
            "in": "path",
            "name": "clearance_id",
            "required": true,
            "schema": {
              "description": "Target clearance's id.",
              "format": "uuid",
              "title": "Clearance Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No clearance exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Clearance is not in Approved status (activate_clearance is single-source from Approved only)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Activate an Approved clearance (Approved -> Active)",
        "tags": [
          "safety"
        ]
      }
    },
    "/clearances/{clearance_id}/approve": {
      "post": {
        "operationId": "post_clearances_approve_clearances__clearance_id__approve_post",
        "parameters": [
          {
            "description": "Target clearance's id.",
            "in": "path",
            "name": "clearance_id",
            "required": true,
            "schema": {
              "description": "Target clearance's id.",
              "format": "uuid",
              "title": "Clearance Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ApproveClearanceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (inverted or zero-duration validity window). The 'terminal review step not Approved' case returns 409, not 400 (see ClearanceCannotApproveError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No clearance exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Clearance is not in UnderReview status (approve_clearance is single-source from UnderReview only), OR the terminal review step does not have decision='Approved'."
          },
          "422": {
            "description": "Request body failed schema validation."
          }
        },
        "summary": "Approve an UnderReview clearance (UnderReview -> Approved)",
        "tags": [
          "safety"
        ]
      }
    },
    "/clearances/{clearance_id}/expire": {
      "post": {
        "operationId": "post_clearances_expire_clearances__clearance_id__expire_post",
        "parameters": [
          {
            "description": "Target clearance's id.",
            "in": "path",
            "name": "clearance_id",
            "required": true,
            "schema": {
              "description": "Target clearance's id.",
              "format": "uuid",
              "title": "Clearance Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ExpireClearanceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No clearance exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Clearance is not in Active status (expire_clearance is single-source from Active only)."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Expire an Active clearance (Active -> Expired)",
        "tags": [
          "safety"
        ]
      }
    },
    "/clearances/{clearance_id}/reject": {
      "post": {
        "operationId": "post_clearances_reject_clearances__clearance_id__reject_post",
        "parameters": [
          {
            "description": "Target clearance's id.",
            "in": "path",
            "name": "clearance_id",
            "required": true,
            "schema": {
              "description": "Target clearance's id.",
              "format": "uuid",
              "title": "Clearance Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RejectClearanceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No clearance exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Clearance is not in UnderReview status (reject_clearance is single-source from UnderReview only)."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Reject an UnderReview clearance (UnderReview -> Rejected)",
        "tags": [
          "safety"
        ]
      }
    },
    "/clearances/{clearance_id}/review-steps": {
      "post": {
        "operationId": "post_clearances_review_steps_clearances__clearance_id__review_steps_post",
        "parameters": [
          {
            "description": "Target clearance's id.",
            "in": "path",
            "name": "clearance_id",
            "required": true,
            "schema": {
              "description": "Target clearance's id.",
              "format": "uuid",
              "title": "Clearance Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AppendClearanceReviewStepRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only role / oversized notes / wrong step_index for append-only contract / future-dated decided_at / decided_at less than prior step's decided_at for chain monotonicity)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No clearance exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Clearance is not in UnderReview status (append_clearance_review_step is single-source from UnderReview only)."
          },
          "422": {
            "description": "Request body failed schema validation (missing fields, decision not in {Approved, Rejected, RequestedChanges}, negative step_index, malformed datetime)."
          }
        },
        "summary": "Append one reviewer step to an UnderReview clearance's chain",
        "tags": [
          "safety"
        ]
      }
    },
    "/clearances/{clearance_id}/start-review": {
      "post": {
        "operationId": "post_clearances_start_review_clearances__clearance_id__start_review_post",
        "parameters": [
          {
            "description": "Target clearance's id.",
            "in": "path",
            "name": "clearance_id",
            "required": true,
            "schema": {
              "description": "Target clearance's id.",
              "format": "uuid",
              "title": "Clearance Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StartClearanceReviewRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only role)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No clearance exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Clearance is not in Submitted status (start_clearance_review is single-source from Submitted only)."
          },
          "422": {
            "description": "Request body failed schema validation (missing first_reviewer_role, empty, or exceeds 50 chars)."
          }
        },
        "summary": "Start reviewing a Submitted clearance (Submitted -> UnderReview)",
        "tags": [
          "safety"
        ]
      }
    },
    "/clearances/{clearance_id}/submit": {
      "post": {
        "operationId": "post_clearances_submit_clearances__clearance_id__submit_post",
        "parameters": [
          {
            "description": "Target clearance's id.",
            "in": "path",
            "name": "clearance_id",
            "required": true,
            "schema": {
              "description": "Target clearance's id.",
              "format": "uuid",
              "title": "Clearance Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No clearance exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Clearance is not in Defined status (submit_clearance is single-source from Defined only)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Submit a Defined clearance for review (Defined -> Submitted)",
        "tags": [
          "safety"
        ]
      }
    },
    "/clearances/{parent_clearance_id}/amend": {
      "post": {
        "operationId": "post_clearances_amend_clearances__parent_clearance_id__amend_post",
        "parameters": [
          {
            "description": "Parent clearance's id.",
            "in": "path",
            "name": "parent_clearance_id",
            "required": true,
            "schema": {
              "description": "Parent clearance's id.",
              "format": "uuid",
              "title": "Parent Clearance Id",
              "type": "string"
            }
          },
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the child clearance.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the child clearance.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AmendClearanceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AmendClearanceResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated on the child's fields (whitespace-only title, empty bindings, inverted validity window, etc.) OR the parent's binding declarations cross-validation failed."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No parent clearance exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Parent is not in Active status (amend_clearance is single-source from Active only) OR optimistic-concurrency conflict on the parent stream (concurrent transition; retry)."
          },
          "422": {
            "description": "Request body failed schema validation, OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Amend an Active clearance (atomic parent:Active->Superseded + child:Defined)",
        "tags": [
          "safety"
        ]
      }
    },
    "/conduits": {
      "get": {
        "operationId": "list_conduits_conduits_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional source-zone filter; omit for any source.",
            "in": "query",
            "name": "source_zone_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional source-zone filter; omit for any source.",
              "title": "Source Zone Id"
            }
          },
          {
            "description": "Optional target-zone filter; omit for any target.",
            "in": "query",
            "name": "target_zone_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional target-zone filter; omit for any target.",
              "title": "Target Zone Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConduitListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List conduits with cursor pagination + endpoint filters",
        "tags": [
          "trust"
        ]
      },
      "post": {
        "operationId": "post_conduits_conduits_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the conduit. Reusing a key with a different body returns 422.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the conduit. Reusing a key with a different body returns 422.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineConduitRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineConduitResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new Trust conduit between two zones",
        "tags": [
          "trust"
        ]
      }
    },
    "/datasets": {
      "get": {
        "operationId": "list_datasets_datasets_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter (Registered / Discarded).",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Registered",
                    "Discarded"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (Registered / Discarded).",
              "title": "Status"
            }
          },
          {
            "description": "Optional Run-id filter.",
            "in": "query",
            "name": "producing_run_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional Run-id filter.",
              "title": "Producing Run Id"
            }
          },
          {
            "description": "Optional Subject-id filter.",
            "in": "query",
            "name": "subject_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional Subject-id filter.",
              "title": "Subject Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DatasetListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List datasets with cursor pagination + status/run/subject filters",
        "tags": [
          "data"
        ]
      },
      "post": {
        "operationId": "post_datasets_datasets_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the Dataset.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the Dataset.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterDatasetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterDatasetResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only name, malformed URI, non-sha256 or malformed checksum, negative byte_size, invalid encoding media_type, or invalid conforms_to entry."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Cross-aggregate reference does not exist: producing_run_id, subject_id, or one or more derived_from ids."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Lineage-state conflict: one or more upstream Datasets referenced by derived_from are in Discarded status."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new Dataset",
        "tags": [
          "data"
        ]
      }
    },
    "/datasets/{dataset_id}": {
      "get": {
        "operationId": "get_datasets_datasets__dataset_id__get",
        "parameters": [
          {
            "description": "Target dataset's id.",
            "in": "path",
            "name": "dataset_id",
            "required": true,
            "schema": {
              "description": "Target dataset's id.",
              "format": "uuid",
              "title": "Dataset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DatasetResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No dataset exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a dataset by id",
        "tags": [
          "data"
        ]
      }
    },
    "/datasets/{dataset_id}/demote": {
      "post": {
        "operationId": "post_datasets_demote_datasets__dataset_id__demote_post",
        "parameters": [
          {
            "description": "Target dataset's id.",
            "in": "path",
            "name": "dataset_id",
            "required": true,
            "schema": {
              "description": "Target dataset's id.",
              "format": "uuid",
              "title": "Dataset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DemoteDatasetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only reason (InvalidDemotionReasonError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No dataset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Demotion guard rejected: dataset is Discarded (DatasetCannotDemoteError); already in Retracted intent (DatasetAlreadyRetractedError, strict-not-idempotent); dataset is in Trial intent (DatasetCannotDemoteError; use discard_dataset for Trial cleanup); OR a concurrent write to the same Dataset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Demote a Dataset (Production intent \u2192 Retracted intent)",
        "tags": [
          "data"
        ]
      }
    },
    "/datasets/{dataset_id}/discard": {
      "post": {
        "operationId": "post_datasets_discard_datasets__dataset_id__discard_post",
        "parameters": [
          {
            "description": "Target dataset's id.",
            "in": "path",
            "name": "dataset_id",
            "required": true,
            "schema": {
              "description": "Target dataset's id.",
              "format": "uuid",
              "title": "Dataset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DiscardDatasetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only reason."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No dataset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Dataset is not in `Registered` status (discard requires it; re-discarding raises), OR a concurrent write to the same Dataset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Discard a Dataset (Registered \u2192 Discarded)",
        "tags": [
          "data"
        ]
      }
    },
    "/datasets/{dataset_id}/promote": {
      "post": {
        "operationId": "post_datasets_promote_datasets__dataset_id__promote_post",
        "parameters": [
          {
            "description": "Target dataset's id.",
            "in": "path",
            "name": "dataset_id",
            "required": true,
            "schema": {
              "description": "Target dataset's id.",
              "format": "uuid",
              "title": "Dataset Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/PromoteDatasetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only reason (InvalidPromotionReasonError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No dataset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Promotion guard rejected: dataset is Discarded (DatasetCannotPromoteError); already in Production intent (DatasetAlreadyPromotedError, strict-not-idempotent); producing Run did not Complete (DatasetCannotPromoteError); one or more derived_from Datasets are still Trial (DatasetCannotPromoteError); OR a concurrent write to the same Dataset stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Promote a Dataset (Trial intent \u2192 Production intent)",
        "tags": [
          "data"
        ]
      }
    },
    "/decisions": {
      "get": {
        "operationId": "list_decisions_decisions_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional confidence-band filter (Low / Medium / High / Certain). Decisions with no confidence are excluded when this filter is set.",
            "in": "query",
            "name": "confidence_band",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Low",
                    "Medium",
                    "High",
                    "Certain"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional confidence-band filter (Low / Medium / High / Certain). Decisions with no confidence are excluded when this filter is set.",
              "title": "Confidence Band"
            }
          },
          {
            "description": "Optional categorical decision-rule label filter.",
            "in": "query",
            "name": "rule",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional categorical decision-rule label filter.",
              "title": "Rule"
            }
          },
          {
            "description": "Optional Actor-id filter.",
            "in": "query",
            "name": "actor_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional Actor-id filter.",
              "title": "Actor Id"
            }
          },
          {
            "description": "Optional DecisionChoice filter (e.g. NominalCompletion). See cora.decision.aggregates.decision RUN_DEBRIEF_CHOICES / CAUTION_PROPOSAL_CHOICES for the closed sets.",
            "in": "query",
            "name": "choice",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional DecisionChoice filter (e.g. NominalCompletion). See cora.decision.aggregates.decision RUN_DEBRIEF_CHOICES / CAUTION_PROPOSAL_CHOICES for the closed sets.",
              "title": "Choice"
            }
          },
          {
            "description": "Optional DecisionChoice exclusion list. Common case: drop the audit-only DebriefConflicted / CautionDraftConflicted rows when computing outcome rates. Repeat the query param for multiple values.",
            "in": "query",
            "name": "exclude_choices",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional DecisionChoice exclusion list. Common case: drop the audit-only DebriefConflicted / CautionDraftConflicted rows when computing outcome rates. Repeat the query param for multiple values.",
              "title": "Exclude Choices"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DecisionListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List decisions with cursor pagination + band/rule/actor filters",
        "tags": [
          "decision"
        ]
      },
      "post": {
        "operationId": "post_decisions_decisions_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key. Retries with the same key + same body return the cached response instead of re-creating the Decision.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key. Retries with the same key + same body return the cached response instead of re-creating the Decision.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterDecisionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterDecisionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only choice / context, malformed alternatives entry, malformed inputs key, or override_kind without parent_id."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Cross-aggregate reference does not exist: actor_id or parent_id."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new Decision",
        "tags": [
          "decision"
        ]
      }
    },
    "/decisions/{decision_id}": {
      "get": {
        "operationId": "get_decisions_decisions__decision_id__get",
        "parameters": [
          {
            "description": "Target decision's id.",
            "in": "path",
            "name": "decision_id",
            "required": true,
            "schema": {
              "description": "Target decision's id.",
              "format": "uuid",
              "title": "Decision Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DecisionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No decision exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a decision by id",
        "tags": [
          "decision"
        ]
      }
    },
    "/decisions/{decision_id}/ratings": {
      "post": {
        "operationId": "post_decisions_ratings_decisions__decision_id__ratings_post",
        "parameters": [
          {
            "description": "Target Decision's id.",
            "in": "path",
            "name": "decision_id",
            "required": true,
            "schema": {
              "description": "Target Decision's id.",
              "format": "uuid",
              "title": "Decision Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RateDecisionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Comment failed domain validation (empty / whitespace-only after trim, or over-cap after trim)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Decision exists with the given id."
          },
          "422": {
            "description": "Request body failed schema validation (unknown rating value, comment length out of bounds, malformed UUID)."
          }
        },
        "summary": "Rate a Decision (operator acceptance-signal capture)",
        "tags": [
          "decision"
        ]
      }
    },
    "/decisions/{decision_id}/reasoning-entries": {
      "post": {
        "operationId": "post_decisions_reasoning_entries_decisions__decision_id__reasoning_entries_post",
        "parameters": [
          {
            "description": "Target decision's id.",
            "in": "path",
            "name": "decision_id",
            "required": true,
            "schema": {
              "description": "Target decision's id.",
              "format": "uuid",
              "title": "Decision Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AppendReasoningEntriesRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AppendReasoningEntriesResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Decision exists with the given id."
          },
          "422": {
            "description": "Request body failed schema validation: empty entries list, batch over cap, missing required OTel fields, invalid types."
          }
        },
        "summary": "Append a batch of AI-decider reasoning entries to a Decision's reasoning logbook (lazy open-on-first-write).",
        "tags": [
          "decision"
        ]
      }
    },
    "/families": {
      "get": {
        "operationId": "list_families_families_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter (one of: Defined, Versioned, Deprecated). Omit to return all statuses.",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Defined",
                    "Versioned",
                    "Deprecated"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (one of: Defined, Versioned, Deprecated). Omit to return all statuses.",
              "title": "Status"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FamilyListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List families with cursor pagination + status filter",
        "tags": [
          "equipment"
        ]
      },
      "post": {
        "operationId": "post_families_families_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the family.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the family.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineFamilyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineFamilyResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new technique-class family",
        "tags": [
          "equipment"
        ]
      }
    },
    "/families/{family_id}": {
      "get": {
        "operationId": "get_families_families__family_id__get",
        "parameters": [
          {
            "description": "Target family's id.",
            "in": "path",
            "name": "family_id",
            "required": true,
            "schema": {
              "description": "Target family's id.",
              "format": "uuid",
              "title": "Family Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FamilyResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No family exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a family by id",
        "tags": [
          "equipment"
        ]
      }
    },
    "/families/{family_id}/deprecate": {
      "post": {
        "operationId": "post_families_deprecate_families__family_id__deprecate_post",
        "parameters": [
          {
            "description": "Target family's id.",
            "in": "path",
            "name": "family_id",
            "required": true,
            "schema": {
              "description": "Target family's id.",
              "format": "uuid",
              "title": "Family Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No family exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Family is not in `Defined` or `Versioned` status (deprecate requires one of those \u2014 re-deprecating a Deprecated family raises), OR a concurrent write to the same family stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Mark an existing family as deprecated",
        "tags": [
          "equipment"
        ]
      }
    },
    "/families/{family_id}/settings-schema": {
      "post": {
        "operationId": "post_families_settings_schema_families__family_id__settings_schema_post",
        "parameters": [
          {
            "description": "Target family's id.",
            "in": "path",
            "name": "family_id",
            "required": true,
            "schema": {
              "description": "Target family's id.",
              "format": "uuid",
              "title": "Family Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateFamilySettingsSchemaRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Schema rejected: missing or wrong `$schema`, forbidden keyword (for example $ref / oneOf / allOf), or jsonschema-rs rejected the schema as malformed."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No family exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Concurrent write to the same family stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Set, replace, or clear a Family's settings_schema",
        "tags": [
          "equipment"
        ]
      }
    },
    "/families/{family_id}/version": {
      "post": {
        "operationId": "post_families_version_families__family_id__version_post",
        "parameters": [
          {
            "description": "Target family's id.",
            "in": "path",
            "name": "family_id",
            "required": true,
            "schema": {
              "description": "Target family's id.",
              "format": "uuid",
              "title": "Family Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VersionFamilyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only version_tag."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No family exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Family is not in `Defined` or `Versioned` status (version requires one of those), OR a concurrent write to the same family stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Issue a new version label for an existing family",
        "tags": [
          "equipment"
        ]
      }
    },
    "/federation/credentials": {
      "get": {
        "operationId": "list_credentials_federation_credentials_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional facility filter; matches the credential's facility_id.",
            "in": "query",
            "name": "facility_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional facility filter; matches the credential's facility_id.",
              "title": "Facility Id"
            }
          },
          {
            "description": "Optional purpose filter (one of the 6 CredentialPurpose values).",
            "in": "query",
            "name": "purpose",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Signing",
                    "Verification",
                    "Authentication",
                    "Encryption",
                    "SealOnlineSigning",
                    "SealOfflineRoot"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional purpose filter (one of the 6 CredentialPurpose values).",
              "title": "Purpose"
            }
          },
          {
            "description": "Optional status filter (one of Active / Rotating / Revoked).",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Active",
                    "Rotating",
                    "Revoked"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (one of Active / Rotating / Revoked).",
              "title": "Status"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CredentialListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed."
          }
        },
        "summary": "List credentials with cursor pagination + facility_id / purpose / status filters. Opaque secret refs are NOT surfaced; fetch get_credential for those.",
        "tags": [
          "federation"
        ]
      },
      "post": {
        "operationId": "post_federation_credentials_federation_credentials_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of writing a duplicate Credential.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of writing a duplicate Credential.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterCredentialRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterCredentialResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only facility_id, audience, or secret_ref)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Defensive guard: the target Credential stream already has events. Essentially impossible in production with UUIDv7 ids."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Request body failed schema validation (missing field, invalid enum value, malformed datetime), OR expires_at is set in the past, OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new federation Credential (cross-BC atomic; emits DecisionRegistered audit)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/credentials/{credential_id}": {
      "get": {
        "operationId": "get_federation_credential_federation_credentials__credential_id__get",
        "parameters": [
          {
            "description": "Target credential's id.",
            "in": "path",
            "name": "credential_id",
            "required": true,
            "schema": {
              "description": "Target credential's id.",
              "format": "uuid",
              "title": "Credential Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CredentialResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No credential exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a federation Credential by id",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/credentials/{credential_id}/revoke": {
      "post": {
        "operationId": "post_federation_credentials_revoke_federation_credentials__credential_id__revoke_post",
        "parameters": [
          {
            "description": "Target credential's id.",
            "in": "path",
            "name": "credential_id",
            "required": true,
            "schema": {
              "description": "Target credential's id.",
              "format": "uuid",
              "title": "Credential Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/RevokeCredentialBody"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Body"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No credential exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Credential is already Revoked (revoke_credential is strict-not-idempotent; Revoked is terminal)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Revoke a Credential (terminal: any non-Revoked -> Revoked)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/credentials/{credential_id}/rotation/abort": {
      "post": {
        "operationId": "post_credentials_rotation_abort_federation_credentials__credential_id__rotation_abort_post",
        "parameters": [
          {
            "description": "Target credential's id.",
            "in": "path",
            "name": "credential_id",
            "required": true,
            "schema": {
              "description": "Target credential's id.",
              "format": "uuid",
              "title": "Credential Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AbortCredentialRotationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No credential exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Credential is not in `Rotating` status (abort_credential_rotation requires Rotating; Active / Revoked credentials are rejected)."
          },
          "422": {
            "description": "Request body failed schema validation (reason exceeds 500 chars) or path parameter is not a UUID."
          }
        },
        "summary": "Abort an in-flight credential rotation (Rotating -> Active)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/credentials/{credential_id}/rotation/complete": {
      "post": {
        "operationId": "post_federation_credentials_rotation_complete_federation_credentials__credential_id__rotation_complete_post",
        "parameters": [
          {
            "description": "Target credential's id.",
            "in": "path",
            "name": "credential_id",
            "required": true,
            "schema": {
              "description": "Target credential's id.",
              "format": "uuid",
              "title": "Credential Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No credential exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Credential is not in `Rotating` status, or pending refs are absent (complete_credential_rotation is single-source from Rotating with pending refs populated)."
          },
          "422": {
            "description": "Path parameter failed schema validation (not a UUID)."
          }
        },
        "summary": "Complete an in-flight credential rotation (Rotating -> Active)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/credentials/{credential_id}/rotation/start": {
      "post": {
        "operationId": "post_federation_credentials_rotation_start_federation_credentials__credential_id__rotation_start_post",
        "parameters": [
          {
            "description": "Target credential's id.",
            "in": "path",
            "name": "credential_id",
            "required": true,
            "schema": {
              "description": "Target credential's id.",
              "format": "uuid",
              "title": "Credential Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StartCredentialRotationRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only new_secret_ref)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No credential exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Credential is not in `Active` status, or the supplied new_secret_ref matches the current secret_ref (start_credential_rotation is single-source from Active only and strict-not-idempotent)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Start a rotation against an Active credential (Active -> Rotating)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/permits": {
      "get": {
        "operationId": "list_permits_federation_permits_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional direction filter (Outbound | Inbound).",
            "in": "query",
            "name": "direction",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Outbound",
                    "Inbound"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional direction filter (Outbound | Inbound).",
              "title": "Direction"
            }
          },
          {
            "description": "Optional status filter (one of the 4 PermitStatus values: Defined / Active / Suspended / Revoked).",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Defined",
                    "Active",
                    "Suspended",
                    "Revoked"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (one of the 4 PermitStatus values: Defined / Active / Suspended / Revoked).",
              "title": "Status"
            }
          },
          {
            "description": "Optional opaque peer-facility-id filter; matches the external string id of the peer facility.",
            "in": "query",
            "name": "peer_facility_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional opaque peer-facility-id filter; matches the external string id of the peer facility.",
              "title": "Peer Facility Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PermitListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed."
          }
        },
        "summary": "List federation Permits with cursor pagination + direction / status / peer_facility_id filters. Per-arc terms detail not surfaced (fetch get_permit for the full polymorphic terms VO).",
        "tags": [
          "federation"
        ]
      },
      "post": {
        "operationId": "post_federation_permits_federation_permits_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of writing a duplicate Permit.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of writing a duplicate Permit.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefinePermitRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefinePermitResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (empty peer_facility_id, empty scope sets, expires_at in the past, direction / terms mismatch, or whitespace-only payload-type / artifact-kind entry)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Defensive guard: the target Permit stream already has events. Essentially impossible in production with UUIDv7 ids."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Request body failed schema validation (missing field, invalid enum value, malformed UUID, OutboundTerms scope collapse, unknown canonicalization version), OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new federation Permit (cross-BC atomic; emits DecisionRegistered audit)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/permits/{permit_id}": {
      "get": {
        "operationId": "get_federation_permit_federation_permits__permit_id__get",
        "parameters": [
          {
            "description": "Target permit's id.",
            "in": "path",
            "name": "permit_id",
            "required": true,
            "schema": {
              "description": "Target permit's id.",
              "format": "uuid",
              "title": "Permit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PermitResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FederationErrorDTO"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FederationErrorDTO"
                }
              }
            },
            "description": "No Permit exists with the given id."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FederationErrorDTO"
                }
              }
            },
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a federation Permit by id",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/permits/{permit_id}/activate": {
      "post": {
        "operationId": "post_federation_permits_activate_federation_permits__permit_id__activate_post",
        "parameters": [
          {
            "description": "Target permit's id.",
            "in": "path",
            "name": "permit_id",
            "required": true,
            "schema": {
              "description": "Target permit's id.",
              "format": "uuid",
              "title": "Permit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No permit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Permit is not in `Defined` status (activate_permit is single-source from Defined only)."
          },
          "422": {
            "description": "Path parameter failed schema validation (not a UUID)."
          }
        },
        "summary": "Activate a Defined permit (Defined -> Active)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/permits/{permit_id}/resume": {
      "post": {
        "operationId": "post_federation_permits_resume_federation_permits__permit_id__resume_post",
        "parameters": [
          {
            "description": "Target permit's id.",
            "in": "path",
            "name": "permit_id",
            "required": true,
            "schema": {
              "description": "Target permit's id.",
              "format": "uuid",
              "title": "Permit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No permit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Permit is not in `Suspended` status (resume_permit is single-source from Suspended; first activation uses activate_permit)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Resume a Suspended Permit back to Active (Suspended -> Active)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/permits/{permit_id}/revoke": {
      "post": {
        "operationId": "post_federation_permits_revoke_federation_permits__permit_id__revoke_post",
        "parameters": [
          {
            "description": "Target permit's id.",
            "in": "path",
            "name": "permit_id",
            "required": true,
            "schema": {
              "description": "Target permit's id.",
              "format": "uuid",
              "title": "Permit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/RevokePermitRequest"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Body"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No permit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Permit is already Revoked (revoke_permit is strict-not-idempotent; Revoked is terminal)."
          },
          "422": {
            "description": "Request body failed schema validation (reason exceeds 500 chars)."
          }
        },
        "summary": "Revoke a Permit (terminal: any non-Revoked -> Revoked)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/permits/{permit_id}/suspend": {
      "post": {
        "operationId": "post_permits_suspend_federation_permits__permit_id__suspend_post",
        "parameters": [
          {
            "description": "Target permit's id.",
            "in": "path",
            "name": "permit_id",
            "required": true,
            "schema": {
              "description": "Target permit's id.",
              "format": "uuid",
              "title": "Permit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SuspendPermitRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No permit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Permit is not in `Active` status (suspend_permit requires Active; Defined/Suspended/Revoked permits are rejected)."
          },
          "422": {
            "description": "Request body failed schema validation (reason exceeds 500 chars)."
          }
        },
        "summary": "Suspend an Active Permit (Active -> Suspended)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/seals": {
      "get": {
        "operationId": "list_seals_federation_seals_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter (Live or Republishing).",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Live",
                    "Republishing"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (Live or Republishing).",
              "title": "Status"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SealListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed."
          }
        },
        "summary": "List Seal singletons (one per facility) with cursor pagination + optional status filter.",
        "tags": [
          "federation"
        ]
      },
      "post": {
        "operationId": "post_federation_seals_federation_seals_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of writing a duplicate Seal.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of writing a duplicate Seal.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InitializeSealRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InitializeSealResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only facility_id)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "The Seal singleton for this facility already exists (stream has prior events)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Request body failed schema validation (missing field, malformed UUID), OR online_credential_id equals offline_credential_id (key-separation invariant), OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Initialize the per-facility Seal singleton (cross-BC atomic; emits DecisionRegistered audit)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/seals/{facility_id}": {
      "get": {
        "operationId": "get_federation_seal_federation_seals__facility_id__get",
        "parameters": [
          {
            "description": "Target facility's opaque string id.",
            "in": "path",
            "name": "facility_id",
            "required": true,
            "schema": {
              "description": "Target facility's opaque string id.",
              "minLength": 1,
              "title": "Facility Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SealResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Seal exists for the given facility."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get the per-facility Seal singleton",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/seals/{facility_id}/online-key/rotate": {
      "post": {
        "operationId": "post_federation_seals_rotate_online_key_federation_seals__facility_id__online_key_rotate_post",
        "parameters": [
          {
            "description": "Target Seal's facility id.",
            "in": "path",
            "name": "facility_id",
            "required": true,
            "schema": {
              "description": "Target Seal's facility id.",
              "title": "Facility Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RotateSealOnlineKeyBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Seal exists for the given facility."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Seal is not Live (rotate requires Live) or the new ref equals the current online_credential_id (no-op rotation rejected)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "The new online credential id equals the current offline_credential_id (key-separation invariant)."
          }
        },
        "summary": "Rotate the Seal singleton's online (warm) signing key",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/seals/{facility_id}/pointer/sign": {
      "post": {
        "operationId": "sign_seal_pointer",
        "parameters": [
          {
            "description": "Target facility's id.",
            "in": "path",
            "name": "facility_id",
            "required": true,
            "schema": {
              "description": "Target facility's id.",
              "minLength": 1,
              "title": "Facility Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/SignSealPointerRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only new_head_hash)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Seal exists for the given facility_id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Seal is not in `Live` status, or the supplied new_sequence_number does not strictly exceed the prior (sign_seal_pointer is single-source from Live only and strict-not-idempotent)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Sign a new head pointer on a Live Seal (Live -> Live)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/seals/{facility_id}/republishing/complete": {
      "post": {
        "operationId": "post_federation_seals_republishing_complete_federation_seals__facility_id__republishing_complete_post",
        "parameters": [
          {
            "description": "Target Seal's facility_id.",
            "in": "path",
            "name": "facility_id",
            "required": true,
            "schema": {
              "description": "Target Seal's facility_id.",
              "title": "Facility Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/CompleteSealRepublishingBody"
                  },
                  {
                    "type": "null"
                  }
                ],
                "title": "Body"
              }
            }
          }
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: head-pair pairing invariant (only one of new_head_hash and new_sequence_number was supplied), or both were omitted while the Seal has no prior head_hash."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Seal exists for the given facility_id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Seal is not in 'Republishing' status (complete_seal_republishing is single-source from Republishing)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Body failed schema validation, or the supplied new_sequence_number was not strictly greater than the current value."
          }
        },
        "summary": "Complete an in-flight Seal republish (Republishing -> Live)",
        "tags": [
          "federation"
        ]
      }
    },
    "/federation/seals/{facility_id}/republishing/start": {
      "post": {
        "operationId": "post_federation_seals_republishing_start_federation_seals__facility_id__republishing_start_post",
        "parameters": [
          {
            "description": "Target Seal's facility id.",
            "in": "path",
            "name": "facility_id",
            "required": true,
            "schema": {
              "description": "Target Seal's facility id.",
              "title": "Facility Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StartSealRepublishingRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Seal exists for the given facility."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Seal is not in `Live` status (start_seal_republishing is single-source from Live only and strict-not-idempotent)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Start a republishing window on the Live Seal (Live -> Republishing)",
        "tags": [
          "federation"
        ]
      }
    },
    "/fixtures": {
      "get": {
        "operationId": "list_fixtures_fixtures_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Only Fixtures of this Assembly blueprint.",
            "in": "query",
            "name": "assembly_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only Fixtures of this Assembly blueprint.",
              "title": "Assembly Id"
            }
          },
          {
            "description": "Only Fixtures registered on this Trust Surface.",
            "in": "query",
            "name": "surface_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only Fixtures registered on this Trust Surface.",
              "title": "Surface Id"
            }
          },
          {
            "description": "Only Fixtures whose snapshot matches this content_hash (cross-Surface federation queries).",
            "in": "query",
            "name": "assembly_content_hash",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Only Fixtures whose snapshot matches this content_hash (cross-Surface federation queries).",
              "title": "Assembly Content Hash"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FixtureListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List Fixtures with cursor pagination + assembly/surface/content_hash filters",
        "tags": [
          "equipment"
        ]
      }
    },
    "/fixtures/{fixture_id}": {
      "get": {
        "operationId": "get_fixtures_fixtures__fixture_id__get",
        "parameters": [
          {
            "description": "Target Fixture's id.",
            "in": "path",
            "name": "fixture_id",
            "required": true,
            "schema": {
              "description": "Target Fixture's id.",
              "format": "uuid",
              "title": "Fixture Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/FixtureResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Fixture exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a Fixture by id",
        "tags": [
          "equipment"
        ]
      }
    },
    "/frames": {
      "post": {
        "operationId": "post_frames_frames_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the frame.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the frame.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterFrameRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterFrameResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only name, negative tolerance, OR root-vs-child mismatch (parent_frame_id and placement must be both null or both non-null, and placement.parent_frame_id must equal parent_frame_id)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation (missing fields, malformed UUID, negative tolerance via Pydantic ge=0) OR Idempotency-Key was reused with a different body."
          }
        },
        "summary": "Register a new coordinate frame",
        "tags": [
          "equipment"
        ]
      }
    },
    "/frames/{frame_id}": {
      "delete": {
        "operationId": "delete_frame_frames__frame_id__delete",
        "parameters": [
          {
            "in": "path",
            "name": "frame_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Frame Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DecommissionFrameRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Frame exists with the given frame_id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Frame cannot be decommissioned: already Decommissioned, OR still referenced by one or more active Mount or child Frame consumers (FrameInUseError)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Decommission a frame (terminal lifecycle)",
        "tags": [
          "equipment"
        ]
      }
    },
    "/frames/{frame_id}/placement": {
      "patch": {
        "operationId": "patch_frame_placement_frames__frame_id__placement_patch",
        "parameters": [
          {
            "in": "path",
            "name": "frame_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Frame Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateFramePlacementRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: negative tolerance via Placement VO OR new_placement.parent_frame_id does not match the Frame's parent_frame_id."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Frame exists with the given frame_id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Frame cannot be updated: currently Decommissioned, or the Frame is a root frame (root frames have no placement to update)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Update a frame's placement relative to its parent",
        "tags": [
          "equipment"
        ]
      }
    },
    "/health": {
      "get": {
        "description": "Liveness probe.",
        "operationId": "health_health_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "additionalProperties": {
                    "type": "string"
                  },
                  "title": "Response Health Health Get",
                  "type": "object"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Health"
      }
    },
    "/methods": {
      "get": {
        "operationId": "list_methods_methods_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter (one of: Defined, Versioned, Deprecated). Omit to return all statuses.",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Defined",
                    "Versioned",
                    "Deprecated"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (one of: Defined, Versioned, Deprecated). Omit to return all statuses.",
              "title": "Status"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MethodListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List methods with cursor pagination + status filter",
        "tags": [
          "recipe"
        ]
      },
      "post": {
        "operationId": "post_methods_methods_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the method.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the method.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineMethodRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineMethodResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new abstract technique-class recipe (Method)",
        "tags": [
          "recipe"
        ]
      }
    },
    "/methods/{method_id}": {
      "get": {
        "operationId": "get_methods_methods__method_id__get",
        "parameters": [
          {
            "description": "Target method's id.",
            "in": "path",
            "name": "method_id",
            "required": true,
            "schema": {
              "description": "Target method's id.",
              "format": "uuid",
              "title": "Method Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/MethodResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No method exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a method by id",
        "tags": [
          "recipe"
        ]
      }
    },
    "/methods/{method_id}/deprecate": {
      "post": {
        "operationId": "post_methods_deprecate_methods__method_id__deprecate_post",
        "parameters": [
          {
            "description": "Target method's id.",
            "in": "path",
            "name": "method_id",
            "required": true,
            "schema": {
              "description": "Target method's id.",
              "format": "uuid",
              "title": "Method Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No method exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Method is not in `Defined` or `Versioned` status (deprecate requires one of those \u2014 re-deprecating a Deprecated method raises), OR a concurrent write to the same method stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Mark an existing method as deprecated",
        "tags": [
          "recipe"
        ]
      }
    },
    "/methods/{method_id}/parameters-schema": {
      "post": {
        "operationId": "post_methods_parameters_schema_methods__method_id__parameters_schema_post",
        "parameters": [
          {
            "description": "Target method's id.",
            "in": "path",
            "name": "method_id",
            "required": true,
            "schema": {
              "description": "Target method's id.",
              "format": "uuid",
              "title": "Method Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateMethodParametersSchemaRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Schema rejected: missing or wrong `$schema`, forbidden keyword (for example $ref / oneOf / allOf), or jsonschema-rs rejected the schema as malformed."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No method exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Concurrent write to the same method stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Set, replace, or clear a Method's parameters_schema",
        "tags": [
          "recipe"
        ]
      }
    },
    "/methods/{method_id}/version": {
      "post": {
        "operationId": "post_methods_version_methods__method_id__version_post",
        "parameters": [
          {
            "description": "Target method's id.",
            "in": "path",
            "name": "method_id",
            "required": true,
            "schema": {
              "description": "Target method's id.",
              "format": "uuid",
              "title": "Method Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VersionMethodRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only version_tag."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No method exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Method is not in `Defined` or `Versioned` status (version requires one of those), OR a concurrent write to the same method stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Issue a new version label for an existing method",
        "tags": [
          "recipe"
        ]
      }
    },
    "/models": {
      "post": {
        "operationId": "post_models_models_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the model.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the model.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineModelRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineModelResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "One or more declared families does not resolve to a registered Family."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new vendor-catalog Model",
        "tags": [
          "equipment"
        ]
      }
    },
    "/models/{model_id}": {
      "get": {
        "operationId": "get_models_models__model_id__get",
        "parameters": [
          {
            "description": "Target model's id.",
            "in": "path",
            "name": "model_id",
            "required": true,
            "schema": {
              "description": "Target model's id.",
              "format": "uuid",
              "title": "Model Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ModelResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No model exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a model by id",
        "tags": [
          "equipment"
        ]
      }
    },
    "/models/{model_id}/deprecation": {
      "post": {
        "operationId": "post_models_deprecation_models__model_id__deprecation_post",
        "parameters": [
          {
            "description": "Target model's id.",
            "in": "path",
            "name": "model_id",
            "required": true,
            "schema": {
              "description": "Target model's id.",
              "format": "uuid",
              "title": "Model Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeprecateModelRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only reason after trimming)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No model exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Model is already in `Deprecated` status (deprecate requires `Defined` or `Versioned`), OR a concurrent write to the same model stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Mark an existing Model as deprecated",
        "tags": [
          "equipment"
        ]
      }
    },
    "/models/{model_id}/families": {
      "post": {
        "operationId": "post_models_add_family_models__model_id__families_post",
        "parameters": [
          {
            "description": "Target model's id.",
            "in": "path",
            "name": "model_id",
            "required": true,
            "schema": {
              "description": "Target model's id.",
              "format": "uuid",
              "title": "Model Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddModelFamilyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No model exists with the given id, OR the supplied family_id does not resolve to a registered Family."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Model is in `Deprecated` status (mutation requires `Defined` or `Versioned`), OR the family_id is already in the Model's declared_family_ids set, OR a concurrent write to the same model stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Add a Family to an existing Model's declared_family_ids set",
        "tags": [
          "equipment"
        ]
      }
    },
    "/models/{model_id}/families/{family_id}": {
      "delete": {
        "operationId": "delete_models_family_models__model_id__families__family_id__delete",
        "parameters": [
          {
            "description": "Target model's id.",
            "in": "path",
            "name": "model_id",
            "required": true,
            "schema": {
              "description": "Target model's id.",
              "format": "uuid",
              "title": "Model Id",
              "type": "string"
            }
          },
          {
            "description": "Family id to remove from the Model.declared_family_ids set.",
            "in": "path",
            "name": "family_id",
            "required": true,
            "schema": {
              "description": "Family id to remove from the Model.declared_family_ids set.",
              "format": "uuid",
              "title": "Family Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No model exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Model is in `Deprecated` status (mutation requires `Defined` or `Versioned`), OR the family_id is not in the Model's declared_family_ids set, OR a concurrent write to the same model stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Remove a Family from an existing Model's declared_family_ids set",
        "tags": [
          "equipment"
        ]
      }
    },
    "/models/{model_id}/versions": {
      "post": {
        "operationId": "post_models_versions_models__model_id__versions_post",
        "parameters": [
          {
            "description": "Target model's id.",
            "in": "path",
            "name": "model_id",
            "required": true,
            "schema": {
              "description": "Target model's id.",
              "format": "uuid",
              "title": "Model Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VersionModelRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only name, whitespace-only part_number, whitespace-only version_tag, or empty declared_family_ids)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No model exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Model is in `Deprecated` status (version requires `Defined` or `Versioned`), OR a concurrent write to the same model stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Issue a new version declaration for an existing Model",
        "tags": [
          "equipment"
        ]
      }
    },
    "/mounts": {
      "post": {
        "operationId": "post_mounts_mounts_post",
        "parameters": [
          {
            "description": "Optional client-supplied key for idempotent retry: replaying the same key returns the original mount_id without creating a new mount. Updates, decommission, install, and uninstall slices have no Idempotency-Key equivalent (the decider is already strict-not-idempotent).",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied key for idempotent retry: replaying the same key returns the original mount_id without creating a new mount. Updates, decommission, install, and uninstall slices have no Idempotency-Key equivalent (the decider is already strict-not-idempotent).",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterMountRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterMountResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only slot_code, non-finite (NaN / Inf) or negative-tolerance Placement value, or invalid Drawing."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Mount with the requested slot_code already exists (MountAlreadyExistsError)."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new mount (slot) in the beamline",
        "tags": [
          "equipment"
        ]
      }
    },
    "/mounts/{mount_id}": {
      "delete": {
        "operationId": "delete_mount_mounts__mount_id__delete",
        "parameters": [
          {
            "in": "path",
            "name": "mount_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Mount Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DecommissionMountRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Forbidden"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Not Found"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Mount cannot be decommissioned: already Decommissioned, still has an installed Asset (uninstall first), OR still has active child Mounts (decommission children first)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Decommission a mount (terminal lifecycle)",
        "tags": [
          "equipment"
        ]
      }
    },
    "/mounts/{mount_id}/installed-asset": {
      "delete": {
        "operationId": "delete_mount_installed_asset_mounts__mount_id__installed_asset_delete",
        "parameters": [
          {
            "in": "path",
            "name": "mount_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Mount Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UninstallAssetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Forbidden"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Not Found"
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Mount cannot uninstall: already Decommissioned OR slot is vacant (MountIsEmptyError)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Uninstall the currently-installed Asset from a mount slot",
        "tags": [
          "equipment"
        ]
      },
      "put": {
        "operationId": "put_mount_installed_asset_mounts__mount_id__installed_asset_put",
        "parameters": [
          {
            "in": "path",
            "name": "mount_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Mount Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InstallAssetRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Forbidden"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Mount or Asset does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "One of: Mount is Decommissioned, Asset is not in Active lifecycle (Commissioned / Maintenance / Decommissioned), Asset is already installed in a different Mount (uninstall from the current Mount first), or slot is already occupied by a different Asset (no implicit eviction). Repeating the SAME asset_id against an occupied slot is an idempotent no-op per PUT's RFC 9110 contract (204)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Install an Asset specimen into a mount slot",
        "tags": [
          "equipment"
        ]
      }
    },
    "/mounts/{mount_id}/placement": {
      "patch": {
        "operationId": "patch_mount_placement_mounts__mount_id__placement_patch",
        "parameters": [
          {
            "in": "path",
            "name": "mount_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Mount Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdateMountPlacementRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Placement VO rejection: non-finite (NaN / Inf) coordinate or tolerance, or negative tolerance on any axis."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Mount does not exist (MountNotFoundError)."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Mount is Decommissioned (MountCannotUpdateError) OR the new placement attempts to reparent to a different Frame (reparenting requires a separate slice; not in v1). Submitting a placement equal to the current value is an idempotent no-op (204)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Update a mount's placement relative to its parent frame",
        "tags": [
          "equipment"
        ]
      }
    },
    "/plans": {
      "get": {
        "operationId": "list_plans_plans_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter (Defined / Versioned / Deprecated).",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Defined",
                    "Versioned",
                    "Deprecated"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (Defined / Versioned / Deprecated).",
              "title": "Status"
            }
          },
          {
            "description": "Optional Practice-id filter.",
            "in": "query",
            "name": "practice_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional Practice-id filter.",
              "title": "Practice Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlanListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List plans with cursor pagination + status/practice_id filters",
        "tags": [
          "recipe"
        ]
      },
      "post": {
        "operationId": "post_plans_plans_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the plan.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the plan.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefinePlanRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefinePlanResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Referenced Practice, Method (via Practice), or Asset does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Binding rejected: upstream Practice or Method is Deprecated, a bound Asset is Decommissioned, or the bound Assets' families don't cover the Method's needed_family_ids."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new Plan: bind a Practice to Asset instances",
        "tags": [
          "recipe"
        ]
      }
    },
    "/plans/inspect-binding": {
      "post": {
        "operationId": "inspect_plan_binding_plans_inspect_binding_post",
        "parameters": [
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/InspectPlanBindingRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InspectPlanBindingResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Practice, Method, Asset, Capability, or Family not found."
          },
          "422": {
            "description": "Request body failed schema validation."
          }
        },
        "summary": "Preview a Plan binding without defining it",
        "tags": [
          "recipe"
        ]
      }
    },
    "/plans/{plan_id}": {
      "get": {
        "operationId": "get_plans_plans__plan_id__get",
        "parameters": [
          {
            "description": "Target plan's id.",
            "in": "path",
            "name": "plan_id",
            "required": true,
            "schema": {
              "description": "Target plan's id.",
              "format": "uuid",
              "title": "Plan Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PlanResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No plan exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a plan by id",
        "tags": [
          "recipe"
        ]
      }
    },
    "/plans/{plan_id}/add-wire": {
      "post": {
        "operationId": "post_plans_add_wire_plans__plan_id__add_wire_post",
        "parameters": [
          {
            "description": "Target plan's id.",
            "in": "path",
            "name": "plan_id",
            "required": true,
            "schema": {
              "description": "Target plan's id.",
              "format": "uuid",
              "title": "Plan Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AddPlanWireRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "A port name is empty / whitespace-only / exceeds the configured max length after trimming (InvalidWireError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No plan exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Plan cannot accept the wire under current conditions: the wire is already in the wire set (PlanWireAlreadyExistsError); the target port is already wired (PlanWireTargetAlreadyConnectedError, fan-in forbidden); a referenced Asset is not bound by this Plan (PlanWireAssetNotBoundError); a referenced port doesn't exist on its Asset (PlanWirePortNotFoundError); direction mismatch (PlanWireDirectionMismatchError); signal_type mismatch (PlanWireSignalTypeMismatchError); self-loop on the same port (PlanWireSelfLoopError); OR a concurrent write to the same plan stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation (missing field, malformed UUID, etc.)."
          }
        },
        "summary": "Add a typed port-to-port Wire to an existing Plan's wire set",
        "tags": [
          "recipe"
        ]
      }
    },
    "/plans/{plan_id}/default-parameters": {
      "patch": {
        "operationId": "patch_plan_default_parameters_plans__plan_id__default_parameters_patch",
        "parameters": [
          {
            "description": "Target plan's id.",
            "in": "path",
            "name": "plan_id",
            "required": true,
            "schema": {
              "description": "Target plan's id.",
              "format": "uuid",
              "title": "Plan Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UpdatePlanDefaultParametersRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "The proposed default_parameters (after merge) failed validation against the owning Method's parameters_schema."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No plan exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "A concurrent write to the same plan stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Request body failed schema validation (missing default_parameters_patch field, malformed UUID in path, etc.)."
          }
        },
        "summary": "Update an existing plan's default_parameters dict (RFC 7396 merge semantics)",
        "tags": [
          "recipe"
        ]
      }
    },
    "/plans/{plan_id}/deprecate": {
      "post": {
        "operationId": "post_plans_deprecate_plans__plan_id__deprecate_post",
        "parameters": [
          {
            "description": "Target plan's id.",
            "in": "path",
            "name": "plan_id",
            "required": true,
            "schema": {
              "description": "Target plan's id.",
              "format": "uuid",
              "title": "Plan Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No plan exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Plan is not in `Defined` or `Versioned` status (deprecate requires one of those \u2014 re-deprecating a Deprecated plan raises), OR a concurrent write to the same plan stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Mark an existing plan as deprecated",
        "tags": [
          "recipe"
        ]
      }
    },
    "/plans/{plan_id}/remove-wire": {
      "post": {
        "operationId": "post_plans_remove_wire_plans__plan_id__remove_wire_post",
        "parameters": [
          {
            "description": "Target plan's id.",
            "in": "path",
            "name": "plan_id",
            "required": true,
            "schema": {
              "description": "Target plan's id.",
              "format": "uuid",
              "title": "Plan Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RemovePlanWireRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "A port name is empty / whitespace-only / exceeds the configured max length after trimming (InvalidWireError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Either no plan exists with the given id (PlanNotFoundError), OR the Wire is not in the Plan's wire set (PlanWireNotFoundError; strict-not-idempotent)."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Concurrent write to the same plan stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation (missing field, malformed UUID, etc.)."
          }
        },
        "summary": "Remove a typed port-to-port Wire from an existing Plan's wire set",
        "tags": [
          "recipe"
        ]
      }
    },
    "/plans/{plan_id}/version": {
      "post": {
        "operationId": "post_plans_version_plans__plan_id__version_post",
        "parameters": [
          {
            "description": "Target plan's id.",
            "in": "path",
            "name": "plan_id",
            "required": true,
            "schema": {
              "description": "Target plan's id.",
              "format": "uuid",
              "title": "Plan Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VersionPlanRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only version_tag."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No plan exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Plan is not in `Defined` or `Versioned` status (version requires one of those), OR a concurrent write to the same plan stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Issue a new version label for an existing plan",
        "tags": [
          "recipe"
        ]
      }
    },
    "/policies": {
      "get": {
        "operationId": "list_policies_policies_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional conduit-scope filter; omit for any conduit.",
            "in": "query",
            "name": "conduit_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional conduit-scope filter; omit for any conduit.",
              "title": "Conduit Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PolicyListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List policies with cursor pagination + conduit filter",
        "tags": [
          "trust"
        ]
      },
      "post": {
        "operationId": "post_policies_policies_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the policy.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the policy.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefinePolicyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefinePolicyResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new authorization Policy for a Conduit",
        "tags": [
          "trust"
        ]
      }
    },
    "/policies/{policy_id}/evaluate": {
      "get": {
        "operationId": "get_policies_evaluate_policies__policy_id__evaluate_get",
        "parameters": [
          {
            "description": "Target policy's id.",
            "in": "path",
            "name": "policy_id",
            "required": true,
            "schema": {
              "description": "Target policy's id.",
              "format": "uuid",
              "title": "Policy Id",
              "type": "string"
            }
          },
          {
            "description": "Principal whose authorization is being checked.",
            "in": "query",
            "name": "evaluated_principal_id",
            "required": true,
            "schema": {
              "description": "Principal whose authorization is being checked.",
              "format": "uuid",
              "title": "Evaluated Principal Id",
              "type": "string"
            }
          },
          {
            "description": "Command name being evaluated (for example 'RegisterActor').",
            "in": "query",
            "name": "evaluated_command_name",
            "required": true,
            "schema": {
              "description": "Command name being evaluated (for example 'RegisterActor').",
              "maxLength": 200,
              "minLength": 1,
              "title": "Evaluated Command Name",
              "type": "string"
            }
          },
          {
            "description": "Conduit through which the command would be issued.",
            "in": "query",
            "name": "evaluated_conduit_id",
            "required": true,
            "schema": {
              "description": "Conduit through which the command would be issued.",
              "format": "uuid",
              "title": "Evaluated Conduit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EvaluatePolicyResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No policy exists with the given id."
          },
          "422": {
            "description": "Path or query parameter failed schema validation."
          }
        },
        "summary": "Evaluate a Policy against a (principal, command, conduit) tuple",
        "tags": [
          "trust"
        ]
      }
    },
    "/policies/{policy_id}/permissions": {
      "get": {
        "description": "Enumerates the commands a principal can execute against a Policy via a conduit. Returns the sorted set plus the `incomplete: bool` flag (always False at v1; future ABAC may flip it). **Do NOT use the returned set to drive authorization decisions**: this is for UX / debugging only; only `Kernel.authorize` (the PEP) authorizes. On-behalf queries (evaluated_principal_id != caller) require a separate `ListPermissionsOfOthers` permission, denied by default.",
        "operationId": "get_policies_permissions_policies__policy_id__permissions_get",
        "parameters": [
          {
            "description": "Target policy's id.",
            "in": "path",
            "name": "policy_id",
            "required": true,
            "schema": {
              "description": "Target policy's id.",
              "format": "uuid",
              "title": "Policy Id",
              "type": "string"
            }
          },
          {
            "description": "Principal whose permissions are being enumerated.",
            "in": "query",
            "name": "evaluated_principal_id",
            "required": true,
            "schema": {
              "description": "Principal whose permissions are being enumerated.",
              "format": "uuid",
              "title": "Evaluated Principal Id",
              "type": "string"
            }
          },
          {
            "description": "Conduit through which the commands would be issued.",
            "in": "query",
            "name": "evaluated_conduit_id",
            "required": true,
            "schema": {
              "description": "Conduit through which the commands would be issued.",
              "format": "uuid",
              "title": "Evaluated Conduit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListPermissionsResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No policy exists with the given id."
          },
          "422": {
            "description": "Path or query parameter failed schema validation."
          }
        },
        "summary": "List a Policy's permitted commands for (principal, conduit)",
        "tags": [
          "trust"
        ]
      }
    },
    "/practices": {
      "get": {
        "operationId": "list_practices_practices_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter (Defined / Versioned / Deprecated).",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Defined",
                    "Versioned",
                    "Deprecated"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (Defined / Versioned / Deprecated).",
              "title": "Status"
            }
          },
          {
            "description": "Optional Method-id filter.",
            "in": "query",
            "name": "method_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional Method-id filter.",
              "title": "Method Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PracticeListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List practices with cursor pagination + status/method_id filters",
        "tags": [
          "recipe"
        ]
      },
      "post": {
        "operationId": "post_practices_practices_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the practice.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the practice.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefinePracticeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefinePracticeResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new facility-adapted Method (Practice)",
        "tags": [
          "recipe"
        ]
      }
    },
    "/practices/{practice_id}": {
      "get": {
        "operationId": "get_practices_practices__practice_id__get",
        "parameters": [
          {
            "description": "Target practice's id.",
            "in": "path",
            "name": "practice_id",
            "required": true,
            "schema": {
              "description": "Target practice's id.",
              "format": "uuid",
              "title": "Practice Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/PracticeResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No practice exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a practice by id",
        "tags": [
          "recipe"
        ]
      }
    },
    "/practices/{practice_id}/deprecate": {
      "post": {
        "operationId": "post_practices_deprecate_practices__practice_id__deprecate_post",
        "parameters": [
          {
            "description": "Target practice's id.",
            "in": "path",
            "name": "practice_id",
            "required": true,
            "schema": {
              "description": "Target practice's id.",
              "format": "uuid",
              "title": "Practice Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No practice exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Practice is not in `Defined` or `Versioned` status (deprecate requires one of those \u2014 re-deprecating a Deprecated practice raises), OR a concurrent write to the same practice stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Mark an existing practice as deprecated",
        "tags": [
          "recipe"
        ]
      }
    },
    "/practices/{practice_id}/version": {
      "post": {
        "operationId": "post_practices_version_practices__practice_id__version_post",
        "parameters": [
          {
            "description": "Target practice's id.",
            "in": "path",
            "name": "practice_id",
            "required": true,
            "schema": {
              "description": "Target practice's id.",
              "format": "uuid",
              "title": "Practice Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VersionPracticeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only version_tag."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No practice exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Practice is not in `Defined` or `Versioned` status (version requires one of those), OR a concurrent write to the same practice stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Issue a new version label for an existing practice",
        "tags": [
          "recipe"
        ]
      }
    },
    "/procedures": {
      "get": {
        "operationId": "list_procedures_procedures_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter (one of: Defined, Running, Completed, Aborted, Truncated). Omit to return all statuses.",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Defined",
                    "Running",
                    "Completed",
                    "Aborted",
                    "Truncated"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (one of: Defined, Running, Completed, Aborted, Truncated). Omit to return all statuses.",
              "title": "Status"
            }
          },
          {
            "description": "Optional kind filter (free-form, exact match; for example 'bakeout', 'alignment'). Omit to return all kinds.",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maxLength": 50,
                  "minLength": 1,
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional kind filter (free-form, exact match; for example 'bakeout', 'alignment'). Omit to return all kinds.",
              "title": "Kind"
            }
          },
          {
            "description": "Optional Phase-of-Run filter; matches Procedures whose parent_run_id equals this UUID. Omit to return Procedures with any parent (including standalone).",
            "in": "query",
            "name": "parent_run_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional Phase-of-Run filter; matches Procedures whose parent_run_id equals this UUID. Omit to return Procedures with any parent (including standalone).",
              "title": "Parent Run Id"
            }
          },
          {
            "description": "Optional target-Asset filter; matches Procedures whose target_asset_ids array contains this UUID.",
            "in": "query",
            "name": "target_asset_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional target-Asset filter; matches Procedures whose target_asset_ids array contains this UUID.",
              "title": "Target Asset Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProcedureListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed."
          }
        },
        "summary": "List procedures with cursor pagination + status / kind / parent_run_id / target_asset_id filters",
        "tags": [
          "operation"
        ]
      },
      "post": {
        "operationId": "post_procedures_procedures_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the procedure.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the procedure.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterProcedureRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterProcedureResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only name or kind)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Defensive guard: the target procedure stream already has events. Essentially impossible in production with UUIDv7 ids; documented for OpenAPI completeness against the BC's exception handler."
          },
          "422": {
            "description": "Request body failed schema validation (missing field, length out of bounds, malformed UUID), OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new episodic operational procedure (lands in Defined)",
        "tags": [
          "operation"
        ]
      }
    },
    "/procedures/from-recipe": {
      "post": {
        "operationId": "post_procedures_from_recipe_procedures_from_recipe_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the procedure.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the procedure.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterProcedureFromRecipeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterProcedureFromRecipeResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only name or kind)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Referenced Recipe does not exist OR Recipe's bound Capability does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Capability.executor_shapes does not include Procedure (cross-BC executor-shape guard)."
          },
          "422": {
            "description": "Request body failed schema validation, OR Recipe's BindingRefs are stale against the current Capability parameters_schema, OR operator-supplied bindings did not validate against the Capability's parameters_schema, OR the expansion produced more than the configured cap."
          },
          "500": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Expansion port returned different results for the same (steps, bindings) input (server-side determinism bug)."
          }
        },
        "summary": "Register a new Procedure by expanding a Recipe with operator bindings",
        "tags": [
          "operation"
        ]
      }
    },
    "/procedures/{procedure_id}": {
      "get": {
        "operationId": "get_procedures_procedures__procedure_id__get",
        "parameters": [
          {
            "description": "Target procedure's id.",
            "in": "path",
            "name": "procedure_id",
            "required": true,
            "schema": {
              "description": "Target procedure's id.",
              "format": "uuid",
              "title": "Procedure Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ProcedureResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No procedure exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a procedure by id",
        "tags": [
          "operation"
        ]
      }
    },
    "/procedures/{procedure_id}/abort": {
      "post": {
        "operationId": "post_procedures_abort_procedures__procedure_id__abort_post",
        "parameters": [
          {
            "description": "Target procedure's id.",
            "in": "path",
            "name": "procedure_id",
            "required": true,
            "schema": {
              "description": "Target procedure's id.",
              "format": "uuid",
              "title": "Procedure Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AbortProcedureRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only reason."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No procedure exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Procedure is not in `Running` status (abort requires `Running` today; aborting a `Defined` / `Completed` / `Aborted` procedure raises), OR a concurrent write to the same procedure stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Mark an existing Procedure as aborted (emergency-exit terminal)",
        "tags": [
          "operation"
        ]
      }
    },
    "/procedures/{procedure_id}/complete": {
      "post": {
        "operationId": "post_procedures_complete_procedures__procedure_id__complete_post",
        "parameters": [
          {
            "description": "Target procedure's id.",
            "in": "path",
            "name": "procedure_id",
            "required": true,
            "schema": {
              "description": "Target procedure's id.",
              "format": "uuid",
              "title": "Procedure Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No procedure exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Procedure is not in `Running` status (complete requires `Running` today; re-completing a `Completed` procedure raises, completing an `Aborted` procedure raises), OR a concurrent write to the same procedure stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Mark an existing Procedure as completed (happy-path terminal)",
        "tags": [
          "operation"
        ]
      }
    },
    "/procedures/{procedure_id}/conduct": {
      "post": {
        "description": "Conduct a Procedure end-to-end. Failures land in the response body.",
        "operationId": "post_procedures_conduct_procedures__procedure_id__conduct_post",
        "parameters": [
          {
            "description": "Target procedure's id.",
            "in": "path",
            "name": "procedure_id",
            "required": true,
            "schema": {
              "description": "Target procedure's id.",
              "format": "uuid",
              "title": "Procedure Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ConductProcedureRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ConductProcedureResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation: unknown step kind, missing required field, batch over cap, invalid criterion shape."
          }
        },
        "summary": "Conduct a Procedure: start -> walk steps via ControlPort + actions + checks -> complete (success) or abort (failure).",
        "tags": [
          "operation"
        ]
      }
    },
    "/procedures/{procedure_id}/start": {
      "post": {
        "operationId": "post_procedures_start_procedures__procedure_id__start_post",
        "parameters": [
          {
            "description": "Target procedure's id.",
            "in": "path",
            "name": "procedure_id",
            "required": true,
            "schema": {
              "description": "Target procedure's id.",
              "format": "uuid",
              "title": "Procedure Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No procedure exists with the given id, OR a target Asset referenced by the procedure no longer exists."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Procedure is not in `Defined` status (start requires `Defined`; re-starting a `Running` procedure raises, starting any terminal raises), OR a target Asset is Decommissioned, OR a concurrent write to the same procedure stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Transition an existing Procedure from Defined to Running",
        "tags": [
          "operation"
        ]
      }
    },
    "/procedures/{procedure_id}/steps": {
      "post": {
        "operationId": "post_procedures_steps_procedures__procedure_id__steps_post",
        "parameters": [
          {
            "description": "Target procedure's id.",
            "in": "path",
            "name": "procedure_id",
            "required": true,
            "schema": {
              "description": "Target procedure's id.",
              "format": "uuid",
              "title": "Procedure Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AppendProcedureStepsRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AppendProcedureStepsResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Per-entry validation failed in the handler (InvalidStepKindError; defensive guard for direct in-process callers that bypass Pydantic)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Procedure exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Procedure is not in `Running` (Defined hasn't started; terminals have ended); the steps logbook is implicitly closed. Or a concurrent write to the same Procedure stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Request body failed schema validation: empty entries list, batch over cap, missing required fields, invalid step_kind value."
          }
        },
        "summary": "Append a batch of polymorphic procedural steps to a Procedure's steps logbook (lazy open-on-first-write).",
        "tags": [
          "operation"
        ]
      }
    },
    "/procedures/{procedure_id}/truncate": {
      "post": {
        "operationId": "post_procedures_truncate_procedures__procedure_id__truncate_post",
        "parameters": [
          {
            "description": "Target procedure's id.",
            "in": "path",
            "name": "procedure_id",
            "required": true,
            "schema": {
              "description": "Target procedure's id.",
              "format": "uuid",
              "title": "Procedure Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TruncateProcedureRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only reason, OR interrupted_at is in the future relative to now."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No procedure exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Procedure is not in `Running` status (truncate requires `Running` today; truncating a `Defined` / `Completed` / `Aborted` / `Truncated` procedure raises), OR a concurrent write to the same procedure stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Cleanup terminal of an interrupted Procedure (Running -> Truncated)",
        "tags": [
          "operation"
        ]
      }
    },
    "/recipes": {
      "post": {
        "operationId": "post_recipes_recipes_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the Recipe.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the Recipe.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineRecipeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineRecipeResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only name, empty steps)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Referenced Capability does not exist."
          },
          "422": {
            "description": "Request body failed schema validation OR BindingRef in steps references a parameter not declared in the Capability's parameters_schema OR steps contain BindingRefs but the Capability has no parameters_schema."
          }
        },
        "summary": "Define a new Recipe against an existing Capability",
        "tags": [
          "recipe"
        ]
      }
    },
    "/recipes/{recipe_id}": {
      "get": {
        "operationId": "get_recipes_recipes__recipe_id__get",
        "parameters": [
          {
            "description": "Target Recipe's id.",
            "in": "path",
            "name": "recipe_id",
            "required": true,
            "schema": {
              "description": "Target Recipe's id.",
              "format": "uuid",
              "title": "Recipe Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RecipeResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Recipe exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a Recipe by id",
        "tags": [
          "recipe"
        ]
      }
    },
    "/recipes/{recipe_id}/deprecate": {
      "post": {
        "operationId": "post_recipes_deprecate_recipes__recipe_id__deprecate_post",
        "parameters": [
          {
            "description": "Target Recipe's id.",
            "in": "path",
            "name": "recipe_id",
            "required": true,
            "schema": {
              "description": "Target Recipe's id.",
              "format": "uuid",
              "title": "Recipe Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeprecateRecipeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Recipe exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Recipe is already Deprecated."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Deprecate an existing Recipe",
        "tags": [
          "recipe"
        ]
      }
    },
    "/recipes/{recipe_id}/version": {
      "post": {
        "operationId": "post_recipes_version_recipes__recipe_id__version_post",
        "parameters": [
          {
            "description": "Target Recipe's id.",
            "in": "path",
            "name": "recipe_id",
            "required": true,
            "schema": {
              "description": "Target Recipe's id.",
              "format": "uuid",
              "title": "Recipe Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VersionRecipeRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only version_tag, empty steps)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Recipe exists with the given id OR referenced Capability does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Recipe is currently Deprecated."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation OR BindingRef in steps references a parameter not declared in the current Capability.parameters_schema."
          }
        },
        "summary": "Issue a new version label + replacement steps for a Recipe",
        "tags": [
          "recipe"
        ]
      }
    },
    "/runs": {
      "get": {
        "operationId": "list_runs_runs_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter (Running / Held / Completed / Aborted / Stopped / Truncated). Omit for all.",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Running",
                    "Held",
                    "Completed",
                    "Aborted",
                    "Stopped",
                    "Truncated"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (Running / Held / Completed / Aborted / Stopped / Truncated). Omit for all.",
              "title": "Status"
            }
          },
          {
            "description": "Optional Plan-id filter.",
            "in": "query",
            "name": "plan_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional Plan-id filter.",
              "title": "Plan Id"
            }
          },
          {
            "description": "Optional Campaign-id filter: returns Runs that are members of the given Campaign.",
            "in": "query",
            "name": "campaign_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional Campaign-id filter: returns Runs that are members of the given Campaign.",
              "title": "Campaign Id"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List runs with cursor pagination + status/plan_id/campaign_id filters",
        "tags": [
          "run"
        ]
      },
      "post": {
        "operationId": "post_runs_runs_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of starting a duplicate run.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of starting a duplicate run.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StartRunRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StartRunResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Referenced Plan, Practice (via Plan), Method (via Practice), Asset (via Plan), Subject, or Campaign (when campaign_id supplied) does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Run-start rejected: Plan is Deprecated, Subject is not in Mounted or Measured, a bound Asset is Decommissioned, the bound Assets' current capabilities don't cover the Method's needed_family_ids, OR the supplied Campaign is in a terminal status (Closed / Abandoned) and refuses new members."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Start a new Run: bind a Plan + (optional) Subject",
        "tags": [
          "run"
        ]
      }
    },
    "/runs/{run_id}": {
      "get": {
        "operationId": "get_runs_runs__run_id__get",
        "parameters": [
          {
            "description": "Target run's id.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Target run's id.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RunResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No run exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a run by id",
        "tags": [
          "run"
        ]
      }
    },
    "/runs/{run_id}/abort": {
      "post": {
        "operationId": "post_runs_abort_runs__run_id__abort_post",
        "parameters": [
          {
            "description": "Target run's id.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Target run's id.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AbortRunRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only reason."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No run exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Run is not in `Running` status (abort requires `Running` today; aborting a `Completed` or `Aborted` run raises), OR a concurrent write to the same run stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Mark an existing Run as aborted (emergency-exit terminal)",
        "tags": [
          "run"
        ]
      }
    },
    "/runs/{run_id}/adjust": {
      "post": {
        "operationId": "post_runs_adjust_runs__run_id__adjust_post",
        "parameters": [
          {
            "description": "Target run's id.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Target run's id.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-applying the patch.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-applying the patch.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AdjustRunRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: empty parameters_patch, whitespace-only reason, or post-merge effective set fails the Method's parameters_schema."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Referenced Run (or its upstream Plan / Practice / Method via the Recipe chain) does not exist."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Run is not in Running or Held (adjust requires an in-progress Run; Idle / terminal states are rejected), OR a concurrent write to the same Run stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Adjust a Run's effective parameters mid-flight",
        "tags": [
          "run"
        ]
      }
    },
    "/runs/{run_id}/complete": {
      "post": {
        "operationId": "post_runs_complete_runs__run_id__complete_post",
        "parameters": [
          {
            "description": "Target run's id.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Target run's id.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No run exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Run is not in `Running` status (complete requires `Running` today; re-completing a `Completed` run raises, completing an `Aborted` run raises), OR a concurrent write to the same run stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Mark an existing Run as completed (happy-path terminal)",
        "tags": [
          "run"
        ]
      }
    },
    "/runs/{run_id}/hold": {
      "post": {
        "operationId": "post_runs_hold_runs__run_id__hold_post",
        "parameters": [
          {
            "description": "Target run's id.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Target run's id.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No run exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Run is not in `Running` status (hold requires `Running`; re-holding a `Held` run raises, holding a terminal run raises), OR a concurrent write to the same run stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Pause an actively-running Run (Running \u2192 Held)",
        "tags": [
          "run"
        ]
      }
    },
    "/runs/{run_id}/readings": {
      "post": {
        "operationId": "post_runs_readings_runs__run_id__readings_post",
        "parameters": [
          {
            "description": "Target run's id.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Target run's id.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AppendRunReadingsRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AppendRunReadingsResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Per-entry validation failed in the handler (InvalidChannelNameError / InvalidReadingValueError / InvalidSamplingProcedureError)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Run exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Run is in a terminal status (Completed | Aborted | Stopped | Truncated); the reading logbook is implicitly closed."
          },
          "422": {
            "description": "Request body failed schema validation: empty entries list, batch over cap, missing required fields, invalid sampling_procedure value, NaN/Infinity reading value, channel_name out of bounds."
          }
        },
        "summary": "Append a batch of polymorphic sensor / motor readings to a Run's reading logbook (lazy open-on-first-write).",
        "tags": [
          "run"
        ]
      }
    },
    "/runs/{run_id}/resume": {
      "post": {
        "operationId": "post_runs_resume_runs__run_id__resume_post",
        "parameters": [
          {
            "description": "Target run's id.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Target run's id.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No run exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Run is not in `Held` status (resume requires `Held`; resuming a `Running` run raises, resuming a terminal run raises), OR a concurrent write to the same run stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Resume a held Run (Held \u2192 Running)",
        "tags": [
          "run"
        ]
      }
    },
    "/runs/{run_id}/stop": {
      "post": {
        "operationId": "post_runs_stop_runs__run_id__stop_post",
        "parameters": [
          {
            "description": "Target run's id.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Target run's id.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/StopRunRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only reason."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No run exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Run is not in `Running` or `Held` status (stop requires either; stopping a terminal run raises), OR a concurrent write to the same run stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Controlled early exit of a Run (Running | Held \u2192 Stopped)",
        "tags": [
          "run"
        ]
      }
    },
    "/runs/{run_id}/truncate": {
      "post": {
        "operationId": "post_runs_truncate_runs__run_id__truncate_post",
        "parameters": [
          {
            "description": "Target run's id.",
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "description": "Target run's id.",
              "format": "uuid",
              "title": "Run Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TruncateRunRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only reason, OR interrupted_at is in the future relative to now."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No run exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Run is not in `Running` or `Held` status (truncate requires either; truncating a terminal run raises), OR a concurrent write to the same run stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Cleanup terminal of an interrupted Run (Running | Held \u2192 Truncated)",
        "tags": [
          "run"
        ]
      }
    },
    "/subjects": {
      "get": {
        "operationId": "list_subjects_subjects_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional status filter (one of: Received, Mounted, Measured, Removed, Returned, Stored, Discarded). Omit to return all statuses.",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Received",
                    "Mounted",
                    "Measured",
                    "Removed",
                    "Returned",
                    "Stored",
                    "Discarded"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (one of: Received, Mounted, Measured, Removed, Returned, Stored, Discarded). Omit to return all statuses.",
              "title": "Status"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SubjectListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List subjects with cursor pagination",
        "tags": [
          "subject"
        ]
      },
      "post": {
        "operationId": "post_subjects_subjects_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the subject.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the subject.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterSubjectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterSubjectResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new subject",
        "tags": [
          "subject"
        ]
      }
    },
    "/subjects/{subject_id}": {
      "get": {
        "operationId": "get_subjects_subjects__subject_id__get",
        "parameters": [
          {
            "description": "Target subject's id.",
            "in": "path",
            "name": "subject_id",
            "required": true,
            "schema": {
              "description": "Target subject's id.",
              "format": "uuid",
              "title": "Subject Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SubjectResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No subject exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a subject by id",
        "tags": [
          "subject"
        ]
      }
    },
    "/subjects/{subject_id}/discard": {
      "post": {
        "operationId": "post_subjects_discard_subjects__subject_id__discard_post",
        "parameters": [
          {
            "description": "Target subject's id.",
            "in": "path",
            "name": "subject_id",
            "required": true,
            "schema": {
              "description": "Target subject's id.",
              "format": "uuid",
              "title": "Subject Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DiscardSubjectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated: whitespace-only reason."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No subject exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Subject is not in `Removed` state (discard requires Removed), OR a concurrent write to the same subject stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Destroy / discard an existing (Removed) subject",
        "tags": [
          "subject"
        ]
      }
    },
    "/subjects/{subject_id}/dismount": {
      "post": {
        "operationId": "post_subjects_dismount_subjects__subject_id__dismount_post",
        "parameters": [
          {
            "description": "Target subject's id.",
            "in": "path",
            "name": "subject_id",
            "required": true,
            "schema": {
              "description": "Target subject's id.",
              "format": "uuid",
              "title": "Subject Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DismountSubjectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Subject exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Subject is not in `Mounted` or `Measured` state (dismount requires the Subject to be currently mounted), OR a concurrent write to the same Subject stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Dismount a Subject from its current sample-environment Asset (4f)",
        "tags": [
          "subject"
        ]
      }
    },
    "/subjects/{subject_id}/measure": {
      "post": {
        "operationId": "post_subjects_measure_subjects__subject_id__measure_post",
        "parameters": [
          {
            "description": "Target subject's id.",
            "in": "path",
            "name": "subject_id",
            "required": true,
            "schema": {
              "description": "Target subject's id.",
              "format": "uuid",
              "title": "Subject Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No subject exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Subject is not in `Mounted` state (measure requires Mounted), OR a concurrent write to the same subject stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Record a measurement on an existing subject",
        "tags": [
          "subject"
        ]
      }
    },
    "/subjects/{subject_id}/mount": {
      "post": {
        "operationId": "post_subjects_mount_subjects__subject_id__mount_post",
        "parameters": [
          {
            "description": "Target subject's id.",
            "in": "path",
            "name": "subject_id",
            "required": true,
            "schema": {
              "description": "Target subject's id.",
              "format": "uuid",
              "title": "Subject Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MountSubjectRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Subject or Asset exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Subject is not in `Received` state (mount requires Received), OR mount-target Asset is not `Active`, OR a concurrent write to the same subject stream conflicted (optimistic concurrency)."
          },
          "422": {
            "description": "Path parameter or request body failed schema validation."
          }
        },
        "summary": "Mount an existing subject onto a sample-environment Asset",
        "tags": [
          "subject"
        ]
      }
    },
    "/subjects/{subject_id}/remove": {
      "post": {
        "operationId": "post_subjects_remove_subjects__subject_id__remove_post",
        "parameters": [
          {
            "description": "Target subject's id.",
            "in": "path",
            "name": "subject_id",
            "required": true,
            "schema": {
              "description": "Target subject's id.",
              "format": "uuid",
              "title": "Subject Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No subject exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Subject is not in `Mounted` or `Measured` state (remove requires either), OR a concurrent write to the same subject stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Remove an existing subject from the apparatus",
        "tags": [
          "subject"
        ]
      }
    },
    "/subjects/{subject_id}/return": {
      "post": {
        "operationId": "post_subjects_return_subjects__subject_id__return_post",
        "parameters": [
          {
            "description": "Target subject's id.",
            "in": "path",
            "name": "subject_id",
            "required": true,
            "schema": {
              "description": "Target subject's id.",
              "format": "uuid",
              "title": "Subject Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No subject exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Subject is not in `Removed` state (return requires Removed), OR a concurrent write to the same subject stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Return an existing (Removed) subject to its owner / submitter",
        "tags": [
          "subject"
        ]
      }
    },
    "/subjects/{subject_id}/store": {
      "post": {
        "operationId": "post_subjects_store_subjects__subject_id__store_post",
        "parameters": [
          {
            "description": "Target subject's id.",
            "in": "path",
            "name": "subject_id",
            "required": true,
            "schema": {
              "description": "Target subject's id.",
              "format": "uuid",
              "title": "Subject Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No subject exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Subject is not in `Removed` state (store requires Removed), OR a concurrent write to the same subject stream conflicted (optimistic concurrency)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Archive an existing (Removed) subject on-site",
        "tags": [
          "subject"
        ]
      }
    },
    "/supplies": {
      "get": {
        "operationId": "list_supplies_supplies_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Optional scope filter (one of: Facility, Sector, Beamline). Omit to return all scopes.",
            "in": "query",
            "name": "scope",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Facility",
                    "Sector",
                    "Beamline"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional scope filter (one of: Facility, Sector, Beamline). Omit to return all scopes.",
              "title": "Scope"
            }
          },
          {
            "description": "Optional kind filter (free-form, exact match; for example 'LiquidNitrogen'). Omit to return all kinds.",
            "in": "query",
            "name": "kind",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "maxLength": 50,
                  "minLength": 1,
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional kind filter (free-form, exact match; for example 'LiquidNitrogen'). Omit to return all kinds.",
              "title": "Kind"
            }
          },
          {
            "description": "Optional status filter (one of: Unknown, Available, Degraded, Unavailable, Recovering, Decommissioned). Omit to return all statuses including Decommissioned (no default-exclude; matches Asset / Subject sibling-BC convention).",
            "in": "query",
            "name": "status",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "Unknown",
                    "Available",
                    "Degraded",
                    "Unavailable",
                    "Recovering",
                    "Decommissioned"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional status filter (one of: Unknown, Available, Degraded, Unavailable, Recovering, Decommissioned). Omit to return all statuses including Decommissioned (no default-exclude; matches Asset / Subject sibling-BC convention).",
              "title": "Status"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SupplyListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List supplies with cursor pagination + scope/kind/status filters",
        "tags": [
          "supply"
        ]
      },
      "post": {
        "operationId": "post_supplies_supplies_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the supply.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the supply.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterSupplyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterSupplyResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only kind or name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Defensive guard: the target supply stream already has events. Essentially impossible in production with UUIDv7 ids; documented for OpenAPI completeness against the BC's exception handler."
          },
          "422": {
            "description": "Request body failed schema validation (missing field, invalid scope enum, length out of bounds), OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Register a new continuously-available resource (lands in Unknown)",
        "tags": [
          "supply"
        ]
      }
    },
    "/supplies/{supply_id}": {
      "get": {
        "operationId": "get_supplies_supplies__supply_id__get",
        "parameters": [
          {
            "description": "Target supply's id.",
            "in": "path",
            "name": "supply_id",
            "required": true,
            "schema": {
              "description": "Target supply's id.",
              "format": "uuid",
              "title": "Supply Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SupplyResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No supply exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a supply by id",
        "tags": [
          "supply"
        ]
      }
    },
    "/supplies/{supply_id}/degrade": {
      "post": {
        "operationId": "post_supplies_degrade_supplies__supply_id__degrade_post",
        "parameters": [
          {
            "description": "Target supply's id.",
            "in": "path",
            "name": "supply_id",
            "required": true,
            "schema": {
              "description": "Target supply's id.",
              "format": "uuid",
              "title": "Supply Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DegradeSupplyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No supply exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Supply is not in a degradable status. Source set: {Unknown, Available, Recovering}. An Unavailable supply cannot transition directly to Degraded (must go via mark_supply_recovering first)."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Mark a Supply as Degraded (resource up but below nominal capacity)",
        "tags": [
          "supply"
        ]
      }
    },
    "/supplies/{supply_id}/deregister": {
      "post": {
        "operationId": "post_supplies_deregister_supplies__supply_id__deregister_post",
        "parameters": [
          {
            "description": "Target supply's id.",
            "in": "path",
            "name": "supply_id",
            "required": true,
            "schema": {
              "description": "Target supply's id.",
              "format": "uuid",
              "title": "Supply Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeregisterSupplyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No supply exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Supply is already Decommissioned (strict-not-idempotent)."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Deregister a Supply (lifecycle terminal: any -> Decommissioned)",
        "tags": [
          "supply"
        ]
      }
    },
    "/supplies/{supply_id}/mark-available": {
      "post": {
        "operationId": "post_supplies_mark_available_supplies__supply_id__mark_available_post",
        "parameters": [
          {
            "description": "Target supply's id.",
            "in": "path",
            "name": "supply_id",
            "required": true,
            "schema": {
              "description": "Target supply's id.",
              "format": "uuid",
              "title": "Supply Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MarkSupplyAvailableRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No supply exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Supply is not in `Unknown` status (mark_supply_available is single-source from Unknown only; recovery acknowledgement uses restore_supply)."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Mark a registered Supply Available (first observation; Unknown -> Available)",
        "tags": [
          "supply"
        ]
      }
    },
    "/supplies/{supply_id}/mark-recovering": {
      "post": {
        "operationId": "post_supplies_mark_recovering_supplies__supply_id__mark_recovering_post",
        "parameters": [
          {
            "description": "Target supply's id.",
            "in": "path",
            "name": "supply_id",
            "required": true,
            "schema": {
              "description": "Target supply's id.",
              "format": "uuid",
              "title": "Supply Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MarkSupplyRecoveringRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No supply exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Supply is not Unavailable. Single-source: only an Unavailable supply can be marked Recovering."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Mark a Supply as Recovering (observation suggests it may be coming back)",
        "tags": [
          "supply"
        ]
      }
    },
    "/supplies/{supply_id}/mark-unavailable": {
      "post": {
        "operationId": "post_supplies_mark_unavailable_supplies__supply_id__mark_unavailable_post",
        "parameters": [
          {
            "description": "Target supply's id.",
            "in": "path",
            "name": "supply_id",
            "required": true,
            "schema": {
              "description": "Target supply's id.",
              "format": "uuid",
              "title": "Supply Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/MarkSupplyUnavailableRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No supply exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Supply is already Unavailable (strict-not-idempotent)."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Mark a Supply as Unavailable (resource is down)",
        "tags": [
          "supply"
        ]
      }
    },
    "/supplies/{supply_id}/restore": {
      "post": {
        "operationId": "post_supplies_restore_supplies__supply_id__restore_post",
        "parameters": [
          {
            "description": "Target supply's id.",
            "in": "path",
            "name": "supply_id",
            "required": true,
            "schema": {
              "description": "Target supply's id.",
              "format": "uuid",
              "title": "Supply Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RestoreSupplyRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No supply exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Supply is not Recovering. Single-source: only a Recovering supply can be restored. The Unknown -> Available transition has distinct audit semantics (first-observation declaration) and uses mark_supply_available instead."
          },
          "422": {
            "description": "Request body failed schema validation (missing reason, empty reason, or reason exceeds 500 chars)."
          }
        },
        "summary": "Operator confirms a Recovering Supply is fully back (Recovering -> Available)",
        "tags": [
          "supply"
        ]
      }
    },
    "/surfaces": {
      "post": {
        "operationId": "post_surfaces_surfaces_post",
        "parameters": [
          {
            "description": "Optional Idempotency-Key for safe retry semantics.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional Idempotency-Key for safe retry semantics.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineSurfaceRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineSurfaceResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation (unknown kind, oversized name, etc.) OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new arrival Surface",
        "tags": [
          "trust"
        ]
      }
    },
    "/surfaces/{target_surface_id}": {
      "get": {
        "operationId": "get_surfaces_surfaces__target_surface_id__get",
        "parameters": [
          {
            "description": "Target surface's id.",
            "in": "path",
            "name": "target_surface_id",
            "required": true,
            "schema": {
              "description": "Target surface's id.",
              "format": "uuid",
              "title": "Target Surface Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/SurfaceResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No surface exists with the given id."
          },
          "422": {
            "description": "Path parameter failed schema validation."
          }
        },
        "summary": "Get a surface by id",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits": {
      "post": {
        "operationId": "post_visits_visits_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/RegisterVisitRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RegisterVisitResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (e.g. planned_end_at <= planned_start_at)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit with this id already exists."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key reuse."
          }
        },
        "summary": "Register a new Visit on a Surface under a Policy",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/abort": {
      "post": {
        "operationId": "post_visits_abort_visits__visit_id__abort_post",
        "parameters": [
          {
            "description": "Target Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Target Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/AbortVisitRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit is not in InProgress or OnHold status (pre-work Visits must use cancel_visit; terminals refuse)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Abort a mid-work Visit (InProgress | OnHold -> Aborted)",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/arrive": {
      "post": {
        "operationId": "post_visits_arrive_visits__visit_id__arrive_post",
        "parameters": [
          {
            "description": "Target Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Target Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit is not in Planned status."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Arrive at a Planned Visit (Planned -> Arrived)",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/cancel": {
      "post": {
        "operationId": "post_visits_cancel_visits__visit_id__cancel_post",
        "parameters": [
          {
            "description": "Target Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Target Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CancelVisitRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit is not in Planned or Arrived status (InProgress/OnHold must use abort_visit; terminals refuse re-cancellation)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Cancel a pre-work Visit (Planned | Arrived -> Cancelled)",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/check-in": {
      "post": {
        "operationId": "post_visits_check_in_visits__visit_id__check_in_post",
        "parameters": [
          {
            "description": "Target Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Target Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CheckInVisitRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit is not in {Arrived, InProgress, OnHold} status OR actor already has an open presence entry (check out first)."
          },
          "422": {
            "description": "Request body failed schema validation."
          }
        },
        "summary": "Check an actor in to a Visit (physical or remote)",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/check-out": {
      "post": {
        "operationId": "post_visits_check_out_visits__visit_id__check_out_post",
        "parameters": [
          {
            "description": "Target Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Target Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CheckOutVisitRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id, OR actor has no open presence entry."
          },
          "422": {
            "description": "Request body failed schema validation."
          }
        },
        "summary": "Check an actor out of a Visit",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/complete": {
      "post": {
        "operationId": "post_visits_complete_visits__visit_id__complete_post",
        "parameters": [
          {
            "description": "Target Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Target Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit is not in InProgress or OnHold status."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Complete a Visit (InProgress | OnHold -> Completed)",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/hold": {
      "post": {
        "operationId": "post_visits_hold_visits__visit_id__hold_post",
        "parameters": [
          {
            "description": "Target Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Target Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/HoldVisitRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit is not in InProgress status."
          },
          "422": {
            "description": "Request body failed schema validation."
          }
        },
        "summary": "Hold an InProgress Visit (InProgress -> OnHold)",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/resume": {
      "post": {
        "operationId": "post_visits_resume_visits__visit_id__resume_post",
        "parameters": [
          {
            "description": "Target Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Target Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit is not in OnHold status."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Resume an OnHold Visit (OnHold -> InProgress)",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/start": {
      "post": {
        "operationId": "post_visits_start_visits__visit_id__start_post",
        "parameters": [
          {
            "description": "Target Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Target Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit is not in Arrived status."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Start an Arrived Visit (Arrived -> InProgress)",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/surface-control/release": {
      "post": {
        "operationId": "post_visits_release_control_visits__visit_id__surface_control_release_post",
        "parameters": [
          {
            "description": "Requesting Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Requesting Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ReleaseControlOfSurfaceBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit cannot release: requesting Visit is not the current Surface holder or surface_id does not match."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Requesting Visit releases operational control of a Surface",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/surface-control/take": {
      "post": {
        "operationId": "post_visits_take_control_visits__visit_id__surface_control_take_post",
        "parameters": [
          {
            "description": "Requesting Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Requesting Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TakeControlOfSurfaceBody"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit cannot take control: status not eligible, surface mismatch, or Surface already held by a non-parent Visit."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Requesting Visit takes operational control of a Surface",
        "tags": [
          "trust"
        ]
      }
    },
    "/visits/{visit_id}/void": {
      "post": {
        "operationId": "post_visits_void_visits__visit_id__void_post",
        "parameters": [
          {
            "description": "Target Visit's id.",
            "in": "path",
            "name": "visit_id",
            "required": true,
            "schema": {
              "description": "Target Visit's id.",
              "format": "uuid",
              "title": "Visit Id",
              "type": "string"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/VoidVisitRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (whitespace-only reason)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "404": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "No Visit exists with the given id."
          },
          "409": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Visit is in a terminal status (Completed/Cancelled/Aborted/Voided)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Void a Visit (any non-terminal -> Voided). FHIR entered-in-error analog.",
        "tags": [
          "trust"
        ]
      }
    },
    "/zones": {
      "get": {
        "operationId": "list_zones_zones_get",
        "parameters": [
          {
            "description": "Opaque cursor from a previous page's `next_cursor`.",
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Opaque cursor from a previous page's `next_cursor`.",
              "title": "Cursor"
            }
          },
          {
            "description": "Page size; capped at 100.",
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 50,
              "description": "Page size; capped at 100.",
              "maximum": 100,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ZoneListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the query."
          },
          "422": {
            "description": "Query parameters failed validation OR `cursor` was malformed (corrupt base64, missing separator, bad timestamp / UUID)."
          }
        },
        "summary": "List zones with cursor pagination",
        "tags": [
          "trust"
        ]
      },
      "post": {
        "operationId": "post_zones_zones_post",
        "parameters": [
          {
            "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the zone. Reusing a key with a different body returns 422.",
            "in": "header",
            "name": "Idempotency-Key",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Optional client-supplied unique key per logical request. Retries with the same key + same body return the cached response instead of re-creating the zone. Reusing a key with a different body returns 422.",
              "title": "Idempotency-Key"
            }
          },
          {
            "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
            "in": "header",
            "name": "X-Principal-Id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Legacy principal-id header (trust-the-proxy shape). When IDENTITY_PROVIDERS is configured (bearer-auth mode), this header is IGNORED and the verified bearer token from `BearerAuthMiddleware` (Authorization: Bearer) sets the principal. When no IdPs are configured (legacy mode), the application TRUSTS this header (no cryptographic verification) -- production deployments in legacy mode MUST front the API with an auth proxy that strips any client-supplied X-Principal-Id and sets it to the verified principal UUID. Behavior when absent: see Settings.require_authenticated_principal.",
              "title": "X-Principal-Id"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DefineZoneRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "201": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/DefineZoneResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Domain invariant violated (for example whitespace-only name)."
          },
          "403": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ErrorResponse"
                }
              }
            },
            "description": "Authorize port denied the command."
          },
          "422": {
            "description": "Request body failed schema validation OR Idempotency-Key was reused with a different request body."
          }
        },
        "summary": "Define a new Trust zone",
        "tags": [
          "trust"
        ]
      }
    }
  }
}
