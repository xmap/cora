# CORA Site descriptor: the NSLS-II site-level surface
#
# NSLS-II (National Synchrotron Light Source II, Brookhaven National Laboratory)
# is a Site CORA models, the home of the FXI, HXN, BMM, SRX, SIX, CHX, CSX, XPD, ESM, SMI, IXS, and SST beamlines. This file is
# the single human-readable source for the facility's site-level instances: the
# Facility itself, the Practices it would run, and the facility principals.
#
# Status: reverse-engineered. FXI is an operational NSLS-II beamline, but CORA
# has not been connected to it and no FXI staff have confirmed this model. Every
# value below is read from public NSLS-II open source (the bluesky profile
# collection NSLS2/fxi-profile-collection and the shared NSLS2/nslsii package) or
# inferred, and is carried as `pending` until FXI staff confirm it. Do not invent
# facility specifics here; carry them as questions on the FXI open-questions page.
#
# Source-of-truth boundary (same as the APS and MAX IV sites):
#   - the closed facility-kind / actor-kind vocabularies are validated against
#     code-mirrored frozensets in scripts/site_descriptor.py.
#   - the facility records the bootstrap invariants (kind=Site, display_name
#     equals code); `heading` carries the human title used on the rendered page.

facility:
  code: nsls2
  display_name: nsls2
  heading: "NSLS-II"
  kind: Site
  institution: Brookhaven National Laboratory
  beamlines: [FXI, HXN, BMM, SRX, SIX, CHX, CSX, XPD, ESM, SMI, IXS, SST, ISS, FMX, CMS]

# ISA-88 Site Recipes: the facility-adapted form of a catalog Method. All pending
# because they are reverse-engineered from the FXI profile collection, not yet
# confirmed with FXI staff. Each names the cross-facility Method it would adapt.
practices:
  - {name: FXI_tomography_practice, method: tomography, pending: true, note: "full-field transmission tomography; reverse-engineered from the FXI bluesky plans, awaiting staff confirmation"}
  - {name: FXI_fly_tomography_practice, method: continuous_rotation_tomography, pending: true, note: "continuous-rotation fly tomography (tomo_zfly / fly_scan), Zebra position-triggered"}
  - {name: FXI_energy_change_practice, method: beamline_energy_change, pending: true, note: "coupled energy move (move_zp_ccd_xh) for XANES and spectro-tomography"}
  - {name: HXN_nano_tomography_practice, method: tomography, pending: true, note: "scanning nano-tomography (raster x rotation); reverse-engineered from the HXN profile collection"}
  - {name: HXN_energy_change_practice, method: beamline_energy_change, pending: true, note: "energy change co-moving the zone-plate refocus per element edge (XANES / spectro-tomo)"}
  - {name: SRX_xrf_tomography_practice, method: tomography, pending: true, note: "XRF-tomography (raster x rotation, fluorescence map per angle); reverse-engineered from the SRX profile collection"}
  # BMM's defining technique is transmission/fluorescence XAS, an energy SCAN whose
  # Method (energy_scan) the catalog anticipates but does not yet coin (ENERGY-1).
  # No BMM Practice is recorded until that Method exists; adding one now would
  # reference a non-existent or wrong method. BMM is bound to the Site via beamlines.
  # SRX's defining techniques (scanning XRF mapping, XANES) sit on the same two
  # deferred Methods, scanning (HXN) and energy_scan (BMM); only its XRF-tomography
  # leg binds an existing Method, so only that Practice is recorded here.
  # SIX's defining technique is RIXS (resonant inelastic X-ray scattering), a soft
  # X-ray scattering Method not in the imaging-heritage catalog; rendered pending
  # until a technique enters scope (TECH-1).
  - {name: SIX_rixs_practice, method: resonant_inelastic_scattering, pending: true, note: "resonant inelastic X-ray scattering on the soft X-ray spectrometer arm; Method not yet in catalog (TECH-1)"}
  - {name: IXS_inelastic_scattering_practice, method: inelastic_x_ray_scattering, pending: true, note: "momentum-resolved hard X-ray inelastic scattering: set Q on the six-circle arm, scan incident energy against the fixed crystal analyzer, point-detect I(Q, energy-loss); Method not yet in catalog (TECH-1)"}
  # CHX's defining techniques (XPCS, small-angle scattering) sit on the deferred
  # xpcs / small_angle_scattering Methods that APS 8-ID left pending (TECH-1); no
  # CHX Practice is recorded until those Methods exist. CHX is bound via beamlines.
  # CSX (23-ID) does coherent / resonant soft X-ray scattering on the TARDIS
  # diffractometer; both legs reuse existing pending Methods (resonant_scattering
  # from 4-ID, diffraction from 4-ID / 8-ID). The coherence is a beam-quality
  # enabler carried as settings, not coined as its own Method (TECH-1).
  - {name: CSX_rsxs_practice, method: resonant_scattering, pending: true, note: "resonant soft X-ray scattering (RSXS); reuses the 4-ID resonant_scattering Method (TECH-1)"}
  - {name: CSX_diffraction_practice, method: diffraction, pending: true, note: "coherent soft X-ray diffraction on the TARDIS E6C; reuses the 4-ID / 8-ID diffraction Method (TECH-1)"}
  # XPD's defining techniques (powder diffraction, total scattering / PDF) sit on the
  # deferred powder_diffraction / total_scattering Capabilities that Diamond i11 and
  # i15-1 left pending (TECH-1); no XPD Practice is recorded until those land. XPD is
  # bound via beamlines.
  # ESM (21-ID) does angle-resolved photoemission (ARPES), a soft X-ray photoemission
  # technique new to the catalog; rendered pending until a technique enters scope
  # (TECH-1). The XPEEM/LEEM branch is deferred (see esm/model.md).
  - {name: ESM_arpes_practice, method: angle_resolved_photoemission, pending: true, note: "angle-resolved photoemission on the hemispherical electron analyzer; Method not yet in catalog (TECH-1)"}
  # SMI's defining techniques (SAXS / WAXS / GISAXS) sit on the deferred scattering
  # Capabilities that Diamond i22 left pending (TECH-1); simultaneous SAXS+WAXS is
  # coordinated Runs not a new technique. No SMI Practice is recorded until those
  # Capabilities land. SMI is bound via beamlines.
  # SST's three technique families (soft-scattering RSoXS, NEXAFS absorption, HAXPES
  # photoemission) each sit on a deferred / pending Capability (TECH-1, ENERGY-1, the
  # ESM angle_resolved_photoemission); no SST Practice is recorded until they land.
  # SST is bound via beamlines.
  # ISS (8-ID) does X-ray absorption (EXAFS, by HHM trajectory energy fly-scan) and
  # X-ray emission (XES, and incident-energy HERFD) on the Johann / von Hamos crystal
  # emission spectrometers. This is the same emission-spectrometer XAS / XES shape that
  # LCLS-MFX left pending as xas_spectroscopy, so ISS reuses that Method as the second
  # consumer rather than coining a new one (TECH-1). The energy sweep also leans on the
  # deferred energy_scan Capability (ENERGY-1, the BMM question), which ISS strengthens
  # as a further consumer without forcing it in this descriptor scaffold.
  - {name: ISS_xas_spectroscopy_practice, method: xas_spectroscopy, pending: true, note: "X-ray absorption (EXAFS) by HHM trajectory energy fly-scan, plus X-ray emission (XES) and high-energy-resolution fluorescence detection (HERFD) on the Johann and von Hamos crystal emission spectrometers; reuses the xas_spectroscopy Method that LCLS-MFX left pending, the second consumer (TECH-1). The incident-energy sweep also leans on the deferred energy_scan Capability (ENERGY-1)"}
  # FMX (17-ID-2) does microfocus macromolecular crystallography: rotation data
  # collection on a single-omega goniometer + Eiger, fast grid scans for centring, and
  # an autonomous robot sample-exchange loop. These reuse the three MX Methods Diamond
  # i03 left pending (mx_data_collection, grid_scan, sample_exchange); FMX is the second
  # consumer of each. They stay pending (no mechanical promotion for Methods; the
  # energy_scan deferral discipline) and the autonomous loop is a Procedure over the
  # spine plus a Subject custody thread (TECH-1 / ROBOT-1). The fixed-target chip-scanner
  # serial mode is deferred (SERIAL-1), not a practice.
  - {name: FMX_mx_data_collection_practice, method: mx_data_collection, pending: true, note: "rotation / oscillation MX data collection on the single-omega goniometer + Eiger via the mxtools vector / Zebra flyer; reuses the i03 mx_data_collection Method, the second consumer (TECH-1)"}
  - {name: FMX_grid_scan_practice, method: grid_scan, pending: true, note: "fast grid scan for sample location / centring via the Zebra-triggered goniometer raster; reuses the i03 grid_scan Method, the second consumer (TECH-1)"}
  - {name: FMX_sample_exchange_practice, method: sample_exchange, pending: true, note: "autonomous robot load / centre / collect / unmount loop coordinated by the LSDC Governor; reuses the i03 sample_exchange Method, the second consumer; a Procedure over the spine + a Subject custody thread (ROBOT-1)"}
  # CMS (11-BM) complex-materials scattering Practices (first cut). SAXS / WAXS /
  # GISAXS overlap SMI (the NSLS-II twin), I22, and 9-ID; the distinct one is
  # specular reflectivity (XR), the second consumer of the reflectivity Method after
  # i10. Methods not yet in the catalog, carried pending (TECH-1).
  - {name: CMS_small_angle_scattering_practice, method: small_angle_scattering, pending: true, note: "transmission small-angle scattering on the Pilatus 2M; shares the i22 / SMI SAXS Method (TECH-1)"}
  - {name: CMS_wide_angle_scattering_practice, method: wide_angle_scattering, pending: true, note: "wide- and medium-angle scattering on the Pilatus 800K detectors; shares the i22 WAXS Method (TECH-1)"}
  - {name: CMS_grazing_incidence_scattering_practice, method: grazing_incidence_scattering, pending: true, note: "grazing-incidence small- and wide-angle scattering (GISAXS / GIWAXS) on thin films; shares the 9-ID Method, SMI the NSLS-II twin (TECH-1)"}
  - {name: CMS_reflectivity_practice, method: reflectivity, pending: true, note: "specular hard X-ray reflectivity (XR): step the sample theta and track the reflected beam in a sliding region on the fixed Pilatus; second consumer of the reflectivity Method after i10 (XR-1, TECH-1)"}

# Access BC Actors conceptually facility-wide at NSLS-II. Pending until the NSLS-II
# operator and review structure is confirmed; the profile collection only exposes
# coarse queue-server groups (root / primary), not the human roster.
actors:
  - {name: NSLS-II beamline staff, kind: human, pending: true, note: "operator and beamline-scientist pool; structure TBD (GOV-1)"}
  - {name: NSLS-II safety review, kind: human, pending: true, note: "facility safety review body; membership TBD"}

# Agent BC Agents seeded at this deployment. None declared: the CORA system agents
# are not yet wired for an NSLS-II deployment.
agents: []

# Supply BC: facility-wide continuously-available resources. Pending in this
# reverse-engineered model. LiquidNitrogen is the DCM crystal cooling read in the
# profile collection (XF:18IDA-UT Cryo:1 levels/flow; cooling valves V4/V5).
supplies:
  - {name: Storage-ring beam current, kind: PhotonBeam, pending: true}
  - {name: Cooling water, kind: CoolingWater, pending: true}
  - {name: Beamline vacuum, kind: Vacuum, pending: true}
  - {name: DCM liquid nitrogen, kind: LiquidNitrogen, pending: true}

# Safety BC: facility-issued Clearances. NSLS-II PSS form names are not confirmed;
# the profile collection exposes only the PPS photon-shutter status, not the
# search-and-secure permit leaves. Carried pending with a note rather than guessed.
clearances:
  - {name: NSLS-II personal safety system, kind: PSS, pending: true, note: "facility safety-form and permit-leaf names TBD; confirm with NSLS-II staff (PSS-1)"}

# Caution BC: operator tribal knowledge. Beamline-level cautions reverse-engineered
# for FXI live on docs/deployments/fxi/cautions.md, not here.
cautions: []
