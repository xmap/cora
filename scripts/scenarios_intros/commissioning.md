Routines that prepare a beamline for users: the alignment chain (`alignment_resolution` → `_focus` → `_center` → `_roll` → `_pitch`, with `sensitivity_characterization` as the pre-step), equipment bring-up (`first_light`, `motor_homing`, `hexapod_reboot`), and detector baselines (`dark_baseline`, `flat_baseline`) captured before any sample acquisition.
