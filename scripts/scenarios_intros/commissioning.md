Routines that prepare a beamline for users: the alignment chain (`alignment_resolution` → `_focus` → `_center` → `_roll` → `_pitch`, with `sensitivity_characterization` as the pre-step), equipment bring-up (`motor_homing`, `hexapod_reboot`), and detector baselines (`dark_field`, `flat_field`) captured before any sample acquisition.
